---
phase: 02-delete-correctness-test-suite-trust
plan: 06
type: execute
wave: 5
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05"]
files_modified:
  - StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift
  - StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift
  - .planning/phases/02-delete-correctness-test-suite-trust/02-DATA-01-EVIDENCE.md
  - .planning/phases/02-delete-correctness-test-suite-trust/02-TRUST-GATE-RECORD.md
autonomous: true
requirements: [DATA-01]
gap_closure: false
user_setup:
  - service: icloud
    why: "DATA-01 surface A — the initiating delete must run on a physical iPhone signed into the team iCloud account (container iCloud.stress.ai.com); simulator+device is explicitly rejected as evidence"
    env_vars: []
    dashboard_config:
      - task: "Sign a physical iPhone into the team iCloud account with the StressMonitor container provisioned; install a dev build carrying user data (measurements, baseline, habits, unlocks)"
        location: "Settings -> iCloud; Xcode run on device"
  - service: cloudkit-console
    why: "DATA-01 surface B — the locked-fallback second surface: CloudKit Console (developer account, team K2TYLYAWMK) queries the private DB of container iCloud.stress.ai.com per record type until stable-empty"
    env_vars: []
    dashboard_config:
      - task: "Open CloudKit Console -> container iCloud.stress.ai.com -> private database; be ready to query each record type and take timestamped screenshots"
        location: "https://icloud.developer.apple.com (or the CloudKit Dashboard in the developer site)"
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "performFactoryReset deletes Habit rows in its local SwiftData sweep — no model the app writes and syncs is left deleted by no code path (unit-pinned: a Habit-seeded fixture is empty after factory reset)"
    - "Factory reset on an empty/already-cleared store completes without error (empty-input case unit-pinned)"
    - "The evidence note certifies the factory-reset surface (the broadest actual sweep), discloses the scoped deleteAllMeasurements path's narrower semantics (StressMeasurement-only; CharacterUnlock/Habit survive it), and enumerates every record type queried"
    - statement: "Deleting all data on the physical initiator device removes the records on the second surface (CloudKit Console, or a second physical iPhone when available) after a documented propagation wait — the observed delay is recorded with timestamps per query round"
      verification: backstop
    - statement: "The console emptiness check enumerates every record type the schema writes — CD_StressMeasurement, CD_PersonalBaseline, CD_SyncMetadata, plus the NSPersistentCloudKitContainer-mirrored types (including CharacterUnlock and Habit) enumerated live in the console's schema browser during the session — no adjacent record-type blind spot"
      verification: backstop
    - statement: "Emptiness is judged per record type by query (set emptiness, independent of result ordering); a second query round after a stability interval returns the same empty state (idempotent observation, recorded)"
      verification: backstop
    - statement: "An interrupted or partially-failed delete never reports success — failures propagate loudly (unit-pinned by plan 02-01's prongs); propagation interruption is retried by eventual consistency and observed via the poll-until-stable-empty procedure"
      verification: backstop
    - "The phase trust gate passes: full suite green in the shipped configuration, xcresulttool enumeration shows zero unexpected skips, and the disable/gate grep maps 1:1 to the disposition set from plan 02-04 with no new constructs"
    - "Planner assumptions surfaced (flagged): mirrored record-type naming in the console is confirmed live during evidence collection (A2); Habit records exist in real user containers because the feature is reachable (A6) — if Habit is unreachable in shipped builds the gap downgrades to dead-schema cleanup, recorded as one disposition line"
  prohibitions:
    - "no narrative claim in the evidence note without a timestamp — surfaces checked, record types queried, and the observed propagation delay are all timestamped facts (01-WIRE-01-EVIDENCE.md pattern)"
    - "no store left populated after a claimed-successful delete without the propagation check observing it (flagged-unverified — unit layer pinned by 02-01; live layer backstopped by the evidence note)"
    - "no simulator-derived evidence anywhere in the DATA-01 record (Pitfall 7 — CloudKit is unreliable on simulator; initiator must be physical hardware)"
    - "no count-only trust-gate claim — the gate enumerates suites and maps every skip/disable to a disposition (locked CONTEXT decision)"
  artifacts:
    - Habit deletion in performFactoryReset + pin test
    - 02-DATA-01-EVIDENCE.md (dated evidence note, 01-WIRE-01-EVIDENCE.md structure)
    - 02-TRUST-GATE-RECORD.md (full-suite run record + suite enumeration + grep mapping)
  key_links:
    - "DataManageView factory-reset surface -> performFactoryReset (now incl. Habit) -> CloudKit container -> second surface (console/second iPhone) observed empty after propagation wait"
---

<objective>
Close DATA-01 (per CONTEXT "Two-Device CloudKit Delete Verification") and run the phase-end trust gate. First close the store-sweep completeness gap research found — the Habit model is synced (AppSchemaV2, cloudKitDatabase .automatic) yet deleted by no code path, so the locked "full store set the app writes" bar is unreachable without a one-line fix (planner decision: FIX, mirroring the CharacterUnlock precedent). Then produce the dated evidence-note skeleton that carries the two-surface manual verification (device + CloudKit Console; second physical iPhone preferred if available), disclosing the scoped path's narrower semantics. Finally run the phase trust gate: full-suite record, xcresulttool enumeration, and the disable/gate grep mapped 1:1 to dispositions.

Purpose: ROADMAP SC-1 (two-surface delete propagation verified end-to-end) and SC-4's enforcement arm (the trust gate is a phase artifact, not just a verification step — the DATA-01 note cites the green full-suite run).

Output: Habit-sweep fix + pin test, 02-DATA-01-EVIDENCE.md ready for human execution, 02-TRUST-GATE-RECORD.md proving the suite is a believable gate.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-CONTEXT.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-RESEARCH.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-PATTERNS.md
@.planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md
</context>

<assumption_delta_decision>
Detector signal: pluralization of "second" surface (DATA-01). Decision: **no-change**. Deletion identity was always account-scoped via CloudKit mirroring — CKRecord identity (account + recordName in the private DB) is the existing generalized noun; the second surface (console or second physical iPhone) exercises the existing identity model, introducing no new identity axis and requiring no schema or model generalization. Recorded 2026-09-03 per the orchestrator-resolved assumption-delta checkpoint.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store-sweep completeness — factory reset deletes Habit (planner decision: FIX, not accept)</name>
  <files>StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift, StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift</files>
  <read_first>StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift (:392-456 performFactoryReset — :430-432 the local sweep with the CharacterUnlock precedent at :431), StressMonitor/StressMonitor/StressMonitorApp.swift (:52-62 and :76-80 AppSchemaV2 with Habit synced via cloudKitDatabase .automatic), StressMonitor/StressMonitor/Views/HabitViewModel.swift (:65 the insert site proving the model is written), StressMonitor/StressMonitorTests/DataDeleterServerWipeTests.swift (:106-121 tuple fixture incl. CharacterUnlock), 02-RESEARCH.md Pitfall 2 (the Habit gap + scoped-path semantics)</read_first>
  <behavior>
    - Test (RED): a fixture seeded with StressMeasurement + CharacterUnlock + Habit rows is NOT fully empty of Habit after performFactoryReset — fails today (no code path deletes Habit)
    - Test (GREEN): after the fix, Habit rows == 0 post factory reset; StressMeasurement and CharacterUnlock still 0 (existing behavior pinned)
    - Test: factory reset on an empty store completes without throwing (empty-input case)
  </behavior>
  <action>Planner decision recorded: FIX the Habit gap rather than accept it — CONTEXT Area 1 locks the record scope to "the full store set the app writes", and Habit is written (HabitViewModel.swift:65) and synced (AppSchemaV2); certifying a sweep that provably leaves a synced model undeleted would fail the phase goal, and the fix is one delete call mirroring the CharacterUnlock precedent. TDD: first add the RED pin (extend the 02-01 suite or the DataDeleterServerWipeTests factory-reset tests with a Habit-seeded tuple fixture asserting Habit emptiness after performFactoryReset) — it fails because no path deletes Habit. GREEN: add the Habit model deletion to the Phase-2 local sweep in performFactoryReset alongside CharacterUnlock (one modelContext delete call at the :430-432 site; the fixture's ModelContainer already registers the needed models — add Habit.self to the fixture's schema list). Do NOT widen deleteAllMeasurements (its narrower semantics are a disclosed fact in the evidence note, not a bug to fix this phase — changing scoped-path breadth is a product decision outside the locked scope; the finding is surfaced in STATE as a carry-forward note). Commit test-first then fix.</action>
  <verify>
    <automated>TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/DataDeleterCloudKitTruthinessTests -only-testing:StressMonitorTests/DataDeleterServerWipeTests -only-testing:StressMonitorTests/DataDeletionConsolidationTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - RED recorded first (Habit-survives assertion failing) in the SUMMARY; then GREEN with the one-line sweep addition in performFactoryReset
    - Post-fix: Habit, StressMeasurement, and CharacterUnlock all empty after performFactoryReset (pin test green); empty-store re-run does not throw
    - deleteAllMeasurements is byte-unchanged (scoped semantics disclosed, not altered)
    - The three deletion suites named in the verify command all green (no regression to the consolidated delete tests)
    - swiftlint clean on the touched files
  </acceptance_criteria>
  <done>The factory-reset sweep now covers every model the app writes and syncs; the scoped path's narrower semantics remain a documented (not silently wrong) fact for the evidence note.</done>
</task>

<task type="auto">
  <name>Task 2: DATA-01 evidence note — skeleton, procedure, human items, disclosures</name>
  <files>.planning/phases/02-delete-correctness-test-suite-trust/02-DATA-01-EVIDENCE.md</files>
  <read_first>.planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md (the structural analog: title/meta + verification path of record, data-source disclosure, timestamped screenshots table, machine-verified checks, pending human items, material discovery), 02-RESEARCH.md Pitfall 2 + Pitfall 7 + Open Question 1, 02-CONTEXT.md "Two-Device CloudKit Delete Verification" (locked apparatus + cleared standard), StressMonitor/StressMonitor/Services/CloudKit/CloudKitSchema.swift (:5-9 record types), StressMonitor/StressMonitor/Views/DataDeleteView.swift (:152/:347/:421 the UI claims + scoped entry)</read_first>
  <action>Create 02-DATA-01-EVIDENCE.md following the 01-WIRE-01-EVIDENCE.md skeleton: (a) meta block — produced-by/date/verification path of record + environment rows (device model, iOS build, iCloud account context, how each surface was reached); (b) data-source disclosure — the certified surface is the factory-reset path (DataManageView), the scoped deleteAllMeasurements path's narrower semantics disclosed as a fact (deletes StressMeasurement only; CharacterUnlock/Habit survive it), the full record-type query list (CD_StressMeasurement, CD_PersonalBaseline, CD_SyncMetadata + the NSPersistentCloudKitContainer-mirrored types enumerated live in the console schema browser — record what the enumeration finds); (c) procedure for the human run — delete on the physical device, then console/second-surface queries per record type in poll rounds with timestamps until stable-empty, recording the observed propagation delay (eventual consistency; immediate-only checks rejected); (d) timestamped screenshots/query table placeholders (one row per record type per round); (e) machine-verified checks section — placeholder citing the Task 3 full-suite run record; (f) pending human items — the device+console execution (user_setup), the second-physical-iPhone-preferred note (use it if available at execution time; console is the locked fallback), simulator evidence explicitly barred; (g) material-discovery section — pre-seeded with the two known findings (Habit gap: FIXED this plan, verify propagation on device; scoped-path semantics: disclosed) plus space for anything the console enumeration surfaces. No narrative claims without timestamps; every placeholder names what must be recorded.</action>
  <verify>
    <automated>test -f .planning/phases/02-delete-correctness-test-suite-trust/02-DATA-01-EVIDENCE.md && grep -c "Timestamp\|propagation" .planning/phases/02-delete-correctness-test-suite-trust/02-DATA-01-EVIDENCE.md && grep -n "deleteAllMeasurements\|Habit\|CD_StressMeasurement" .planning/phases/02-delete-correctness-test-suite-trust/02-DATA-01-EVIDENCE.md | head -8; echo NOTE_OK</automated>
    <human-check>End-of-phase human item (human_verify_mode=end-of-phase): physically execute the two-surface verification per the note — delete all data on the physical iPhone, observe per-record-type emptiness on the CloudKit Console (or second physical iPhone if available) after the propagation wait, capture timestamped screenshots, fill the note's table and observed delay.</human-check>
  </verify>
  <acceptance_criteria>
    - Note exists with all seven skeleton sections present (meta, disclosure, procedure, table, machine-verified, human items, material discovery)
    - Disclosure names the factory-reset surface as certified AND the scoped path's narrower semantics
    - The record-type query list includes the three CD_ service types and instructs live enumeration of mirrored types in the console
    - The procedure mandates poll rounds until stable-empty with the observed delay recorded (no immediate-only check)
    - Human items name the physical-device requirement and the simulator prohibition explicitly
  </acceptance_criteria>
  <done>The evidence note is execution-ready: a human can perform the two-surface verification and fill timestamped facts; all known disclosures are pre-seeded.</done>
</task>

<task type="auto">
  <name>Task 3: Phase trust gate — full-suite record, enumeration, disable-grep mapping</name>
  <files>.planning/phases/02-delete-correctness-test-suite-trust/02-TRUST-GATE-RECORD.md</files>
  <read_first>02-CONTEXT.md "Phase-end trust gate" (locked: enumeration not counts), 02-RESEARCH.md Pitfall 6 + Validation Architecture "Sampling Rate", 02-04-SUMMARY.md + 02-05-SUMMARY.md (the disposition set + final invocation form), .github/workflows/_test.yml (:190/:206-212 the xcresult bundle paths)</read_first>
  <precondition>Plans 02-01 through 02-05 complete (their dispositions and doc state are the inputs this gate audits)</precondition>
  <action>Run the phase trust gate and write 02-TRUST-GATE-RECORD.md: (1) Full suite in the shipped configuration — the AGENTS.md canonical CI-parity invocation as finalized by 02-05 (env-var form if the gates remain); if any suite remains GSD_CI-gated per 02-04's outcome, ALSO run the local full-coverage form without the env var and record both results. (2) Enumerate: xcrun xcresulttool get test-results summary and get test-results tests over the result bundle — every suite listed with pass/skip status; zero unexpected skips (expected = exactly the dispositioned set). (3) Map the disable/gate grep (disabled( and enabled(if constructs across StressMonitorTests) 1:1 to the 02-04 disposition outcomes — fixed-and-enabled, dated disposition, conditional-by-design — with zero unaccounted constructs and zero NEW constructs vs the 02-04 baseline. (4) Record zero test failures in the shipped configuration. The record carries commands, dates, exit codes, and the mapping table; DATA-01's note §machine-verified cites this record. Any failure found here is fixed or dispositioned before the phase closes — the gate is not passed on a promise.</action>
  <verify>
    <automated>grep -rn "disabled(\|enabled(if" StressMonitor/StressMonitorTests --include="*.swift" | grep -v FirebaseBootstrap; echo GREP_DONE; test -f .planning/phases/02-delete-correctness-test-suite-trust/02-TRUST-GATE-RECORD.md && grep -c "Suite\|suite" .planning/phases/02-delete-correctness-test-suite-trust/02-TRUST-GATE-RECORD.md; echo RECORD_OK</automated>
  </verify>
  <acceptance_criteria>
    - 02-TRUST-GATE-RECORD.md exists with: full-suite command(s) + exit codes + date, the xcresulttool enumeration (every suite named with status), the disable/grep mapping table, and the zero-unexpected-skips statement
    - The full suite is green in the shipped configuration (and additionally in the ungated local form if gates remain)
    - Every construct in the verify grep maps to a disposition from 02-04 (or FirebaseBootstrapTests by-design) — zero unaccounted, zero new
    - The record is not count-only: suites are enumerated by name (locked decision — a disabled suite does not change counts)
  </acceptance_criteria>
  <done>The test suite is a gate that can be believed: one documented invocation (02-05), every skip explained, zero unexplained failures — all captured as a dated phase artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device local stores -> iCloud private DB | delete propagation crosses iCloud infrastructure; only observable, not implementable |
| evidence note -> phase verification | the note's claims must be timestamp-backed, not narrative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Information Disclosure | Habit (and pre-fix, any) records surviving a claimed all-data delete across devices — privacy promise violated | high | mitigate | Habit deletion added + unit-pinned (Task 1); live propagation observed via the two-surface evidence procedure with query-based emptiness per record type |
| T-02-12 | Information Disclosure | evidence note leaking account identifiers/PII in screenshots or environment rows | low | mitigate | Note redacts account emails/user names; screenshots show record-type queries, not account pages |
| T-02-13 | Tampering (process) | trust gate passed on counts while a suite silently skips | medium | mitigate | Gate mandates per-suite enumeration (xcresulttool) + construct-to-disposition mapping; count-only claims rejected |
</threat_model>

<verification>
- Habit pin test green; delete suites regression-green; deleteAllMeasurements byte-unchanged
- 02-DATA-01-EVIDENCE.md exists, execution-ready, disclosures pre-seeded (human execution is the end-of-phase item)
- 02-TRUST-GATE-RECORD.md: full-suite green, enumeration complete, mapping table with zero unaccounted constructs
</verification>

<success_criteria>
- DATA-01 closed to the extent automatable: sweep completeness fixed (Habit), evidence apparatus ready, disclosures recorded; the live two-surface verification stands as the end-of-phase human item with the note as its artifact
- The phase trust gate passes and is recorded — SC-4's enforcement arm exists as a dated artifact
</success_criteria>

## Artifacts this phase produces (this plan)

- Habit deletion in performFactoryReset + its pin test (store-sweep completeness)
- 02-DATA-01-EVIDENCE.md — the dated two-surface verification note (01-WIRE-01-EVIDENCE.md pattern), with the scoped-path disclosure and record-type enumeration procedure
- 02-TRUST-GATE-RECORD.md — the phase trust gate artifact (full-suite run record, suite enumeration, disable/gate mapping)
- The assumption-delta no-change decision recorded above (account-scoped CloudKit identity, no new identity axis)

<output>
Create `.planning/phases/02-delete-correctness-test-suite-trust/02-06-SUMMARY.md` when done
</output>
