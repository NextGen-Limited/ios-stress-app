---
phase: 02-delete-correctness-test-suite-trust
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift
  - StressMonitor/StressMonitorTests/CharacterEntitlementSyncTests.swift
  - StressMonitor/StressMonitorTests/StoreKitServiceTests.swift
  - StressMonitor/StressMonitorTests/EntitlementForegroundCorrectionTests.swift
  - .planning/WINDOWS.md
  - .github/workflows/_test.yml
autonomous: false
requirements: [ENV-01, ENV-02]
gap_closure: false
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every disable/gate construct the trust grep finds across StressMonitorTests (the 7 known constructs in 5 files: CharacterEntitlementSyncTests.swift:27, DataDeletionConsolidationTests.swift:238/:375, StoreKitServiceTests.swift:17, EntitlementForegroundCorrectionTests.swift:12, FirebaseBootstrapTests.swift:28/:42) maps 1:1 to exactly one of {fixed-and-enabled, dated disposition meeting the bar, conditional-by-design}"
    - "The container-lifetime hypothesis (return-context-only fixtures — STATE v1.1 P03-04 decision, never tested against these suites and NOT among their 5 ruled-out causes) is tested first: fixtures converted to the (ModelContainer, ModelContext) tuple shape, gates/quarantine lifted in targeted runs, outcome recorded either way"
    - "Each disposition written this plan names: the failure signature (exit 65, 0 assertion failures, affected suites), ruled-out causes (including the container-lifetime outcome), residual risk, and date — in the suite file header AND the WINDOWS.md ledger via the gsd-tools windows verbs"
    - "Either outcome leaves no silently disabled suite: fixed suites run ungated in the default run with their gate and its now-dead env plumbing removed; unfixed suites carry dated dispositions"
    - "Planner assumption (ENV-01, flagged): the bounded session is sufficient to fix-or-disposition within the timebox — the locked fallback is the dated disposition (fix-or-bust was rejected after 5 falsified hypotheses)"
    - "Planner assumption (ENV-02, flagged): CharacterEntitlementSyncTests belongs to the same host-crash family as #8 (same fixture shape) — if the container-lifetime fix clears #8 it is expected to clear this quarantine too; an independent cause is dispositioned separately"
  prohibitions:
    - "no suite left disabled without a dated disposition in the same change (flagged-unverified — enforced by the phase-end trust gate in plan 02-06)"
    - "no dead config: a removed gate's TEST_RUNNER_GSD_CI plumbing in _test.yml is removed with it (or kept with a live reason)"
    - "do not hand-edit the WINDOWS.md ledger JSON mirror — use the gsd-tools windows waive/fixed verbs"
    - "no blind re-enable: gates come off only after green targeted proof (locked CONTEXT decision)"
    - "no re-running already-ruled-out hypotheses from the file-header bisection (wasted budget — Pitfall 1)"
  artifacts:
    - Converted tuple fixtures in the three SwiftData-heavy suites (DataDeletionConsolidationTests, CharacterEntitlementSyncTests) if retained
    - Dated dispositions in suite file headers + WINDOWS.md ledger entries (#8 lineage; CharacterEntitlementSyncTests quarantine; StoreKitServiceTests/EntitlementForegroundCorrectionTests outcomes)
  key_links:
    - "Bounded-session evidence (targeted runs, .ips correlation) -> checkpoint:decision -> either {ungated suites + removed env plumbing} or {dated dispositions + ledger writes}"
---

<objective>
Resolve ENV-01 and ENV-02 (per CONTEXT "Test-Suite Truth Dispositions"): run ONE bounded re-diagnosis session over the WINDOWS.md #8 host-crash lineage and the CharacterEntitlementSyncTests quarantine, starting from the never-tested container-lifetime hypothesis and the six live crash reports already on disk. Each affected suite ends either fixed-and-enabled or carrying a dated disposition that meets the bar; the two extra disabled suites the trust grep surfaces (StoreKitServiceTests, EntitlementForegroundCorrectionTests) get the same fix-or-disposition treatment.

Purpose: ROADMAP SC-4 — zero unexplained failures and no silently disabled suite; every skip/disable is either gone or explained by a dated, root-cause-naming disposition.

Output: Suite-state decisions applied (gates off / dispositions written), WINDOWS.md ledger updated via the windows verbs, and the bounded-session evidence recorded in the SUMMARY.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-CONTEXT.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-RESEARCH.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bounded re-diagnosis — crash-report correlation + container-lifetime hypothesis test</name>
  <files>StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift, StressMonitor/StressMonitorTests/CharacterEntitlementSyncTests.swift</files>
  <read_first>02-RESEARCH.md (Pitfall 1 — the container-lifetime hypothesis and the ruled-out list; "ENV-01/02: bounded re-diagnosis command kit" — the full command list), StressMonitor/StressMonitorTests/CharacterEntitlementSyncTests.swift (:5-27 file header with the 5 ruled-out hypotheses + the quarantine line, :31-45 makeSeededContext), StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift (:236-241 and :373-378 the GSD_CI-gated suites, :243-250 and :380-384 the return-context-only fixtures), StressMonitor/StressMonitorTests/DataDeleterServerWipeTests.swift (:106-121 the tuple-fixture analog to convert to), 02-PATTERNS.md "DataDeletionConsolidationTests.swift + CharacterEntitlementSyncTests.swift MODIFY" section</read_first>
  <precondition>Two checkable facts this task assumes: (1) crash reports exist on disk — ls of the user DiagnosticReports directory filtered to StressMonitor returns at least one (research found 6 from 2026-09-03; if none, run one gated suite without the env var first to produce a fresh report); (2) TEST_RUNNER_GSD_CI is UNSET in the shell (env -u form in the verify command) — an empty-but-set value still forwards as non-nil GSD_CI and silently skips the suites under test</precondition>
  <action>Run the bounded session in order (time-boxed; do NOT re-run the five ruled-out hypotheses from the CharacterEntitlementSyncTests header): (1) Correlate the newest StressMonitor .ips reports — decode one or two (JSON header + payload; coalitionName com.apple.CoreSimulator.SimDevice.<UDID> ties the report to a simulator; the research already decoded StressMonitor-2026-09-03-150705.ips as a SwiftData faulting frame via Testing frames, EXC_BREAKPOINT) and record the faulting-frame + coalition evidence in the SUMMARY. (2) Convert the three return-context-only fixtures to the (ModelContainer, ModelContext) tuple shape with the _ = container keep-alive idiom (DataDeletionConsolidationTests.swift:243-250 and :380-384; CharacterEntitlementSyncTests.swift:31-45 makeSeededContext), updating every affected test body to bind the tuple. (3) Run the three suites targeted WITHOUT the GSD_CI env var so they actually execute: -only-testing:StressMonitorTests/DataDeleterFailureAndCancellationTests, :DataExportFieldSelectionTests, :CharacterEntitlementSyncTests — on the current simulator, then once more on a fresh/erased simulator (runtime isolation per the command kit). Record per-run exit codes and outcomes. This task only PROVES the hypothesis; gate removal happens in Task 2 after the checkpoint.</action>
  <verify>
    <automated>env -u TEST_RUNNER_GSD_CI xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/DataDeleterFailureAndCancellationTests -only-testing:StressMonitorTests/DataExportFieldSelectionTests -only-testing:StressMonitorTests/CharacterEntitlementSyncTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records: decoded .ips evidence (faulting frame + coalition name) and both fixture conversions with before/after line references
    - Targeted runs of all three suites executed with the env var absent (suites actually ran — not skipped), exit codes recorded, including at least one fresh-simulator round
    - grep -c "return container.mainContext" over the two modified files returns 0 (no return-context-only fixture remains)
    - Outcome classification recorded per suite: cleared / still crashing, with the raw xcodebuild tail quoted
  </acceptance_criteria>
  <done>Container-lifetime hypothesis tested with recorded evidence; every affected suite has a green-or-crashing verdict from a real targeted run.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide and apply the fix-or-disposition outcome (one-way: gate/quarantine removal + env plumbing)</name>
  <files>StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift, StressMonitor/StressMonitorTests/CharacterEntitlementSyncTests.swift, .github/workflows/_test.yml, .planning/WINDOWS.md</files>
  <read_first>Task 1 SUMMARY evidence, StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift (:236-241/:373-378 gates to remove), StressMonitor/StressMonitorTests/CharacterEntitlementSyncTests.swift (:27 quarantine line, :12-21 header), .github/workflows/_test.yml (:177-196 the TEST_RUNNER_GSD_CI env + comment), 02-CONTEXT.md "Test-Suite Truth Dispositions" (the disposition bar + no-dead-config rule)</read_first>
  <reversibility rating="one-way">Removing the CI env gate and the suite quarantines changes what every future CI run executes; a wrong call stalls CI hosts (exit 65) for all PRs. The locked decision says gates come off only after green proof — this checkpoint enforces that proof is presented before the door is opened.</reversibility>
  <decision>Apply the fix path (remove all gates/quarantine + the now-dead TEST_RUNNER_GSD_CI plumbing) or the disposition path (dated dispositions meeting the bar, gates stay) for the WINDOWS.md #8 lineage suites and the CharacterEntitlementSyncTests quarantine</decision>
  <context>Task 1 produced the bounded-session evidence: the .ips decode (faulting frame + coalition) and the container-lifetime hypothesis verdict (fixture conversion + targeted runs with the env var absent, current and fresh simulator, exit codes recorded). The locked CONTEXT policy is "if a fix lands: remove the gate/quarantine and the dead env plumbing; fix-or-bust is rejected". Green proof bar for the fix path: all three suites green on BOTH simulator rounds. Present the evidence, then apply the chosen option per the action below.</context>
  <options>
    <option id="fix-landed">
      <name>Fix landed — restore all three suites to the default run</name>
      <pros>All CR-01-lineage and entitlement-sync coverage runs in CI again; no dead config; ENV-01/ENV-02 closed by fix (strongest outcome)</pros>
      <cons>One-way for CI: any residual host flake now stalls every PR (exit 65); requires the green proof bar met on both simulator rounds</cons>
    </option>
    <option id="disposition">
      <name>Evidence-backed disposition — suites stay gated, dispositions written to the bar</name>
      <pros>CI stability preserved; locked fallback after 5 previously falsified hypotheses; coverage loss is named and dated</pros>
      <cons>Coverage stays CI-invisible; the bar must be met (signature + ruled-out causes incl. container-lifetime outcome + residual risk + date) or the disposition fails review</cons>
    </option>
  </options>
  <resume-signal>User picks fix-landed or disposition (with reference to the Task 1 evidence); execution resumes applying that option</resume-signal>
  <action>After the choice, apply it. FIX PATH: remove the .enabled(if: GSD_CI == nil) traits at DataDeletionConsolidationTests.swift:238/:375 and the @Suite(.disabled) at CharacterEntitlementSyncTests.swift:27; update file headers with the root cause + fix date; remove the now-dead TEST_RUNNER_GSD_CI step env from _test.yml leaving a one-line history note in the workflow comment; mark WINDOWS #8 fixed via gsd-tools windows fixed 8; record the quarantine resolution via the ledger verbs. DISPOSITION PATH: keep or revert the fixture conversion based on whether it helped; write the dated disposition per the bar into each affected suite's file header (failure signature: exit 65 with 0 assertion failures + affected suites; ruled-out causes: the header's five PLUS the container-lifetime outcome; residual risk: the named coverage loss; date); write matching WINDOWS.md ledger entries via the verbs (#8 update + quarantine disposition); gates stay. EITHER WAY: record the checkpoint (evidence + verdict + date) in the SUMMARY, then run the full suite in the resulting configuration (CI-parity form per AGENTS.md) to confirm the end state is green-with-dispositions.</action>
  <verify>
    <automated>grep -rn "disabled(\|enabled(if" StressMonitor/StressMonitorTests --include="*.swift" | grep -v FirebaseBootstrap; echo GREP_DONE; TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -parallel-testing-enabled NO CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - Checkpoint record in the SUMMARY: evidence presented, user verdict (fix-landed or disposition), date
    - FIX path: gates/quarantine removed, _test.yml env plumbing removed with suites running in the default CI run, WINDOWS #8 marked fixed via the verb, full suite green
    - DISPOSITION path: every still-disabled suite carries a header disposition meeting the bar (signature + ruled-out causes incl. container-lifetime outcome + residual risk + date) and a matching WINDOWS.md ledger entry written via the verbs; full suite green in the shipped configuration
    - The trust grep output (verify command) shows only constructs accounted for: enabled suites, dispositioned suites, or FirebaseBootstrapTests conditional-by-design
    - No WINDOWS.md JSON hand-edit (ledger writes went through gsd-tools windows verbs; the markdown table + JSON mirror agree)
  </acceptance_criteria>
  <done>ENV-01 and ENV-02 closed either by fix (suites restored to the default run, no dead config) or by dated dispositions meeting the bar; ledger updated via verbs; end-state full-suite run green.</done>
</task>

<task type="auto">
  <name>Task 3: Disposition the two remaining disabled suites (StoreKitServiceTests, EntitlementForegroundCorrectionTests)</name>
  <files>StressMonitor/StressMonitorTests/StoreKitServiceTests.swift, StressMonitor/StressMonitorTests/EntitlementForegroundCorrectionTests.swift, .planning/WINDOWS.md</files>
  <read_first>StressMonitor/StressMonitorTests/StoreKitServiceTests.swift (:6-17 the serialized+disabled header citing CI StoreKitTest session-isolation), StressMonitor/StressMonitorTests/EntitlementForegroundCorrectionTests.swift (:12 the disabled line citing IAP-01 productNotFound), StressMonitor/StressMonitorTests/FirebaseBootstrapTests.swift (:22-46 the conditional-by-design good pattern to cite), .planning/WINDOWS.md (entries #6 and #7 — note the mapping: #6 IS EntitlementForegroundCorrectionTests; #7 is StoreKitProductCatalogLiveTests, a DIFFERENT suite re-enabled in v1.1 whose entry never closed; StoreKitServiceTests has NO ledger entry), 02-RESEARCH.md Pitfall 6 + Open Question 3</read_first>
  <action>Close the trust-grep tail (Pitfall 6: the phase goal names two suites but the grep finds four unconditional disables). Include both suites in one isolation-matrix run (targeted, current simulator + one fresh-simulator round; StoreKitTest sessions resolve via the committed .storekit config — StressMonitorProducts.storekit). Ledger mapping — keep the wires uncrossed: #6 IS EntitlementForegroundCorrectionTests, so close #6 via gsd-tools windows fixed 6 only if THAT suite's own fix lands and it runs green; #7 is StoreKitProductCatalogLiveTests, a DIFFERENT suite (re-enabled in v1.1, entry never closed) — #7 is closable only as verified catch-up: first confirm StoreKitProductCatalogLiveTests itself is enabled and green, and never mark #7 fixed because StoreKitServiceTests went green; StoreKitServiceTests has NO ledger entry — when it is fixed-and-enabled or dispositioned, create a NEW entry via the gsd-tools windows verb rather than reusing #6/#7. Per-suite outcomes: if a suite runs green, un-disable it (edit the @Suite(.serialized, .disabled(...)) line, update the header with fix note + date); if it fails, write the dated disposition per the bar into its header (signature, ruled-out causes, residual risk, date) and record/update its WINDOWS.md ledger entry via the verbs. Cite FirebaseBootstrapTests' conditional .disabled(if:) as the by-design pattern in the SUMMARY (no action there). Every construct the grep finds must end this task mapped to exactly one of {fixed-and-enabled, dated disposition, conditional-by-design}.</action>
  <verify>
    <automated>grep -rn "disabled(\|enabled(if" StressMonitor/StressMonitorTests --include="*.swift" | grep -v FirebaseBootstrap; echo GREP_DONE</automated>
  </verify>
  <acceptance_criteria>
    - Isolation-matrix run output recorded for both suites (exit codes quoted in SUMMARY)
    - Each of the two suites is either enabled-and-green or carries a header disposition meeting the bar; ledger updates keep the exact mapping: #6 closed only on EntitlementForegroundCorrectionTests' own green fix; StoreKitServiceTests gets a NEW ledger entry via the verb (it has none today); #7 closed only after verifying StoreKitProductCatalogLiveTests itself is enabled and green (verified catch-up) — never as a side effect of StoreKitServiceTests' outcome
    - The verify grep output maps 1:1 to {fixed-and-enabled, dated disposition, conditional-by-design} with zero unaccounted constructs — the mapping table is in the SUMMARY
    - No new disable constructs introduced anywhere in StressMonitorTests
  </acceptance_criteria>
  <done>All seven disable/gate constructs dispositioned or enabled; the trust-grep mapping table exists; no silently disabled coverage remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local test host -> CI runner | suite enable state decides what CI can prove; a silent skip is an unverified claim |
| bounded session -> ledger | dispositions must be evidence-backed, not narrative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering (process) | premature un-gate destabilizes CI for all PRs (exit-65 host stalls) | medium | mitigate | One-way checkpoint before gate removal; green proof on two simulator rounds required; no-dead-config rule removes stale plumbing with the gate |
| T-02-08 | Information Disclosure (process) | vague dispositions hide real coverage loss ("known flaky" notes) | medium | mitigate | Disposition bar enforced (signature + ruled-out causes + residual risk + date); trust gate in 02-06 re-checks the mapping |
</threat_model>

<verification>
- Trust grep over StressMonitorTests maps every construct to a disposition outcome (mapping table in SUMMARY)
- Full suite green in the final configuration (CI-parity invocation per AGENTS.md; gated-suites run additionally if the gates remain, per the env-var rule)
- WINDOWS.md ledger consistent (verbs used; markdown + JSON agree)
</verification>

<success_criteria>
- ENV-01 closed: #8 lineage fixed (suites ungated, plumbing removed) or dated disposition meeting the bar in the ledger + file header
- ENV-02 closed: CharacterEntitlementSyncTests restored to the default run or permanently skipped with a rationale meeting the bar
- StoreKitServiceTests and EntitlementForegroundCorrectionTests each fixed-or-dispositioned; zero unaccounted disable constructs
</success_criteria>

## Artifacts this phase produces (this plan)

- Suite-state outcome per affected suite: ungated+enabled, or dated header disposition
- WINDOWS.md ledger dispositions/fixes for entries #6, #7, #8 and the CharacterEntitlementSyncTests quarantine (via gsd-tools windows verbs)
- Bounded-session evidence pack in 02-04-SUMMARY.md (.ips decode, per-run exit codes, fresh-simulator round, trust-grep mapping table)
- Possibly: converted tuple fixtures retained in the two DataDeletion suites + CharacterEntitlementSyncTests

<output>
Create `.planning/phases/02-delete-correctness-test-suite-trust/02-04-SUMMARY.md` when done
</output>
