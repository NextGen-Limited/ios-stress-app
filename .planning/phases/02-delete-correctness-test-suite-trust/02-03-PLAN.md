---
phase: 02-delete-correctness-test-suite-trust
plan: 03
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - StressMonitor/StressMonitor/StressMonitorApp.swift
  - StressMonitor/StressMonitor/Services/StoreKit/StoreKitServiceEnvironment.swift
  - StressMonitor/StressMonitorTests/StoreKitServiceWiringTests.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
autonomous: true
requirements: [ENV-03]
gap_closure: false
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "In DEBUG builds, the app factory (StressMonitorApp.makeStoreKitService) resolves the real StoreKit service when the mock opt-in flag is absent"
    - "In DEBUG builds, the environment default (StoreKitServiceKey.defaultValue) resolves the real StoreKit service when the opt-in flag is absent — BOTH wiring sites flip behind one named condition"
    - "The mock resolves only when explicitly opted in (launch argument), pinned by a test asserting both resolution outcomes through the shared decision logic"
    - "Release behavior is unchanged and unmockable: MockStoreKitService is #if DEBUG-only by construction (compile-time exclusion)"
    - "Money-path suites stay green after the wiring flip (CreditPurchaseFlowTests and the rest of the suite run unchanged — tests already inject their doubles via DI)"
  prohibitions:
    - "the mock must not resolve in any configuration unless explicitly opted in (flagged-unverified — Release excluded at compile time; DEBUG pinned by the new wiring test)"
    - "no WR-03 diff touching only one of the two wiring sites (Pitfall 4 — a half-flipped diff is the known failure mode)"
  artifacts:
    - StressMonitor/StressMonitorTests/StoreKitServiceWiringTests.swift (registered in the StressMonitorTests target)
    - The named mock-opt-in decision helper (launch-arg mechanism, DemoMode precedent) shared by both wiring sites
  key_links:
    - "StoreKitServiceWiringTests -> the shared factory/decision helper -> both wiring sites (app factory + environment default)"
---

<objective>
Fix WR-03 (per CONTEXT "Money-Path Advisories (ENV-03)"): DEBUG builds currently route purchases through MockStoreKitService by default at BOTH wiring sites (StressMonitorApp.swift:243-253 and StoreKitServiceEnvironment.swift:11-17) — a silent no-op money path in every debug run. Fix = DEBUG defaults to the REAL StoreKit service; the mock becomes explicit opt-in via a launch argument (DemoMode precedent); tests keep the mock via DI.

Purpose: ROADMAP SC-5 / ENV-03 — WR-03 must be fixed (locked decision), with a pinning test proving the DEBUG configuration resolves the real service absent the override.

Output: Both wiring sites flipped behind one named condition, a new #if DEBUG wiring pin suite, and the launch-arg opt-in mechanism.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-CONTEXT.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-RESEARCH.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-PATTERNS.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-01-SUMMARY.md
</context>

<feature>
  <name>DEBUG defaults to the real StoreKit service; mock is explicit opt-in (WR-03, per CONTEXT "Money-Path Advisories")</name>
  <files>StressMonitor/StressMonitor/StressMonitorApp.swift, StressMonitor/StressMonitor/Services/StoreKit/StoreKitServiceEnvironment.swift, StressMonitor/StressMonitorTests/StoreKitServiceWiringTests.swift</files>
  <behavior>
    - DEBUG, no opt-in argument: factory-resolved service is StoreKitService (not the mock)
    - DEBUG, opt-in argument present: resolved service is MockStoreKitService
    - DEBUG, no opt-in argument: EnvironmentValues default for storeKitService is StoreKitService
    Cases: absent-flag -> real; present-flag -> mock; environment-default -> real absent flag. Both outcomes asserted through the shared decision logic (injectable arguments — the process's own launch args are fixed at test time, so the decision helper must accept injected arguments).
  </behavior>
  <implementation>Introduce one named decision helper (e.g. a MockIAPMode/IAPServiceResolution enum) reading ProcessInfo arguments with an injectable parameter, following the DemoMode shape (StressMonitorApp.swift:6-10). Flip BOTH wiring sites behind it: site A makeStoreKitService (:243-253) and site B StoreKitServiceKey.defaultValue (:11-17). Widen makeStoreKitService from private static to internal static (or extract an internal resolver) so the pin test asserts through the factory, not a direct construction (Pitfall 4 warning). The pin suite wraps in #if DEBUG (the mock type exists only in DEBUG builds, MockStoreKitService.swift:3).</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — wiring pin suite asserting the real service resolves absent the override</name>
  <files>StressMonitor/StressMonitorTests/StoreKitServiceWiringTests.swift, StressMonitor/StressMonitor/StressMonitorApp.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj</files>
  <read_first>StressMonitor/StressMonitor/StressMonitorApp.swift (:6-10 DemoMode launch-arg precedent, :243-253 the DEBUG factory — site A), StressMonitor/StressMonitor/Services/StoreKit/StoreKitServiceEnvironment.swift (:11-17 defaultValue — site B), StressMonitor/StressMonitor/Services/StoreKit/MockStoreKitService.swift (:3-4 DEBUG-only construction), StressMonitor/StressMonitorTests/FirebaseBootstrapTests.swift (:19-38 the assertion-suite + environment-probing analog), 02-RESEARCH.md Pitfall 4 + Open Question 4, 02-PATTERNS.md "NEW WR-03 wiring pin" section</read_first>
  <behavior>
    - Test (RED): in DEBUG, with arguments not containing the opt-in flag, the factory resolves StoreKitService — fails today (current DEBUG default is the mock)
    - Test: with the flag injected into the decision helper, the resolution is MockStoreKitService
    - Test: the environment default (EnvironmentValues().storeKitService) resolves StoreKitService absent the flag
  </behavior>
  <action>Create StressMonitor/StressMonitorTests/StoreKitServiceWiringTests.swift (name executor's discretion), wrapped in #if DEBUG — the mock type only exists there. Because a test process cannot change its own launch arguments, the assertions must go through injectable decision logic: widen makeStoreKitService to internal static AND make the flag decision a function of an arguments parameter (defaulting to ProcessInfo.processInfo.arguments) so the suite can drive both cases — compile-only access changes, no behavior change in this task. Register the new file in the StressMonitorTests target via project.pbxproj (PBXBuildFile + PBXFileReference + group + Sources phase, mirroring CreditPurchaseFlowTests.swift entries). Run: the absent-flag assertions MUST fail (current DEBUG default resolves the mock). Commit the failing test first.</action>
  <verify>
    <automated>TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/StoreKitServiceWiringTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - New suite registered in the test target (both pbxproj entries present) and compiling under #if DEBUG
    - The absent-flag factory-resolution test FAILS in this run (red recorded, test name quoted in SUMMARY)
    - The suite asserts through the factory/decision helper, not by constructing services directly (Pitfall 4 warning honored)
    - No production behavior changed yet in this task (access/width changes only)
  </acceptance_criteria>
  <done>Failing wiring pin suite committed with the minimal seam (internal factory + injectable decision) in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — flip both wiring sites behind one named opt-in condition</name>
  <files>StressMonitor/StressMonitor/StressMonitorApp.swift, StressMonitor/StressMonitor/Services/StoreKit/StoreKitServiceEnvironment.swift</files>
  <read_first>StressMonitor/StressMonitor/StressMonitorApp.swift (:209 the .environment injection the default backs up, :243-253 site A), StressMonitor/StressMonitor/Services/StoreKit/StoreKitServiceEnvironment.swift (:11-17 site B — PaywallView.swift:20 reads it), StressMonitor/StressMonitor/Views/PaywallView.swift (:20 the environment consumer), 02-RESEARCH.md Pitfall 4</read_first>
  <action>GREEN: introduce the single named condition (launch-arg opt-in, DemoMode shape, e.g. a -mock-iap flag — exact name executor's discretion) and flip BOTH sites in the same commit: site A (StressMonitorApp.makeStoreKitService :243-253) and site B (StoreKitServiceKey.defaultValue :11-17) each resolve MockStoreKitService only when the flag is present, the real StoreKitService otherwise (DEBUG); the #else Release branches are untouched (compile-time real service, unchanged). No third site exists — verify with a repo grep that every MockStoreKitService construction in app code sits behind the one named condition (the mock's own definition file and test targets excepted). Run the wiring suite green, then the full money-path surfaces (CreditPurchaseFlowTests + StoreKitServiceTests current state) to confirm no suite depended on the mock default; swiftlint. Commit feat(02-03).</action>
  <verify>
    <automated>TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/StoreKitServiceWiringTests -only-testing:StressMonitorTests/CreditPurchaseFlowTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3 && swiftlint lint --quiet | head -5; echo LINT_DONE</automated>
  </verify>
  <acceptance_criteria>
    - StoreKitServiceWiringTests green (both resolution outcomes + environment default)
    - CreditPurchaseFlowTests still green (no money-path suite depended on the mock default)
    - git diff shows BOTH wiring-site files changed in the same commit behind one named condition (Pitfall 4: a single-file WR-03 diff fails this criterion)
    - A repo grep over StressMonitor/StressMonitor app sources shows no MockStoreKitService construction outside the named opt-in condition (definition file excepted)
    - Release (#else) branches byte-unchanged
    - swiftlint: no new violations
  </acceptance_criteria>
  <done>WR-03 closed by fix: DEBUG defaults to the real StoreKit path at both sites; mock is explicit opt-in; pin test green; money-path suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build configuration -> money path | DEBUG/Release + launch args decide real vs mocked purchases; a silent mock masks real-path defects |
| release-adjacent configs | any configuration where the mock could resolve without explicit opt-in |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering (process) | DEBUG money path silently mocked (false-negative testing of the purchase path) | high | mitigate | Real-default at both wiring sites behind one named condition; pinned by StoreKitServiceWiringTests (both outcomes); Release compile-time excluded |
| T-02-06 | Denial of Service | views reading the environment default outside app injection keep a no-op purchase surface | medium | mitigate | Site B flipped in the same commit as site A; environment-default assertion in the pin suite covers the PaywallView fallback path |
</threat_model>

<verification>
- RED then GREEN commits (2 atomic commits: failing pin, then both-site flip)
- Wiring suite + CreditPurchaseFlowTests green together
- Both wiring-site files in one GREEN commit; Release branches untouched
</verification>

<success_criteria>
- WR-03 closed by fix per the locked ENV-03 decision: real-default DEBUG, explicit mock opt-in (launch arg), tests keep mocks via DI, both wiring sites verified
</success_criteria>

## Artifacts this phase produces (this plan)

- New suite: StressMonitor/StressMonitorTests/StoreKitServiceWiringTests.swift (the WR-03 config-resolution pin, #if DEBUG)
- New launch-arg mechanism: the mock opt-in flag (DemoMode-shaped, name executor's discretion) + shared decision helper
- Widened factory seam: makeStoreKitService internal (testable through the factory, not direct construction)

<output>
Create `.planning/phases/02-delete-correctness-test-suite-trust/02-03-SUMMARY.md` when done
</output>
