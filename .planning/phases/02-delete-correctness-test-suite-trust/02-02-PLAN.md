---
phase: 02-delete-correctness-test-suite-trust
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - StressMonitor/StressMonitor/Services/StoreKit/StoreKitService.swift
  - StressMonitor/StressMonitorTests/CreditPurchaseFlowTests.swift
autonomous: true
requirements: [ENV-03]
gap_closure: false
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An unverified transaction delivered through the updates-listener entry is finished zero times"
    - "A redelivered unverified transaction is still finished zero times across repeated deliveries (idempotent observation — finishCallCount stays 0)"
    - "Listener and purchase paths share one rule: no code path finishes a transaction whose verification result is unverified; the four verified-only finish sites keep their existing pinned behavior (CreditPurchaseFlowTests :181-357 stays green)"
    - "A written reachability note covers all five finish sites — the four completePurchase sites verified-only by construction, and the one unverified-reachable site now fixed"
  prohibitions:
    - "never finish an unverified transaction (flagged-unverified — pinned by the new red/green test; verified-only paths remain test-pinned by the existing suite)"
    - "no runtime verification checks re-architected inside the completePurchase grant choke point (the four verified-only sites are not bugs — Pitfall 5)"
  artifacts:
    - WR-04 pinning test(s) in StressMonitor/StressMonitorTests/CreditPurchaseFlowTests.swift
    - Reachability note (doc comment on the listener entry + SUMMARY record)
  key_links:
    - "handle(transactionVerification:) .unverified branch -> observable via FakePurchaseTransaction.finishCallCount through the @testable seam"
---

<objective>
Fix WR-04 (per CONTEXT "Money-Path Advisories (ENV-03)"): the updates-listener's unverified branch finishes the transaction today (StoreKitService.swift:314-318) — destroying the only consumable proof-of-purchase and the redelivery retry path. Fix = stop finishing unverified transactions, pin it with a red/green test, and deliver the five-site reachability note.

Purpose: ROADMAP SC-5 / ENV-03 — WR-04 must be fixed (locked decision), with a pinning test proving an unverified transaction is never finished; a finished unverified consumable is unrecoverable (Apple's canonical observer ignores unverified transactions without finishing them).

Output: One-line-behavior fix in StoreKitService.swift, one failing-first pin test in CreditPurchaseFlowTests.swift, and a reachability note covering all five finish sites.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-CONTEXT.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-RESEARCH.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-PATTERNS.md
</context>

<feature>
  <name>Unverified transactions are never finished (WR-04, per CONTEXT "Money-Path Advisories")</name>
  <files>StressMonitor/StressMonitor/Services/StoreKit/StoreKitService.swift, StressMonitor/StressMonitorTests/CreditPurchaseFlowTests.swift</files>
  <behavior>
    - Drive the listener entry with an unverified result wrapping a FakePurchaseTransaction -> finishCallCount == 0
    - Drive it twice (redelivery) -> finishCallCount still == 0
    - Verified paths unchanged: existing CreditPurchaseFlowTests pins (:181-357) stay green
    Cases: unverified delivery -> no finish; unverified redelivery -> no finish; verified flow -> finishes exactly once (existing pins).
  </behavior>
  <implementation>Remove the finish call in the .unverified case (StoreKitService.swift:317); replace its comment with a log + Apple-canonical ignore-without-finish, mirroring the existing unverified handling at :226-228 (fetchPurchaseHistory) and :259-261 (refreshEntitlements). Widen handle(transactionVerification:) from private to internal (:309) — or extract a protocol-typed internal entry so FakePurchaseTransaction can drive the branch (the real VerificationResult<Transaction> is not constructible in tests; executor's discretion on the exact seam, per CONTEXT "the agent's Discretion"). Do NOT touch :375/:379/:388/:401 — verified-only by construction (Pitfall 5).</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin test asserting an unverified transaction is never finished (fails today)</name>
  <files>StressMonitor/StressMonitorTests/CreditPurchaseFlowTests.swift, StressMonitor/StressMonitor/Services/StoreKit/StoreKitService.swift</files>
  <read_first>StressMonitor/StressMonitorTests/CreditPurchaseFlowTests.swift (:33-55 FakePurchaseTransaction with finishCallCount, :115-133 isolation fixture + makeService, :181-357 existing pins incl. :220-234 the leave-unfinished precedent), StressMonitor/StressMonitor/Services/StoreKit/StoreKitService.swift (:301-319 the listener + unverified branch, :404-418 the leave-unfinished retry contract), 02-PATTERNS.md "CreditPurchaseFlowTests.swift MODIFY" section, 02-RESEARCH.md Pitfall 5 + "WR-04: the Apple-canonical unverified branch"</read_first>
  <behavior>
    - Test: unverified result driven through the listener entry -> finishCallCount == 0 (RED today: the branch finishes, count is 1)
    - Test: second delivery of the same unverified result -> finishCallCount still == 0
    - Existing verified-path pins remain green (no edits to them)
  </behavior>
  <action>Add the pin test(s) to CreditPurchaseFlowTests.swift. Because VerificationResult<Transaction> cannot be constructed with a fake payload, introduce the minimal test seam now (compile-only change, no behavior change): widen handle(transactionVerification:) to internal, or extract an internal protocol-typed entry point that the real listener delegates to — whichever lets FakePurchaseTransaction drive the unverified branch (executor's discretion; the seam change is additive and @testable-visible only). Use the per-test UserDefaults suite isolation fixture (:115-118) and constructor-injected spies only — no RequestCaptureURLProtocol statics (WINDOWS #12, Pitfall 8). Run the suite: the new test(s) MUST fail against current behavior (assert the failure output). Commit the failing test first (test(02-02): add failing pin for unverified transactions).</action>
  <verify>
    <automated>TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/CreditPurchaseFlowTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - New pin test exists in CreditPurchaseFlowTests.swift and FAILS in this run (red recorded — the unverified branch currently finishes the fake)
    - The failing test name is quoted in the SUMMARY
    - No existing test in the suite was modified or deleted
    - No RequestCaptureURLProtocol references added
  </acceptance_criteria>
  <done>Failing pin test committed; minimal @testable seam in place; no behavior change yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — remove the finish from the unverified branch + write the five-site reachability note</name>
  <files>StressMonitor/StressMonitor/Services/StoreKit/StoreKitService.swift</files>
  <read_first>StressMonitor/StressMonitor/Services/StoreKit/StoreKitService.swift (:301-319 the branch to fix, :226-228 and :259-261 the in-file ignore-precedents to mirror, :366-402 completePurchase finish sites, :158/:189 purchase entries, :439-446 checkVerified), 02-RESEARCH.md "State of the Art" row 1 (Apple canonical observer), 02-PATTERNS.md "StoreKitService.swift" section</read_first>
  <action>GREEN: in the .unverified case of the listener entry, remove the finish call and replace the comment with a short log + Apple-canonical ignore-without-finish (mirroring the :226-228/:259-261 precedent): the transaction stays unfinished so Transaction.updates redelivers it — the same retry contract handle(transaction:) already documents at :404-418. Then write the reachability note: a doc comment on the listener entry (or its extracted seam) stating why the four completePurchase finish sites (:375, :379, :388, :401) execute only for verified transactions — purchase(_:)/purchase(pack:) throw via checkVerified (:158/:189, checks at :439-446) and handle(transaction:) is reached only from the .verified case (:311-312) — and copy the same note into the SUMMARY under "WR-04 reachability audit". Do NOT add runtime verification inside completePurchase and do NOT touch :375-:401 (Pitfall 5). Run the full CreditPurchaseFlowTests suite green; swiftlint. Commits: feat(02-02) for the fix; the reachability note rides the same commit.</action>
  <verify>
    <automated>TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/CreditPurchaseFlowTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3 && swiftlint lint --quiet | grep StressKitService; echo LINT_DONE_$?</automated>
  </verify>
  <acceptance_criteria>
    - Full CreditPurchaseFlowTests suite green (new pins + all existing pins :181-357)
    - The unverified branch contains no finish call; its comment cites redelivery as the retry path (matches the :404-418 contract wording)
    - Diff touches only the unverified branch + comments/seam in StoreKitService.swift — the four verified-only finish sites are byte-unchanged (verifiable via git diff line ranges)
    - SUMMARY carries the "WR-04 reachability audit" note covering all five sites with the construction argument (checkVerified throws; .verified-only entry)
    - swiftlint: no new violations
  </acceptance_criteria>
  <done>WR-04 fixed and pinned: unverified transactions never finish; verified paths unchanged and green; reachability note delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App Store JWS -> StoreKitService listener | untrusted verification result crosses here; unverified payloads must never trigger irreversible actions |
| consumable purchase -> server redeem | finishing destroys the proof the redeem path needs on retry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Repudiation / Denial of Service | StoreKitService.swift:317 finish of unverified consumable (paid credits lost, unrecoverable) | high | mitigate | Remove the finish; pin with finishCallCount == 0 test (red first); leave-unfinished matches the existing :404-418 retry contract |
| T-02-04 | Tampering | Forged JWS forcing a grant | high | accept (existing) | checkVerified already throws on .unverified for grant paths (:439-446); this plan removes the evidence-destroying finish, it does not weaken verification |
</threat_model>

<verification>
- RED then GREEN commits exist for the pin test (2 atomic commits)
- CreditPurchaseFlowTests fully green under the -only-testing invocation
- git diff for StoreKitService.swift confined to the unverified branch + seam/comments; four verified-only finish sites untouched
</verification>

<success_criteria>
- WR-04 closed by fix: no code path finishes an unverified transaction; the pin test fails-first evidence is in the SUMMARY; the reachability note explains all five finish sites
</success_criteria>

## Artifacts this phase produces (this plan)

- WR-04 pinning tests (unverified -> finishCallCount == 0, redelivery variant) in CreditPurchaseFlowTests.swift
- The internal seam widening on the listener entry (handle(transactionVerification:) accessibility, or extracted protocol-typed entry)
- "WR-04 reachability audit" note (doc comment + SUMMARY) — the ENV-03 deliverable for the five finish sites

<output>
Create `.planning/phases/02-delete-correctness-test-suite-trust/02-02-SUMMARY.md` when done
</output>
