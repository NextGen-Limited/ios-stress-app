---
phase: 02-delete-correctness-test-suite-trust
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - AGENTS.md
  - docs/TESTING.md
  - .github/workflows/_test.yml
autonomous: true
requirements: [BUILD-04]
gap_closure: false
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One documented xcodebuild test invocation in AGENTS.md is flag-for-flag what _test.yml runs — project, scheme, destination form, the parallel-testing flag, and the env-var gating mechanism"
    - "Where the GSD_CI mechanism still exists after plan 02-04, AGENTS.md documents it exactly as an exported shell environment variable (TEST_RUNNER_ prefix stripped into the host), and no doc ever shows it as an xcodebuild argument"
    - "docs/TESTING.md no longer contains a self-contained stale invocation — it cross-references AGENTS.md as the canonical source with a one-liner, and no longer claims CI runs build validation instead of tests"
    - "A doc-truth note records that custom INFOPLIST_KEY_* settings never merge into product plists (the plist file is the source), covering the Phase-1 UIBackgroundModes finding"
    - "Planner assumption (BUILD-04, flagged unclassified-lifted-to-explicit): CI and dev docs cannot diverge if AGENTS.md is the single canonical block and the other surfaces only point at it — verified by grep + diff against the workflow file"
  prohibitions:
    - "no documented invocation diverges from what CI runs (flagged-unverified — diff check repeated at the phase trust gate in plan 02-06)"
    - "no doc extends the stale docs/TESTING.md content (replace-by-reference, not additive edits — locked CONTEXT decision)"
    - "the GSD_CI env var must never be documented as an xcodebuild flag (Pitfall 3 — the forwarding mechanism strips only the TEST_RUNNER_ prefix)"
  artifacts:
    - AGENTS.md canonical CI-parity invocation block (env-var form) + INFOPLIST_KEY doc-truth note
    - docs/TESTING.md invocation section replaced by a cross-reference one-liner
  key_links:
    - "AGENTS.md canonical block <-> _test.yml Run Tests step (they must not diverge) <-> docs/TESTING.md pointer"
---

<objective>
Close BUILD-04 (per CONTEXT "Regression Seam & Documented Invocation"): make AGENTS.md the canonical, CI-parity test-invocation source — including the TEST_RUNNER_GSD_CI env-var mechanism Phase 1 learned — with _test.yml and docs/TESTING.md as cross-references that cannot drift, plus the one doc-truth note folding in the Phase-1 UIBackgroundModes finding.

Purpose: ROADMAP SC-3 — one documented invocation is what CI runs and what dev docs tell a human to run; the two do not diverge.

Output: Updated AGENTS.md build/test block, a de-staled docs/TESTING.md pointer, a verified _test.yml cross-reference comment, and the INFOPLIST_KEY doc-truth note.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-CONTEXT.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-RESEARCH.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-PATTERNS.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AGENTS.md canonical CI-parity invocation (env-var form) + INFOPLIST_KEY doc-truth note</name>
  <files>AGENTS.md</files>
  <read_first>AGENTS.md (the "Build & test" section — current canonical block), .github/workflows/_test.yml (:177-212 the Run Tests step to mirror flag-for-flag), 02-RESEARCH.md Pitfall 3 (GSD_CI forwarding mechanics) + "Invocation Documentation State", .planning/phases/01-binary-manifest-truth/deferred-items.md (the UIBackgroundModes finding inputs), 02-PATTERNS.md "Doc/ledger surfaces" section, 02-04-SUMMARY.md (final gate state — determines whether the env-var form is documented as live or as history)</read_first>
  <action>Update the AGENTS.md "Build & test" section: the All-tests command becomes the CI-parity form matching _test.yml:177-196 flag-for-flag — including the parallel-testing flag and, per the 02-04 outcome, either the exported env-var gating (TEST_RUNNER_ prefix stripped into the host; shown as a shell-exported variable preceding the xcodebuild command, never as a command-line flag — Pitfall 3) or, if 02-04 removed the gates, the un-gated form with a one-line history note explaining the former env var. Keep the existing single-suite -only-testing examples and the testing-quirks prose (which already explains the local gating behavior — reconcile it with the new block, do not duplicate). Add one doc-truth note (short, factual) in the build section: custom INFOPLIST_KEY_* build settings are never merged into product plists by this project's Xcode version — the plist file is the source of truth for such keys — citing the Phase-1 finding that a UIBackgroundModes-style setting was absent from the built product precisely because it never merges. Do not invent new commands; mirror _test.yml exactly.</action>
  <verify>
    <automated>grep -n "parallel-testing-enabled NO" AGENTS.md .github/workflows/_test.yml && grep -n "TEST_RUNNER_GSD_CI" AGENTS.md .github/workflows/_test.yml && grep -c "INFOPLIST_KEY" AGENTS.md; echo GREP_DONE</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md All-tests block contains the parallel-testing flag and the same destination form as _test.yml (iPhone 16 CI parity, or documents the local iPhone 17 equivalent alongside)
    - Every GSD_CI mention in AGENTS.md is the TEST_RUNNER_-prefixed exported-env-var form (verify grep shows no argument-style use)
    - The INFOPLIST_KEY doc-truth note exists in AGENTS.md (grep count >= 1)
    - No other AGENTS.md guidance regressed (watchOS build, lint, test-quirks sections intact)
  </acceptance_criteria>
  <done>AGENTS.md is the canonical invocation source: CI-parity flag-for-flag, env-var mechanism documented correctly, UIBackgroundModes finding folded in as doc truth.</done>
</task>

<task type="auto">
  <name>Task 2: docs/TESTING.md replace-by-reference + _test.yml cross-reference verification</name>
  <files>docs/TESTING.md, .github/workflows/_test.yml</files>
  <read_first>docs/TESTING.md (whole file — lines 33-39 the stale command recipe, 82-88 the stale file table, 152-158 the stale build-validation claim), .github/workflows/_test.yml (:178-182 the existing comment — already correct per research), 02-RESEARCH.md "Deprecated/outdated" entry for docs/TESTING.md, 02-CONTEXT.md "Regression Seam & Documented Invocation" (locked: AGENTS.md canonical, one-liner cross-references)</read_first>
  <action>Replace docs/TESTING.md's invocation content with a one-liner cross-reference: the canonical xcodebuild invocation lives in AGENTS.md (Build & test) and CI runs exactly that form via _test.yml — do NOT extend or repair the stale prose (locked decision: replace, not extend). Remove the claims the research falsified (the build-validation-not-tests statement and any command recipe that omits the parallel-testing flag). Keep still-true content outside the invocation scope only where it names its source; otherwise point at AGENTS.md. In _test.yml, verify the comment block at :178-182 still matches reality after 02-04 (if the env gate was removed there, update the comment to the history note; if kept, leave as-is — the comment is already accurate). No workflow-behavior changes beyond that comment.</action>
  <verify>
    <automated>grep -n "AGENTS.md" docs/TESTING.md; grep -c "xcodebuild test" docs/TESTING.md; git diff --stat .github/workflows/_test.yml; echo VERIFY_DONE</automated>
  </verify>
  <acceptance_criteria>
    - docs/TESTING.md contains the AGENTS.md cross-reference (grep >= 1 hit)
    - docs/TESTING.md contains at most one xcodebuild-test mention and it is the pointer context, not a divergent recipe (no full command block with flags that differ from _test.yml)
    - The stale build-validation claim is gone (grep for the old claim text returns 0)
    - _test.yml diff (if any) is comment-only — no step/env/flag changes beyond what 02-04 already applied
  </acceptance_criteria>
  <done>docs/TESTING.md points at the canonical source with zero divergence risk; _test.yml's comment matches the post-02-04 reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev docs -> CI workflow | documentation claims cross into executable truth; divergence means humans verify something CI does not run |
| build settings -> product plist | the INFOPLIST_KEY doc-truth note prevents re-adding dead settings that silently never merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering (process) | doc/CI invocation drift (a human runs an un-gated form, hits the exit-65 stall, distrusts the suite) | medium | mitigate | AGENTS.md canonical block mirrored flag-for-flag from _test.yml; TESTING.md pointer-only; env-var form pinned to the exported-variable shape; trust gate in 02-06 re-diffs |
| T-02-10 | Information Disclosure (process) | dead INFOPLIST_KEY settings imply plist keys exist that never ship (false manifest claims) | low | mitigate | Doc-truth note records the never-merges rule; prevents reintroduction |
</threat_model>

<verification>
- grep shows the parallel-testing flag present in both AGENTS.md and _test.yml with matching form
- GSD_CI appears only in TEST_RUNNER_-prefixed exported-env form in docs
- docs/TESTING.md is pointer-only; _test.yml diff comment-only
</verification>

<success_criteria>
- BUILD-04 closed: one documented CI-parity invocation (AGENTS.md canonical), two non-diverging cross-references, env-var rule documented, UIBackgroundModes finding folded in as doc truth
</success_criteria>

## Artifacts this phase produces (this plan)

- AGENTS.md canonical CI-parity invocation block (exported env-var form, per 02-04's final gate state)
- The INFOPLIST_KEY doc-truth note (UIBackgroundModes finding recorded)
- docs/TESTING.md invocation section as a one-liner cross-reference (stale claims removed)

<output>
Create `.planning/phases/02-delete-correctness-test-suite-trust/02-05-SUMMARY.md` when done
</output>
