---
phase: 01.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - StressMonitor/StressMonitor/StressMonitorApp.swift
  - StressMonitor/StressMonitor/Models/StressMeasurement.swift
  - StressMonitor/StressMonitorTests/ModelContainerRecoveryTests.swift
  - StressMonitor/StressMonitorTests/StressMeasurementMigrationTests.swift
autonomous: true
requirements: [BUILD-04]
tags: [swiftdata, migration, launch-crash, schema, persistence]

estimate:
  tokens: 88000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - An app installed over an existing store created by ANY prior build shape launches without throwing loadIssueModelContainer — verified by a round-trip integration test (seed a divergent store, reopen under the migration plan, assert recovery succeeds and Habit is queryable).
    - The fatalError at StressMonitorApp.swift:82 is gone; a migration defect in RELEASE creates a fresh container and logs telemetry instead of bricking the app.
    - All non-optional StressMeasurement stored properties have property-level defaults so the type is lightweight-migratable.
    - The SwiftData ModelConfiguration's cloudKitDatabase setting matches the entitlements declaration (iCloud.stress.ai.com) or CloudKit is explicitly disabled (.none).
    - The three orphaned root source files (./StressMonitor/StressMonitorApp.swift, ./StressMonitor/StressMonitorSchema.swift, ./StressMonitor/Models/StressMeasurement.swift) no longer exist.
  artifacts:
    - StressMonitor/StressMonitorTests/ModelContainerRecoveryTests.swift
    - StressMonitor/StressMonitorTests/StressMeasurementMigrationTests.swift
  key_links:
    - StressMonitorApp.sharedModelContainer catch block → fresh-container recovery path
    - ModelConfiguration → cloudKitDatabase binding consistent with StressMonitor.entitlements
---

<objective>
Make the app launch without crashing for any prior on-disk SwiftData store shape, and remove the three compounding schema defects (fatalError, non-lightweight-migratable fields, CloudKit config drift) plus the orphaned root source set that documents a contradictory schema.

Purpose: The current `sharedModelContainer` initializer calls `fatalError` on any schema mismatch, permanently bricking the app on affected devices. This is a launch-blocking defect that must be fixed before any other phase work can be verified — nothing else matters if the app will not open.

Output: A non-fatal container-creation path with a tested recovery fallback, lightweight-migratable `@Model` properties, consistent CloudKit wiring, and a deleted orphaned source set. Backed by two test files proving (a) recovery from a schema-mismatched store and (b) property-default lightweight migration.

D5 RESOLUTION (per CONTEXT.md + .claude/CLAUDE.md "feature-complete but not submittable"): Option A — the installed base has no real user data to preserve. This plan implements Option A's intent (accept data loss on schema mismatch) but uses the REAL SwiftData recovery API, not the `eraseDatabaseOnSchemaChange` parameter named in RESEARCH.md (see Execution Note below).
</objective>

<execution_context>
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.claude/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.planning/PROJECT.md
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.planning/ROADMAP.md
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.planning/STATE.md
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.planning/phases/01.1-swiftdata-schema-migration-safety/01.1-CONTEXT.md
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.planning/phases/01.1-swiftdata-schema-migration-safety/01.1-RESEARCH.md
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/StressMonitor/StressMonitor/StressMonitorApp.swift
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/StressMonitor/StressMonitor/Models/StressMeasurement.swift
</context>

<execution_note>
CRITICAL CORRECTION TO RESEARCH.MD OPTION A: The `ModelConfiguration(...eraseDatabaseOnSchemaChange: true)` parameter named in 01.1-RESEARCH.md DOES NOT EXIST in the SwiftData SDK. Verified against `iPhoneSimulator26.2.sdk/.../SwiftData.swiftmodule/arm64-apple-ios-simulator.swiftinterface`: `ModelConfiguration` exposes only `name/schema/isStoredInMemoryOnly/allowsSave/groupContainer/cloudKitDatabase/url` — there is no erase-on-mismatch flag.

The real recovery path for "store schema does not match any declared VersionedSchema" is:
1. Catch the thrown error from `ModelContainer(for:migrationPlan:configurations:)`.
2. Delete the on-disk store at the default location: `URL.applicationSupportDirectory.appending(path: "default.store")` plus its `-wal` and `-shm` sidecars (the inspected simulator store lives at `Library/Application Support/default.store`).
3. Recreate the container with the same configuration and NO migrationPlan (a fresh store always matches the live schema).

This achieves Option A's intent (accept data loss on schema mismatch, never brick the app) using a real API. The plan's <action> bodies reflect this. If the executor finds a different `eraseOnSchemaChange`-style API in their SDK, they should prefer it — but as of iOS 26.2 it does not exist.
</execution_note>

<scope_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By | Status |
|--------|------|-----------|--------|
| GOAL (ROADMAP) | App launches without crashing for any prior store schema state | Task 1 (tracer: integration test) + Task 2 (fix) | COVERED |
| GOAL (ROADMAP) | No fatalError; non-fatal recovery in RELEASE | Task 2 (schema safety fix) | COVERED |
| GOAL (ROADMAP) | eraseDatabaseOnSchemaChange DEBUG-gated OR frozen snapshots | Task 2 (uses real recovery API per Execution Note; D5=Option A data-loss acceptance documented) | COVERED (adapted) |
| GOAL (ROADMAP) | CloudKit config consistent | Task 2 (schema safety fix — cloudKitDatabase binding) | COVERED |
| GOAL (ROADMAP) | Orphaned root source set deleted | Task 3 (cleanup) | COVERED |
| REQ BUILD-04 | Test execution — launch-path integration test runnable | Task 1 (integration test) + Task 2 (property-default unit test) | COVERED |
| RESEARCH Option A step 3 | Property-level defaults on StressMeasurement non-optional fields | Task 2 (tdd sub-task) | COVERED |
| RESEARCH Option A step 5 | Delete orphaned root source | Task 3 | COVERED |
| CONTEXT D5 | Option A (pre-release, accept data loss) | Task 2 catch-block recovery | COVERED |
| CONTEXT compounding #1 | Non-optional StressMeasurement fields lack defaults | Task 2 | COVERED |
| CONTEXT compounding #3 | fatalError with no recovery | Task 1 (test) + Task 2 (fix) | COVERED |
| CONTEXT compounding #4 | CloudKit config inconsistency (3 IDs) | Task 2 + Task 3 | COVERED |
| CONTEXT LOW #9 | Orphaned root StressMeasurement duplicate | Task 3 | COVERED |

### Out of scope (other phases / deferred)
- CONTEXT HIGH #5 (StressRepository.mergeRemoteMeasurement O(n) fetch-all) — performance concern, Phase 2 scope.
- CONTEXT MEDIUM #6/7/8 (try? save swallowing, saveBatch chunking, HabitViewModel unconditional save) — data-integrity hardening, Phase 2 scope.
- CONTEXT LOW #10 (PhoneConnectivityManager implicit main-queue assumption) — currently safe, monitor only.
- RESEARCH Option B (frozen V1 @Model snapshots + custom MigrationStage) — CONDITIONAL scope expansion, gated on D5 escalation. NOT implemented. If a TestFlight population with real stress history is later identified, a follow-up plan (01.1-02) must add `StressMeasurementV1`/`CharacterUnlockV1` frozen types inside `AppSchemaV1` and a `MigrationStage.custom`. Do not implement preemptively.
- CONTEXT compounding #2 (`#Unique<CharacterUnlock>` not captured by any migration stage) — on the inspected store the unique index already exists with no duplicates, so it is not launch-blocking. Under Option A's accept-data-loss policy any drift is resolved by the fresh-container fallback. Not given its own task.
</scope_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (TRACER): Round-trip integration test proving the container opens (or recovers) for a divergent prior store</name>
  <files>StressMonitor/StressMonitorTests/ModelContainerRecoveryTests.swift</files>
  <behavior>
    - Test 1 (RED — reproduces the crash path): build a ModelContainer from a deliberately DIVERGENT schema (e.g. a Schema containing only a throwaway `@Model final class LegacyShape { var legacy: String }` with its own unrelated store URL), write a row, close it. Then attempt `ModelContainer(for: StressMonitorApp.schema, migrationPlan: AppMigrationPlan.self, configurations: [config pointed at the SAME url])`. This MUST throw (the on-disk schema is unknown to the migration plan). The RED assertion is that the throw is caught rather than fatal.
    - Test 2 (GREEN — recovery works): after the throw in Test 1, delete the store file at the config URL (the store plus its `-wal`/`-shm` sidecars), then recreate `ModelContainer(for: StressMonitorApp.schema, configurations: [config at the same url])` with NO migrationPlan. Assert: (a) no throw, (b) `container.mainContext.fetch(FetchDescriptor<Habit>())` returns an empty array (Habit is queryable → schema is live), (c) re-inserting a Habit and saving succeeds.
    - Test 3 (happy path regression guard): a pristine store at a fresh URL opens on the FIRST try with the migration plan; insert one StressMeasurement + one CharacterUnlock + one Habit, fetch all three back, assert round-trip integrity.
    - Each test uses its OWN isolated store URL under `URL.temporaryDirectory` (e.g. `appending(path: "t-\(UUID().uuidString)/default.store")`) and tears down the files in a `defer`/`addTeardownBlock`. Do NOT touch the simulator's real `default.store`.
  </behavior>
  <action>
    Create `StressMonitorTests/ModelContainerRecoveryTests.swift` as an XCTest case (`@testable import StressMonitor`, `import SwiftData`). The test reproduces the failure mode in 01.1-CONTEXT.md (on-disk schema unknown to the migration plan → `loadIssueModelContainer`) at the library level, WITHOUT launching the full app — the `StressMonitorApp.schema` static and `AppMigrationPlan` are accessible via `@testable import`.

    Key implementation details:
    - Use the `ModelConfiguration(url:)` initializer to point each test at an isolated file URL under `URL.temporaryDirectory` so tests are hermetic and parallelizable.
    - For the divergent-store setup, declare a file-local `@Model final class LegacyShape { var legacyValue: String = "" }` INSIDE the test file (not in the app target) and create its container with `ModelContainer(for: LegacyShape.self, configurations: [config(url:)])`. This is the stand-in for "a store written by an older build shape."
    - For store deletion, delete `default.store`, `default.store-wal`, and `default.store-shm` at the test URL (the SQLite sidecars). Use `FileManager.default`.
    - The RED→GREEN loop: commit the failing Test 1 assertion first (it documents that the current `fatalError` path is unrecoverable), then Task 2 makes Test 2's recovery path real in the app and Test 1's "caught rather than fatal" assertion passes once Task 2 ships.
    - Do NOT reference `eraseDatabaseOnSchemaChange` — it is not a real API (see Execution Note).

    Per D5 = Option A.
  </action>
  <verify>
    <automated>set -o pipefail && xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:'StressMonitorTests/ModelContainerRecoveryTests' 2>&1 | xcpretty</automated>
  </verify>
  <done>
    Test file exists, compiles, and runs. Test 2 (recovery via store-delete + container-recreate) passes. Test 1 passes once Task 2 replaces fatalError. Test 3 (happy-path round trip) passes. All three tests use isolated store URLs and clean up their files. Committed as the RED commit (failing assertions for the unrecovered path) then left GREEN-ready for Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace fatalError with non-fatal recovery, add property-level defaults, and bind CloudKit consistently (D5 = Option A)</name>
  <files>StressMonitor/StressMonitor/StressMonitorApp.swift, StressMonitor/StressMonitor/Models/StressMeasurement.swift, StressMonitor/StressMonitorTests/StressMeasurementMigrationTests.swift</files>
  <behavior>
    - Test 1 (StressMeasurementMigrationTests): constructing `StressMeasurement(timestamp:stressLevel:hrv:)` with only the three historical fields produces an instance whose non-optional sync fields take their documented defaults — `restingHeartRate == 0`, `categoryRawValue == StressCategory.mild.rawValue`, `isSynced == false`, `deviceID == CloudKitDeviceID.current` (non-empty). This proves the type is safe for SwiftData lightweight migration (property-level defaults exist).
    - Test 2: `seedDefaultCharacterUnlocks` is callable against a freshly recovered (empty) container without throwing — re-run it after the recovery path and assert exactly the free characters are inserted.
  </behavior>
  <action>
    THREE coordinated edits in the active app target (all per D5 = Option A):

    1. `StressMeasurement.swift` — add property-level defaults to the four non-optional stored properties identified in 01.1-CONTEXT.md compounding #1, so SwiftData can backfill them during a lightweight migration:
       - `restingHeartRate: Double = 0`
       - `categoryRawValue: String = StressCategory.mild.rawValue`
       - `isSynced: Bool = false`
       - `deviceID: String = CloudKitDeviceID.current`
       The existing memberwise `init` already passes these through; adding the default at the stored-property declaration site is what makes the type lightweight-migratable. Do NOT change any other property, the computed `category`, or the `CloudKitDeviceID` enum. Keep 4-space indentation (file currently uses 2 — convert the touched lines to 4 to match the codebase convention; do not reformat untouched lines).

    2. `StressMonitorApp.swift` — replace the `fatalError("Could not create ModelContainer: \(error)")` catch block (line 82) with a recovery path that NEVER traps in RELEASE:
       - Extract container creation into a `private static func makeContainer() -> ModelContainer` helper so it is testable and so the catch block can recurse/branch cleanly.
       - PRIMARY attempt: `ModelContainer(for: schema, migrationPlan: AppMigrationPlan.self, configurations: [modelConfiguration])` (unchanged).
       - On catch in `#if DEBUG`: keep dev visibility by `fatalError("ModelContainer creation failed in DEBUG: \(error)")` — developers must see schema bugs immediately during development.
       - On catch in RELEASE (the `#else`): delete the on-disk store at `URL.applicationSupportDirectory.appending(path: "default.store")` and its `-wal`/`-shm` sidecars via `FileManager.default`, then recreate `ModelContainer(for: schema, configurations: [ModelConfiguration(schema: schema, isStoredInMemoryOnly: false)])` with NO migrationPlan (fresh store always matches live schema). Log via `os.Logger(subsystem: "com.stressmonitor.app", category: "Persistence")` (add `import os`). If the recreation ALSO throws, fall back to an in-memory-only container (`isStoredInMemoryOnly: true`) so the app still launches read-only rather than trapping; log at `.fault` level.
       - Also bind the CloudKit database consistently in `modelConfiguration` (01.1-CONTEXT.md compounding #4): change `ModelConfiguration(schema: schema, isStoredInMemoryOnly: false)` to `ModelConfiguration(schema: schema, isStoredInMemoryOnly: false, cloudKitDatabase: .automatic)`. `.automatic` resolves against the entitlement's `iCloud.stress.ai.com` container without hardcoding the identifier. Do NOT use the orphaned `iCloud.com.stressmonitor.app` ID anywhere. (Rationale: `.automatic` is the documented way to bind to the entitled CloudKit container; an explicit `.identifier(...)` would re-introduce a string that can drift from entitlements. If `.automatic` is rejected at runtime because not all `@Model` properties have CloudKit-conformant defaults, the property-default edit in step 1 resolves it for `StressMeasurement`; verify `Habit` and `CharacterUnlock` already conform — they do, their non-optional fields all have init defaults.)
       - Keep `seedDefaultCharacterUnlocks(in:)` invocation after BOTH the primary and the recovered container paths so a recovered (empty) store still gets its default CharacterUnlock rows.
       - Preserve the existing `os_signpost` DEBUG launch instrumentation.

    3. `StressMeasurementMigrationTests.swift` — add the two behavior tests above (property defaults; seed-after-recover). Use XCTest, `@testable import StressMonitor`, `import SwiftData`. For the seed-after-recover test, build an in-memory container (`ModelConfiguration(schema: StressMonitorApp.schema, isStoredInMemoryOnly: true)`), call `StressMonitorApp.seedDefaultCharacterUnlocks(in:)` (lower its access from `private static` to `internal static` if needed for the test — prefer keeping it private and testing via the public container path if access change is awkward), and assert the free characters are present.

    Per D5 (Option A). Per Execution Note — do NOT use `eraseDatabaseOnSchemaChange`; use store-file deletion + container recreation. Per CLAUDE.md comment policy — no explanatory comments; the recovery rationale lives in this PLAN.md and the commit message, not in code comments. A single `// MARK: - ModelContainer Recovery` divider is acceptable per codebase convention.
  </action>
  <verify>
    <automated>set -o pipefail && xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:'StressMonitorTests/ModelContainerRecoveryTests' -only-testing:'StressMonitorTests/StressMeasurementMigrationTests' 2>&1 | xcpretty</automated>
  </verify>
  <done>
    (a) `grep -n 'fatalError("Could not create ModelContainer' StressMonitor/StressMonitor/StressMonitorApp.swift` returns NO match inside the RELEASE catch path (DEBUG-only fatalError is acceptable). (b) All four StressMeasurement non-optional stored properties show `= <default>` at their declaration site. (c) `modelConfiguration` binds `cloudKitDatabase: .automatic` and no source file references `iCloud.com.stressmonitor.app`. (d) ModelContainerRecoveryTests (Task 1) AND StressMeasurementMigrationTests (Task 2) both pass. (e) A Release build of the scheme succeeds (`xcodebuild build -scheme StressMonitor -configuration Release`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete the orphaned root source set (criterion 5)</name>
  <files>StressMonitor/StressMonitorApp.swift, StressMonitor/StressMonitorSchema.swift, StressMonitor/Models/StressMeasurement.swift</files>
  <precondition>The three paths above resolve to the orphaned root-level source set (NOT the active `StressMonitor/StressMonitor/` target sources). Verify with `grep -c "PBXFileSystemSynchronizedRootGroup" StressMonitor/StressMonitor.xcodeproj/project.pbxproj` — the synchronized root is `StressMonitor/StressMonitor/`, so files directly under `StressMonitor/` are in no target.</precondition>
  <action>
    Delete the three orphaned files. They are NOT compiled (the iOS target's `PBXFileSystemSynchronizedRootGroup` is `StressMonitor/StressMonitor/`), but they are maintenance traps that document a contradictory schema:

    - `StressMonitor/StressMonitorApp.swift` — declares a different `@main` app with `StressMonitorSchema.modelContainer` and `@StateObject`-based managers. Contradicts the active entry point.
    - `StressMonitor/StressMonitorSchema.swift` — references the WRONG CloudKit container ID `iCloud.com.stressmonitor.app` (entitlements say `iCloud.stress.ai.com`). This is compounding issue #4 in 01.1-CONTEXT.md.
    - `StressMonitor/Models/StressMeasurement.swift` — a divergent `@Model` with explicit `id: UUID`, `heartRate`/`source`/`cloudKitMetadata` fields that do not exist in the active model, and a `toCKRecord()`/`from(reading:)` bridge to the legacy `StressReading` struct. Compounding issue #9.

    After deletion, confirm with `grep -rn "iCloud.com.stressmonitor.app" StressMonitor/` returns nothing. Do NOT delete the active target sources under `StressMonitor/StressMonitor/` — only the three root-level paths. No code references these orphans (they are not in any target), so deletion is safe with no follow-up edits. Use `git rm` so the deletion is tracked.
  </action>
  <verify>
    <automated>cd StressMonitor && rm -f StressMonitorApp.swift StressMonitorSchema.swift Models/StressMeasurement.swift && rmdir Models 2>/dev/null; test ! -e StressMonitorApp.swift && test ! -e StressMonitorSchema.swift && test ! -e Models/StressMeasurement.swift && ! grep -rq "iCloud.com.stressmonitor.app" . && echo "ORPHANS_GONE"</automated>
  </verify>
  <done>
    `git status --short` shows the three files as deleted. `grep -rn "iCloud.com.stressmonitor.app" StressMonitor/` returns nothing. The active build is unaffected: `xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone Air' -configuration Debug 2>&1 | tail -1` reports BUILD SUCCEEDED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| On-disk SwiftData store → ModelContainer | A persisted store written by an arbitrary prior build is re-opened by the current schema. Untrusted schema state crosses this boundary at launch. |
| App process → CloudKit container | The SwiftData ModelConfiguration binds to the iCloud container declared in entitlements. A mismatch between config and entitlement causes a silent sync failure or a launch fault. |

## STRIDE Threat Register (ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-01 | Tampering | On-disk `default.store` (prior build) | high | mitigate | Catch `loadIssueModelContainer` and recover by deleting the store + recreating the container, instead of trapping. A tampered/divergent store can never brick the app (Task 2). |
| T-01.1-02 | Denial of Service | `sharedModelContainer` initializer (line 82) | critical | mitigate | Remove `fatalError` from the RELEASE path; RELEASE falls back to a fresh, then in-memory container so launch always succeeds (Task 2). DEBUG retains `fatalError` for developer visibility. |
| T-01.1-03 | Information Disclosure | RELEASE telemetry log on recovery | low | accept | The recovery `os.Logger` line records that a store was discarded. It logs no user health data (only the SwiftData error description). Acceptable at ASVS L1; the subsystem/category tags scope it to the developer log. |
| T-01.1-04 | Tampering | CloudKit container identifier binding | medium | mitigate | Bind `cloudKitDatabase: .automatic` so the container resolves from entitlements (`iCloud.stress.ai.com`) rather than a hardcoded string; delete the orphaned `iCloud.com.stressmonitor.app` reference (Tasks 2 + 3). |
| T-01.1-03R | Repudiation | seedDefaultCharacterUnlocks after recovery | low | accept | After a recovery wipe, default CharacterUnlock rows are re-seeded deterministically; no user-authored data is silently altered. Logged via the existing recovery log line. |

No third-party package installs in this plan — the Package Legitimacy Gate does not apply (SwiftData/CloudKit/Foundation/os are system frameworks).
</threat_model>

<verification>
Phase-level checks (run after all three tasks):

1. Full test bundle green:
   `xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | xcpretty`

2. Release archive builds (the catch path must compile under `#else`):
   `xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -configuration Release -destination 'generic/platform=iOS' 2>&1 | tail -3`

3. No fatalError in the RELEASE catch path:
   `awk '/catch/,/^\s*\}/' StressMonitor/StressMonitor/StressMonitorApp.swift | grep -v '#if DEBUG'` — manually confirm the only `fatalError` is inside the `#if DEBUG` branch.

4. No orphaned CloudKit ID anywhere:
   `grep -rn "iCloud.com.stressmonitor.app" StressMonitor/` returns empty.

5. Orphaned root files gone:
   `test ! -e StressMonitor/StressMonitorApp.swift && test ! -e StressMonitor/StressMonitorSchema.swift && test ! -e StressMonitor/Models/StressMeasurement.swift`

6. Property defaults present on StressMeasurement non-optional fields — visual diff of `StressMeasurement.swift` shows `= 0`, `= StressCategory.mild.rawValue`, `= false`, `= CloudKitDeviceID.current` at the four declaration sites.
</verification>

<success_criteria>
This plan is complete when ALL of the following are true (maps to ROADMAP Phase 01.1 criteria 1–5):

1. ModelContainerRecoveryTests passes: a divergent prior store is recovered (deleted + container recreated) without trapping, and Habit is queryable afterward. → ROADMAP criterion 1.
2. The RELEASE catch path in `sharedModelContainer` has no `fatalError`; it recovers via store-delete + recreate, with an in-memory last resort. → ROADMAP criterion 2.
3. Option A is implemented (D5 = pre-release, accept data loss) via the real recovery API (store-file deletion), NOT the nonexistent `eraseDatabaseOnSchemaChange` flag. The data-loss acceptance is documented in the commit and this PLAN.md. → ROADMAP criterion 3 (Option A branch).
4. `modelConfiguration` binds `cloudKitDatabase: .automatic` (resolves to the entitled `iCloud.stress.ai.com`), and no source references `iCloud.com.stressmonitor.app`. → ROADMAP criterion 4.
5. The three orphaned root files are `git rm`'d. → ROADMAP criterion 5.

CONDITIONAL ESCALATION (not in this plan): if a TestFlight population WITH real stress history is identified, D5 flips to Option B and a follow-up plan (01.1-02) must reconstruct frozen V1 `@Model` snapshots + a custom MigrationStage. Do NOT implement Option B preemptively.
</success_criteria>

<output>
Create `.planning/phases/01.1-swiftdata-schema-migration-safety/01.1-01-SUMMARY.md` when done.
</output>
