---
phase: 02-credits-system-iap-transition
plan: 04
type: execute
wave: 3
depends_on: ['02-02', '02-03']
files_modified:
  - StressMonitor/StressMonitor/ViewModels/CreditsViewModel.swift
  - StressMonitor/StressMonitor/Views/Premium/IAPPremiumView.swift
  - StressMonitor/StressMonitor/Views/Premium/PaywallView.swift
  - StressMonitor/StressMonitor/Views/Premium/Components/PlanCard.swift
  - StressMonitor/StressMonitor/Views/Premium/PurchaseSuccessView.swift
  - StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift
  - StressMonitor/StressMonitor/Views/Settings/SettingsView.swift
  - StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift
  - StressMonitor/StressMonitorTests/CreditsViewModelTests.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
autonomous: false
requirements: [IAP-04, IAP-05, IAP-06, AUTH-02, derived-CR-06]
user_setup:
  - service: stress-app-be deployment (dokploy)
    why: "Live end-to-end purchase verification needs the redemption endpoint deployed with its migration applied — outward-facing, user-confirmed"
    dashboard_config:
      - task: "Deploy branch feat/credits-redeem (image rebuild; note the documented --force requirement for same-tag updates) and apply migration 20260816120000"
        location: "Dokploy dashboard for stress-api.dropitx.site (see stress-app-be deployment guide)"
  - service: App Store Connect sandbox
    why: "Sandbox purchase verification of the consumable flow"
    dashboard_config:
      - task: "Ensure a sandbox tester account exists and the DEC-2 consumables are in Ready to Submit state (from 02-03 user_setup)"
        location: "App Store Connect → Users and Access → Sandbox Testers"
estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The paywall presents credit pack cards (from the catalog) with accurate prices and per-unit savings, a live balance header, the free reset date, and premium users see Unlimited — never a raw sentinel count"
    - "Tapping a pack runs the full purchase flow and success is derived from the post-purchase server balance, not from the purchase call's return alone"
    - "Balance appears at every DEC-2 placement (chat sheet pill / Settings row per the recorded option)"
    - "A real sandbox purchase of the small pack against the deployed backend increments the displayed balance exactly once"
    - "A user who exhausts credits mid-chat lands on the paywall with a clear path to buy more (no dead-end error)"
    - "All UI meets the design system: dual coding, accessibleDynamicType, >=44pt targets, HapticManager feedback"
  artifacts:
    - "StressMonitor/StressMonitor/ViewModels/CreditsViewModel.swift"
    - "Reworked IAPPremiumView (pack mode) + PackCard-or-PlanCard reuse"
    - "Balance pill in ChatBottomSheetView; balance value rows in SettingsView"
    - "StressMonitorTests/CreditsViewModelTests.swift (pbxproj-registered)"
  key_links:
    - "CreditsViewModel → StoreKitService.purchase(pack:) (02-03) + CreditService balance (02-01)"
    - "ChatBottomSheetView pill ← ChatViewModel balance exposure ← CreditService"
    - "Paywall pack cards ← CreditPack catalog ← .storekit/ASC products"
---

<objective>
Turn the tracer spine plus the purchase machinery into the shipped user experience: a packs-era paywall, balance surfaces at every DEC-2 placement, and a verified end-to-end money path (sandbox purchase → server grant → visible balance) against the deployed backend.

Purpose: The phase goal is user-visible — credits-gated chat with a working paywall. This plan closes IAP-05/IAP-06 display-and-verification requirements and the AUTH-02 residual (credit-state surface as live session probe).
Output: Production paywall UX, balance surfaces, end-to-end verification evidence.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-credits-system-iap-transition/02-RESEARCH.md
@.planning/phases/02-credits-system-iap-transition/02-PATTERNS.md
@.planning/phases/02-credits-system-iap-transition/02-01-SUMMARY.md (DEC-1/DEC-2, CreditService)
@.planning/phases/02-credits-system-iap-transition/02-03-SUMMARY.md (purchase(pack:), catalog, JWS spelling)
@.planning/phases/02-credits-system-iap-transition/02-02-SUMMARY.md (deployed contract responses)
@StressMonitor/StressMonitor/Views/Premium/IAPPremiumView.swift
@StressMonitor/StressMonitor/Views/Premium/Components/PlanCard.swift
@StressMonitor/StressMonitor/ViewModels/PremiumViewModel.swift
@StressMonitor/StressMonitor/ViewModels/AccountViewModel.swift
@StressMonitor/StressMonitor/Views/Settings/SettingsView.swift

human_verify_mode is end-of-phase: do not insert inline checkpoint:human-verify tasks; final verification uses the <verify><human-check> block below.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN — CreditsViewModel + packs-era paywall</name>
  <read_first>
  - StressMonitor/StressMonitor/ViewModels/PremiumViewModel.swift — purchaseSelectedPlan() state machine (lines 38–66): guard, defer, silent cancel, pending message, post-state-derived success
  - StressMonitor/StressMonitor/ViewModels/AccountViewModel.swift — @Observable VM contract analog
  - StressMonitor/StressMonitor/Views/Premium/IAPPremiumView.swift — plan grid + skeleton pattern (lines 42–77), trial banner (lines 152–186), fine print (lines 88–92)
  - StressMonitor/StressMonitor/Views/Premium/Components/PlanCard.swift — card tokens to reuse/share
  - StressMonitor/StressMonitor/Views/Premium/PaywallView.swift — wrapper + reason
  - StressMonitor/StressMonitorTests/PremiumViewModelTests.swift — FakeStoreKitService double to extend for pack purchases
  - .planning/phases/02-credits-system-iap-transition/02-RESEARCH.md — Pitfall 4 (sentinel), Pitfall 5 (maxTokens shrink copy), Open Q4
  </read_first>
  <files>StressMonitor/StressMonitor/ViewModels/CreditsViewModel.swift, StressMonitor/StressMonitor/Views/Premium/IAPPremiumView.swift, StressMonitor/StressMonitor/Views/Premium/PaywallView.swift, StressMonitor/StressMonitor/Views/Premium/Components/PlanCard.swift, StressMonitor/StressMonitor/Views/Premium/PurchaseSuccessView.swift, StressMonitor/StressMonitorTests/CreditsViewModelTests.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj</files>
  <behavior>
    - RED (CreditsViewModelTests, new file, pbxproj-registered next-free A0xx/B0xx): purchaseSelectedPack() with a fake store kit succeeding and a mock credit service whose balance increases derives showSuccess from the observed balance change; purchase-cancelled error is silent (no errorMessage, no alert); pending error sets a message; failure of the purchase call sets a message and resets loading; balance display strings render the premium plan_type as the word Unlimited and a free tier as a count, and a low-remaining state (below the 20-credit backend token-tier threshold) exposes the short-responses expectation copy flag
    - GREEN: VM + views implemented; suite passes
  </behavior>
  <action>RED first. Build CreditsViewModel on the AccountViewModel/PremiumViewModel contract: injected CreditService + StoreKitServiceProtocol, selected pack state, isLoading/errorMessage/showSuccess, rethrow-silent-cancel semantics, success derived from CreditService balance after the purchase call returns. Rework the paywall for packs per DEC-1: pack cards replace (option-b) or lead (option-a) the plan grid — reuse PlanCard's token structure either via a PackCard sibling sharing Typography.iap*/Color.iap* or by generalizing PlanCard; keep the nil-placeholder skeleton trick for unresolved products. Header shows live balance (count or Unlimited) plus the free reset date formatted from free_reset_at. Trial banner and subscription auto-renew fine print are gated per DEC-1 (option-b: deleted; option-a: moved to a secondary section for the subscription tier). Add the low-credits expectation line when remaining is under 20 (research Pitfall 5). PurchaseSuccessView shows granted credits (pack amount + new balance). All interactive targets >= 44pt, .accessibleDynamicType() on text, color paired with icon/text (dual coding), HapticManager on selection/purchase taps. PaywallView passes the reason through for outOfCredits-contextual copy ("You're out of credits" heading variant) without changing layout structure.</action>
  <verify>
    <automated>cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath "$HOME/Library/Developer/Xcode/DerivedData/StressMonitor-gsfjxdxaciivyygrqgnuxctytuxw" -only-testing:StressMonitorTests/CreditsViewModelTests -only-testing:StressMonitorTests/PremiumViewModelTests -parallel-testing-enabled NO</automated>
    <automated>cd StressMonitor && grep -rn '999999' StressMonitor/ --include='*.swift' | grep -vc '^\s*//'</automated>
  </verify>
  <acceptance_criteria>
    - CreditsViewModelTests + PremiumViewModelTests exit 0
    - Sentinel-literal count in shipped Swift sources is 0 (comment-filtered)
    - Paywall compiles in both DEC-1 modes used (only the selected mode's branch is exercised; the other is deleted, not commented out)
    - New test file registered: orphan-count check from 02-01 Task 3 still prints 0
  </acceptance_criteria>
  <done>Packs-era paywall and its view model are test-pinned; premium sentinel never renders raw; design-system rules applied.</done>
  <reversibility rating="costly">Paywall rework is the phase's most user-visible surface; reverting loses polish work but not data — costly, not one-way.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN — balance surfaces at DEC-2 placements</name>
  <read_first>
  - StressMonitor/StressMonitor/Views/Settings/SettingsView.swift — navRow(value:) pattern, chat row (lines 135–145), Plus row (lines 260–270), MeHeroCard plus tap (line 108)
  - StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift — sheet header layout
  - StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift — 02-01's balance exposure + paywall trigger injection
  - StressMonitor/StressMonitor/StressMonitorApp.swift — creditService environment injection from 02-01
  </read_first>
  <files>StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift, StressMonitor/StressMonitor/Views/Settings/SettingsView.swift, StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift, StressMonitor/StressMonitorTests/CreditsViewModelTests.swift</files>
  <behavior>
    - RED: CreditsViewModelTests display-rule cases for surface strings — chat pill and Settings row render the same balance string source (single formatter); free tier shows count, premium shows Unlimited; unavailable state (nil balance) shows a neutral placeholder, never a zero
    - GREEN: surfaces wired; suite passes
  </behavior>
  <action>RED first. Implement exactly the DEC-2 placement recorded in 02-01-SUMMARY: placement-a adds all three surfaces (compact balance pill in the ChatBottomSheetView header reading credit state from the environment; Settings chat-row value shows the balance string beside availability, refreshed onAppear alongside the existing account refresh; Plus row value reflects live state instead of the static teaser string), placement-b adds paywall (already done in Task 1) + the two Settings values only. Route every surface through one shared balance-string formatter on CreditService or CreditsViewModel so strings cannot diverge. Settings plus-row tap keeps presenting the paywall with the general reason. Chat sheet pill tap presents the paywall with the out-of-credits reason when remaining is zero, else is non-interactive informational. Keep targets >= 44pt where tappable, dual coding on any color-coded state (e.g. low balance pairs color with an icon).</action>
  <verify>
    <automated>cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath "$HOME/Library/Developer/Xcode/DerivedData/StressMonitor-gsfjxdxaciivyygrqgnuxctytuxw" -only-testing:StressMonitorTests/CreditsViewModelTests -parallel-testing-enabled NO</automated>
    <automated>cd StressMonitor && xcodebuild build -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath "$HOME/Library/Developer/Xcode/DerivedData/StressMonitor-gsfjxdxaciivyygrqgnuxctytuxw"</automated>
  </verify>
  <acceptance_criteria>
    - CreditsViewModelTests exits 0 including the surface-string cases
    - App build exits 0
    - grep count of the shared formatter usage across ChatBottomSheetView + SettingsView >= 2 (single source of balance strings)
  </acceptance_criteria>
  <done>Balance is visible wherever DEC-2 placed it, from one formatter, with correct premium rendering.</done>
  <reversibility rating="reversible">Additive UI surfaces.</reversibility>
</task>

<task type="auto">
  <name>Task 3: End-to-end integration sweep + live money-path verification</name>
  <read_first>
  - .planning/phases/02-credits-system-iap-transition/02-VALIDATION.md — sampling contract
  - .planning/phases/02-credits-system-iap-transition/02-02-SUMMARY.md — deployed endpoint responses
  - StressMonitor/StressMonitor.xcodeproj/xcshareddata/xcschemes/StressMonitor.xcscheme — Launch action loads StressMonitorProducts.storekit (sandbox purchases in simulator)
  </read_first>
  <files>StressMonitor/StressMonitorTests/CreditsViewModelTests.swift</files>
  <action>Run the full verification sweep in order: full test suite; Release-configuration build re-check; then the live smoke against the deployed backend on the simulator (scheme Launch action already provides the .storekit sandbox): (1) fresh anonymous user — first authenticated request provisions 50 credits, balance surfaces show 50; (2) send chat messages until the server returns 402 — the paywall presents with the out-of-credits context, balance zero, reset date shown; (3) purchase the small pack in the sandbox sheet — verify via the purchase sheet, the deferred grant completes against the deployed endpoint, the balance increments by exactly the pack amount once, and the success view shows granted credits; (4) relaunch the app — balance persists from the server (proves server authority + no local phantom credit); (5) restore purchases — the packs-era copy appears (no misleading subscription-only claim). Capture evidence: simulator screenshots of the paywall before/after purchase and the balance pill, plus the backend ledger row for the test uid if accessible. Any failure in steps 3–5 is a blocker, not a polish note — triage whether it is iOS (fix here) or backend (coordinate with 02-02 owner; halt if the endpoint misbehaves). Update the auth-session probe expectation: killing the session then foregrounding shows the typed unavailable state, not a phantom balance (AUTH-02 residual close).</action>
  <verify>
    <automated>cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath "$HOME/Library/Developer/Xcode/DerivedData/StressMonitor-gsfjxdxaciivyygrqgnuxctytuxw" -parallel-testing-enabled NO</automated>
    <automated>curl -s -o /dev/null -w '%{http_code}' https://stress-api.dropitx.site/health</automated>
    <human-check>
      On the simulator (user_setup deploy + sandbox tester done): walk the five live-smoke steps above. Confirm the sandbox purchase increments the displayed balance exactly once and the relaunch shows the server-persisted balance. Confirm the 402 path lands on the paywall, not a dead error. Type "approved" or describe issues.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Full suite exits 0; Release build exits 0; backend /health returns 200
    - Human-check approved on all five live-smoke steps (this is the IAP-06 evidence: purchase verified against a .storekit file end-to-end)
    - Evidence artifacts (screenshots at minimum) referenced from the SUMMARY
  </acceptance_criteria>
  <done>The phase's money path is proven live: provision → deplete → paywall → purchase → server grant → visible persisted balance; suite and Release build green.</done>
  <precondition>Backend deployed with migration applied AND ASC consumables filed (both user_setup items above) — assert before starting; halt with a clear message if either is missing.</precondition>
  <reversibility rating="reversible">Verification task; no product code changes expected beyond fixes surfaced by the sweep.</reversibility>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces

| Symbol / Artifact | Kind | Location |
|---|---|---|
| `CreditsViewModel` (selected pack, purchase state machine, balance display rules) | viewmodel | StressMonitor/StressMonitor/ViewModels/CreditsViewModel.swift |
| Shared balance-string formatter | display rule | CreditsViewModel or CreditService extension |
| Packs-era paywall (pack cards, balance header, reset date, DEC-1-gated trial/fine print) | view rework | Views/Premium/IAPPremiumView.swift (+ PackCard in Components if added) |
| outOfCredits-contextual paywall copy | copy variant | Views/Premium/PaywallView.swift |
| Purchase success showing granted credits | view update | Views/Premium/PurchaseSuccessView.swift |
| Chat sheet balance pill | component | Views/Chat/ChatBottomSheetView.swift |
| Settings balance values | row updates | Views/Settings/SettingsView.swift |
| `CreditsViewModelTests` (pbxproj-registered) | test suite | StressMonitor/StressMonitorTests/ |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → all balance surfaces | Display-only convergence; no surface may gate or compute balance locally |
| paywall → StoreKit sandbox/ASC | Purchase intent crosses; only verified transactions flow onward |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-13 | Spoofing | stale balance displayed as current | low | accept | Three-source convergence + foreground refresh; nil state renders a neutral placeholder, never an authoritative-looking zero (pinned in Task 2 tests) |
| T-2-14 | Tampering / deception | restore copy implying consumables are recoverable | medium | mitigate | Packs-era copy from 02-03 + UI review in the human-check step (restore path verified in smoke step 5) |
| T-2-15 | Information Disclosure | balance shown for wrong account after session change | medium | mitigate | Foreground refresh acts as session probe (AUTH-02); typed unavailable state on 401 — verified in smoke step 5's session-kill check |
</threat_model>

<verification>
- Full suite green with -parallel-testing-enabled NO; Release build green; backend health 200
- Human-check approved on the five live-smoke steps (IAP-06 evidence; screenshots captured)
- All DEC-2 placements live; single balance formatter; sentinel never rendered
- SUMMARY records: smoke outcomes, screenshots, any backend-coordination items, AUTH-02 residual closure
</verification>

<success_criteria>
- Phase goal demonstrably true for a real user: chat is credits-gated with a 402→paywall path, packs are purchasable with server-verified grants, balance is visible at the decided surfaces
- IAP-04 unchanged-and-confirmed (character gating semantics per DEC-1, no pack purchase flips premium)
- No new orphans; design-system compliance in the reworked surfaces
</success_criteria>

<output>
Create `.planning/phases/02-credits-system-iap-transition/02-04-SUMMARY.md` when done — the last plan SUMMARY before `$gsd-verify-work`.
</output>
