---
phase: 02-credits-system-iap-transition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  - StressMonitor/StressMonitorTests/ChatAvailabilityTests.swift
  - StressMonitor/StressMonitorTests/ChatLifecycleTests.swift
  - StressMonitor/StressMonitorTests/SSEParserTests.swift
  - StressMonitor/StressMonitorTests/LLMServiceErrorTests.swift
  - StressMonitor/StressMonitorTests/EntitlementForegroundCorrectionTests.swift
  - StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift
  - StressMonitor/StressMonitorTests/StoreKitProductCatalogLiveTests.swift
  - StressMonitor/StressMonitorTests/CreditServiceTests.swift
  - StressMonitor/StressMonitorTests/StressAPIClientCreditsTests.swift
  - StressMonitor/StressMonitor/Models/CreditBalance.swift
  - StressMonitor/StressMonitor/Services/Credits/CreditServiceProtocol.swift
  - StressMonitor/StressMonitor/Services/Credits/CreditService.swift
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+Credits.swift
  - StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift
  - StressMonitor/StressMonitor/Services/LLM/LLMServiceProtocol.swift
  - StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift
  - StressMonitor/StressMonitor/Services/Premium/PaywallController.swift
  - StressMonitor/StressMonitor/Views/Premium/PaywallView.swift
  - StressMonitor/StressMonitor/StressMonitorApp.swift
autonomous: false
requirements: [AUTH-02, IAP-04, derived-CR-01, derived-CR-02, derived-CR-03, derived-CR-04]
estimate:
  tokens: 34000
  raw_tokens: 17000
  tasks: 5
  confidence: low
must_haves:
  truths:
    - "An authenticated user's credit balance is fetched from GET /credits and visible in the paywall header (placement per DEC-2); premium plan_type renders as Unlimited, never the 999999 sentinel"
    - "A chat send rejected with HTTP 402 INSUFFICIENT_CREDITS routes the user to the paywall (PaywallController.present(reason: .outOfCredits)) instead of leaving a dead-end error string"
    - "Every completed chat's terminal SSE metadata event (credits_remaining) converges the displayed balance without any client-side decrement arithmetic"
    - "The stress-context side-channel flagged in 01-REVIEW CR-01 is gone: context flows through the send() call chain (data race + stale-context bug closed)"
    - "All previously orphaned test suites compile and run in the StressMonitorTests Sources phase; no new orphans are created"
    - "Every xcodebuild invocation on this host uses -parallel-testing-enabled NO"
  artifacts:
    - "StressMonitor/StressMonitor/Models/CreditBalance.swift"
    - "StressMonitor/StressMonitor/Services/Credits/CreditServiceProtocol.swift"
    - "StressMonitor/StressMonitor/Services/Credits/CreditService.swift"
    - "StressMonitor/StressMonitor/Services/API/StressAPIClient+Credits.swift"
    - "StressMonitor/StressMonitorTests/CreditServiceTests.swift (pbxproj-registered)"
    - "StressMonitor/StressMonitorTests/StressAPIClientCreditsTests.swift (pbxproj-registered)"
  key_links:
    - "StressMonitorApp (app-scope @State creditService) → .environment injection → PaywallView/ChatViewModel readers"
    - "StressLLMService.apply(metadata:) → CreditService.apply(creditsRemaining:) convergence sink"
    - "ChatViewModel catch of LLMServiceError.insufficientCredits → PaywallController.present(reason: .outOfCredits)"
    - "StressAPIClient.getBalance() → authorizedRequest(path: \"credits\") → CreditBalance decode"
---

<objective>
Lock the phase's four open product decisions (research Open Questions Q1–Q5) as blocking decision checkpoints, then prove the credit-visibility spine end-to-end with one tracer slice: GET /credits balance → CreditService → chat 402 → paywall presentation showing the live balance.

Purpose: Every subsequent plan (backend redemption endpoint, StoreKit packs, paywall UX) hangs off decisions DEC-1/DEC-2 and the CreditService/protocol seams established here. Deciding first prevents building the wrong monetization shape; the tracer proves the architecture before expansion.
Output: Decision records, repaired test substrate, CR-01 closed, and the working balance→paywall path with unit coverage.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-credits-system-iap-transition/02-RESEARCH.md
@.planning/phases/02-credits-system-iap-transition/02-PATTERNS.md
@.planning/phases/01-firebase-auth-api-client-chat-migration/01-04-SUMMARY.md
@StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift
@StressMonitor/StressMonitor/Services/API/StressAPIClient.swift
@StressMonitor/StressMonitor/Services/Premium/PaywallController.swift
@StressMonitor/StressMonitor/ViewModels/AccountViewModel.swift

Pre-flight (STATE.md carry-over, surface before Task 1): repo sits on `main` with 27+ unpushed commits and no milestone branch. Do not push (outward-facing; needs user). Recommend the user pick a branch strategy before Wave 2 starts; record the outcome in this plan's SUMMARY.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: DEC-1 — Monetization architecture (research Q1+Q2+Q5)</name>
  <read_first>
  - .planning/phases/02-credits-system-iap-transition/02-RESEARCH.md — sections "Critical Backend Gap", "Pitfall 1", "Open Questions" Q1/Q2/Q5
  - .planning/phases/02-credits-system-iap-transition/COVERAGE.md
  </read_first>
  <decision>How subscriptions, server-side premium, and credit packs coexist: (a) what the backend redemption scope is (research Q1 — no route exists today), (b) what happens to existing subscriptions (Q2 — zero production subscribers exist; only sandbox/test transactions), (c) whether PaywallController's premium guard suppresses the out-of-credits paywall (Q5).</decision>
  <context>One-way-door elements: retiring subscription products in App Store Connect is effectively irreversible, and shipping client-authoritative credit grants is a security hole that cannot be patched retroactively (consumables cannot be restored once finished). These choices gate task design in plans 02-02, 02-03, and 02-04.</context>
  <options>
    <option id="option-a">
      <name>Credits for chat, subscription stays for premium (recommended)</name>
      <pros>Cheapest correct path per research: zero production subscribers means no grandfathering work; no ASC product retirement; characters/premium UX keep the existing local PremiumState flow untouched (IAP-04 stays "intentional"); backend plan_type remains 'free' for everyone so no sync endpoint is needed; the outOfCredits paywall bypasses the premium guard so subscribers can still buy packs.</pros>
      <cons>Two monetization surfaces in one paywall (packs primary, subscription secondary) — paywall copy must be mode-aware; a subscriber's server balance behaves like a free user's (this is by design under this option).</cons>
    </option>
    <option id="option-b">
      <name>Full subscription retirement</name>
      <pros>Simplest end-state mental model: credits + the existing permanent character unlocks are the only monetization; removes trial-banner/savings copy dead code entirely.</pros>
      <cons>One-way door: ASC product retirement + .storekit removal; PremiumState remains only for already-unlocked characters; any future subscription idea restarts from ASC filing lead time; more UI deletion work in 02-04.</cons>
    </option>
    <option id="option-c">
      <name>Synced premium (subscription = server-side unlimited chat)</name>
      <pros>Classic model: paying subscribers keep unlimited chat (backend plan_type='premium', 999999 sentinel, maxTokens 2048); the premium guard on present() is then correct as-is (research Q5).</pros>
      <cons>Most work: a second backend endpoint (subscription JWS → set plan_type) plus entitlement sync on refresh; keeps two premium notions alive permanently; exceeds this phase's coarse 3–5 plan budget — would force a phase split.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c. Record the choice plus any user amendments verbatim in the plan SUMMARY before Task 3.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: DEC-2 — Product parameters (research Q3+Q4)</name>
  <read_first>
  - .planning/phases/02-credits-system-iap-transition/02-RESEARCH.md — Open Questions Q3/Q4, Code Examples (.storekit entry shape)
  </read_first>
  <decision>(a) Credit pack SKU set: count, exact product IDs, prices, and credit amounts per pack (drives .storekit entries, StoreKitProductCatalog keys, the server-side productId→credits map in 02-02, and the ASC filing in 02-03's user_setup). (b) Balance display placement (drives the tracer's display target and 02-04's surface expansion).</decision>
  <context>SKU IDs/prices must be final before ASC filing — App Store Connect changes have review latency. Suggested starting point (research Q3 + backend docs sketch): three packs — com.stressmonitor.app.credits.small (~10 credits), .medium (~50), .large (~150) — with a per-unit discount on larger packs. Exact prices are the user's call.</context>
  <options>
    <option id="packs-3">
      <name>Three packs: small / medium / large (recommended)</name>
      <pros>Classic good/better/best ladder with per-unit discount on larger packs; matches SubscriptionPlan's three-tier precedent; room for savings-percent badges (IAP-05) without new layout work.</pros>
      <cons>One more SKU to file in ASC and keep in sync across .storekit / catalog / backend map.</cons>
    </option>
    <option id="packs-2">
      <name>Two packs: small / large</name>
      <pros>Smallest SKU surface to file, test, and keep in sync.</pros>
      <cons>No middle anchor; weaker upsell ladder; savings math is binary.</cons>
    </option>
    <option id="placement-a">
      <name>Balance placement: chat sheet pill + paywall header + Settings row value (recommended)</name>
      <pros>Balance visible exactly where credits are spent (chat), bought (paywall), and managed (Settings); all three hosts already exist with value-text slots (research Q4 recommendation).</pros>
      <cons>Three surfaces to keep consistent (mitigated: single CreditService source of truth).</cons>
    </option>
    <option id="placement-b">
      <name>Balance placement: paywall + Settings only</name>
      <pros>Chat sheet stays visually unchanged; less UI work.</pros>
      <cons>User discovers depletion only via the 402 error — the moment of confusion the paywall trigger exists to fix.</cons>
    </option>
  </options>
  <resume-signal>Select one pack option (packs-3 or packs-2) AND one placement option (placement-a or placement-b), and supply concrete prices/credit-amounts for each pack. Record verbatim in the SUMMARY.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Repair the orphaned test substrate</name>
  <read_first>
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj — Sources phase 3828578ADDAD4AC5925394DB (16 entries, lines ~480–501); AccountViewModelTests registration anchors (lines 41, 121, 268, 500)
  - StressMonitor/StressMonitorTests/ChatLifecycleTests.swift — FakeLLMService double (lines ~90–124)
  - StressMonitor/StressMonitorTests/ChatAvailabilityTests.swift — the file referencing removed legacy-auth symbols
  - .planning/phases/02-credits-system-iap-transition/02-RESEARCH.md — Pitfall 7
  </read_first>
  <files>StressMonitor/StressMonitor.xcodeproj/project.pbxproj, StressMonitor/StressMonitorTests/ChatAvailabilityTests.swift, StressMonitor/StressMonitorTests/ChatLifecycleTests.swift, StressMonitor/StressMonitorTests/SSEParserTests.swift, StressMonitor/StressMonitorTests/LLMServiceErrorTests.swift, StressMonitor/StressMonitorTests/EntitlementForegroundCorrectionTests.swift, StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift, StressMonitor/StressMonitorTests/StoreKitProductCatalogLiveTests.swift</files>
  <action>Register the seven orphaned test files into the StressMonitorTests Sources build phase using the established 4-line pbxproj edit per file (PBXBuildFile + PBXFileReference + group child + Sources entry) with the next-free F1A1B2C3D4E500000000A0xx/B0xx IDs — A007/B007 onward (verified free at planning time). Rewrite ChatAvailabilityTests.swift: it references the removed legacy-auth SDK symbols (guest JWT constant and masked-placeholder config check), so replace its body with a minimal contract pin that ChatAvailability.current == .enabled unconditionally (Phase 1 decision D-02). Do NOT touch CharacterEntitlementSyncTests (already compiled; stays quarantined). For each newly registered suite, run it; if a suite fails because v1.0-era expectations no longer match current behavior, minimally update the expectations to pin current behavior; if a suite instead reveals what looks like a genuine production regression, halt and escalate in the SUMMARY rather than weakening the test. StoreKitProductCatalogLiveTests performs live Product.products resolution — it runs under the scheme whose Test action already loads StressMonitorProducts.storekit; if it still fails for environment reasons, disable it with @Suite(.disabled(...)) plus a reason comment (same precedent as StoreKitServiceTests) and record that in the SUMMARY. This task creates zero new test files — it only repairs existing ones.</action>
  <verify>
    <automated>cd StressMonitor && ls StressMonitorTests/*.swift | while read f; do b=$(basename "$f"); grep -q "/\* $b \*/" <(grep -A200 "3828578ADDAD4AC5925394DB /\* Sources \*/" StressMonitor.xcodeproj/project.pbxproj) || echo "ORPHAN: $b"; done | grep -c ORPHAN</automated>
    <automated>cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath "$HOME/Library/Developer/Xcode/DerivedData/StressMonitor-gsfjxdxaciivyygrqgnuxctytuxw" -only-testing:StressMonitorTests/SSEParserTests -only-testing:StressMonitorTests/LLMServiceErrorTests -only-testing:StressMonitorTests/ChatLifecycleTests -only-testing:StressMonitorTests/ChatAvailabilityTests -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - The orphan-count check prints 0 (every *.swift in StressMonitorTests/ appears in Sources phase 3828578ADDAD4AC5925394DB)
    - The four core repaired suites (SSEParserTests, LLMServiceErrorTests, ChatLifecycleTests, ChatAvailabilityTests) exit 0
    - EntitlementForegroundCorrectionTests, DataDeletionConsolidationTests, StoreKitProductCatalogLiveTests are registered and either green or explicitly disabled-with-reason in the SUMMARY
    - pbxproj still parses: build-for-testing exits 0
  </acceptance_criteria>
  <done>All seven orphaned files are in the Sources phase; repaired suites compile and run green (or documented-disable); full build-for-testing passes with -parallel-testing-enabled NO.</done>
  <precondition>iPhone 17 simulator runtime present (host-verified: iPhone 17 iOS 26.x booted during Phase 01 close).</precondition>
  <reversibility rating="reversible">pbxproj registration and test rewrites are git-reversible.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Close 01-REVIEW CR-01 — stress context through the send() call chain</name>
  <read_first>
  - StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift — static side-channel at line 119; send() at line 56; apply(metadata:) at lines 105–112
  - StressMonitor/StressMonitor/Services/LLM/LLMServiceProtocol.swift — send signature at line 56
  - StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift — streamResponse() at line 108; static write at line 119
  - .planning/phases/01-firebase-auth-api-client-chat-migration/01-REVIEW.md — CR-01 full analysis
  - StressMonitor/StressMonitorTests/ChatLifecycleTests.swift — FakeLLMService (signature must track the protocol)
  </read_first>
  <files>StressMonitor/StressMonitor/Services/LLM/LLMServiceProtocol.swift, StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift, StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift, StressMonitor/StressMonitorTests/ChatLifecycleTests.swift</files>
  <behavior>
    - RED test (ChatLifecycleTests, using the FakeLLMService double extended to record the context argument): calling the view model's send path twice with two different built payloads delivers each call its own payload — the second send must observe the second payload, not a leftover from the first (pins the stale-context half of CR-01)
    - RED test: the fake conforms to the extended protocol signature with the new optional payload parameter, so the suite fails to compile until the protocol gains the parameter (pins the call-chain refactor)
    - Existing ChatLifecycleTests cases stay green after the signature change
  </behavior>
  <action>RED first: extend the failing tests above, run them, confirm failure/compile error. Then: add a stress-context parameter (optional payload, defaulting to nil) to the protocol's send method and to StressLLMService.send, capturing it in the existing Task closure the same way the session id is captured; delete the static payload side-channel property declared at StressLLMService.swift:119 entirely; in ChatViewModel.streamResponse pass the freshly built payload into the send call that previously wrote the static; update FakeLLMService's send signature to match and record the last received payload for assertions. Do not change StressContextPayload itself (CR-02 is explicitly deferred — see plan context note). Rationale: this phase wires balance convergence through the exact same service/task region; leaving a known cross-actor data race there would be flagged by review immediately.</action>
  <verify>
    <automated>cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath "$HOME/Library/Developer/Xcode/DerivedData/StressMonitor-gsfjxdxaciivyygrqgnuxctytuxw" -only-testing:StressMonitorTests/ChatLifecycleTests -parallel-testing-enabled NO</automated>
    <automated>cd StressMonitor && grep -v '^\s*//' StressMonitor/Services/LLM/StressLLMService.swift | grep -c 'static var currentStressContext'</automated>
  </verify>
  <acceptance_criteria>
    - ChatLifecycleTests (including the two new CR-01 cases) exits 0
    - The static side-channel declaration count is 0 in StressLLMService.swift (comment-filtered grep)
    - Full ChatViewModel/LLM build unaffected: xcodebuild build exits 0
  </acceptance_criteria>
  <done>CR-01 closed: no static mutable context channel exists; context flows per-call through send(); regression tests pin both the freshness and the signature.</done>
  <reversibility rating="reversible">Pure refactor within one call chain, git-reversible, no stored data affected.</reversibility>
</task>

<task type="tracer" tdd="true">
  <name>Task 5: Tracer — server balance to visible paywall on 402, one path only</name>
  <read_first>
  - .planning/phases/02-credits-system-iap-transition/02-RESEARCH.md — Architecture diagram, Pattern 3 (balance convergence), Pitfalls 3/4/6/8, GET /credits contract
  - .planning/phases/02-credits-system-iap-transition/02-PATTERNS.md — sections for CreditBalance, StressAPIClient+Credits, CreditServiceProtocol/CreditService, PaywallController, StressMonitorApp, test-file analogs (MockAuthService, RequestCaptureURLProtocol)
  - StressMonitor/StressMonitor/Services/API/StressAPIClient.swift — authorizedRequest (lines 29–53), getHealth (lines 56–63)
  - StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift — creditsRemaining state (line 24), apply(metadata:) (lines 105–112), mapHTTPError (lines 127–140)
  - StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift — LLMServiceError catch block (lines 157–169), test-injectable init (lines 68+)
  - StressMonitor/StressMonitor/Services/Premium/PaywallController.swift — PaywallReason enum (lines 6–13), present(reason:) with premium guard (lines 57–66)
  - StressMonitor/StressMonitor/Views/Premium/PaywallView.swift — environment-reading wrapper
  - StressMonitor/StressMonitor/StressMonitorApp.swift — storeKitService ownership pattern (lines 18–22, 183–186, 196–205)
  - StressMonitor/StressMonitor/ViewModels/AccountViewModel.swift — ViewModel contract analog
  - StressMonitor/StressMonitorTests/StressAPIClientTests.swift — MockAuthService + RequestCaptureURLProtocol
  </read_first>
  <files>StressMonitor/StressMonitor/Models/CreditBalance.swift, StressMonitor/StressMonitor/Services/Credits/CreditServiceProtocol.swift, StressMonitor/StressMonitor/Services/Credits/CreditService.swift, StressMonitor/StressMonitor/Services/API/StressAPIClient+Credits.swift, StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift, StressMonitor/StressMonitor/Services/LLM/LLMServiceProtocol.swift, StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift, StressMonitor/StressMonitor/Services/Premium/PaywallController.swift, StressMonitor/StressMonitor/Views/Premium/PaywallView.swift, StressMonitor/StressMonitor/StressMonitorApp.swift, StressMonitor/StressMonitorTests/CreditServiceTests.swift, StressMonitor/StressMonitorTests/StressAPIClientCreditsTests.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj</files>
  <behavior>
    - RED (CreditServiceTests): refreshBalance() with a mock API returning a fixed balance populates observable state; apply of a metadata remaining value converges state without local arithmetic; premium plan_type exposes an "Unlimited" display rule and never formats the 999999 sentinel raw
    - RED (StressAPIClientCreditsTests, RequestCaptureURLProtocol with a JSON body fixture): getBalance() decodes {total, used, remaining, plan_type, free_reset_at} from GET credits with a Bearer header present; 401 and 500 map to error cases (derived-CR-01)
    - RED (ChatLifecycleTests): when the injected LLM service throws the insufficient-credits error, the view model sets a short message AND invokes the injected paywall presentation with the out-of-credits reason — asserted via a recording closure, never by constructing UI in the VM (derived-CR-03)
    - GREEN: all three suites pass after implementation; existing suites (StressAPIClientTests, StoreKitServiceTests, PremiumViewModelTests) stay green
  </behavior>
  <action>RED first (the three suites above, registered via the 4-line pbxproj pattern at IDs A009/A009-adjacent free suffixes), then implement the single path: CreditBalance as a Codable+Sendable struct mirroring the GET /credits response with CodingKeys for the snake_case reset-date field and a plan_type union ("free"/"premium"), plus a display helper enforcing the premium-sentinel rule; StressAPIClient extension in a new file with getBalance() built on authorizedRequest(path: "credits", method: "GET") and non-2xx mapped to typed errors, following the existing guard-on-HTTPURLResponse style; CreditServiceProtocol + CreditService as @MainActor @Observable with balance state, refreshBalance() delegating to the client, and two apply overloads (full balance; metadata remaining) — never any decrement arithmetic; StressMonitorApp gains @State creditService app-scope ownership, environment injection mirroring the storeKitService pattern, and a refreshBalance() call added inside the existing scenePhase-active block (this doubles as the AUTH-02 live-session probe); StressLLMService.apply(metadata:) routes the remaining value into an injectable convergence sink so the chat path updates CreditService without a hard dependency (closure or protocol, injected at construction); ChatViewModel's LLMServiceError catch adds an insufficient-credits branch that sets a one-line message and fires an injected presentation closure with the new paywall reason; PaywallReason gains case outOfCredits and present() applies the premium-guard semantics chosen in DEC-1 (option-a/b: the out-of-credits reason bypasses premium suppression so subscribers can buy packs; option-c: guard unchanged); PaywallView renders a minimal balance line (remaining count or Unlimited, plus nothing else — the full paywall rework is plan 02-04) reading credit state from the environment; LLMServiceProtocol's insufficient-credits user-facing copy becomes a short nudge referencing buying more credits since the paywall now carries the detail. App target auto-compiles new files via its synchronized folder — only the two new TEST files need pbxproj registration. No pack purchases, no Settings/chat-sheet surfaces, no redemption calls in this task — that is 02-03/02-04 expansion.</action>
  <verify>
    <automated>cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath "$HOME/Library/Developer/Xcode/DerivedData/StressMonitor-gsfjxdxaciivyygrqgnuxctytuxw" -only-testing:StressMonitorTests/CreditServiceTests -only-testing:StressMonitorTests/StressAPIClientCreditsTests -only-testing:StressMonitorTests/ChatLifecycleTests -parallel-testing-enabled NO</automated>
    <automated>cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath "$HOME/Library/Developer/Xcode/DerivedData/StressMonitor-gsfjxdxaciivyygrqgnuxctytuxw" -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - The three targeted suites exit 0; the full suite exits 0 (baseline 87 tests / 14 suites from Phase 01 close, plus repairs and additions)
    - grep -c 'case outOfCredits' on PaywallController.swift returns 1
    - grep -c 'getBalance' on StressAPIClient+Credits.swift is >= 1 and grep -c 'authorizedRequest' on the same file is >= 1
    - grep -c '999999' across StressMonitor/StressMonitor/ is 0 (sentinel never rendered — display rule instead)
    - No test file was created without Sources-phase registration (Task 3's orphan-count check still prints 0)
  </acceptance_criteria>
  <done>The single path works end-to-end: authenticated balance fetch → observable credit state → chat 402 → paywall presented showing the live balance; unit suites pin the behavior; full suite green.</done>
  <precondition>Backend live at https://stress-api.dropitx.site (verified 200 during research); unit tests do not require it — only the end-of-phase live smoke does.</precondition>
  <reversibility rating="reversible">All new code is additive; the paywall-reason and copy changes are trivially revertible.</reversibility>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces

| Symbol / Artifact | Kind | Location |
|---|---|---|
| `CreditBalance` (Codable+Sendable struct, CodingKeys, premium display rule) | model | StressMonitor/StressMonitor/Models/CreditBalance.swift |
| `CreditServiceProtocol` (+ `CreditServiceError` if needed) | protocol | StressMonitor/StressMonitor/Services/Credits/CreditServiceProtocol.swift |
| `CreditService` (@MainActor @Observable: `balance`, `refreshBalance()`, `apply(_:)`, `apply(creditsRemaining:)`) | service | StressMonitor/StressMonitor/Services/Credits/CreditService.swift |
| `StressAPIClient.getBalance()` | API method | StressMonitor/StressMonitor/Services/API/StressAPIClient+Credits.swift |
| `PaywallReason.outOfCredits` | enum case | StressMonitor/StressMonitor/Services/Premium/PaywallController.swift |
| ChatViewModel paywall presentation injection | closure dependency | StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift |
| StressLLMService convergence sink | injectable closure/protocol | StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift |
| `LLMServiceProtocol.send` stress-context parameter | signature change | StressMonitor/StressMonitor/Services/LLM/LLMServiceProtocol.swift |
| creditService app-scope ownership + environment + foreground refresh | wiring | StressMonitor/StressMonitor/StressMonitorApp.swift |
| `MockCreditService`, extended `RequestCaptureURLProtocol` body support | test doubles | StressMonitorTests (test target only) |
| `CreditServiceTests`, `StressAPIClientCreditsTests` | test suites (pbxproj-registered) | StressMonitor/StressMonitorTests/ |
| DEC-1 / DEC-2 decision records | documentation | 02-01-SUMMARY.md |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → backend /credits | Firebase ID token crosses; balance JSON returns |
| backend → client balance values | server-authoritative numbers rendered as-is (display-only cache) |
| StoreKit → client (already established) | untouched by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | CreditService cached balance | medium | accept | Cache is display-only; server is the sole authority for gating/deduction; no client-side decrement arithmetic exists to poison (pinned by CreditServiceTests) |
| T-2-02 | Information Disclosure / Spoofing | getBalance on stale session | low | mitigate | 401 maps to a typed error (derived-CR-01 test); foreground refresh doubles as the AUTH-02 stale-session probe |
| T-2-03 | Elevation of Privilege | PaywallController premium guard vs out-of-credits reason | medium | mitigate | Guard semantics fixed explicitly by DEC-1 before wiring; behavior pinned by a ChatLifecycleTests case asserting the reason reaches presentation regardless of the guard branch chosen |
| T-2-04 | Repudiation | Balance convergence from metadata | low | accept | Metadata treated as advisory only (research Pattern 3 / Pitfall 6); GET /credits re-syncs on foreground |
</threat_model>

<verification>
- Full suite green with -parallel-testing-enabled NO on iPhone 17 (includes the seven repaired suites)
- Tracer suites: CreditServiceTests, StressAPIClientCreditsTests, ChatLifecycleTests all green
- Orphan count 0; pbxproj parses; DEBUG build + build-for-testing exit 0
- DEC-1/DEC-2 outcomes recorded verbatim in 02-01-SUMMARY.md before Wave 2 starts
</verification>

<success_criteria>
- All must_haves.truths hold on the simulator at plan close
- Decisions DEC-1/DEC-2 recorded; every downstream plan (02-02/02-03/02-04) can proceed without re-asking
- CR-01 closed with regression coverage; CR-02 explicitly deferred (see below)
</success_criteria>

<output>
Create `.planning/phases/02-credits-system-iap-transition/02-01-SUMMARY.md` when done.
Record in the SUMMARY: DEC-1/DEC-2 verbatim outcomes, any suite disabled-with-reason, and the branch-strategy recommendation outcome from the pre-flight note.
</output>

<explicit_deferrals>
- 01-REVIEW CR-02 (trend direction inverted in StressContextPayload.build): DEFERRED to Phase 3. Rationale: the file is untouched by Phase 2 scope; the fix belongs with Phase 3's chat-history/session work that constructs recentHistory inputs. If the user prefers earlier, run `$gsd quick fix-trend-direction` — it is a self-contained one-function fix with an obvious test.
</explicit_deferrals>
