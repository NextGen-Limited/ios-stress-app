---
phase: 02-credits-system-iap-transition
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  - StressMonitor/StressMonitor/Info.plist
  - StressMonitor/StressMonitorTests/StoreKitProductCatalogLiveTests.swift
autonomous: true
requirements: [IAP-01, derived-CR-04]
estimate:
  tokens: 16000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "StoreKitProductCatalog.live resolves both pack product IDs in a real hosted build — packID(for: .small) returns com.stressmonitor.app.credits.small and packID(for: .large) returns com.stressmonitor.app.credits.large (gap 1 / CR-04: pack purchase dead in every real build)"
    - "project.pbxproj carries INFOPLIST_KEY_STOREKIT_CREDITS_SMALL_PRODUCT_ID and INFOPLIST_KEY_STOREKIT_CREDITS_LARGE_PRODUCT_ID in BOTH app-target build configurations (the two blocks owning PRODUCT_BUNDLE_IDENTIFIER = stress.ai.com), alongside the existing premium keys"
    - "The Info.plist delivery tier is real, not assumed: the STOREKIT_* keys land in the built app's Bundle.main Info.plist and the re-enabled hosted live-catalog suite proves it"
    - "StoreKitProductCatalogLiveTests runs enabled (it is a normal suite again) and passes, including new pack-ID assertions"
  artifacts:
    - "project.pbxproj: 4 new build-setting lines (2 pack keys x 2 configurations)"
    - "StressMonitor/StressMonitor/Info.plist: 6 STOREKIT_* top-level keys with literal values"
    - "StoreKitProductCatalogLiveTests.swift: disabled annotation and DISABLED header removed, pack assertions added"
  key_links:
    - "Info.plist file (wired via INFOPLIST_FILE at pbxproj lines 842/897) → merged into the generated app Info.plist → Bundle.main.object(forInfoDictionaryKey:) → StoreKitProductCatalog.resolve tier 1 → packID(for:) → purchaseProduct(resolving:) → Product.products"
---

<objective>
Close verification gap 1 (CR-04): make credit-pack product IDs resolvable in real builds. Today `grep STOREKIT_CREDITS project.pbxproj` returns 0, so `StoreKitProductCatalog.packID(for:)` is nil on device, `availablePacks` falls back to `defaultPacks` with nil product IDs, and `purchase(pack:)` throws `missingProductConfiguration` (StoreKitService.swift:177) — pack purchase is dead in every real build.

Purpose: This is the direct blocker for the phase goal's "transition StoreKit to consumable credit packs". Without ID resolution, the DEC-2 packs cannot be bought even after ASC filing.
Output: Build settings in both configurations, a verified Bundle.main delivery tier (Info.plist file keys), and the previously disabled live-catalog suite re-enabled with pack assertions as the standing proof.
</objective>

<execution_context>
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.claude/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-credits-system-iap-transition/02-VERIFICATION.md (gap 1 — source of truth)
@.planning/phases/02-credits-system-iap-transition/02-REVIEW.md (CR-04)
@.planning/phases/02-credits-system-iap-transition/02-03-SUMMARY.md (catalog pack keys added; live suite left disabled)
@StressMonitor/StressMonitor/Services/StoreKit/StoreKitProductCatalog.swift
@StressMonitor/StressMonitor.xcodeproj/project.pbxproj
@StressMonitor/StressMonitor/Info.plist
@StressMonitor/StressMonitorTests/StoreKitProductCatalogLiveTests.swift

Mechanism facts (verified during planning):
- The app target sets `INFOPLIST_FILE = StressMonitor/Info.plist` AND `GENERATE_INFOPLIST_FILE = YES` (pbxproj lines 841-842 Debug, 896-897 Release). Xcode merges the file's keys into the final Info.plist — this is the delivery tier that provably reaches Bundle.main. The disabled suite's header documents that custom `INFOPLIST_KEY_*` settings never reach the generated plist, so the file entries are the load-bearing mechanism and the build settings are the configuration record (and env-tier parity).
- The two app-target build configurations are the blocks containing `PRODUCT_BUNDLE_IDENTIFIER = stress.ai.com;` (~lines 840-866 Debug, ~895-921 Release); the premium keys sit at 850-853 / 905-908.
- DEC-2 pack SKUs (user-confirmed, 02-01-SUMMARY): small = com.stressmonitor.app.credits.small, large = com.stressmonitor.app.credits.large. Both already exist as Consumable entries in StressMonitorProducts.storekit.
- Known residual, NOT this plan: ASC filing of the two consumables and the live sandbox money-path smoke remain the end-of-phase human verification items (02-VERIFICATION human_verification). This plan closes the ID-resolution half of IAP-01 only.
- Do NOT touch EntitlementForegroundCorrectionTests — its disabled state is a separate StoreKitTest session-isolation issue, not this gap.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pack product-ID build settings in both configurations + literal keys in the Info.plist file</name>
  <read_first>
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj lines 840-870 and 895-925 (the two app-target XCBuildConfiguration blocks; note INFOPLIST_FILE at 842/897 and the premium keys at 850-853 / 905-908)
  - StressMonitor/StressMonitor/Info.plist (whole file — currently only CFBundleURLTypes; tab-indented)
  - StressMonitor/StressMonitor/Services/StoreKit/StoreKitProductCatalog.swift (resolve tiers lines 147-174; pack keys lines 111-127)
  - StressMonitor/StressMonitorTests/StoreKitProductCatalogLiveTests.swift lines 5-13 (the documented delivery-mechanism failure this task fixes)
  </read_first>
  <files>StressMonitor/StressMonitor.xcodeproj/project.pbxproj, StressMonitor/StressMonitor/Info.plist</files>
  <action>
  In project.pbxproj, add two build settings to BOTH app-target configuration blocks (the two blocks owning PRODUCT_BUNDLE_IDENTIFIER = stress.ai.com), placed alphabetically before the existing INFOPLIST_KEY_STOREKIT_PREMIUM_* lines (CREDITS sorts before PREMIUM): INFOPLIST_KEY_STOREKIT_CREDITS_LARGE_PRODUCT_ID = "com.stressmonitor.app.credits.large" and INFOPLIST_KEY_STOREKIT_CREDITS_SMALL_PRODUCT_ID = "com.stressmonitor.app.credits.small". Match the quoting style of the neighboring premium values. Edit only these two blocks — the watch/widget/test-target configurations are untouched.

  Then make the Bundle.main tier real: StressMonitor/StressMonitor/Info.plist is wired as INFOPLIST_FILE, so its keys are merged into the built app's Info.plist (the mechanism custom INFOPLIST_KEY_* settings lack). Add six top-level string keys with the exact literal values: STOREKIT_CREDITS_LARGE_PRODUCT_ID = com.stressmonitor.app.credits.large, STOREKIT_CREDITS_SMALL_PRODUCT_ID = com.stressmonitor.app.credits.small, STOREKIT_PREMIUM_ANNUAL_PRODUCT_ID = com.stressmonitor.app.premium.annual, STOREKIT_PREMIUM_MONTHLY_PRODUCT_ID = com.stressmonitor.app.premium.monthly, STOREKIT_PREMIUM_SUBSCRIPTION_GROUP_ID = SMPREMIUM01, STOREKIT_PREMIUM_WEEKLY_PRODUCT_ID = com.stressmonitor.app.premium.weekly. Use tab indentation matching the existing file; keep CFBundleURLTypes untouched. The premium keys ride along because their delivery was equally unproven (02-VERIFICATION gap 1 reason) — same mechanism, same fix, zero extra surface.

  No Swift code changes in this task — StoreKitProductCatalog already resolves these exact Info.plist keys (tier 1 of its 3-tier resolve, lines 147-174).
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/StressMonitor && grep -c "STOREKIT_CREDITS" StressMonitor.xcodeproj/project.pbxproj | grep -qx "4" && grep -c "STOREKIT_" StressMonitor/Info.plist | grep -qx "6" && plutil -lint StressMonitor/Info.plist</automated>
  </verify>
  <acceptance_criteria>
  - grep -c "STOREKIT_CREDITS" on project.pbxproj returns exactly 4 (2 keys x Debug+Release)
  - grep -c "INFOPLIST_KEY_STOREKIT_CREDITS_LARGE_PRODUCT_ID" on project.pbxproj returns 2 (both configurations), same for the SMALL key
  - StressMonitor/Info.plist passes plutil -lint and contains all six STOREKIT_* keys with the exact values above (2 credits pack IDs + 3 premium subscription IDs + 1 subscription group ID)
  - The watch, widget, and test-target configuration blocks are unmodified (the four new pbxproj lines all live inside the two stress.ai.com app-target blocks)
  </acceptance_criteria>
  <done>Pack product IDs are declared in both app-target configurations and delivered through the Info.plist file that Xcode actually merges into the built product's Bundle.main.</done>
  <reversibility rating="reversible">Pure configuration additions; revert by deleting the added lines.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Re-enable StoreKitProductCatalogLiveTests with pack assertions — the Bundle.main delivery proof</name>
  <read_first>
  - StressMonitor/StressMonitorTests/StoreKitProductCatalogLiveTests.swift (whole file — 3 existing tests + the disabled-suite header to remove)
  - StressMonitor/StressMonitorTests/StoreKitProductCatalogTests.swift (existing non-live pack resolution cases, to avoid duplicating them)
  - StressMonitor/StressMonitor/Services/StoreKit/StoreKitProductCatalog.swift (packID(for:)/pack(for:) lookups, lines 46-60)
  </read_first>
  <files>StressMonitor/StressMonitorTests/StoreKitProductCatalogLiveTests.swift</files>
  <action>
  Remove the suite-level disabled annotation and the DISABLED header comment block (lines 5-13) so the suite runs again — the contract it pins now holds via Task 1's Info.plist delivery. Keep the three existing tests unchanged (non-empty catalog, annual ID, group ID). Add two pack tests in the same style: live packID(for: .small) equals "com.stressmonitor.app.credits.small" and live packID(for: .large) equals "com.stressmonitor.app.credits.large", each with a failure message naming the Info.plist key that must be present. Add one round-trip case: pack(for:) on the resolved small ID returns .small. Because StressMonitorTests runs hosted in the StressMonitor app, Bundle.main inside this suite is the app bundle — a green run is end-to-end proof that the keys landed in the built Info.plist, closing the "verify the delivery mechanism actually lands in Bundle.main" gap item.

  Run the suite on the simulator with the pinned flag (-parallel-testing-enabled NO). If the two pack tests fail while the three premium tests pass, the Info.plist keys are wrong or undelivered — fix Task 1's entries, not the tests.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:StressMonitorTests/StoreKitProductCatalogLiveTests -parallel-testing-enabled NO 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - StoreKitProductCatalogLiveTests contains no suite-level disabled annotation and no DISABLED header prose (grep for the disabled marker macro on the file returns 0)
  - The suite has 6 tests (3 original + small/large packID + round trip) and the targeted xcodebuild run reports Suite StoreKitProductCatalogLiveTests passed, exit 0
  - The two pack assertions passed against Bundle.main of the hosted app (this is the empirical Info.plist-delivery verification the gap demands)
  </acceptance_criteria>
  <done>The live catalog suite is a standing regression guard for real-build product-ID resolution, including packs; gap 1's three missing items (build settings, verified delivery, re-enabled suite) are all closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build config → app binary | Product IDs flow from repo build config into the shipped Info.plist; a wrong/missing value silently kills the money path (the CR-04 defect class) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-501 | Tampering | Info.plist product-ID keys | low | mitigate | Values are Apple-product identifiers already public in any App Store binary and mirrored in ASC; the re-enabled live suite fails the build-test gate if they drift or vanish |
| T-2-502 | DoS (availability) | Pack purchase path | high | mitigate | This plan IS the mitigation for CR-04: without Bundle.main resolution, purchase(pack:) throws missingProductConfiguration for every real user; closed by Task 1 + pinned by Task 2 |
</threat_model>

<verification>
- `grep -c "STOREKIT_CREDITS" StressMonitor/StressMonitor.xcodeproj/project.pbxproj` = 4
- `plutil -lint StressMonitor/StressMonitor/Info.plist` exits 0 and the file holds all six STOREKIT_* keys
- Targeted hosted-suite run green (6/6) — proves Bundle.main delivery end-to-end
- One Release-configuration build still compiles: `xcodebuild build -scheme StressMonitor -configuration Release -destination 'platform=iOS Simulator,name=iPhone 17'` exit 0 (config changes must not break BUILD-05)
</verification>

<success_criteria>
- Gap 1 (truth 13: "Pack product IDs resolve through the 3-tier mechanism in real builds") is closable: packID(for:) resolves both DEC-2 SKUs from Bundle.main in a hosted app run
- The live-catalog suite is enabled, extended to packs, and green
- No behavior change anywhere else; no Swift production code touched
</success_criteria>

## Artifacts this phase produces

- project.pbxproj: build settings `INFOPLIST_KEY_STOREKIT_CREDITS_SMALL_PRODUCT_ID` / `INFOPLIST_KEY_STOREKIT_CREDITS_LARGE_PRODUCT_ID` in Debug and Release app-target configurations
- StressMonitor/Info.plist: keys `STOREKIT_CREDITS_SMALL_PRODUCT_ID`, `STOREKIT_CREDITS_LARGE_PRODUCT_ID`, `STOREKIT_PREMIUM_WEEKLY_PRODUCT_ID`, `STOREKIT_PREMIUM_MONTHLY_PRODUCT_ID`, `STOREKIT_PREMIUM_ANNUAL_PRODUCT_ID`, `STOREKIT_PREMIUM_SUBSCRIPTION_GROUP_ID` (the Bundle.main delivery tier)
- StoreKitProductCatalogLiveTests: enabled suite with 2 new pack-ID tests + 1 round-trip test

<output>
Create `.planning/phases/02-credits-system-iap-transition/02-05-SUMMARY.md` when done.
Commits land in the iOS repo (ios-stress-app) per the standard GSD protocol; no backend files in this plan.
</output>
