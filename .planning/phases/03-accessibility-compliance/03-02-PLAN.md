---
phase: 03-accessibility-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - StressMonitor/StressMonitor/Utilities/DynamicTypeScaling.swift
  - StressMonitor/StressMonitor/Theme/Font+WellnessType.swift
  - StressMonitor/StressMonitor/Views/Action/ActionView.swift
  - StressMonitor/StressMonitor/Views/Trends/TrendsView.swift
  - StressMonitor/StressMonitor/Views/Settings/DataManagement/DataExportView.swift
  - StressMonitor/StressMonitor/Views/Settings/DataManagement/DataManageView.swift
  - StressMonitor/StressMonitor/Views/Settings/DataManagement/DataDeleteView.swift
  - StressMonitor/StressMonitor/Views/Characters/CharacterCollectionView.swift
  - StressMonitor/StressMonitor/Views/Settings/AppearanceSettingsView.swift
  - StressMonitor/StressMonitor/Views/Settings/AboutView.swift
  - StressMonitor/StressMonitor/Views/Settings/WatchFacePreferencesView.swift
  - StressMonitor/StressMonitor/Views/History/MeasurementDetailView.swift
autonomous: true
requirements: [A11Y-04]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "The reworked .accessibleDynamicType() applies no Dynamic Type size cap, no text-shrink factor, and only removes line limits — text scales through AX5 and wraps instead of shrinking (D-08/D-10)"
    - "AdaptiveTextSizeModifier, scalableText/adaptiveTextSize, and the accessibleWellnessType* modifiers no longer exist in the source tree (zero-adopter deletions, verified by grep before removal)"
    - "Font.WellnessType text tokens are anchored to platform system text styles (.title, .title2, .body, .headline, .footnote, .caption2) so they ride the system ramp — rendered sizes byte-identical at the Large default, with the platform ramp as the only scaling mechanism (zero-addition lock: no new sizes, no new weights, no custom multiplier table)"
    - "heroNumber and largeMetric remain fixed-size gauge class per D-09 (labeled via accessibility value in plan 03-04, not scaled)"
    - "All 14 manifest surfaces apply .accessibleDynamicType() at their root container — grep-enumerated 1:1 against the D-03 manifest with zero unaccounted files (the four existing adopters inherit the reworked defaults automatically)"
    - ".limitedDynamicType() survives only for dated-exception use; no manifest surface uses it without a dated exception note"
  artifacts:
    - Reworked StressMonitor/StressMonitor/Utilities/DynamicTypeScaling.swift
    - Anchored StressMonitor/StressMonitor/Theme/Font+WellnessType.swift
  key_links:
    - "Root container of each of the 14 D-03 manifest surfaces <-> reworked .accessibleDynamicType()"
    - "Font.WellnessType statics <-> system text-style ramp (scaling follows the real system curve; the hand-rolled multiplier table is deleted)"
  prohibitions:
    - statement: "MUST NOT cap Dynamic Type below AX5 or apply a text-shrink factor on manifest surfaces — text wraps, stacks, or the container scrolls instead; the only escape is .limitedDynamicType() behind a dated exception note"
      status: resolved
      verification: judgment
    - statement: "MUST NOT hide, clip, or ellipsize content at accessibility sizes to preserve layout — content stays reachable through wrap, stack, or scroll (D-08 zero-truncation bar)"
      status: resolved
      verification: judgment
---

<objective>
Rework the Dynamic Type mechanism so its defaults satisfy the zero-truncation contract instead of contradicting it (D-10), anchor the typography tokens to the platform system ramp, and adopt the reworked modifier on every manifest surface.

Purpose: A11Y-04's machine-checked half. Today the helper's defaults (AX3 cap + 0.75 shrink) prevent AX4/AX5 from rendering at all and fight scaling toward ellipsis — the acceptance bar is unreachable until the mechanism itself is fixed. Then the adoption sweep makes the D-03 manifest 1:1 with reality.
Output: reworked DynamicTypeScaling.swift, anchored Font+WellnessType.swift, adoption on 10 remaining surfaces, preliminary adoption-gate grep.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-accessibility-compliance/03-CONTEXT.md
@.planning/phases/03-accessibility-compliance/03-RESEARCH.md
@.planning/phases/03-accessibility-compliance/03-PATTERNS.md
@StressMonitor/StressMonitor/Utilities/DynamicTypeScaling.swift
@StressMonitor/StressMonitor/Theme/Font+WellnessType.swift
@StressMonitor/StressMonitor/Views/DashboardView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework .accessibleDynamicType() — no cap, no shrink; delete the superseded zero-adopter variants</name>
  <files>StressMonitor/StressMonitor/Utilities/DynamicTypeScaling.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Utilities/DynamicTypeScaling.swift (full file: scalableText at 7-30, AdaptiveTextSizeModifier hand-rolled 12-step multiplier table at 35-98, limitedDynamicType at 104-117, AccessibleDynamicTypeModifier at 122-148 with the contract-violating defaults)
  - .planning/phases/03-accessibility-compliance/03-RESEARCH.md Pattern 2 (the rework contract, current code quoted verbatim)
  </read_first>
  <action>
  Implement the D-10 rework. First verify zero-adopter status with grep for each symbol you remove (branch note: scalableText, adaptiveTextSize, and the multiplier modifier have zero call sites on this branch — re-run the grep; if an adopter appeared, rework it onto the surviving modifier instead of deleting). Then: delete scalableText and its modifier, delete AdaptiveTextSizeModifier entirely (the hand-rolled 12-row size-multiplier table duplicates what text-style scaling does natively and drifts from the real system curve), and rework AccessibleDynamicTypeModifier so its body applies ONLY the unlimited-lines modifier — the size range restriction and the shrink factor come out of the body and their initializer parameters and defaults come out with them. The public entry becomes a no-argument view extension returning the modifier, so the four existing manifest adopters (DashboardView, SettingsView, MiniWalkView, BreathingExerciseView) and the exempt-surface adopters inherit the new contract without edits — but grep every current call site first: any caller passing explicit arguments to the old parameterized form migrates to the no-argument form in the same commit. Keep limitedDynamicType in the file unchanged — it is the dated-exception escape hatch per D-10, and after this phase it must never appear on a manifest surface without a dated exception note next to the call.
  </action>
  <verify>
    <automated>grep -rn "scalableText\|adaptiveTextSize\|AdaptiveTextSizeModifier\|accessibleWellnessType" StressMonitor/StressMonitor --include="*.swift"; echo DELETED_GREP_DONE; grep -n "minimumScale\|dynamicTypeSize(" StressMonitor/StressMonitor/Utilities/DynamicTypeScaling.swift; echo CAP_GREP_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
    <fails_when>any result line prints between the command start and DELETED_GREP_DONE (a deleted symbol still referenced), or any line prints between DELETED_GREP_DONE and CAP_GREP_DONE other than the limitedDynamicType line (a cap or shrink still applied in the file), or the build tail contains "** BUILD FAILED **"</fails_when>
  </verify>
  <done>The reworked modifier scales through AX5 with no cap and no shrink; superseded zero-adopter variants are gone; the app target builds with the four existing adopters inheriting the new behavior.</done>
  <acceptance_criteria>
  - grep for scalableText, adaptiveTextSize, AdaptiveTextSizeModifier across StressMonitor/StressMonitor returns no hits
  - DynamicTypeScaling.swift's reworked modifier body contains only the unlimited-lines modifier application (no size-range restriction, no shrink factor); the only remaining capped construct in the file is inside limitedDynamicType
  - limitedDynamicType still exists unchanged
  - App scheme builds with "** BUILD SUCCEEDED **"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Anchor Font.WellnessType to the system text-style ramp (byte-identical at Large)</name>
  <files>StressMonitor/StressMonitor/Theme/Font+WellnessType.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Theme/Font+WellnessType.swift (tokens at 13-36 — all bare fixed-size system fonts; accessibleWellnessType* modifiers at 44-67 with AX3-cap + 0.7-shrink defaults, zero adopters on this branch)
  - .planning/phases/03-accessibility-compliance/03-RESEARCH.md Pattern 3 (anchor table + the byte-identical-at-default hard rule + the A1 assumption on default ramp sizes)
  - .planning/phases/03-accessibility-compliance/03-UI-SPEC.md Typography section (the remediation contract and zero-addition lock)
  </read_first>
  <action>
  Implement the typography retrofit per the UI-SPEC contract: redeclare each scaling token as the system text style whose default size equals today's rendered size, preserving weight and design — cardTitle (28 bold) anchors to the title style with bold weight; sectionHeader (22 semibold) to title2 with semibold; body (17 regular) to the body style; bodyEmphasized (17 semibold) to the headline style (already semibold); caption (13 regular) to footnote; caption2 (11 regular) to caption2. heroNumber and largeMetric stay exactly as they are — gauge class, fixed per D-09. Zero-addition lock: no new sizes, no new weights, no new tokens — the platform system ramp is the only scaling mechanism. Before adopting, PROVE byte-identical rendering at the Large (default) setting per research assumption A1: a temporary side-by-side #Preview (or ImageRenderer measurement) rendering each old fixed-size token next to its anchored replacement at default size; if any style's default drifts on this SDK, compensate that one token with a ScaledMetric-scaled point size anchored to the matching style instead of accepting the drift, and record the measurement in the SUMMARY. Delete the accessibleWellnessType* modifiers outright (zero adopters verified by grep in Task 1's sweep — re-verify before deleting). Leave the separate Views/DesignSystem/Typography.swift foundation struct alone: whether it lives or dies is decided by which of its consumers survive the plan 03-06 orphan audit; anchoring a possibly-doomed file now is waste.
  </action>
  <verify>
    <automated>grep -c "Font.system(size:" StressMonitor/StressMonitor/Theme/Font+WellnessType.swift; echo FIXED_GREP_DONE && grep -c "accessibleWellnessType" StressMonitor/StressMonitor/Theme/Font+WellnessType.swift; echo MOD_GREP_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
    <fails_when>the first count is above 2 (more than the two gauge-class tokens still fixed-size), or the accessibleWellnessType count is not 0, or the build tail contains "** BUILD FAILED **"</fails_when>
  </verify>
  <done>Six text tokens ride the system ramp, the two gauge tokens stay fixed, rendered sizes are byte-identical at Large (measured, recorded), and the zero-adopter wellness-type modifiers are deleted.</done>
  <acceptance_criteria>
  - Font+WellnessType.swift contains exactly two remaining fixed-size declarations (heroNumber, largeMetric)
  - The six scaling tokens are text-style-anchored with weights preserved (bold title / semibold title2 / body / headline / footnote / caption2)
  - grep for accessibleWellnessType in the file returns 0
  - The parity measurement (old vs anchored at Large) is recorded in the SUMMARY with per-token results
  - App scheme builds with "** BUILD SUCCEEDED **"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Adoption sweep — the reworked modifier on all 14 manifest surfaces (D-03 gate)</name>
  <files>StressMonitor/StressMonitor/Views/Action/ActionView.swift, StressMonitor/StressMonitor/Views/Trends/TrendsView.swift, StressMonitor/StressMonitor/Views/Settings/DataManagement/DataExportView.swift, StressMonitor/StressMonitor/Views/Settings/DataManagement/DataManageView.swift, StressMonitor/StressMonitor/Views/Settings/DataManagement/DataDeleteView.swift, StressMonitor/StressMonitor/Views/Characters/CharacterCollectionView.swift, StressMonitor/StressMonitor/Views/Settings/AppearanceSettingsView.swift, StressMonitor/StressMonitor/Views/Settings/AboutView.swift, StressMonitor/StressMonitor/Views/Settings/WatchFacePreferencesView.swift, StressMonitor/StressMonitor/Views/History/MeasurementDetailView.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Views/DashboardView.swift (the adoption analog: root Group with the modifier applied at its root container, around lines 40-41)
  - Each of the ten files listed above (current root-container shape before editing)
  - .planning/phases/03-accessibility-compliance/03-RESEARCH.md Code Examples (the exact adoption-gate grep loop over the 14 files)
  </read_first>
  <action>
  Adopt the reworked no-argument modifier at the root container of each of the ten manifest surfaces missing it, copying the DashboardView pattern (one line on the outermost content group of the body) — this completes the D-03 manifest to 14 of 14 alongside the four existing adopters (DashboardView, SettingsView, MiniWalkView, BreathingExerciseView, which already carry the line and now inherit the reworked defaults from Task 1). This is D-10 layer 1: the machine-checked adoption gate. Where a surface's layout visibly fights the now-unlimited scaling in an obvious way during adoption (a fixed-height row that would clip at AX sizes), make the minimal layout adaptation per D-08 — prefer letting the container scroll, then stack or wrap; reduce a spacing token one step only where required to prevent clipping; never re-introduce a cap, a shrink factor, or a line limit to force fit. Deeper AX5 failures found beyond the obvious are expected to surface in the phase walkthrough (plan 03-06 apparatus) and get fixed then — do not redesign screens speculatively in this sweep. Do NOT apply the modifier to breathing session/summary views (D-01 exempts them; their motion behavior is D-11 territory in plan 03-05) or to Onboarding/Paywall (exempt this phase). Run the adoption grep and paste the output into the SUMMARY; the final dated gate record is re-run against the post-deletion tree in plan 03-06.
  </action>
  <verify>
    <automated>for f in Views/DashboardView.swift Views/Action/ActionView.swift Views/Trends/TrendsView.swift Views/Settings/SettingsView.swift Views/Settings/DataManagement/DataExportView.swift Views/Settings/DataManagement/DataManageView.swift Views/Settings/DataManagement/DataDeleteView.swift Views/Characters/CharacterCollectionView.swift Views/Settings/AppearanceSettingsView.swift Views/Settings/AboutView.swift Views/Settings/WatchFacePreferencesView.swift Views/History/MeasurementDetailView.swift Views/Breathing/BreathingExerciseView.swift Views/MiniWalk/MiniWalkView.swift; do grep -q "accessibleDynamicType()" "StressMonitor/StressMonitor/$f" || echo "MISSING: $f"; done; echo SWEEP_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
    <fails_when>any "MISSING:" line prints before SWEEP_DONE (an unadopted manifest surface), or the build tail contains "** BUILD FAILED **"</fails_when>
  </verify>
  <done>All 14 D-03 manifest surfaces apply the reworked modifier (grep output pasted in SUMMARY, zero MISSING), exempt surfaces untouched, app target builds.</done>
  <acceptance_criteria>
  - The 14-file grep loop prints zero "MISSING:" lines (output pasted into the SUMMARY)
  - No breathing session/summary view, Onboarding, or Paywall file was modified
  - grep -rn "limitedDynamicType()" StressMonitor/StressMonitor/Views --include="*.swift" returns no manifest-surface call without a dated exception note
  - App scheme builds with "** BUILD SUCCEEDED **"
  </acceptance_criteria>
</task>

</tasks>

<flagged_assumptions>
- **Research A1 (carried, executor-verified in Task 2):** the iOS text-style default ramp at Large (title 28 / title2 22 / body 17 / headline 17 semibold / footnote 13 / caption2 11) is training-data-sourced. The byte-identical parity measurement is the guard; a ScaledMetric point-size anchor is the per-token fallback. If a drift is found and compensated, record it — it is a discovered fact, not a failure.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | View-modifier and font-token rework only; no data, network, or privilege surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | deletion of superseded helper variants breaks compilation | low | mitigate | Zero-adopter status grep-verified immediately before each deletion; full app-target build gates the task |
| T-03-SC | Supply chain | package installs | low | accept | No installs — first-party SwiftUI only |
</threat_model>

<verification>
- App scheme builds after each task.
- The 14-surface adoption grep prints zero MISSING lines (output pasted into SUMMARY — this is the D-10 layer-1 gate shape; the dated record is finalized in 03-06 against the post-deletion tree).
- Deleted-symbol greps return zero hits.
</verification>

<success_criteria>
- The Dynamic Type mechanism no longer contradicts the zero-truncation contract (no cap, no shrink anywhere on manifest surfaces).
- Typography scales through the platform system ramp with rendered sizes unchanged at default.
- The D-03 manifest and the source tree agree 1:1 on adoption.
</success_criteria>

<artifacts_this_phase_produces>
New symbols/files created by this plan:
- Reworked API: View.accessibleDynamicType() (no-argument form; AccessibleDynamicTypeModifier body reduced to the unlimited-lines modifier)
- Anchored Font.WellnessType tokens: cardTitle, sectionHeader, body, bodyEmphasized, caption, caption2 (text-style-anchored declarations)
- Deleted symbols: scalableText + its modifier, AdaptiveTextSizeModifier, accessibleWellnessType* modifiers
- Adoption: one .accessibleDynamicType() line at the root container of 10 surface files
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-accessibility-compliance/03-02-SUMMARY.md` when done
</output>
