---
phase: 03-accessibility-compliance
plan: 06
type: execute
wave: 4
depends_on: ["03-05"]
files_modified:
  - StressMonitor/StressMonitor/Views/DesignSystem/Components/Badge.swift
  - StressMonitor/StressMonitor/Theme/Color+Extensions.swift
  - StressMonitor/StressMonitor/Utilities/HighContrastModifier.swift
  - StressMonitor/StressMonitor/Utilities/PatternOverlay.swift
  - StressMonitor/StressMonitor/Utilities/ColorBlindnessSimulator.swift
  - .planning/phases/03-accessibility-compliance/03-ORPHAN-AUDIT-RECORD.md
  - .planning/phases/03-accessibility-compliance/03-TRUST-GATE-RECORD.md
  - .planning/phases/03-accessibility-compliance/03-A11Y-UAT.md
autonomous: false
requirements: [A11Y-05]
estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Orphaned compiled view types unreachable from the navigation graph are deleted from disk (no conditional-compilation hiding) — a dated audit record enumerates every candidate file with a per-file disposition: deleted, or retained-because with the live reference named"
    - "StressCategory.displayName was relocated out of the orphan-candidate Badge.swift into a live file BEFORE any deletion, and every live consumer compiles"
    - "All three targets (app, watch via the StressMonitorWatch Watch App scheme, widget via StressMonitorWidgetExtension) build clean after the deletion batches from a clean build directory (delete-compile ground truth)"
    - "HighContrastModifier.swift, PatternOverlay.swift, and ColorBlindnessSimulator.swift no longer exist on disk (D-05 plus the verified zero-call-site class)"
    - "The app-side duplicate stressRelaxed…stressSevere statics in Color+Extensions.swift are dispositioned: deleted if grep confirms zero direct users (Color.stressColor(for:) delegation preserved), or retained-because recorded"
    - "Both phase trust gates were re-run against the FINAL post-deletion tree with pasted command output: the D-10 adoption grep (14/14 manifest surfaces, zero MISSING) and the D-13 reduce-motion grep (zero raw reads outside the helper file)"
    - "The full canonical test suite (AGENTS.md invocation) is green on the final tree, including ContrastComplianceTests and ChartAccessibilityTests"
    - "A dated provenance anchor for the UI-SPEC component inventory exists: the audit record's per-file disposition covers every Views/DesignSystem component, closing the checker's inventory-provenance defect"
    - statement: "At AX5 no label truncates, clips, or overlaps another element on any manifest surface — per-surface walkthrough screenshots recorded in UAT, light and dark"
      verification: backstop
    - statement: "When layouts stack or wrap at accessibility sizes, content order matches the default-size reading order — verified in the AX5 walkthrough"
      verification: backstop
    - statement: "Adjacent interactive controls retain separate, non-overlapping hit regions — no shared tap points between neighboring controls, confirmed per surface in the Accessibility Inspector scan"
      verification: backstop
    - statement: "VoiceOver focus order follows the visual reading order on each manifest surface — confirmed in the per-surface UAT walkthrough"
      verification: backstop
    - statement: "With Reduce Motion enabled (Settings or the DEBUG seam), no looping, scaling, or parallax animation plays on manifest surfaces — verified in the RM walkthrough"
      verification: backstop
    - statement: "TrendsView's async load failure UI follows the operation-title + next-step shape — verified in the per-surface UAT sweep"
      verification: backstop
    - statement: "DataManageView's zero-rows state follows the NoDataCard pattern or carries a recorded cannot-render-empty disposition — verified in UAT"
      verification: backstop
    - statement: "Chart data renders correctly at both extremes — a single-point series presents the steady summary (no percent token) and a many-point series the up/down variant, verified in the UAT walkthrough (machine-pinned by ChartAccessibilityTests from plan 03-04)"
      verification: backstop
    - statement: "Widget surfaces (gallery Small/Medium/Large, Lock Screen widget, Live Activity) and watch surfaces render legibly at AX5 — anchored text scales, no truncation or overflow is introduced by the D-02 anchors, and every dated exception from the 03-02 Task 4 sweep renders correctly — verified light + dark in the UAT walkthrough"
      verification: backstop
  artifacts:
    - .planning/phases/03-accessibility-compliance/03-ORPHAN-AUDIT-RECORD.md
    - .planning/phases/03-accessibility-compliance/03-TRUST-GATE-RECORD.md
    - .planning/phases/03-accessibility-compliance/03-A11Y-UAT.md
  key_links:
    - "UI-SPEC 81-file candidate list (audit INPUT) <-> D-14 audit record (the OUTPUT: finalized delete/retain set with reasons)"
    - "Deletion batches <-> three-scheme clean build (delete-compile ground truth)"
    - "Both grep gates <-> the final post-deletion source tree"
  prohibitions:
    - statement: "MUST NOT use conditional-compilation or feature-flag hiding as a substitute for deletion — orphans are removed from disk entirely (D-14)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT delete a candidate file before its extension members used by live code are relocated and before the three-target delete-compile passes (Badge.swift displayName precedent)"
      status: resolved
      verification: judgment
---

<objective>
Close A11Y-05 and the phase: run the D-14 reachability audit (grep BFS input + extension-member screen + Periphery cross-check), delete the finalized orphan set from disk behind a decision checkpoint, prove all three targets still compile, then record both standing trust gates and the human-verification apparatus against the final tree.

Purpose: "No unreachable duplicate screen ships alongside the real one" is a deletion-and-proof requirement — the audit decides the set, the compiler proves it, and the gates make the state durable. Deletion runs LAST so it removes superseded helper variants and dead views instead of colliding with in-flight edits.
Output: three phase records (audit, trust gates, UAT checklist), a smaller compiled UI layer, a green full suite on the final tree.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-accessibility-compliance/03-CONTEXT.md
@.planning/phases/03-accessibility-compliance/03-RESEARCH.md
@.planning/phases/03-accessibility-compliance/03-PATTERNS.md
@.planning/phases/03-accessibility-compliance/03-UI-SPEC.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-TRUST-GATE-RECORD.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-14 audit — finalize the deletion set and write 03-ORPHAN-AUDIT-RECORD.md</name>
  <files>.planning/phases/03-accessibility-compliance/03-ORPHAN-AUDIT-RECORD.md</files>
  <read_first>
  - .planning/phases/03-accessibility-compliance/03-UI-SPEC.md "A11Y-05 Orphan Candidates" appendix (the 81-file scan result and its two method caveats)
  - StressMonitor/StressMonitor/Navigation/Route.swift + Navigation/View+NavigationDestinations.swift (the reachable-set definition)
  - StressMonitor/StressMonitor/Views/MainTabView.swift (the four tab roots the BFS starts from)
  - StressMonitor/StressMonitor/Views/DesignSystem/Components/Badge.swift (lines 40-54: StressCategory.displayName — the relocation trap)
  - .planning/phases/03-accessibility-compliance/03-RESEARCH.md Pattern 5 (the audit pipeline) and Pitfall 3 (the extension-member false-positive class)
  </read_first>
  <action>
  Produce the finalized deletion set as a dated record — the audit decides, nothing is deleted in this task. Regenerate the candidate input mechanically: file-level BFS over view-constructor edges from the four tab roots plus every Route destination, with #Preview blocks stripped, over the app target (the UI-SPEC's 81-file list is the cross-reference; regenerate rather than trust it — the tree changed this phase). Screen the known false-positive classes before any file enters the delete set: (a) extension members declared in a candidate but consumed by live code — grep every extension member each candidate declares; the verified instance is the StressCategory displayName extension living inside orphan-candidate Badge.swift while live code calls it, and any sibling instance gets the same relocate-then-delete disposition; (b) in-file helper usage — ShimmerEffectView reads orphaned but is live via the shimmerLoading extension in AnimationPresets (plan 03-05 kept it live); (c) references from exempt surfaces — the premium view referenced by PaywallView is NOT an orphan. Cross-check with Periphery (pre-installed at /opt/homebrew/bin/periphery; run against the project and the app scheme, preview retention OFF, excluding nothing silently): every disagreement between Periphery's unused-set and the grep-BFS set becomes a hand-review row in the record with its verdict — Periphery is the cross-check, the delete-compile is the judge, and if the tool cannot index the proxy SPM setup, record that and proceed on BFS + delete-compile alone (research A6 fallback). Include in the delete set: the three zero-call-site utilities (HighContrastModifier per D-05, PatternOverlay, ColorBlindnessSimulator) and a disposition for the app-side duplicate stressRelaxed…stressSevere statics in Color+Extensions.swift (delete if grep confirms zero direct users beyond the delegating Color.stressColor(for:), which is preserved; else retain-because). Write 03-ORPHAN-AUDIT-RECORD.md phase-2 trust-gate style: dated header, method section, the per-file disposition table covering EVERY candidate (deleted / retained-because + the live reference), the Periphery disagreement rows, and the component-inventory provenance section enumerating every Views/DesignSystem component with its liveness verdict — that section is the dated provenance anchor the UI-SPEC inventory lacked. The watch target keeps its own duplicated files; nothing watch-side is touched by this audit except where a watch file mirrors an app token (none are deletion candidates — mirror convention, not orphans).
  </action>
  <precondition>command -v periphery succeeds OR the record documents the fallback (BFS + delete-compile alone close the audit per research A6)</precondition>
  <verify>
    <automated>test -f .planning/phases/03-accessibility-compliance/03-ORPHAN-AUDIT-RECORD.md && grep -c "retained-because\|Retained" .planning/phases/03-accessibility-compliance/03-ORPHAN-AUDIT-RECORD.md && grep -c "displayName\|HighContrastModifier\|PatternOverlay\|ColorBlindnessSimulator\|stressRelaxed" .planning/phases/03-accessibility-compliance/03-ORPHAN-AUDIT-RECORD.md; echo AUDIT_RECORD_OK</automated>
    <fails_when>the record file is missing (test -f fails, non-zero exit), either grep count is 0 (no retained-because dispositions, or the four named trap tokens absent), or AUDIT_RECORD_OK never prints</fails_when>
  </verify>
  <done>Every candidate has a per-file disposition; the four trap classes are explicitly dispositioned; the record is dated and complete — the deletion set is final.</done>
  <acceptance_criteria>
  - 03-ORPHAN-AUDIT-RECORD.md exists with a dated header and a method section
  - The disposition table covers every regenerated candidate (count matches the regenerated input; the UI-SPEC's 81 is cross-referenced, drift explained)
  - The displayName relocation, ShimmerEffectView liveness, premium-view liveness, three zero-call-site utilities, and the duplicate statics each carry an explicit named disposition
  - A Views/DesignSystem component provenance section exists with a liveness verdict per component
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: DECISION — approve the mass orphan deletion (one-way door)</name>
  <files>.planning/phases/03-accessibility-compliance/03-ORPHAN-AUDIT-RECORD.md</files>
  <decision>Approve the finalized D-14 orphan deletion set (approximately half the compiled UI layer, ~80 view files) for deletion from disk</decision>
  <context>
  Task 1's audit record holds the finalized per-file disposition set. Deleting wrongly-classified files is recoverable in principle (git revert) but expensive in practice — restored files must also regain their call-graph context, and A11Y-05 is a one-way scope decision about what ships in the binary. The user sees: the deletion-set file list, the retained-because set with live references named, the displayName relocation plan, and the batched delete-compile proof plan. The user's decision is recorded in the audit record with a timestamp before Task 3 touches any file.
  </context>
  <options>
    <option id="approve-full-set">
      <name>Approve the full audited set</name>
      <pros>Completes A11Y-05 exactly as audited; smallest compiled surface; audit already screened the false-positive classes</pros>
      <cons>~80 files removed in one phase; any audit miss surfaces as a delete-compile break (caught, but costs a restore cycle)</cons>
    </option>
    <option id="approve-trimmed">
      <name>Approve a trimmed set (defer borderline families)</name>
      <pros>Smaller blast radius; borderline families (e.g. the classic card set) can delete in a follow-up once the surface work has soaked</pros>
      <cons>A11Y-05 stays partially open; the phase trust gates record a larger retained-because set</cons>
    </option>
    <option id="reject">
      <name>Reject deletion this phase</name>
      <pros>Zero risk to the tree; audit record still lands as documented debt</pros>
      <cons>A11Y-05 unmet — unreachable duplicate screens keep compiling into the binary; phase cannot fully close</cons>
    </option>
  </options>
  <resume-signal>Reply with the chosen option id (approve-full-set / approve-trimmed with the families to defer / reject); the executor records the decision in 03-ORPHAN-AUDIT-RECORD.md and proceeds to Task 3 accordingly</resume-signal>
  <verify>
    <automated>grep -c "Approved\|approved\|Rejected\|rejected" .planning/phases/03-accessibility-compliance/03-ORPHAN-AUDIT-RECORD.md; echo DECISION_RECORDED</automated>
    <fails_when>the count is 0 (no decision recorded in the audit record) or DECISION_RECORDED never prints</fails_when>
  </verify>
  <done>The user's dated decision on the deletion set is recorded in the audit record.</done>
  <acceptance_criteria>
  - The audit record contains a dated decision entry naming the approved (or modified) deletion set
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Execute the deletion + delete-compile proof, then record both trust gates and the UAT apparatus against the final tree</name>
  <files>StressMonitor/StressMonitor/Views/DesignSystem/Components/Badge.swift, StressMonitor/StressMonitor/Theme/Color+Extensions.swift, StressMonitor/StressMonitor/Utilities/HighContrastModifier.swift, StressMonitor/StressMonitor/Utilities/PatternOverlay.swift, StressMonitor/StressMonitor/Utilities/ColorBlindnessSimulator.swift, .planning/phases/03-accessibility-compliance/03-TRUST-GATE-RECORD.md, .planning/phases/03-accessibility-compliance/03-A11Y-UAT.md</files>
  <read_first>
  - .planning/phases/03-accessibility-compliance/03-ORPHAN-AUDIT-RECORD.md (the approved disposition table — the work order)
  - StressMonitor/StressMonitor/Views/DesignSystem/Components/Badge.swift (the displayName extension to relocate first)
  - StressMonitor/StressMonitor/Models/StressCategory.swift (the relocation destination — the extension joins its type)
  - StressMonitor/StressMonitor/Theme/Color+Extensions.swift (the duplicate statics disposition)
  - .planning/phases/02-delete-correctness-test-suite-trust/02-TRUST-GATE-RECORD.md (the record shape: dated header, pasted output, enumeration, verdict)
  - AGENTS.md (the canonical full-suite invocation both gates and the final run use)
  </read_first>
  <action>
  Execute in strict order. (1) Relocate first: move the StressCategory displayName extension out of Badge.swift into a live home beside its type (Models/StressCategory.swift or an extension file that the audit marks live); build once to prove the relocation before any deletion. (2) Delete per the approved set, in small batches by candidate family (classic card set, ripple-era leftovers, legacy history, unused gauges, unused DesignSystem components, the three zero-call-site utilities, the duplicate color statics if approved) — deletion is removing the files from disk; the app sources folder is a synchronized root group so no project-file surgery applies, and the test target is untouched (its registered files are all live). After EACH batch: clean-build all three schemes (app; watch via the StressMonitorWatch Watch App scheme against the generic watchOS Simulator destination; widget via StressMonitorWidgetExtension) — a batch that breaks compilation is either a missed live reference (restore the file, add a retained-because row) or a missed relocation (relocate, retry); the compiler is the ground truth and the audit record is amended, never the build settings. Use a clean build directory for the final pass (stale DerivedData after mass deletion lies). (3) Write 03-TRUST-GATE-RECORD.md against the FINAL tree with pasted output for all three gates: the D-10 adoption grep — the 14-file loop from plan 03-02 over the D-03 manifest, zero MISSING lines; the D-13 reduce-motion grep — zero raw reads outside the helper file, with the D-11 breathing carve-out explicitly dispositioned; the D-02 widget/watch Dynamic Type anchor grep — zero unanchored Font.system(size:) sites and zero undocumented minimumScaleFactor sites across widget Views, the Live Activity, watch Views, and complications (the two gate greps from plan 03-02 Task 4, re-run against the final tree); plus the symmetric app-side shrink grep — minimumScaleFactor over StressMonitor/StressMonitor/Views with the same inline dated-exception escape ("dated exception 2026-09-DD: <reason>"); plan-time baseline is 7 sites across 6 files (MoodCheckInView:66, BioAgeCardView:80, StressSourcesCard:152, BioAgeDetailView:67, CharacterPickerSheet:65, EvolutionStageRow:55 and :64) — each is either removed per the D-10 no-shrink contract during execution or becomes a dated exception enumerated in the SUMMARY; plus the full canonical suite run (AGENTS.md invocation, -parallel-testing-enabled NO) green with the two new suites (ContrastComplianceTests, ChartAccessibilityTests) enumerated in the results. (4) Write 03-A11Y-UAT.md — the per-surface human-verification apparatus for end-of-phase /gsd-verify-work: for each of the 14 manifest surfaces, an AX5 walkthrough row (set AX5 via xcrun simctl ui <device> content_size accessibility-extra-extra-extra-large, capture light + dark via the appearance subcommand, verify zero truncation/overlap, reset to large), an Accessibility Inspector hit-target scan row checked against plan 03-03's enumeration, a reduce-motion walkthrough row (Settings toggle or the DEBUG launch-arg seam — no simctl subcommand exists on Xcode 26.3), and the breathing fallback walkthrough row. Add the D-02 widget and watch rows: widget rows for the gallery Small/Medium/Large widgets, the Lock Screen widget, and the Live Activity (set AX5 on the iPhone simulator, then force a widget re-render — relaunch the host app or re-edit the widget — because widget snapshots cache at the rendered size; screenshot light + dark; verify anchored text scales without overflow and every dated exception from 03-02 Task 4's enumeration still renders legibly), and watch rows for the watch app's main surfaces (home, menu, cycle, history, workout) plus one complication family (set AX5 on the watch simulator via simctl ui where supported, else watch Settings -> Accessibility -> Larger Text; screenshot light + dark; same checks against the exception enumeration). Close the record with the explicit zero-unaccounted verdict line, phase-2 style.
  </action>
  <verify>
    <automated>test ! -f StressMonitor/StressMonitor/Utilities/HighContrastModifier.swift && test ! -f StressMonitor/StressMonitor/Utilities/PatternOverlay.swift && test ! -f StressMonitor/StressMonitor/Utilities/ColorBlindnessSimulator.swift; echo DELETIONS_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3 && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme "StressMonitorWatch Watch App" -destination 'generic/platform=watchOS Simulator' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3 && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitorWidgetExtension -destination 'generic/platform=iOS Simulator' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3 && for f in Views/DashboardView.swift Views/Action/ActionView.swift Views/Trends/TrendsView.swift Views/Settings/SettingsView.swift Views/Settings/DataManagement/DataExportView.swift Views/Settings/DataManagement/DataManageView.swift Views/Settings/DataManagement/DataDeleteView.swift Views/Characters/CharacterCollectionView.swift Views/Settings/AppearanceSettingsView.swift Views/Settings/AboutView.swift Views/Settings/WatchFacePreferencesView.swift Views/History/MeasurementDetailView.swift Views/Breathing/BreathingExerciseView.swift Views/MiniWalk/MiniWalkView.swift; do grep -q "accessibleDynamicType()" "StressMonitor/StressMonitor/$f" || echo "MISSING: $f"; done; grep -rn "accessibilityReduceMotion\|isReduceMotionEnabled" StressMonitor/StressMonitor --include="*.swift" | grep -v "Utilities/Animation+Wellness.swift"; grep -rn "\.font(\.system(size:" StressMonitor/StressMonitorWidget/Views StressMonitor/StressMonitorWidget/StressMonitorWidgetLiveActivity.swift "StressMonitor/StressMonitorWatch Watch App/Views" "StressMonitor/StressMonitorWatch Watch App/Complications" --include="*.swift" | grep -v "relativeTo:" | grep -v "dated exception"; grep -rn "minimumScaleFactor" "StressMonitor/StressMonitorWatch Watch App/Views" "StressMonitor/StressMonitorWatch Watch App/Complications" --include="*.swift" | grep -v "dated exception"; grep -rn "minimumScaleFactor" StressMonitor/StressMonitor/Views --include="*.swift" | grep -v "dated exception"; echo FINAL_GATES_DONE</automated>
    <fails_when>DELETIONS_DONE never prints (a utility file still exists — test ! -f failed), any of the three build tails contains "** BUILD FAILED **", any "MISSING:" line prints before FINAL_GATES_DONE (adoption gate broken by deletion), any raw reduce-motion grep line prints before FINAL_GATES_DONE (consolidation gate broken), or any anchor/shrink gate line prints before FINAL_GATES_DONE (D-02 widget/watch or app-side no-shrink gate broken — an unanchored font site or undocumented shrink survived, watch- or app-side)</fails_when>
    <automated>xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -derivedDataPath build -skipPackagePluginValidation -parallel-testing-enabled NO -maximum-concurrent-test-simulator-destinations 1 CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -5</automated>
    <fails_when>the tail output contains "** TEST FAILED **" or a failing-tests summary line — the canonical full-suite run must end "** TEST SUCCEEDED **" with 0 failures on the final tree</fails_when>
  </verify>
  <done>Approved orphans gone from disk, all three targets clean-build, all three grep gates green against the final tree (adoption, reduce-motion, D-02 anchor), full canonical suite green, and the three phase records exist for verification.</done>
  <acceptance_criteria>
  - The three zero-call-site utility files no longer exist on disk
  - All three schemes report "** BUILD SUCCEEDED **" from a clean build directory
  - The 14-file adoption grep prints zero MISSING lines; the reduce-motion grep, the D-02 widget/watch anchor greps, and the app-side shrink grep print zero result lines (all outputs pasted in 03-TRUST-GATE-RECORD.md)
  - The full canonical suite (AGENTS.md invocation) reports 0 failures with ContrastComplianceTests and ChartAccessibilityTests present in the run
  - 03-TRUST-GATE-RECORD.md and 03-A11Y-UAT.md exist; the trust record carries the zero-unaccounted verdict; the UAT record has all 14 per-surface rows with AX5/Inspector/RM columns plus the widget rows (gallery Small/Medium/Large, Lock Screen, Live Activity) and watch rows (main surfaces + one complication family) at AX5 light+dark
  - StressCategory.displayName compiles from its relocated home (Badge.swift deletion did not break live consumers)
  </acceptance_criteria>
  <reversibility rating="one-way">Mass deletion of roughly half the UI layer: git-revertable in principle, but a wrong deletion surfacing after later commits requires restoring call-graph context, not just files — the Task 2 checkpoint plus batched delete-compile is the safety mechanism.</reversibility>
</task>

</tasks>

<flagged_assumptions>
- **A11Y-05 edge probe (unclassified — flagged, not auto-resolved):** the 81-file candidate list is an INPUT produced by a constructor-edge scan, not a verdict. The D-14 audit (regenerated BFS + extension-member screen + Periphery cross-check + delete-compile) finalizes the set; delete-compile is ground truth. Residual risk: a view reachable only through a mechanism the scan does not model (string-based routing does not exist in this codebase — value-typed Route enum verified — so the risk is bounded to extension members and preview-only references, both screened).
- **UI-SPEC UI Considerations (backstop rows):** the seven backstop-class rows are lifted into must_haves.truths above as structured flat-scalar markers — at verify time an unconfirmable one abstains to human_needed (reason insufficient_spec), never a silent pass.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compiled binary <-> shipped attack surface | Dead view code compiled into the binary is reconnaissance aid and needless surface (ASVS V14, reduced applicability) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Information Disclosure | dead view layer remains compiled into the shipped binary | medium | mitigate | A11Y-05 deletes orphans from disk; the synchronized root group removes them from the target automatically; three-scheme clean build is the proof |
| T-03-09 | Tampering | deletion breaks live code through hidden extension-member dependencies | high | mitigate | Extension-member screen in the audit (displayName relocation is the verified instance); Periphery index cross-check; batched delete-compile with restore-on-break — the compiler is ground truth |
| T-03-10 | Repudiation | deletion set drifts from the recorded audit | medium | mitigate | The blocking decision checkpoint pins the approved set; every mid-execution change amends the record's disposition table in the same batch |
| T-03-SC | Supply chain | package installs | low | accept | No installs — periphery is a pre-installed dev-only CLI, never shipped; delete-compile alone suffices if it cannot index the project |
</threat_model>

<verification>
- Three-scheme clean builds green after the final deletion batch (delete-compile ground truth).
- All three grep gates (D-10 adoption, D-13 reduce-motion, D-02 widget/watch anchor) green against the final tree with output pasted in 03-TRUST-GATE-RECORD.md.
- Full canonical suite green on the final tree.
- The three phase records exist and are complete for /gsd-verify-work 3.
</verification>

<success_criteria>
- No unreachable duplicate screen compiles into the binary; the audit record accounts for every candidate.
- Both standing a11y gates (adoption, reduce-motion) are recorded durable against the final tree.
- The human-verification apparatus (AX5 walkthrough, Inspector scans, RM walkthrough, breathing fallback) is ready for end-of-phase verification.
</success_criteria>

<artifacts_this_phase_produces>
New symbols/files created by this plan:
- Files: .planning/phases/03-accessibility-compliance/03-ORPHAN-AUDIT-RECORD.md, 03-TRUST-GATE-RECORD.md, 03-A11Y-UAT.md
- Relocation: the StressCategory displayName extension moves from Views/DesignSystem/Components/Badge.swift to a live home beside its type
- Deletions: the approved orphan set (file removals from disk), the three zero-call-site utilities, and (if approved) the duplicate stressRelaxed…stressSevere statics in Color+Extensions.swift
- No new compiled symbols — this plan only removes and records
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-accessibility-compliance/03-06-SUMMARY.md` when done
</output>
