---
phase: 03-accessibility-compliance
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - StressMonitor/StressMonitor/Views/DashboardView.swift
  - StressMonitor/StressMonitor/Views/Action/ActionView.swift
  - StressMonitor/StressMonitor/Views/Trends/TrendsView.swift
  - StressMonitor/StressMonitor/Views/Settings/SettingsView.swift
  - StressMonitor/StressMonitor/Views/Settings/DataManagement/DataExportView.swift
  - StressMonitor/StressMonitor/Views/Settings/DataManagement/DataManageView.swift
  - StressMonitor/StressMonitor/Views/Settings/DataManagement/DataDeleteView.swift
  - StressMonitor/StressMonitor/Views/Characters/CharacterCollectionView.swift
  - StressMonitor/StressMonitor/Views/Settings/AppearanceSettingsView.swift
  - StressMonitor/StressMonitor/Views/Settings/AboutView.swift
  - StressMonitor/StressMonitor/Views/Settings/WatchFacePreferencesView.swift
  - StressMonitor/StressMonitor/Views/History/MeasurementDetailView.swift
  - StressMonitor/StressMonitor/Views/MiniWalk/MiniWalkView.swift
  - StressMonitor/StressMonitor/Views/Dashboard/Components/MoodCheckInView.swift
  - StressMonitor/StressMonitor/Views/Dashboard/Components/QuickActionGrid.swift
autonomous: true
requirements: [A11Y-01]
estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Controls visually smaller than 44x44pt (icon-only buttons, compact rows, mood check-in controls, quick-action tiles) receive a >=44x44pt hit area via .minimumTouchTarget(DesignTokens.Layout.minTouchTarget) — the hit area may exceed the visual glyph; intrinsically >=44pt controls (52pt buttons) comply unchanged"
    - "Every icon-only interactive control on the 14 manifest surfaces carries a non-empty accessibilityLabel (or visible adjacent text) — zero unlabeled icon controls remain"
    - "The 44pt floor is the exact DesignTokens.Layout.minTouchTarget constant (44) — every sweep adoption passes the token; no ad-hoc frame literal below 44 sizes an interactive control's hit area"
    - "Everywhere a stress category shows on a manifest surface, the .stressDualCoding modifier is applied — hue + SF Symbol + display name together (the modifier's caption now renders on the adaptive text token per plan 03-01)"
    - "The Dashboard error alert states the failing operation and a next step (title such as 'Couldn't load health data' with a 'Try Again' action) instead of a generic title with a bare localizedDescription"
    - "Dashboard's zero-data state renders the existing NoDataCard pattern with per-data-type title/description/action copy (existing behavior — verified, not redesigned)"
    - "Missing biometric factors keep their existing behavior: the factor card omits the factor (weight redistribution) and labels stay on the adaptive primary text token (verified, not changed)"
    - "Locked character cards dim the illustration only — card text never renders below full contrast (no whole-card opacity dimming over text)"
  artifacts:
    - Enumeration record in 03-03-SUMMARY.md: per surface, the controls upgraded with the touch-target helper and the icon-only labels added
  key_links:
    - "Sub-44pt interactive controls on the 14 D-03 surfaces <-> .minimumTouchTarget(DesignTokens.Layout.minTouchTarget)"
    - "Category displays on manifest surfaces <-> .stressDualCoding (dual coding per UI-SPEC)"
  prohibitions:
    - statement: "MUST NOT reduce any existing control's hit area below 44x44pt while restyling — adoption may only enlarge hit areas, never shrink them"
      status: resolved
      verification: judgment
    - statement: "MUST NOT introduce invisible hit-area overlays that intercept taps intended for neighboring controls — expansion is bounded to the control's own frame via the contentShape modifier"
      status: resolved
      verification: judgment
---

<objective>
Sweep the 14 manifest surfaces for A11Y-01: every interactive control reaches the 44pt floor through the existing helper and token, every icon-only control is labeled, dual coding is adopted wherever a stress category shows, and the audit-surfaced state-shape gaps (error alert copy, empty-state triage, locked-character dimming) are resolved with documented patterns rather than improvisation.

Purpose: A11Y-01 ("All touch targets meet the 44pt minimum") plus the UI-SPEC interaction contracts that ride the same per-surface sweep. The helper (minimumTouchTarget) and token (minTouchTarget = 44) already exist with zero adopters — this is pure adoption with judgment about which controls need it.
Output: 14 swept surfaces + risk-class components, a per-surface enumeration record for the UAT apparatus.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-accessibility-compliance/03-CONTEXT.md
@.planning/phases/03-accessibility-compliance/03-RESEARCH.md
@.planning/phases/03-accessibility-compliance/03-UI-SPEC.md
@StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift
@StressMonitor/StressMonitor/Theme/DesignTokens.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Touch-target + icon-only-label sweep on the 13 non-character surfaces and risk-class components</name>
  <files>StressMonitor/StressMonitor/Views/DashboardView.swift, StressMonitor/StressMonitor/Views/Action/ActionView.swift, StressMonitor/StressMonitor/Views/Trends/TrendsView.swift, StressMonitor/StressMonitor/Views/Settings/SettingsView.swift, StressMonitor/StressMonitor/Views/Settings/DataManagement/DataExportView.swift, StressMonitor/StressMonitor/Views/Settings/DataManagement/DataManageView.swift, StressMonitor/StressMonitor/Views/Settings/DataManagement/DataDeleteView.swift, StressMonitor/StressMonitor/Views/Settings/AppearanceSettingsView.swift, StressMonitor/StressMonitor/Views/Settings/AboutView.swift, StressMonitor/StressMonitor/Views/Settings/WatchFacePreferencesView.swift, StressMonitor/StressMonitor/Views/History/MeasurementDetailView.swift, StressMonitor/StressMonitor/Views/MiniWalk/MiniWalkView.swift, StressMonitor/StressMonitor/Views/Breathing/BreathingExerciseView.swift, StressMonitor/StressMonitor/Views/Dashboard/Components/MoodCheckInView.swift, StressMonitor/StressMonitor/Views/Dashboard/Components/QuickActionGrid.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift (the MinimumTouchTargetModifier mechanism at lines 50-58 and the free-function entry at 11-14 — frame minWidth/minHeight plus contentShape on Rectangle)
  - StressMonitor/StressMonitor/Theme/DesignTokens.swift (Spacing.Layout.minTouchTarget = 44 at line 16 — the assertable constant; zero current users)
  - Each surface file listed above (interactive-control inventory before editing)
  - .planning/phases/03-accessibility-compliance/03-UI-SPEC.md "A11Y-01 — Touch targets (SC-1)" (risk classes: icon-only toolbar/close/info buttons, mood check-in, quick-action tiles, watch-face preference rows, chart legends)
  </read_first>
  <action>
  Implement the A11Y-01 sweep per the UI-SPEC contract. For each of the 13 surfaces (all D-03 manifest surfaces except CharacterCollectionView, which is Task 3) plus the two named risk-class components: inventory every interactive control (Button, onTapGesture-bearing views, toggles, steppers, swipe actions, menu triggers); leave intrinsically >=44pt controls untouched (the 52pt PrimaryButton/SecondaryButton comply by construction); apply .minimumTouchTarget(DesignTokens.Layout.minTouchTarget) to every control whose visual frame is below 44x44pt — icon-only toolbar/close/info buttons get an invisible 44pt target that may exceed the glyph. Every icon-only control (an Image with a system symbol inside a tappable context with no visible text) gains a non-empty accessibilityLabel naming its action ("Export data", "Close", not "Button"). Do not touch chart-component files (StressBarChartView, HRVTrendChart, SparklineChart, MiniLineChartView, StressRingView) — legend and chart-internal interactive controls belong to plan 03-04's file ownership. Chart legends living in a surface root file ARE this task's. Do not shrink any existing hit area (adoption only enlarges). Widget and watch surfaces are out of this sweep entirely — per D-02 the 44pt rule does not apply to them (the widget is not touch UI; watch surfaces are outside the D-03 manifest). As you go, build the per-surface enumeration in the SUMMARY: surface -> controls upgraded -> labels added — this record is the input the phase UAT's Accessibility Inspector scan verifies against.
  </action>
  <verify>
    <automated>grep -rn "minimumTouchTarget(" StressMonitor/StressMonitor/Views --include="*.swift" | wc -l; echo TARGET_COUNT_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3 && swiftlint lint --quiet | grep -v '^$' | head -5; echo LINT_DONE</automated>
    <fails_when>the adoption count prints 0 (baseline was zero adopters — a no-op sweep), or the build tail contains "** BUILD FAILED **", or lint violation lines name files this task touched before LINT_DONE</fails_when>
  </verify>
  <done>Every sub-44pt interactive control on the swept surfaces carries the token-backed minimum hit area, every icon-only control is labeled, and the per-surface enumeration is recorded in the SUMMARY.</done>
  <acceptance_criteria>
  - grep -rn "minimumTouchTarget(" over StressMonitor/StressMonitor/Views returns a count > 0 and every occurrence passes DesignTokens.Layout.minTouchTarget (no ad-hoc numeric literal argument on manifest surfaces)
  - The SUMMARY enumerates per surface the upgraded controls and added labels
  - No chart-component file (StressBarChartView, HRVTrendChart, SparklineChart, MiniLineChartView, StressRingView) was modified by this task
  - App scheme builds with "** BUILD SUCCEEDED **" and lint introduces no new violations in touched files
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Dual-coding adoption wherever a stress category shows</name>
  <files>StressMonitor/StressMonitor/Views/DashboardView.swift, StressMonitor/StressMonitor/Views/Trends/TrendsView.swift, StressMonitor/StressMonitor/Views/History/MeasurementDetailView.swift, StressMonitor/StressMonitor/Views/Settings/DataManagement/DataManageView.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift (StressDualCodingModifier and the stressDualCoding entry — now caption-safe per plan 03-01 Task 3; currently zero adopters)
  - StressMonitor/StressMonitor/Views/DashboardView.swift + TrendsView.swift + MeasurementDetailView.swift + DataManageView.swift (find every site rendering a StressCategory's color, name, or badge — the adoption points)
  - .planning/phases/03-accessibility-compliance/03-UI-SPEC.md "Stress-category palette — indicator class, dual-coded" (dual coding mandatory wherever a category shows)
  </read_first>
  <action>
  Adopt .stressDualCoding at every site on the manifest surfaces where a stress category is displayed to the user — the dashboard's current-reading category line, the trends surfaces' category indicators, the measurement-detail category, and the data-manage list's category column if it renders one (verify by reading each file; the exact set is what the read produces — the UI-SPEC makes the rule, the code shows the sites). The modifier already delivers hue + SF Symbol + display name with its caption on the adaptive text token, so adoption is applying it to the category display element. Where a site renders the category hue alone today (a colored dot/ring/badge with no symbol or name next to it), restructure that one element to carry all three channels — never leave the hue as the only channel, which is the WCAG use-of-color failure class this closes. Keep each adoption minimal: this is not a redesign pass; only the category-display element changes.
  </action>
  <verify>
    <automated>grep -rn "stressDualCoding" StressMonitor/StressMonitor/Views --include="*.swift" | wc -l; echo DUALCODING_COUNT_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
    <fails_when>the count prints 0 (no adoption happened — the helper had zero adopters at phase start), or the build tail contains "** BUILD FAILED **"</fails_when>
  </verify>
  <done>Every category display on manifest surfaces carries hue + symbol + name via the shared modifier; no hue-alone category indicator remains on the swept surfaces.</done>
  <acceptance_criteria>
  - grep for stressDualCoding across Views returns a count > 0 with call sites in at least the surfaces that display a category
  - The SUMMARY lists each adoption site (surface + element)
  - No site on the swept surfaces renders a category hue with neither symbol nor adjacent text
  - App scheme builds with "** BUILD SUCCEEDED **"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: State-shape triage — error alert copy, empty states, locked-character dimming, probe dispositions</name>
  <files>StressMonitor/StressMonitor/Views/DashboardView.swift, StressMonitor/StressMonitor/Views/Trends/TrendsView.swift, StressMonitor/StressMonitor/Views/Action/ActionView.swift, StressMonitor/StressMonitor/Views/Settings/DataManagement/DataManageView.swift, StressMonitor/StressMonitor/Views/Characters/CharacterCollectionView.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Views/DashboardView.swift (the generic error alert around lines 42-50 and the NoDataCard usage — the copy shapes to match)
  - The existing NoDataCard component and its per-data-type title/description/actionTitle copy (find via the Dashboard's zero-data state; this is the documented pattern all triage outcomes must reuse)
  - StressMonitor/StressMonitor/Views/Characters/CharacterCollectionView.swift (locked-character card rendering — locate the 0.65-opacity dimming and what it covers)
  - .planning/phases/03-accessibility-compliance/03-UI-SPEC.md "UI Considerations" (the unresolved and probe rows this task dispositions) and "Copywriting Contract" (error-state shape)
  </read_first>
  <action>
  Close the four audit-surfaced state gaps with documented patterns, never improvised visuals. (1) Error alert: retitle the Dashboard's generic error alert to state the failing operation with a next-step action — title "Couldn't load health data", action "Try Again" — keeping the existing error message for the genuine no-data case unchanged (its next step is the NoDataCard action). (2) Empty-state triage: for TrendsView, ActionView, and DataManageView, determine whether each can actually render zero rows/data — if yes, render the existing NoDataCard pattern with per-surface title/description/action copy in that component's established voice; if no, record the disposition ("cannot render empty — static content" / "seeded at launch") in the SUMMARY. The two probe rows the UI-SPEC marked dismissed (ActionView is static quick-action tiles with no async read path; CharacterCollection rows are seeded at app launch over a fixed set) are confirmed or refuted by this triage — a refutation adds the NoDataCard branch, a confirmation just records the disposition. (3) Locked-character dimming: on CharacterCollectionView, scope the 0.65 opacity to the illustration only — the card's name, stage, and status text render at full contrast on the card token the 03-01 suite already proves. (4) While in CharacterCollectionView, complete its A11Y-01 package left out of Task 1: grid-card tap targets at the 44pt floor and character labels per D-09 — accessibilityLabel "{name}, {stage} stage" (locked: "{name}, locked") with accessibilityValue "Evolution stage {n} of 5" — exact copy from the UI-SPEC contract. Do not invent new empty-state components or copy shapes; everything reuses NoDataCard or an explicit dated disposition.
  </action>
  <verify>
    <automated>grep -c "Couldn't load health data" StressMonitor/StressMonitor/Views/DashboardView.swift; grep -c "Try Again" StressMonitor/StressMonitor/Views/DashboardView.swift; grep -c "NoDataCard" StressMonitor/StressMonitor/Views/Trends/TrendsView.swift StressMonitor/StressMonitor/Views/Settings/DataManagement/DataManageView.swift StressMonitor/StressMonitor/Views/Action/ActionView.swift; grep -c "Evolution stage" StressMonitor/StressMonitor/Views/Characters/CharacterCollectionView.swift; echo TRIAGE_GREP_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
    <fails_when>"Couldn't load health data" or "Try Again" count is 0 in DashboardView, or the "Evolution stage" count is 0 in CharacterCollectionView, or all three NoDataCard counts are 0 without a recorded cannot-render-empty disposition for that surface in the SUMMARY, or the build tail contains "** BUILD FAILED **"</fails_when>
  </verify>
  <done>Error copy states problem + next step, every truly-empty-capable surface renders the NoDataCard pattern (or carries a recorded disposition), locked-character text renders at full contrast, and the character collection carries its complete A11Y-01/D-09 package.</done>
  <acceptance_criteria>
  - DashboardView contains the operation-titled alert with a Try Again action
  - Each of Trends/DataManage/Action either contains a NoDataCard usage or its SUMMARY disposition names why it cannot render empty
  - CharacterCollectionView's dimming no longer applies to text elements (illustration-only) and carries the character label/value copy verbatim from the UI-SPEC
  - App scheme builds with "** BUILD SUCCEEDED **"
  </acceptance_criteria>
</task>

</tasks>

<flagged_assumptions>
- **UI-SPEC UI Considerations (unresolved row — empty Trends/Action roots):** assumed triageable during the sweep: whether TrendsView/ActionView can render zero rows is not determined by the spec. Task 3 resolves each surface either to a NoDataCard branch or to a recorded cannot-render-empty disposition — no silent default.
- **UI-SPEC UI Considerations (unresolved row — locked-character 0.65 opacity):** assumed the dimming may push card text below 4.5:1 on the cream canvas. Task 3 removes text from the dimming scope entirely (illustration-only), which moots the measurement; if the executor finds the text was never dimmed, record that and keep the illustration dimming as-is.
- **A11Y-01 edge probe (adjacency — flagged, backstop-verified):** adjacent targets retaining separate non-overlapping hit regions cannot be asserted from source; it is verified per surface in the Accessibility Inspector scan recorded in phase UAT (see the backstop marker in plan 03-06's must_haves).
- **A11Y-01 edge probe (ordering — flagged, backstop-verified):** VoiceOver focus order following visual reading order is a runtime property; verified in the same per-surface UAT walkthrough (marker in plan 03-06).
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | View-layer hit-area and label work; no data or privilege surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Elevation of Abuse | enlarged hit areas overlapping neighbors capture unintended taps | medium | mitigate | The helper's contentShape bounds expansion to the control's own frame minimums — no overlay views are introduced; adjacency confirmed in the Inspector UAT scan (backstop marker) |
| T-03-SC | Supply chain | package installs | low | accept | No installs — first-party SwiftUI only |
</threat_model>

<verification>
- App scheme builds after each task; SwiftLint introduces no new violations in touched files.
- Touch-target adoption count > 0 with token-only arguments; dual-coding adoption count > 0; triage greps pass or carry recorded dispositions.
- The SUMMARY's per-surface enumeration feeds the phase UAT Inspector scan (03-06 apparatus).
</verification>

<success_criteria>
- No sub-44pt interactive control remains on the manifest surfaces; no unlabeled icon-only control remains.
- Category displays are dual-coded everywhere they appear.
- The three state gaps (error copy, empty states, locked dimming) are closed with the documented patterns or explicitly dispositioned.
</success_criteria>

<artifacts_this_phase_produces>
New symbols/files created by this plan:
- Adoption only (no new types): .minimumTouchTarget(DesignTokens.Layout.minTouchTarget) call sites; .stressDualCoding call sites; icon-only accessibilityLabel values; character a11y label/value copy; Dashboard alert title/action strings; NoDataCard branches (or dispositions) on triaged surfaces
- Record: per-surface enumeration in 03-03-SUMMARY.md (controls upgraded, labels added, triage dispositions)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-accessibility-compliance/03-03-SUMMARY.md` when done
</output>
