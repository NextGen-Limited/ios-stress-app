---
phase: 01-binary-manifest-truth
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - .planning/phases/01-binary-manifest-truth/01-ARTIFACT-AUDIT.md
  - .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md
autonomous: true
requirements:
  - BUILD-02
  - AUTH-01
  - WIRE-01
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "BUILD-02/adjacency edge: all three targets declare the identical suite value in three separate per-target entitlements files — equality across separate files IS the pass condition; no merge into a shared file, no rename, no single source of truth is invented"
    - "BUILD-02/empty edge: no target has an empty or missing com.apple.security.application-groups array; the widget's app group is its only declared capability (app + watch additionally carry healthkit + CloudKit container keys)"
    - "BUILD-02/ordering edge: the suite audit compares literal extracted values from single-element arrays — the check is order-insensitive by construction and does not depend on plist key ordering"
    - "Every Swift suite constant (WidgetSharedData.swift, WidgetDataProvider.swift, WatchSharedDataStore.swift, WatchFacePreferences.swift, ComplicationDataProvider.swift, CharacterCollectionViewModel.swift) quotes the canonical suite group.stress.ai.com; the CONTEXT.md decision-text suite string occurs zero times anywhere under StressMonitor/ (documented typo, recorded in the audit file)"
    - "WidgetPublisherKeyMatchingTests and the suite-writing parts of DataDeletionConsolidationTests pass — the shared container opens non-nil and keys round-trip (the WidgetDataProvider fatalError-on-nil-suite path is not triggered)"
    - "AUTH-01: strings -a over all three Mach-O binaries of the phase-final archive returns zero credential hits after the documented benign allowlist (the eyJ error constant, the four supabase Keychain-cleanup literals, endpoint/bundle-id strings); the AIza prefix appears only inside GoogleService-Info.plist"
    - "AUTH-01/concurrency edge: the scan is read-only and repeatable on the completed artifact — re-running yields the identical verdict"
    - "WIRE-01/boundary edge: the frozen WidgetDataState contract holds — timestamp age exactly 24h renders fresh (the comparison is ≤), beyond 24h renders stale, a missing timestamp renders empty — unchanged by this phase and pinned by WidgetDataStateTests"
    - "WIRE-01/precision edge: the widget displays the value read verbatim from the shared-suite keys the app wrote — no re-derivation, rounding, or unit conversion happens between WidgetPublisher.publish and the widget entry (pinned by WidgetPublisherKeyMatchingTests)"
    - "The phase-final archive contains PlugIns/StressMonitorWidgetExtension.appex (D4: the widget stays in the build)"
    - "fastlane/report.xml contains no token material (spot-check recorded in the audit file)"
  artifacts:
    - .planning/phases/01-binary-manifest-truth/01-ARTIFACT-AUDIT.md (BUILD-02 + AUTH-01 evidence record)
    - .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md (widget evidence note: demo-mode disclosure, same-timestamp screenshots, suite-keys check, physical-device item surfaced for end-of-phase human verification)
    - StressMonitor/build/Phase1-Final.xcarchive (fresh unsigned Release archive reflecting plans 02+03 changes)
  key_links:
    - "Per-bundle entitlements chain: source plutil on the three .entitlements files + codesign dump on the signed build-13 golden (via verify-archive.sh) + runtime suite tests + widget-render evidence — the build-12 no-entitlements-blob regression class stays guarded"
    - "Widget data flow: StressRepository.save → WidgetPublisher.publish (six latest_* keys into UserDefaults(suiteName: group.stress.ai.com)) → WidgetCenter.reloadAllTimelines → widget getTimeline → WidgetDataProvider.getLatestStress → WidgetDataState.resolve"
    - "codesign entitlements truth ↔ entitlements source files — this plan edits neither side; any diff touching a suite string anywhere is a red flag (Pitfall 1)"
  prohibitions:
    - statement: "No entitlements file's suite string may be edited — the canonical suite stays group.stress.ai.com (repo truth overrides the CONTEXT.md decision-text typo); any task diff touching an entitlements suite string is a stop-and-flag (Pitfall 1)"
      status: flagged-unverified
      verification: "git diff for the phase shows zero changes to the three .entitlements files"
    - statement: "WidgetDataState fresh/stale/empty resolver behavior must NOT be redesigned this phase (D4 decision: keep the v1.0 behavior as-is)"
      status: flagged-unverified
      verification: "git diff shows WidgetDataState.swift and WidgetDataStateTests.swift unchanged"
    - statement: "Simulator evidence must disclose demo mode honestly — the evidence note states the data source is -demo-mode cycling (no HealthKit data exists on simulator); no claim of live HealthKit provenance"
      status: flagged-unverified
      verification: "01-WIRE-01-EVIDENCE.md contains a demo-mode disclosure section"
    - statement: "The dead WidgetConstants key names (latestMeasurementKey, widgetHistoryKey, lastUpdateKey) are noted in the audit, not cleaned up — out of phase scope per the surgical-changes rule"
      status: flagged-unverified
      verification: "audit file mentions them; git diff shows WidgetSharedData.swift unchanged"
  flagged_assumptions:
    - "WIRE-01 device availability (RESEARCH Open Question 4): the executable path of record is the CONTEXT-sanctioned fallback — simulator widget gallery + documented human UAT. The physical-device parity check (widget score == app score on a real device after refresh) is surfaced as an end-of-phase human-verification item (human_verify_mode: end-of-phase), not silently dropped."
---

<objective>
Produce the phase-final archive and gather the three evidence bundles: the BUILD-02 App Group audit (one suite across three targets), the AUTH-01 credential-scan gate over the final binaries, and the WIRE-01 widget evidence (simulator gallery + documented human UAT note, physical-device item surfaced).

Purpose: these are the phase's truth claims about the shipped artifact — one App Group suite, no extractable credential, a widget that renders real (demo-mode-driven) data. All three were validated as methodology during research against build 13; this plan re-runs them against the archive that includes every Phase-1 change (plans 01-03).

Output: 01-ARTIFACT-AUDIT.md (BUILD-02 + AUTH-01 verdicts with raw command output), 01-WIRE-01-EVIDENCE.md (screenshots + disclosure + human item), a fresh Phase1-Final.xcarchive passing scripts/verify-archive.sh.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-binary-manifest-truth/01-CONTEXT.md
@.planning/phases/01-binary-manifest-truth/01-RESEARCH.md
@.planning/phases/01-binary-manifest-truth/01-PATTERNS.md
@.planning/phases/01-binary-manifest-truth/01-01-SUMMARY.md
@.planning/phases/01-binary-manifest-truth/01-02-SUMMARY.md
@.planning/phases/01-binary-manifest-truth/01-03-SUMMARY.md

@scripts/verify-archive.sh
@StressMonitor/StressMonitor/Models/WidgetSharedData.swift
@StressMonitor/StressMonitorWidget/Models/WidgetDataProvider.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUILD-02 App Group audit — one suite across three targets, recorded in 01-ARTIFACT-AUDIT.md</name>
  <files>.planning/phases/01-binary-manifest-truth/01-ARTIFACT-AUDIT.md</files>
  <read_first>
  - StressMonitor/StressMonitor/StressMonitor.entitlements, StressMonitor/StressMonitorWidget/StressMonitorWidget.entitlements, "StressMonitor/StressMonitorWatch Watch App/StressMonitorWatch Watch App.entitlements" (the three audit targets — expect the identical application-groups block with the suite group.stress.ai.com; app + watch additionally carry healthkit + icloud keys, widget carries the app group only)
  - .planning/phases/01-binary-manifest-truth/01-RESEARCH.md (§4 — the full constant list and the CONTEXT-typo resolution)
  - scripts/verify-archive.sh (its entitlements check runs the golden-archive half of this chain)
  </read_first>
  <action>
  Audit only — no source edits (the suite is NOT renamed; repo truth group.stress.ai.com overrides the CONTEXT.md decision-text string, per the orchestrator-settled open question 1). Create 01-ARTIFACT-AUDIT.md and record:

  1. Source entitlements: plutil -extract com.apple.security.application-groups raw -o - on each of the three .entitlements files → each returns exactly the one suite value. Record per-file results plus the app/watch additional capabilities (healthkit, icloud container iCloud.stress.ai.com, CloudKit service) and the widget's app-group-only shape.
  2. Swift constants: grep -rn "UserDefaults(suiteName" across the three real target directories → every literal equals the canonical suite; the six known constant sites (WidgetSharedData.swift ~line 100 WidgetConstants.appGroupID, WidgetDataProvider.swift ~line 10, WatchSharedDataStore.swift ~line 16, WatchFacePreferences.swift ~line 15, ComplicationDataProvider.swift ~line 14, CharacterCollectionViewModel.swift ~line 116) each quote it verbatim. Record the site list.
  3. Typo note: grep -rn for the CONTEXT.md decision-text suite string (the reversed-segments form naming stressmonitor) under StressMonitor/ → zero matches; record this as the documented-typo note the phase wrap-up carries.
  4. Runtime: run xcodebuild test with -only-testing for WidgetPublisherKeyMatchingTests (and the DataDeletionConsolidationTests suite, remembering it runs locally — the GSD_CI gating is a CI-only quirk, do not touch it) using the pinned destination and -parallel-testing-enabled NO.
  5. Golden signed-artifact half: bash scripts/verify-archive.sh .asc/artifacts/StressMonitor.xcarchive → entitlements check PASS ×3 (the build-12 regression guard; the entitlements files are unchanged by this phase so the golden dump plus unchanged sources proves the chain).
  6. Note in the audit: WidgetDataProvider.init fatalError's when UserDefaults(suiteName:) returns nil — an entitlement mismatch crashes the widget process rather than degrading; the per-bundle entitlements dump (check 1 of verify-archive.sh) before any publish is the standing guard (STATE.md mandate — the signed-artifact dump lands with the next locally-controlled signed build / Phase 4 publish flow).
  7. Note the dead WidgetConstants keys (latestMeasurementKey, widgetHistoryKey, lastUpdateKey) as observed-and-left (out of scope).
  </action>
  <verify>
  <automated>for f in StressMonitor/StressMonitor/StressMonitor.entitlements StressMonitor/StressMonitorWidget/StressMonitorWidget.entitlements "StressMonitor/StressMonitorWatch Watch App/StressMonitorWatch Watch App.entitlements"; do plutil -extract com.apple.security.application-groups.0 raw -o - "$f"; done; xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/WidgetPublisherKeyMatchingTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
  - plutil -extract of application-groups.0 from each of the three entitlements files prints the identical canonical suite value (group.stress.ai.com).
  - grep -rn "UserDefaults(suiteName" over the three real target directories shows every literal equal to the canonical suite (zero divergent literals).
  - The reversed-segment suite string from the CONTEXT decision text returns zero matches under StressMonitor/ (grep recorded in the audit file as the typo note).
  - WidgetPublisherKeyMatchingTests passes (TEST SUCCEEDED / no failures in output tail).
  - scripts/verify-archive.sh on the golden archive exits 0 (entitlements PASS ×3 included).
  - 01-ARTIFACT-AUDIT.md exists containing sections: source entitlements, Swift constants, typo note, runtime tests, golden dump, WidgetDataProvider fatalError observation, dead-keys note.
  </acceptance_criteria>
  <done>One canonical suite is proven across entitlements, Swift constants, runtime tests, and the golden signed artifact — with the audit trail committed as a phase artifact.</done>
</task>

<task type="auto">
  <name>Task 2: AUTH-01 gate — credential scan over the phase-final archive, triaged and recorded</name>
  <files>.planning/phases/01-binary-manifest-truth/01-ARTIFACT-AUDIT.md</files>
  <read_first>
  - .planning/phases/01-binary-manifest-truth/01-RESEARCH.md (§7 — the validated scan pipeline, the exact benign triage list, and the baseline verdict)
  - scripts/verify-archive.sh (the gate encodes §7; this task runs it against the FRESH archive and records the outcome)
  - fastlane/report.xml (spot-check target for token material — deploy.yml uploads it as a CI artifact)
  </read_first>
  <action>
  1. Produce the phase-final archive: xcodebuild archive -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -configuration Release -destination 'generic/platform=iOS Simulator' -archivePath StressMonitor/build/Phase1-Final.xcarchive CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO (this archive includes every change from plans 02 and 03 — manifest reasons, plist consolidation, pbxproj cleanup).
  2. Run bash scripts/verify-archive.sh StressMonitor/build/Phase1-Final.xcarchive --skip-entitlements → all checks PASS; the credential-scan and AIza-resource checks are the AUTH-01 verdict for the final artifact. The signing-disabled archive is a faithful proxy: credential strings are a property of the compiled Mach-O, not of the signature.
  3. Manual triage pass (the script allowlists known-benign hits; if it reports any NEW hit, triage before recording): re-run the §7 strings pipeline by hand over any reported binary — every hit must resolve to a documented benign class (Google SDK error constant, Keychain-cleanup literal names, endpoint/bundle-id strings) or be escalated as a real finding. Record the full hit list with dispositions.
  4. Two additional greps the research flagged for the new archive: STRESS_API env-style key forms and the sb_publishable/sb_secret forms over the three binaries — expect zero.
  5. Spot-check fastlane/report.xml for token material (grep for the secret env var NAMES is fine; the VALUES must not appear) — record the result.
  6. Append an AUTH-01 section to 01-ARTIFACT-AUDIT.md: verdict, the benign-allowlist table as applied, the additional-grep results, and the report.xml spot-check.
  </action>
  <verify>
  <automated>xcodebuild archive -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -configuration Release -destination 'generic/platform=iOS Simulator' -archivePath StressMonitor/build/Phase1-Final.xcarchive CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -2; bash scripts/verify-archive.sh StressMonitor/build/Phase1-Final.xcarchive --skip-entitlements; echo "auth01_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
  - The Release archive builds (ARCHIVE SUCCEEDED) and scripts/verify-archive.sh exits 0 on it — credential scan, resource grep, merged plists, SDK manifests all PASS.
  - The audit file's AUTH-01 section lists every strings hit encountered (expect: the one eyJ error constant per binary at most, the supabase Keychain-cleanup literal names, zero AIza in binaries) with a benign disposition each — or escalates a real finding.
  - The STRESS_API and sb_publishable/sb_secret greps over the three binaries return zero.
  - grep of fastlane/report.xml for token values returns nothing suspicious; result recorded.
  - .asc/artifacts/ baseline still untouched.
  </acceptance_criteria>
  <done>AUTH-01's empirical confirmation is complete against the phase-final artifact: no usable credential is extractable from any of the three binaries or bundled resources, and the evidence is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: WIRE-01 widget evidence — simulator gallery + same-timestamp screenshots + documented human-UAT note</name>
  <files>.planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md</files>
  <read_first>
  - .planning/phases/01-binary-manifest-truth/01-RESEARCH.md (§10 — the write→read data flow, the evidence spec, and the frozen WidgetDataState contract)
  - StressMonitor/StressMonitor/Models/WidgetSharedData.swift (write side: WidgetPublisher.publish writes the six latest_* keys then reloads timelines)
  - StressMonitor/StressMonitorWidget/Providers/StressWidgetProvider.swift (read side: getTimeline → getLatestStress → WidgetDataState.resolve, 15-min refresh policy)
  - .planning/phases/01-binary-manifest-truth/01-CONTEXT.md (WIRE-01 verification decision: physical-device gallery first; simulator + documented human UAT as fallback — this task executes the fallback path of record and surfaces the device item)
  </read_first>
  <action>
  Execute the CONTEXT-sanctioned fallback evidence path (physical-device check is surfaced as a human item — see flagged assumption):

  1. Build and install on an iPhone simulator: boot via the argent MCP tooling (preferred per AGENTS.md; scripts may shell out like run-tests.py does), build the app scheme for the booted simulator destination, install the .app, and launch with the -demo-mode launch argument (demo mode cycles all stress levels through the REAL pipeline — StressRepository.save → WidgetPublisher.publish — not mocks). Let at least one stress save fire (the demo cycle produces a fresh timestamp).
  2. Add the widget: long-press the home screen, open the gallery (+), find the StressMonitor widget, add it to the home screen (use argent gesture tools; run a discovery tool — describe — before every tap; never derive coordinates from a screenshot alone).
  3. Capture the evidence pair: a screenshot of the widget on the home screen and a screenshot of the in-app dashboard showing the current stress score, taken at the same timestamp window (immediately sequential). Save both under StressMonitor/build/wire-01/ with descriptive names.
  4. Verify the six suite keys are non-nil after the save — this is already machine-checked by WidgetPublisherKeyMatchingTests (task 1); reference that green result rather than duplicating it, and note that the widget rendering data at all proves the suite opened (a nil suite crashes WidgetDataProvider init — the fatalError path).
  5. Write 01-WIRE-01-EVIDENCE.md with sections: Data source disclosure (demo mode — no HealthKit data exists on simulator; the pipeline is the real one), Screenshots (paths + timestamps + the score value visible in each), Suite-keys check (test reference), Widget-present-in-archive (PlugIns/StressMonitorWidgetExtension.appex exists in Phase1-Final.xcarchive — from task 2), Frozen-contract note (WidgetDataState ≤24h fresh / >24h stale / missing empty — unchanged), and Physical-device verification PENDING (the human item: widget score equals app score on a real device after a refresh — to be executed at end-of-phase human verification or by the user on hardware).
  </action>
  <verify>
  <automated>ls -la StressMonitor/build/wire-01/ | grep -c "png\|jpg\|jpeg"; ls StressMonitor/build/Phase1-Final.xcarchive/Products/Applications/StressMonitor.app/PlugIns/ | grep -c "StressMonitorWidgetExtension.appex"; test -s .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md && grep -c "demo" .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
  - Two screenshot files exist under StressMonitor/build/wire-01/ (size > 0), one showing the widget with a stress value, one showing the app dashboard.
  - The visible stress value in the widget screenshot matches the app dashboard screenshot value (same-timestamp pair; stated in the evidence note).
  - StressMonitorWidgetExtension.appex is present in Phase1-Final.xcarchive/Products/Applications/StressMonitor.app/PlugIns/.
  - 01-WIRE-01-EVIDENCE.md contains all six required sections, including the demo-mode disclosure and the pending physical-device human item.
  - The evidence note references the green WidgetPublisherKeyMatchingTests result from task 1.
  </acceptance_criteria>
  <done>WIRE-01's executable evidence path is complete — simulator gallery rendering proven with same-timestamp screenshots and an honest disclosure — with the physical-device check explicitly surfaced for end-of-phase human verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped binary → extraction | an attacker holding the IPA runs strings over the Mach-Os and greps bundled resources — the AUTH-01 threat model in miniature |
| per-bundle entitlements ↔ shared container access | a bundle whose entitlements drift from the suite either crashes (widget fatalError path) or reads nothing — the build-12 regression class |
| simulator evidence ↔ claimed reality | demo-mode data must not be presented as HealthKit-live data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Credential extractable from the Release binary (AUTH-01 anchor) | critical | mitigate | §7 strings pipeline over all three binaries + resource grep with the documented benign allowlist, run against the phase-final archive via verify-archive.sh; new hits triaged by hand before verdict; fastlane/report.xml spot-check |
| T-04-02 | Elevation of Privilege / DoS | Entitlement confusion — a bundle without the matching suite (build-12 shipped with no entitlements blob) | high | mitigate | Per-bundle entitlements chain: source plutil ×3 + golden signed-artifact codesign dump via verify-archive.sh + suite runtime tests + the widget rendering without crash as live proof |
| T-04-03 | Spoofing | Demo-mode data presented as live-device evidence | low | mitigate | Evidence note carries a mandatory demo-mode disclosure section; suite-keys check is mechanical (test-referenced), not narrative |
</threat_model>

<verification>
- BUILD-02: three entitlements files + six constants + typo-grep zero + suite tests green + golden entitlements dump PASS — all recorded in 01-ARTIFACT-AUDIT.md.
- AUTH-01: verify-archive.sh exit 0 on Phase1-Final.xcarchive; manual triage table in the audit file; extra greps zero; report.xml clean.
- WIRE-01: screenshot pair + appex presence + evidence note with disclosure and human item.
</verification>

<success_criteria>
- The phase-final archive passes the full artifact gate, and every truth claim (one suite, no credential, widget real) has committed evidence artifacts.
- The physical-device WIRE-01 check is surfaced, not dropped.
</success_criteria>

## Artifacts this phase/plan produces

| Artifact | Kind | Notes |
|----------|------|-------|
| .planning/phases/01-binary-manifest-truth/01-ARTIFACT-AUDIT.md | evidence record | BUILD-02 audit + AUTH-01 verdict + triage table + report.xml spot-check |
| .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md | evidence record | demo-mode disclosure, screenshot pair, appex presence, pending device item |
| StressMonitor/build/Phase1-Final.xcarchive | verification artifact | unsigned Release archive reflecting all phase changes |
| StressMonitor/build/wire-01/*.png | screenshots | widget + dashboard same-timestamp pair |
| No source-code changes | — | this plan is evidence-gathering; entitlements and widget code untouched by design |

<output>
Create `.planning/phases/01-binary-manifest-truth/01-04-SUMMARY.md` when done
</output>
