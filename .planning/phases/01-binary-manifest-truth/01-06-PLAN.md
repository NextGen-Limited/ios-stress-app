---
phase: 01-binary-manifest-truth
plan: 06
type: tdd
wave: 5
depends_on: ["01-04"]
gap_closure: true
files_modified:
  - StressMonitor/StressMonitor/ViewModels/StressViewModel.swift
  - StressMonitor/StressMonitor/Services/MockServices.swift
  - StressMonitor/StressMonitorTests/WidgetPublisherKeyMatchingTests.swift
  - .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md
autonomous: true
requirements:
  - WIRE-01
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SC-5 executable truth (D4 wire branch): after a foreground dashboard refresh produces a stress result, the shared App Group suite contains all six latest_* keys (latest_stress_level, latest_stress_category, latest_hrv, latest_heart_rate, latest_confidence, latest_timestamp) with latest_stress_level equal to StressViewModel.currentStress?.level — the value the dashboard renders — and WidgetCenter.reloadAllTimelines has fired, so the home-screen widget displays that same value after its timeline reload (simulator/demo-mode proven by this plan; physical device remains the standing end-of-phase human item)"
    - "The live write path is StressViewModel.loadCurrentStress → StressRepository.save → WidgetPublisher.publish — loadCurrentStress is the single funnel every live foreground path already passes through (DashboardView .task → loadDashboardData, requestHealthKitAccess, NoDataCard retry, scenePhase-active retry, HKObserverQuery handleHealthKitUpdate, DEBUG demo 15s timer), so every real app open refreshes the widget"
    - "One persisted StressMeasurement per new underlying HRV reading: a private lastPersistedReadingDate guard in StressViewModel skips the save when hrvData?.timestamp is unchanged since the previous save, so repeated UI-driven loads of the same HealthKit sample do not spam SwiftData/CloudKit or skew baseline, bio-age, and weekly-average statistics"
    - "A save failure inside loadCurrentStress is swallowed (try?) and never degrades the dashboard read path — same graceful-degradation stance the secondary factors already use"
    - "Frozen contract carried forward unchanged: WidgetDataState.resolve behaves exactly as v1.0 shipped (timestamp age ≤ 24h → fresh, > 24h → stale, missing timestamp → empty); WidgetPublisher.publish writes the six keys verbatim from the measurement (no re-derivation or rounding); both stay byte-identical, pinned by the unmodified WidgetDataStateTests and WidgetPublisherKeyMatchingTests originals"
    - "D3 privacy contract untouched: StressContextPayload, the /chat payload, CLAUDE.md, and both privacy-policy locales have zero churn — the new write goes only to the local App Group suite; no data leaves the device that did not already"
    - "No background execution is introduced: HealthBackgroundScheduler stays uninstantiated and no UIBackgroundModes or BGTaskSchedulerPermittedIdentifiers keys are added (BUILD-04 deferral to Phase 2 intact)"
    - "Widget/app same-value evidence re-captured on the simulator (demo mode, disclosed as such) superseding the empty-state pair, and 01-WIRE-01-EVIDENCE.md updated to record the wiring"
  artifacts:
    - StressMonitor/StressMonitor/ViewModels/StressViewModel.swift (modified: shared makeMeasurement helper + guarded save inside loadCurrentStress)
    - StressMonitor/StressMonitorTests/WidgetPublisherKeyMatchingTests.swift (extended: three new @MainActor tests, original two untouched)
    - StressMonitor/StressMonitor/Services/MockServices.swift (extended: optional injectable MockHealthKitService.mockHRVTimestamp)
    - .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md (updated: gap-closure section + new same-value screenshot pair)
    - StressMonitor/build/wire-01/*.png (new widget-with-value + dashboard pair, same capture window)
  key_links:
    - "The SC-5 chain, now live end-to-end: DashboardView .task → loadDashboardData/loadCurrentStress → (new) StressRepository.save → WidgetPublisher.publish → UserDefaults(suiteName: group.stress.ai.com) latest_* keys → WidgetCenter.reloadAllTimelines → StressWidgetProvider.getTimeline → WidgetDataProvider.getLatestStress → WidgetDataState.resolve(.fresh) → rendered score equal to the dashboard's"
  prohibitions:
    - statement: "WidgetDataState fresh/stale/empty resolver behavior must NOT be redesigned (D4: frozen v1.0 contract)"
      status: flagged-unverified
      verification: "git diff shows WidgetDataState.swift and WidgetDataStateTests.swift unchanged; the duplicated resolver in StressWidgetProvider.swift unchanged"
    - statement: "WidgetPublisher.publish and its six key literals must NOT change; StressContextPayload / the /chat payload / D3 docs must NOT be touched"
      status: flagged-unverified
      verification: "git diff shows WidgetSharedData.swift, StressContextPayload.swift, CLAUDE.md, docs-site/legal/privacy.md, docs-site/vi/legal/privacy.md all unchanged"
    - statement: "Do NOT wire HealthBackgroundScheduler, add UIBackgroundModes, or add BGTaskSchedulerPermittedIdentifiers — background execution stays deferred to Phase 2 BUILD-04"
      status: flagged-unverified
      verification: "git grep over StressMonitor/ for HealthBackgroundScheduler call sites and both plist key names returns no new references; HealthBackgroundScheduler.swift unchanged"
    - statement: "Entitlements and suite strings are untouched — the canonical suite stays group.stress.ai.com"
      status: flagged-unverified
      verification: "git diff shows all three .entitlements files unchanged"
    - statement: "Dead-code hygiene beyond the gap is out of scope (precedent: 01-04 left the dead WidgetConstants keys): calculateAndSaveStress and DashboardViewModel stay as-is, now delegating to the shared helper where applicable — the gap is the missing live trigger, not unused-code cleanup"
      status: flagged-unverified
      verification: "git diff shows HealthBackgroundScheduler.swift and DashboardViewModel.swift unchanged; StressViewModel.calculateAndSaveStress still present, behavior identical"
---

<objective>
Close the single Phase 1 verification gap (SC-5 / WIRE-01): give WidgetPublisher.publish its first live caller by wiring one foreground save trigger into StressViewModel.loadCurrentStress — the funnel every live dashboard path already passes through — so the widget shows the same stress score the app shows after a refresh.

Purpose: Per D4 (keep the widget and make it true), the widget currently ships in the build but renders its empty state on every device because nothing live ever writes the latest_* suite keys. VERIFICATION.md scores the phase 5/6 on exactly this gap; all other requirements are satisfied. The write→read path is fully built, unit-tested, and entitlement-wired — the missing piece is one live call site.

Trigger selection (verified against the live call graph this session): loadCurrentStress is reached from DashboardView .task → loadInitialData → loadDashboardData, from requestHealthKitAccess, from the NoDataCard retry and scenePhase-active retry, from the HKObserverQuery handler (device), and from the DEBUG demo 15s timer — every live foreground refresh. Wiring the save there is one function in one file. HealthBackgroundScheduler was ruled out: it is never instantiated, requires UIBackgroundModes and BGTaskSchedulerPermittedIdentifiers plist keys (the BUILD-04 deferred condition — background modes are absent from every shipped plist), and uses the legacy 2-factor calculateStress. DashboardViewModel was ruled out: it is not in the live view tree (MainTabView constructs StressViewModel via DashboardView).

Output: a live save trigger with RED→GREEN tests in the extended WidgetPublisherKeyMatchingTests suite, a guarded dedupe so repeated loads of the same HealthKit sample persist exactly once, re-captured widget/app same-value simulator evidence, and an updated 01-WIRE-01-EVIDENCE.md.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-binary-manifest-truth/01-VERIFICATION.md
@.planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md
@.planning/phases/01-binary-manifest-truth/01-CONTEXT.md
@.planning/STATE.md

@StressMonitor/StressMonitor/ViewModels/StressViewModel.swift
@StressMonitor/StressMonitor/Services/Repository/StressRepository.swift
@StressMonitor/StressMonitor/Models/WidgetSharedData.swift
@StressMonitor/StressMonitor/Services/MockServices.swift
@StressMonitor/StressMonitorTests/WidgetPublisherKeyMatchingTests.swift
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire the live widget write trigger — RED tests then the guarded save inside loadCurrentStress</name>
  <files>StressMonitor/StressMonitorTests/WidgetPublisherKeyMatchingTests.swift, StressMonitor/StressMonitor/Services/MockServices.swift, StressMonitor/StressMonitor/ViewModels/StressViewModel.swift</files>
  <reversibility rating="reversible">Additive viewmodel change + tests; a single commit revert restores the pre-gap behavior; new SwiftData rows are ordinary app data.</reversibility>
  <read_first>
  - StressMonitor/StressMonitor/ViewModels/StressViewModel.swift (the modification target: loadCurrentStress lines ~110-187 is the live funnel; calculateAndSaveStress lines ~276-328 holds the existing StressResult→StressMeasurement mapping to extract; lastRefreshTime/canRefresh show the existing debounce style)
  - StressMonitor/StressMonitor/Services/Repository/StressRepository.swift (save at line 47-63: SwiftData insert then WidgetPublisher.publish(measurement) at line 57, then optional CloudKit sync — publish is unconditional once save succeeds)
  - StressMonitor/StressMonitor/Models/WidgetSharedData.swift (WidgetPublisher.publish lines 122-147: writes the six latest_* keys into UserDefaults(suiteName: group.stress.ai.com) then WidgetCenter.shared.reloadAllTimelines(); WidgetDataState lines 155-161: the frozen resolver — do not touch)
  - StressMonitor/StressMonitorTests/WidgetPublisherKeyMatchingTests.swift (the suite to extend: suiteName constant, allKeys array, cleanUp helper, Swift Testing @Test style)
  - StressMonitor/StressMonitor/Services/MockServices.swift (MockHealthKitService, MockStressAlgorithmService, @MainActor MockStressRepository recording mockMeasurements — the existing fixtures the new tests reuse)
  - StressMonitor/StressMonitor/Services/Protocols/StressAlgorithmServiceProtocol.swift (protocol default: calculateMultiFactorStress delegates to calculateStress, so MockStressAlgorithmService drives loadCurrentStress without extra conformance)
  - StressMonitor/StressMonitor/Views/MainTabView.swift (homeView lines ~110-130: the live chain MainTabView → DashboardView(repository:) with the real persistent StressRepository — confirms the funnel; no edits here)
  </read_first>
  <behavior>
  RED phase — add three @MainActor @Test functions inside the existing WidgetPublisherKeyMatchingTests struct (reuse its suiteName/allKeys/cleanUp helpers; do not modify the two existing tests):
  - Test 1 "loadCurrentStress saves the calculated measurement through the repository": StressViewModel with MockHealthKitService (mockHRV 55, mockHeartRate 72), MockStressAlgorithmService (mockStressLevel 61), MockStressRepository; after await viewModel.loadCurrentStress() expect repository.mockMeasurements.count == 1, the saved measurement's stressLevel == 61, hrv == 55, restingHeartRate == 72, and viewModel.currentStress?.level == 61.
  - Test 2 "the live dashboard path writes all six widget suite keys": StressViewModel with MockHealthKitService + MockStressAlgorithmService (mockStressLevel 42) and the REAL StressRepository over an in-memory ModelContainer for StressMeasurement (hold the container alive in the test scope for the whole test — the v1.1 known crash lineage when a container is deallocated before its mainContext); clean the six keys, await loadCurrentStress(), expect every key in allKeys non-nil, latest_stress_level == 42, and latest_stress_level == viewModel.currentStress?.level (the same-value claim), then cleanUp.
  - Test 3 "repeated loads of the same underlying reading persist exactly once": MockHealthKitService with its new mockHRVTimestamp pinned to a fixed date T0; two loadCurrentStress() calls expect mockMeasurements.count == 1; then move mockHRVTimestamp forward to a later date T1 and call once more, expecting count == 2.
  All three fail before the wiring (zero saves, nil suite keys) while the two original key-matching tests keep passing.
  GREEN phase — behavior after wiring: every successful loadCurrentStress calculation persists exactly one StressMeasurement per new underlying HRV reading via repository.save (which publishes the six suite keys and reloads widget timelines), and repeated loads of an unchanged reading persist nothing further.
  </behavior>
  <action>
  RED (commit test(01-06): add failing tests for the live widget write path):

  1. In StressMonitor/StressMonitor/Services/MockServices.swift, give MockHealthKitService one new optional property mockHRVTimestamp: Date? (default nil meaning current behavior — fetchLatestHRV stamps Date()). When set, fetchLatestHRV returns HRVMeasurement(value: mockHRV, timestamp: mockHRVTimestamp!). Use optional binding rather than force-unwrap syntax to satisfy the SwiftLint force_unwrapping rule. Change nothing else in the mock — MockStressRepository and MockStressAlgorithmService are used as-is.
  2. Write the three tests from the behavior block into the existing WidgetPublisherKeyMatchingTests struct in StressMonitor/StressMonitorTests/WidgetPublisherKeyMatchingTests.swift. Mark each new test @MainActor (StressViewModel and MockStressRepository are MainActor-isolated; the struct itself and its two existing tests stay untouched and nonisolated). For test 2, build the in-memory container with ModelContainer(for: StressMeasurement.self, configurations: ModelConfiguration(isStoredInMemoryOnly: true)) — the same pattern DashboardView's default init uses — and keep the container referenced in a local constant that lives until after the assertions. Read the published level back with defaults.double(forKey: "latest_stress_level") and compare with accuracy-free equality against 42 and against viewModel.currentStress?.level.
  3. Run the suite and record the RED result (the three new tests fail; the original two pass), then commit.

  GREEN (commit feat(01-06): persist stress on live load so the widget shows the app's score):

  4. In StressMonitor/StressMonitor/ViewModels/StressViewModel.swift, extract the StressResult→StressMeasurement mapping that calculateAndSaveStress currently builds inline (StressMeasurement with timestamp: result.timestamp, stressLevel: result.level, hrv: result.hrv, restingHeartRate: result.heartRate, confidences: [result.confidence], then — when result.factorBreakdown exists — the five component fields hrvComponent, hrComponent, sleepComponent, activityComponent, recoveryComponent and dataCompleteness copied from the breakdown) into a private func makeMeasurement(from result: StressResult) -> StressMeasurement. Refactor calculateAndSaveStress to call it; its behavior must stay identical.
  5. Add a private var lastPersistedReadingDate: Date? to StressViewModel. In loadCurrentStress, after the dashboard state assignments (currentStress, respiratoryRate, the behavioral tiles, baseline, lastRefresh, errorMessage, and the dataQualityInfo block) and before the DEBUG signpost event — still inside the do-block — persist: guard hrvData?.timestamp differs from lastPersistedReadingDate, otherwise skip; when it differs, build makeMeasurement(from: result), call try? await repository.save(measurement) (the swallow is deliberate: a persistence failure must not surface as a dashboard error, matching how secondary-factor failures degrade), and set lastPersistedReadingDate = hrvData?.timestamp. Add no new UI state, no error surface, no other call sites.
  6. Re-run the extended suite: all five tests pass. Note in passing: the DEBUG demo 15s timer path now saves each tick because SimulatorHealthKitService.fetchLatestHRV stamps a fresh Date() per call — this is what makes the simulator evidence in Task 2 show a live value.
  7. Verify the frozen contracts held: git diff for WidgetDataState.swift, WidgetDataStateTests.swift, WidgetSharedData.swift, StressContextPayload.swift, HealthBackgroundScheduler.swift, DashboardViewModel.swift, and the three .entitlements files is empty.

  Gap reason (VERIFICATION.md): WidgetPublisher.publish had zero live callers — every caller of StressRepository.save sat on a dead path. The single new save site inside loadCurrentStress converts the existing wired-but-dead StressRepository.save → WidgetPublisher.publish link into a live one. This implements D4 (keep the widget and make it true); no feature beyond the trigger.
  </action>
  <verify>
  <automated>xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' -parallel-testing-enabled NO -only-testing:StressMonitorTests/WidgetPublisherKeyMatchingTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3; rg -n "repository\.save" StressMonitor/StressMonitor/ViewModels/StressViewModel.swift; git diff --stat -- StressMonitor/StressMonitor/Models/WidgetSharedData.swift StressMonitor/StressMonitor/Services/Background/HealthBackgroundScheduler.swift "StressMonitor/StressMonitor/Views/Dashboard/DashboardViewModel.swift" StressMonitor/StressMonitor/Services/LLM/StressContextPayload.swift | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - The three new @MainActor tests exist inside the WidgetPublisherKeyMatchingTests struct and were observed failing before the wiring (RED run recorded in the SUMMARY); the commit history contains the RED commit preceding the GREEN commit.
  - After the wiring, the -only-testing run prints TEST SUCCEEDED with all tests in the suite passing — including the two original key-matching tests, unmodified.
  - rg shows a repository.save call site inside loadCurrentStress (the live funnel), and StressViewModel.swift still contains calculateAndSaveStress delegating to the shared makeMeasurement helper.
  - The dedupe is pinned: the same-reading-twice test asserts exactly one save, and the fresh-timestamp third call asserts a second save.
  - git diff --stat for WidgetSharedData.swift, HealthBackgroundScheduler.swift, DashboardViewModel.swift, and StressContextPayload.swift outputs zero changed files (the wc -l guard prints 0), and the three .entitlements files are unchanged.
  - No UIBackgroundModes or BGTaskSchedulerPermittedIdentifiers string was added anywhere (git grep over StressMonitor/ returns no new occurrences).
  </acceptance_criteria>
  <done>Every live foreground dashboard refresh persists the calculated measurement and publishes the six latest_* suite keys with latest_stress_level equal to the dashboard's currentStress level — the write→read chain is live, deduped per underlying reading, and the frozen contracts are byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Regression suite + re-captured widget/app same-value evidence + evidence-note update</name>
  <files>.planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md</files>
  <precondition>An iPhone 17 (iOS 26.3) simulator runtime is installed locally — it was used for the 01-04 evidence on this machine; confirm via argent list-devices (or xcrun simctl list devices available) and halt-and-report if missing rather than substituting an untested destination.</precondition>
  <read_first>
  - .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md (the artifact being updated: §2 empty-state pair, §5 frozen-contract note, §6 pending device item, §7 the finding this plan closes — keep all of them; append the closure)
  - .planning/phases/01-binary-manifest-truth/01-VERIFICATION.md (gap.missing item 2: "re-captured widget/app same-value evidence (simulator + physical device)" — this task delivers the simulator half; the physical half stays the end-of-phase human item)
  - StressMonitor/StressMonitor/Services/HealthKit/SimulatorHealthKitService.swift (demo data source: fetchLatestHRV stamps Date() per call with a 5% nil chance — one clean tick is enough; disclose demo mode in the evidence, never claim HealthKit provenance)
  - StressMonitor/StressMonitorWidget/Providers/StressWidgetProvider.swift (read side: getLatestStress → WidgetDataState.resolve, 15-min refresh policy — reloadAllTimelines from the app makes the update immediate)
  </read_first>
  <action>
  1. Full unit-suite regression: run the entire StressMonitor scheme test suite with the pinned destination and -parallel-testing-enabled NO (or python3 scripts/run-tests.py). This is the safety net for having touched the core StressViewModel — zero failures expected; the DataDeletion suites run locally (the GSD_CI skip is CI-only behavior, do not touch it).
  2. Build and install the Debug app on the booted iPhone 17 simulator (argent MCP tools preferred for simulator interaction per AGENTS.md; build via xcodebuild for the booted destination). Launch with the -demo-mode launch argument.
  3. Wait at least 20 seconds for one 15-second demo tick to calculate, save, publish, and reload the widget timelines. If the StressMonitor widget is not already on the home screen, add it through the real gallery flow (long-press home screen → Edit → + → search Stress → Add Widget → Done) — run a discovery tool (describe) before every tap and take coordinates only from accessibility frames, never from screenshots alone. If a tap fails twice at the same coordinates, stop and re-run discovery.
  4. Capture the evidence pair into StressMonitor/build/wire-01/ with descriptive filenames: the home-screen widget now rendering a live stress value (not the empty state) and the in-app dashboard, captured in the same immediate window. Verify the two visible values match by reading them from the accessibility tree (describe), since model-context cannot render images — quote both readings in the evidence note.
  5. Update 01-WIRE-01-EVIDENCE.md: append a section titled "§8 Gap closure — live write path wired (plan 01-06)" containing: the trigger summary (guarded save inside StressViewModel.loadCurrentStress, one measurement per new underlying HRV reading, repository.save → WidgetPublisher.publish → reloadAllTimelines), the RED→GREEN test reference (the three tests added to WidgetPublisherKeyMatchingTests), the new same-value pair (file paths, capture timestamps, and the value read from each side), an explicit statement that the §2 empty-state pair is superseded historical evidence kept for the record, and a note that §6's physical-device item remains open with its expected outcome updated — the widget should now match the app on hardware once this build reaches a device; confirm there. Keep the demo-mode disclosure language: the data is demo-mode synthetic through the real pipeline, no HealthKit provenance claimed. Do not delete or rewrite §1-§7.
  6. Commit the evidence update.
  </action>
  <verify>
  <automated>ls StressMonitor/build/wire-01/ | grep -c png; test -s .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md && grep -c "01-06" .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md && grep -c "demo" .planning/phases/01-binary-manifest-truth/01-WIRE-01-EVIDENCE.md; git status --porcelain StressMonitor/StressMonitorWidget/ "StressMonitor/StressMonitorWatch Watch App/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - The full StressMonitor unit suite passes (TEST SUCCEEDED, zero failures) on the pinned destination with parallel testing disabled.
  - At least two new screenshot files exist under StressMonitor/build/wire-01/ (non-empty), captured in the same window: one showing the widget rendering a live stress value and one showing the dashboard.
  - The values read from the widget and the dashboard via the accessibility tree match, and both readings are quoted in the evidence note.
  - 01-WIRE-01-EVIDENCE.md contains the new gap-closure section referencing the wired trigger and the new pair, marks the §2 pair superseded, retains the demo-mode disclosure, and leaves §6 (physical-device check) open with the updated expected outcome.
  - git status shows zero modifications under StressMonitor/StressMonitorWidget/ and StressMonitor/StressMonitorWatch Watch App/ (the read side and the watch target are untouched — write-side-only change).
  </acceptance_criteria>
  <done>The regression net is green, the simulator now demonstrates the widget rendering the same live value the dashboard shows, and the phase's widget evidence artifact records the closed gap honestly — with only the physical-device confirmation left to the standing human item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HealthKit (read-only source) → app calculation pipeline → App Group suite (group.stress.ai.com) → widget extension process | the trigger lengthens an existing on-device path; the suite is local, readable only by same-team bundles, and was already entitlement-verified by BUILD-02 |
| app → backend (/chat) | explicitly untouched — the D3 derived-score payload contract is unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering (data integrity) | StressViewModel.loadCurrentStress → StressRepository.save → SwiftData/CloudKit history | medium | mitigate | Repeated UI-driven loads of the same underlying HealthKit sample would insert duplicate measurements and skew baseline/bio-age/weekly statistics (and, via CloudKit, propagate the skew): the lastPersistedReadingDate guard persists exactly one measurement per new reading, pinned by the same-reading-twice unit test |
| T-06-02 | Information Disclosure | App Group suite now holds real derived stress values where it previously stayed empty | low | accept | Device-local App Group scoped to same-team bundles (BUILD-02 entitlement chain); no new egress — the /chat payload and D3 docs are untouched (git-diff-empty gates in Task 1) |
| T-06-03 | DoS (resource) | WidgetCenter.reloadAllTimelines fired on every save | low | mitigate | Reload rides the same dedupe guard — it fires only when a new reading actually persists; the 15s DEBUG demo cadence does not ship (SimulatorHealthKitService is compiled out of Release) |
| T-06-04 | Spoofing (evidence) | Demo-mode data presented as HealthKit-live evidence | low | mitigate | Evidence note carries the standing demo-mode disclosure; suite-key writes are machine-verified by the unit tests, not narrated |

ASVS L1 note: no high or critical threats — no blocking human checkpoint required; autonomous execution stands.
</threat_model>

<verification>
- WIRE-01 / SC-5 (wire branch, per D4): the extended WidgetPublisherKeyMatchingTests suite is green — the live-path integration test proves loadCurrentStress → real StressRepository.save → all six latest_* suite keys non-nil with latest_stress_level equal to currentStress.level; the call-graph grep shows repository.save inside loadCurrentStress, the funnel reached from DashboardView .task on every app open.
- Regression: full StressMonitor unit suite green after touching the core viewmodel.
- Simulator evidence: same-window pair with matching values; widget no longer rendering the empty state after a demo save.
- Frozen contracts: git diff empty for WidgetDataState.swift, WidgetDataStateTests.swift, WidgetSharedData.swift, StressContextPayload.swift, HealthBackgroundScheduler.swift, DashboardViewModel.swift, all three .entitlements files, and both widget/watch target trees.
- Re-verification hooks for the phase verifier: rerun the -only-testing command (Task 1) and the call-graph greps from VERIFICATION.md behavioral spot-checks — publish now has a live ancestor.
</verification>

<success_criteria>
- The phase's sole failed truth flips: after a refresh, the widget shows the same stress score the app shows (simulator-proven; device check remains the surfaced end-of-phase human item with an expected-pass outcome).
- WidgetDataState behavior, WidgetPublisher keys, the /chat payload, entitlements, and the BUILD-04 background-modes deferral are all provably untouched.
- The plan is surgical: two commits of production/test change confined to StressViewModel + the test suite + the mock fixture, plus the evidence update.
</success_criteria>

## Artifacts this phase/plan produces

| Artifact | Kind | Notes |
|----------|------|-------|
| StressViewModel.makeMeasurement(from:) + lastPersistedReadingDate guard + save call in loadCurrentStress | production code (new symbols) | the live widget write trigger; calculateAndSaveStress refactored onto the shared helper, behavior identical |
| MockHealthKitService.mockHRVTimestamp | test fixture (new property) | pins the underlying reading date for the dedupe test; nil preserves current behavior |
| Three tests in WidgetPublisherKeyMatchingTests | tests (new) | live-path save (mock repo), full suite-key integration (real repo), same-reading dedupe |
| 01-WIRE-01-EVIDENCE.md §8 + new screenshot pair | evidence record | gap closure, same-value pair, §2 marked superseded, demo disclosure retained |
| No changes | — | widget target, watch target, entitlements, plists, background scheduler, /chat payload — all byte-identical |

<output>
Create `.planning/phases/01-binary-manifest-truth/01-06-SUMMARY.md` when done
</output>
