---
phase: 01-binary-manifest-truth
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - StressMonitor/StressMonitor/Info.plist
  - StressMonitor/StressMonitorWidget/Info.plist
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - BUILD-03
  - BUILD-01
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The app source-tree Info.plist contains exactly one top-level key: CFBundleURLTypes (the GoogleSignIn callback scheme block preserved verbatim)"
    - "The merged product plist of the built app still resolves all six STOREKIT keys with values byte-equal to the INFOPLIST_KEY_STOREKIT_* build settings, plus at least one CFBundleURLSchemes entry and all previously-present usage strings"
    - "The merged widget product plist still contains NSExtensionPointIdentifier equal to com.apple.widgetkit-extension — whether the source plist file was deleted (auto-injection verified) or retained as a one-key file"
    - "BUILD-03/adjacency edge: keys expressible as INFOPLIST_KEY_* live only in build settings; keys with no build-setting equivalent (CFBundleURLTypes) live only in the plist file — the two sources no longer contribute overlapping keys (today's duplication was drift risk, not value conflict, and it is gone)"
    - "BUILD-03/empty edge: no empty Info.plist file remains in any real target's source tree; the widget either has no plist file at all or keeps exactly its single live NSExtension key"
    - "BUILD-03/ordering edge: merged-plist verification asserts key presence and value equality via plutil output, not key ordering — ordering is Xcode-determined and not a contract"
    - "BUILD-03/concurrency edge: every plist assertion runs against a completed built product — no partial or in-flight build state is ever inspected"
    - "No PBXShellScriptBuildPhase remains in project.pbxproj (the dead Giphy dSYM stub phase — definition and reference — is removed), and zero live references to Giphy/Kingfisher/exyte MediaPicker remain in the compiled targets"
  artifacts:
    - Reduced StressMonitor/StressMonitor/Info.plist (CFBundleURLTypes only)
    - Deleted-or-retained StressMonitor/StressMonitorWidget/Info.plist per the empirical check outcome
    - project.pbxproj edits: widget INFOPLIST_FILE removal (if deletion lands) + Giphy script-phase removal
  key_links:
    - "StoreKitProductCatalog.swift (~line 73) reads the six product-ID keys via the 3-tier resolution (Info.plist → process env → UserDefaults, StressAPIConfig precedent) — tier 1 must keep resolving from the merged plist or StoreKit silently falls back"
    - "GoogleSignIn callback ↔ CFBundleURLSchemes entry com.googleusercontent.apps.595426793312-45qv7fttusn55km8l5m60lln0amfi5rf ↔ REVERSED_CLIENT_ID in GoogleService-Info.plist — the scheme's only home is the plist file (no INFOPLIST_KEY_* equivalent exists)"
  prohibitions:
    - statement: "The app's Info.plist file must NOT be deleted outright — CFBundleURLTypes has no INFOPLIST_KEY_* equivalent and its loss breaks the GoogleSignIn callback (Pitfall 2)"
      status: flagged-unverified
      verification: "file still exists post-task containing only the CFBundleURLTypes key"
    - statement: "The widget Info.plist must NOT be deleted without the empirical NSExtension auto-injection check on a built product — delete only after plutil proves the key survives (Pitfall 3 / assumption A2)"
      status: flagged-unverified
      verification: "deletion lands only in the same commit as the product-plist proof, or the file is retained"
    - statement: "Orphaned directories (repo-root StressMonitorTests/, StressMonitor/{Models,Services,Views}/) must not be touched — edits there compile nowhere (Pitfall 7)"
      status: flagged-unverified
      verification: "git diff paths all start with StressMonitor/StressMonitor{,Widget,Tests,Watch}/ or the pbxproj"
    - statement: "Do not retype the six STOREKIT values from memory — transcribe verbatim from the pbxproj INFOPLIST_KEY_STOREKIT_* settings when asserting (values in the action's read targets)"
      status: flagged-unverified
      verification: "assertion values match pbxproj strings exactly"
---

<objective>
Consolidate Info.plist key resolution onto INFOPLIST_KEY_* build settings (BUILD-03) and remove the dead media-dependency build machinery (the Giphy dSYM stub script phase — BUILD-01's unused-media residue).

Purpose: today the six STOREKIT_* keys are duplicated byte-identically in both the plist file and build settings — a drift risk where a future edit changes one side silently. The end-state model is the watch target: GENERATE_INFOPLIST_FILE with all expressible keys as build settings, the plist file holding only what has no build-setting equivalent (CFBundleURLTypes). The Giphy script phase references a dependency that no longer exists — dead build machinery that must not ship.

Output: app Info.plist reduced to CFBundleURLTypes-only, widget plist deleted-or-verified per the empirical check, merged product plists proven key-complete, pbxproj free of the dead script phase, zero live media-dep references.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-binary-manifest-truth/01-CONTEXT.md
@.planning/phases/01-binary-manifest-truth/01-RESEARCH.md
@.planning/phases/01-binary-manifest-truth/01-PATTERNS.md

@StressMonitor/StressMonitor/Info.plist
@StressMonitor/StressMonitorWidget/Info.plist
@StressMonitor/StressMonitor.xcodeproj/project.pbxproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: App plist reduced to CFBundleURLTypes + widget plist delete-or-verify, proven on merged product plists</name>
  <files>StressMonitor/StressMonitor/Info.plist, StressMonitor/StressMonitorWidget/Info.plist, StressMonitor/StressMonitor.xcodeproj/project.pbxproj</files>
  <read_first>
  - StressMonitor/StressMonitor/Info.plist (edit target: the CFBundleURLTypes block at lines ~5-17 to keep verbatim; the six STOREKIT_* entries at lines ~18-29 to delete)
  - StressMonitor/StressMonitorWidget/Info.plist (11-line file whose only key is the NSExtension dict)
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj (app config blocks ~lines 883-903 / 940-960 for the INFOPLIST_KEY_STOREKIT_* values; widget blocks ~lines 687-690 / 723-726 for INFOPLIST_FILE + GENERATE_INFOPLIST_FILE; watch blocks ~lines 998-1010 as the generate-only end-state model)
  - StressMonitor/StressMonitor/Services/StoreKit/StoreKitProductCatalog.swift (~line 73 — the 3-tier key consumer that must keep resolving)
  - .planning/phases/01-binary-manifest-truth/01-PATTERNS.md (sections: app plist reduction with the verbatim URL-types block, widget delete-or-verify, the six verbatim STOREKIT values)
  - scripts/verify-archive.sh (from plan 01 — check 2 already asserts the merged-plist contract; this task makes it meaningful)
  </read_first>
  <action>
  1. App plist reduction: delete the six STOREKIT_* keys (the credits large/small product IDs, the premium annual/monthly/weekly product IDs, and the numeric subscription group ID — currently duplicated byte-identically from the INFOPLIST_KEY_STOREKIT_* build settings at pbxproj lines ~892-897 Debug and ~949-954 Release). Keep the CFBundleURLTypes block verbatim (CFBundleTypeRole Editor, CFBundleURLName and CFBundleURLSchemes both com.googleusercontent.apps.595426793312-45qv7fttusn55km8l5m60lln0amfi5rf). Leave the pbxproj app-level INFOPLIST_FILE setting pointing at this now-URL-types-only file, and leave GENERATE_INFOPLIST_FILE = YES — the merged plist comes from both, per the watch target's model inverted only where no build-setting equivalent exists.
  2. Widget plist empirical check FIRST, deletion second (Pitfall 3): remove the widget's INFOPLIST_FILE setting from both config blocks (pbxproj lines ~688 and ~724, the string assignments naming StressMonitorWidget/Info.plist), delete StressMonitor/StressMonitorWidget/Info.plist, then build the widget scheme: xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitorWidgetExtension -destination 'generic/platform=iOS Simulator' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO. plutil -p the built product's Info.plist and check NSExtension → NSExtensionPointIdentifier equals com.apple.widgetkit-extension. If the key is MISSING (assumption A2 false), restore the one-key plist file and its INFOPLIST_FILE settings via git checkout and keep the file — that retained-file outcome is an acceptable end-state; record which branch happened in the SUMMARY.
  3. Merged-plist proof: build the app scheme (CI-parity command) and assert on the built product's Info.plist via plutil -p: all six STOREKIT keys present with values byte-equal to the build settings (com.stressmonitor.app.credits.large, com.stressmonitor.app.credits.small, com.stressmonitor.app.premium.annual, com.stressmonitor.app.premium.monthly, com.stressmonitor.app.premium.weekly, subscription group 22353146), CFBundleURLSchemes present, and usage-string keys that were previously present all still resolve (compare against the golden merged plist at .asc/artifacts/StressMonitor.xcarchive — the build-13 app Info.plist is the no-lost-keys reference).
  </action>
  <verify>
  <automated>plutil -p StressMonitor/StressMonitor/Info.plist; xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'generic/platform=iOS Simulator' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -2; APP_DIR=$(ls -td ~/Library/Developer/Xcode/DerivedData/*/Build/Products/Debug-iphonesimulator/StressMonitor.app 2>/dev/null | head -1); APP_PLIST="$APP_DIR/Info.plist"; plutil -extract STOREKIT_CREDITS_LARGE_PRODUCT_ID raw "$APP_PLIST" && plutil -extract STOREKIT_CREDITS_SMALL_PRODUCT_ID raw "$APP_PLIST" && plutil -extract STOREKIT_PREMIUM_ANNUAL_PRODUCT_ID raw "$APP_PLIST" && plutil -extract STOREKIT_PREMIUM_MONTHLY_PRODUCT_ID raw "$APP_PLIST" && plutil -extract STOREKIT_PREMIUM_WEEKLY_PRODUCT_ID raw "$APP_PLIST" && plutil -extract STOREKIT_PREMIUM_SUBSCRIPTION_GROUP_ID raw "$APP_PLIST"</automated>
  </verify>
  <acceptance_criteria>
  - plutil -p on the source StressMonitor/StressMonitor/Info.plist shows exactly one top-level key: CFBundleURLTypes.
  - The built app product's merged Info.plist resolves all six STOREKIT_* keys with values byte-equal to the pbxproj INFOPLIST_KEY_STOREKIT_* settings (transcribed from the pbxproj, not from memory).
  - The built app product's merged Info.plist still contains the com.googleusercontent.apps.595426793312-45qv7fttusn55km8l5m60lln0amfi5rf URL scheme.
  - The built widget product's merged Info.plist contains NSExtensionPointIdentifier = com.apple.widgetkit-extension, and the SUMMARY records whether that came from auto-injection (file deleted) or the retained one-key file.
  - Diffing the new merged app plist against the build-13 golden (.asc/artifacts/.../StressMonitor.app/Info.plist) shows no lost keys (only expected identical values).
  - No empty Info.plist file exists under StressMonitor/StressMonitor/, StressMonitor/StressMonitorWidget/, or "StressMonitor/StressMonitorWatch Watch App/".
  </acceptance_criteria>
  <done>Every key that has an INFOPLIST_KEY_* equivalent lives only in build settings; the plist files hold only non-expressible keys; merged product plists prove nothing was lost.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the dead Giphy dSYM stub script phase and re-verify zero live media-dep references</name>
  <files>StressMonitor/StressMonitor.xcodeproj/project.pbxproj</files>
  <read_first>
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj (the PBXShellScriptBuildPhase definition block with ID F2A1B0012AAA000100DE6E8F named "Generate Giphy dSYM Stub" at lines ~526-544, and its reference inside the app target's buildPhases array at line ~406; it is the only shell-script build phase in the file)
  - .planning/phases/01-binary-manifest-truth/01-RESEARCH.md (§5.5 — the unused-media evaluation greps and the surviving residue)
  </read_first>
  <action>
  Delete both parts of the dead script phase: (1) the reference line F2A1B0012AAA000100DE6E8F /* Generate Giphy dSYM Stub */, from the app target's buildPhases array (~line 406), and (2) the full PBXShellScriptBuildPhase definition block carrying that ID (~lines 526-544). Since it is the only entry in the PBXShellScriptBuildPhase section, also remove the Begin/End section markers so the pbxproj stays well-formed. Then re-run the live-reference greps over the four real target directories (paths quoted — one contains spaces): case-insensitive grep for the media SDK names (Giphy, Kingfisher, exyte, MediaPicker) over StressMonitor/StressMonitor, StressMonitor/StressMonitorWidget, "StressMonitor/StressMonitorWatch Watch App", and StressMonitor/StressMonitorTests — expected zero hits per research (the pbxproj had zero package references for them already; the script phase was the only residue). If any hit appears, it is a live reference the removal decision did not know about — stop and record it in the SUMMARY instead of forcing removal. Cheap adjacent check the research flagged: confirm the StoreKit config StressMonitor/StressMonitorTests/StressMonitorProducts.storekit references only com.stressmonitor.app.* product IDs (no media-adjacent leftovers).
  </action>
  <verify>
  <automated>grep -c "F2A1B0012AAA000100DE6E8F\|Giphy" StressMonitor/StressMonitor.xcodeproj/project.pbxproj; grep -rniE "giphy|kingfisher|exyte|mediapicker" StressMonitor/StressMonitor "StressMonitor/StressMonitorWidget" "StressMonitor/StressMonitorWatch Watch App" StressMonitor/StressMonitorTests --include="*.swift" | wc -l; xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'generic/platform=iOS Simulator' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
  - grep -c for the script-phase ID and the Giphy name in project.pbxproj returns 0.
  - The case-insensitive media-SDK grep across the four real target directories returns 0 lines.
  - The CI-parity app build still succeeds (BUILD SUCCEEDED) — the pbxproj edit did not break the target.
  - plutil -lint style sanity: xcodebuild -list or the build itself confirms the project parses (no orphaned section markers).
  </acceptance_criteria>
  <done>The pbxproj carries no dead build machinery referencing removed dependencies, and the unused-media removal decision is fully landed (zero live references + zero build residue).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plist file + build settings → merged product plist | Xcode merges GENERATE_INFOPLIST_FILE output with INFOPLIST_FILE content; wrong deletions silently starve runtime key consumers |
| pbxproj edits → all targets' buildability | malformed section removal or buildPhases dangling references break every build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering (silent config drift) | Merged-plist key loss (StoreKit product resolution silently falls back to defaults) | high | mitigate | Merged-product plutil assertions for all six keys with byte-equal values + diff against the build-13 golden merged plist (no lost keys); verify-archive.sh check 2 re-runs the contract |
| T-03-02 | DoS | Widget extension fails to load without NSExtensionPointIdentifier in merged plist | medium | mitigate | Empirical one-build check before deletion; retained-file fallback path defined; widget render evidence re-proven in plan 04 task 3 |
| T-03-03 | Tampering | Dead script phase surviving (executable build machinery referencing a removed dependency — attack surface and noise) | medium | mitigate | Delete definition + reference + section markers; zero-reference greps; build green |
</threat_model>

<verification>
- Source-tree audit: app plist is CFBundleURLTypes-only; widget file deleted-or-one-key; no empty plists.
- Product audit: merged plists resolve all keys (six STOREKIT values, URL scheme, NSExtensionPointIdentifier) — checked via plutil on built products and via scripts/verify-archive.sh in plan 04's fresh archive.
- pbxproj audit: zero Giphy references; both builds (app + widget schemes) succeed.
</verification>

<success_criteria>
- BUILD-03's single-source truth holds: every key with an INFOPLIST_KEY_* equivalent resolves only from build settings; the plist file keeps only non-expressible keys, documented.
- The media-dependency residue is gone: no script phase, no live references, StoreKit config clean.
</success_criteria>

## Artifacts this phase/plan produces

| Artifact | Kind | Notes |
|----------|------|-------|
| App Info.plist (CFBundleURLTypes only) | bundle plist (reduced) | six STOREKIT_* file-side copies deleted |
| Widget Info.plist (deleted, or retained one-key) | bundle plist | outcome decided by the empirical NSExtension check, recorded in SUMMARY |
| pbxproj: widget INFOPLIST_FILE settings removal (conditional) + Giphy PBXShellScriptBuildPhase removal | build config | section markers removed with the last shell phase |
| No new build-settings keys introduced | — | keys already existed; only the file-side duplicates were removed |

<output>
Create `.planning/phases/01-binary-manifest-truth/01-03-SUMMARY.md` when done
</output>
