---
phase: 01-binary-manifest-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-archive.sh
  - StressMonitor/spm-cache/packages/proxy/.proxies/Firebase_proxy/Package.swift
  - StressMonitor/spm-cache/packages/proxy/.proxies/Firebase_proxy/Sources/Firebase_FirebaseAuth_shim/Firebase_FirebaseAuth_shim.swift
  - StressMonitor/spm-cache/packages/proxy/.proxies/Firebase_proxy/Sources/Firebase_FirebaseCore_shim/Firebase_FirebaseCore_shim.swift
  - StressMonitor/spm-cache/packages/proxy/.proxies/GoogleSignIn-iOS_proxy/Package.swift
  - StressMonitor/spm-cache/packages/proxy/Package.swift
  - StressMonitor/spm-cache/packages/umbrella/Package.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  - StressMonitor/StressMonitor.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
autonomous: true
requirements:
  - ENV-04
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "scripts/verify-archive.sh exits 0 against the build-13 golden archive (.asc/artifacts/StressMonitor.xcarchive)"
    - "scripts/verify-archive.sh exits non-zero against a copy of a bundle binary with a planted JWT-shaped string appended — the gate detects planted secrets, not just known-benign hits"
    - "swift package resolve succeeds inside StressMonitor/spm-cache/packages/proxy and the project Package.resolved regains firebase-ios-sdk pinned to revision fdc352fabaf5916e7faa1f96ad02b1957e93e5a5 (version 11.15.0)"
    - "A Release .xcarchive is producible from the working tree via xcodebuild archive with signing disabled (ENV-04: archive producible from the unmodified working tree, not only from HEAD)"
    - "The new archive still ships third-party SDK privacy manifests — Firebase_FirebaseCore.bundle/PrivacyInfo.xcprivacy and GoogleSignIn_GoogleSignIn.bundle/PrivacyInfo.xcprivacy exist inside StressMonitor.app (the proxy migration keeps SDK resource bundles flowing)"
    - "AUTH-01/concurrency edge: the artifact scan is read-only and repeatable — an identical archive yields an identical verdict; an interrupted run leaves the archive byte-identical"
  artifacts:
    - scripts/verify-archive.sh
    - StressMonitor/spm-cache/packages/proxy/.proxies/Firebase_proxy/ (Package.swift + 2 shim sources)
    - Regenerated StressMonitor/StressMonitor.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved containing the firebase-ios-sdk pin
    - StressMonitor/build/Phase1-Verify.xcarchive (unsigned, local verification artifact)
  key_links:
    - "pbxproj XCSwiftPackageProductDependency productName (line ~1131) ↔ GoogleSignIn shim Package.swift product name ↔ proxy root Package.swift .product(name:) — the rename must land in all three places atomically or resolution fails (Pitfall 4)"
    - "pbxproj XCLocalSwiftPackageReference relativePath spm-cache/packages/proxy (ID C3ECFE78514FF7DFB2DBDB1E) ↔ proxy Package.swift spm_cache_root target ↔ .proxies/* shim packages"
  prohibitions:
    - statement: "The proxy migration must NOT be solved by reverting to HEAD remote package references — complete the user's in-flight migration in place (CONTEXT ENV-04 decision; .asc/backup/spm-migration/ is a snapshot, never a restore source)"
      status: flagged-unverified
      verification: "git diff shows the XCLocalSwiftPackageReference survives; no XCRemoteSwiftPackageReference for firebase-ios-sdk or GoogleSignIn-iOS reappears"
    - statement: "No floating version requirements — upstream pins are exact revisions (08d8dcecafb575f98879ffdbb8302c1b9ad65d19 for GoogleSignIn-iOS, fdc352fabaf5916e7faa1f96ad02b1957e93e5a5 for firebase-ios-sdk)"
      status: flagged-unverified
      verification: "grep both Package.swift shim files for revision-pinned .package(url:) entries"
    - statement: "No credential may be introduced into build settings, scripts, or CI env to make signing or dependency resolution pass"
      status: flagged-unverified
      verification: "diff review of pbxproj and scripts/ contains no secret-like values"
    - statement: "The build-13 golden artifacts under .asc/artifacts/ must not be modified or deleted — they are the AUTH-01/BUILD-01 baseline reference"
      status: flagged-unverified
      verification: "git status shows .asc/artifacts untouched; file mtimes unchanged"
  flagged_assumptions:
    - "ENV-04 edge probe (unclassified): the local-proxy pattern — swift-tools-version 6.0, @_exported import shims, revision-pinned upstream deps, renamed proxy product GoogleSignIn_proxied — remains fully archivable under Xcode 26.3. This is an assumption until the Task 3 archive gate proves it empirically. If SwiftPM still rejects the graph after the 3-place rename, the same rename recipe applies to the Firebase shim product names (RESEARCH assumption A4); the locked direction (no revert to remote refs) does not change."
---

<objective>
Tracer plan: establish the phase's end-to-end verification capability (artifact-inspection gate over a real archive), then complete the SPM-cache proxy migration (ENV-04) so a Release archive is producible from the unmodified working tree.

Purpose: ENV-04 gates every archive-producing task in this phase (BUILD-01/02/03 verification, AUTH-01 final scan). The working tree currently cannot archive because the proxy package lacks Firebase products entirely and its GoogleSignIn product name collides with upstream (HANDOFF.json blocker). The verify-archive.sh script built here becomes the reusable gate for plans 02-04.

Output: scripts/verify-archive.sh (green on the build-13 golden archive), a completed .proxies/Firebase_proxy shim package, a collision-free GoogleSignIn proxy rename, a regenerated Package.resolved regaining the firebase-ios-sdk pin, and an unsigned Release archive produced from the tree that passes the new gate.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-binary-manifest-truth/01-CONTEXT.md
@.planning/phases/01-binary-manifest-truth/01-RESEARCH.md
@.planning/phases/01-binary-manifest-truth/01-PATTERNS.md

@scripts/run-tests.py
@StressMonitor/spm-cache/packages/proxy/Package.swift
@StressMonitor/spm-cache/packages/proxy/.proxies/GoogleSignIn-iOS_proxy/Package.swift
@StressMonitor/StressMonitor.xcodeproj/project.pbxproj
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build scripts/verify-archive.sh and prove it green on the build-13 golden archive (and red on a planted secret)</name>
  <files>scripts/verify-archive.sh</files>
  <read_first>
  - scripts/run-tests.py (conventions: shebang + docstring header stating local/CI parity, path constants rooted at repo layout, print-then-exit-nonzero error style)
  - .planning/phases/01-binary-manifest-truth/01-PATTERNS.md (section "scripts/verify-archive.sh (NEW, Wave 0 artifact-inspection gate)" — the wrapped command trio and the triage-allowlist requirement)
  - .planning/phases/01-binary-manifest-truth/01-RESEARCH.md (§7 AUTH-01 methodology + baseline results; §4 BUILD-02 entitlements values; §5.4 SDK manifest bundle paths)
  - .asc/artifacts/StressMonitor.xcarchive (target of the first run — confirm bundle layout: Products/Applications/StressMonitor.app, PlugIns/StressMonitorWidgetExtension.appex, Watch/StressMonitorWatch Watch App.app)
  </read_first>
  <behavior>
  - Test 1 (green direction): bash scripts/verify-archive.sh .asc/artifacts/StressMonitor.xcarchive exits 0 — all checks PASS on the known-good build-13 artifact.
  - Test 2 (red direction — proves the gate detects secrets): copy the golden app binary to a temp dir, append the literal string eyJhbGciOiJIUzI1NiJ9.PLANTEDSECRETVALUE.forTestingPurposes to the copy, run the credential-scan check against that copy, and the script (or its scan mode) exits non-zero.
  - Test 3 (entitlements direction): the per-bundle entitlements check finds com.apple.security.application-groups containing group.stress.ai.com in all three bundles of the golden archive.
  </behavior>
  <action>
  Create scripts/verify-archive.sh as a bash script taking one positional argument (path to an .xcarchive OR a directory containing Payload/ for an unzipped IPA) plus a --skip-entitlements flag for unsigned archives. Header comment: verify StressMonitor archive — entitlements, merged plists, credential scan; works locally and in CI (mirroring run-tests.py's docstring convention).

  Five checks, each printing PASS/FAIL with a one-line reason, script exits non-zero if any FAIL:
  1. ENTITLEMENTS (skipped when --skip-entitlements): run codesign -d --entitlements :- on StressMonitor.app, PlugIns/StressMonitorWidgetExtension.appex, and Watch/StressMonitorWatch Watch App.app; PASS when each dump contains the application-groups array with group.stress.ai.com. This is the build-12 no-entitlements-blob regression guard STATE.md mandates.
  2. MERGED PLISTS: plutil -p on the app and widget Info.plists; PASS when the app plist contains all six STOREKIT keys (STRESS_API-style names: the credits/premium product IDs and the numeric subscription group), at least one CFBundleURLSchemes entry, and the widget plist contains NSExtensionPointIdentifier equal to com.apple.widgetkit-extension.
  3. CREDENTIAL SCAN (AUTH-01 §7 pipeline): for each of the three Mach-O binaries run strings -a piped to grep -inE over the RESEARCH §7 pattern set (PRIVATE KEY, sk- prefix, anon key, api secret, BEGIN RSA, BEGIN EC, Bearer followed by 20+ token chars, plus the sb_publishable/sb_secret forms and eyJ JWT-shape). Apply the documented benign allowlist so these known hits do not fail the gate: the literal eyJlcnJvciI6IlVOS05PV05fRVJST1IifQ== (Google App Check error constant), the supabaseAccessToken/supabaseRefreshToken/supabaseSessionExpiresAt/supabaseChatSessionId Keychain-cleanup literals (FirebaseAuthService.swift line ~133 removes old tokens; names, not tokens), and expected endpoint/bundle-id strings (https://stress-api.dropitx.site, stress.ai.com, com.googleusercontent.apps prefix). PASS when zero hits remain after allowlist subtraction. Include the allowlist as comments in the script so triage survives context loss.
  4. RESOURCE GREP: grep -rl over the app bundle for the AIza prefix — PASS when the only matching file is GoogleService-Info.plist (Firebase identifiers are public by Google's model; appearing in a Mach-O would be a change worth triage).
  5. SDK MANIFESTS: PASS when Firebase_FirebaseCore.bundle/PrivacyInfo.xcprivacy and GoogleSignIn_GoogleSignIn.bundle/PrivacyInfo.xcprivacy exist under StressMonitor.app (third-party SDK privacy-manifest presence — BUILD-01's no-regression half).

  Conventions: set -euo pipefail; resolve the repo root from the script location; never print raw matched secret content — print the pattern name and the file (the operator re-runs strings manually when triage is needed).
  </action>
  <verify>
  <automated>bash scripts/verify-archive.sh .asc/artifacts/StressMonitor.xcarchive; echo "golden_exit=$?"; TMPD=$(mktemp -d); cp ".asc/artifacts/StressMonitor.xcarchive/Products/Applications/StressMonitor.app/StressMonitor" "$TMPD/tampered"; printf 'eyJhbGciOiJIUzI1NiJ9.PLANTEDSECRETVALUE.forTestingPurposes' >> "$TMPD/tampered"; strings -a "$TMPD/tampered" | grep -c "PLANTEDSECRETVALUE"</automated>
  </verify>
  <acceptance_criteria>
  - bash scripts/verify-archive.sh .asc/artifacts/StressMonitor.xcarchive prints PASS for all five checks and exits 0.
  - The planted-secret red check: strings over the tampered copy finds PLANTEDSECRETVALUE (count ≥ 1), proving the scan pattern set catches a planted JWT-shaped credential.
  - The script contains the triage allowlist as comments, including the eyJ error-constant literal and the four supabase Keychain-cleanup literal names.
  - The script accepts --skip-entitlements and, in that mode, skips only check 1.
  - grep -c "fastlane match\|setup_match" scripts/verify-archive.sh returns 0 (the script inspects artifacts only; it never signs).
  </acceptance_criteria>
  <done>verify-archive.sh is committed, green on the build-13 golden archive in all five checks, and red on a planted secret — the phase's reusable artifact gate exists and detects what it claims to detect.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the SPM proxy migration — Firebase shim package + non-colliding GoogleSignIn rename + umbrella pin</name>
  <files>
  StressMonitor/spm-cache/packages/proxy/.proxies/Firebase_proxy/Package.swift,
  StressMonitor/spm-cache/packages/proxy/.proxies/Firebase_proxy/Sources/Firebase_FirebaseAuth_shim/Firebase_FirebaseAuth_shim.swift,
  StressMonitor/spm-cache/packages/proxy/.proxies/Firebase_proxy/Sources/Firebase_FirebaseCore_shim/Firebase_FirebaseCore_shim.swift,
  StressMonitor/spm-cache/packages/proxy/.proxies/GoogleSignIn-iOS_proxy/Package.swift,
  StressMonitor/spm-cache/packages/proxy/Package.swift,
  StressMonitor/spm-cache/packages/umbrella/Package.swift,
  StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  </files>
  <read_first>
  - StressMonitor/spm-cache/packages/proxy/.proxies/GoogleSignIn-iOS_proxy/Package.swift (the complete analog to clone — product/target naming, revision pin shape)
  - StressMonitor/spm-cache/packages/proxy/.proxies/GoogleSignIn-iOS_proxy/Sources/GoogleSignIn-iOS_GoogleSignIn_shim/GoogleSignIn-iOS_GoogleSignIn_shim.swift (shim header comment + single @_exported import line)
  - StressMonitor/spm-cache/packages/proxy/Package.swift (where the Firebase entries get inserted and where the rename lands)
  - StressMonitor/spm-cache/packages/umbrella/Package.swift (existing google-family revision pins, lines ~7-16)
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj (XCSwiftPackageProductDependency section, lines ~1126-1142; XCLocalSwiftPackageReference section, lines ~1120-1125 — must remain untouched except the productName rename)
  - .planning/phases/01-binary-manifest-truth/01-PATTERNS.md (sections: Firebase_proxy pattern, the 3-place atomic rename, umbrella pin)
  </read_first>
  <precondition>Network access to github.com is available — firebase-ios-sdk is NOT in the local clone cache (spm-cache/clones/repositories/), so swift package resolve performs one fetch (RESEARCH §8 risk note).</precondition>
  <action>
  Complete the user's in-flight migration in place (ENV-04 decision; never revert to HEAD remote references — the .asc/backup/spm-migration/ snapshot is reference-only):

  1. Create .proxies/Firebase_proxy/ mirroring the GoogleSignIn-iOS_proxy shape exactly: Package.swift with name "Firebase_proxy", one dependency .package(url: "https://github.com/firebase/firebase-ios-sdk.git", revision: "fdc352fabaf5916e7faa1f96ad02b1957e93e5a5") (the HEAD-resolved revision — keeps the shipped binary equivalent, RESEARCH A3), products .library(name: "FirebaseAuth", targets: ["Firebase_FirebaseAuth_shim"]) and .library(name: "FirebaseCore", targets: ["Firebase_FirebaseCore_shim"]), and two shim targets whose sources are one file each containing the auto-generated header comment (Auto-generated by spm-cache-proxy: re-exports the source package module(s).) followed by @_exported import FirebaseAuth / @_exported import FirebaseCore. These product names do NOT need renaming — upstream firebase-ios-sdk enters the graph only through this shim, so the names stay unique (unlike the GoogleSignIn case where upstream entered one hop away).
  2. Rename the GoogleSignIn proxy products to non-colliding names — atomically in three places (Pitfall 4): (a) .proxies/GoogleSignIn-iOS_proxy/Package.swift products GoogleSignIn/GoogleSignInSwift become GoogleSignIn_proxied/GoogleSignInSwift_proxied (target names and the inner upstream .product(name: "GoogleSignIn", package: "GoogleSignIn-iOS") dependency refs stay as-is — they point upstream); (b) proxy/Package.swift spm_cache_root target deps .product(name:) refs updated to the _proxied names; (c) pbxproj XCSwiftPackageProductDependency productName = GoogleSignIn; (line ~1131, including the /* comment */ label) becomes GoogleSignIn_proxied. The FirebaseAuth/FirebaseCore product dependencies (lines ~1133-1142) stay unchanged. App Swift sources are NOT touched — import GoogleSignIn resolves to the upstream module compiled through the dependency chain; only the product (linking) name changes.
  3. Extend proxy/Package.swift: add .package(path: ".proxies/Firebase_proxy") to dependencies and the two Firebase .product(name:) entries to the spm_cache_root target.
  4. Add the firebase pin to the umbrella cache-warmer package so future machines warm the clone: .package(url: "https://github.com/firebase/firebase-ios-sdk.git", revision: "fdc352fabaf5916e7faa1f96ad02b1957e93e5a5") alongside the existing google-family pins.
  5. Resolve: cd StressMonitor/spm-cache/packages/proxy and run swift package resolve (expect the one network fetch), then xcodebuild -resolvePackageDependencies -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor from repo root to update the project lockfile. Confirm the project Package.resolved regains firebase-ios-sdk at version 11.15.0 / revision fdc352faba…, while googlesignin-ios keeps 08d8dcecaf….
  </action>
  <verify>
  <automated>cd StressMonitor/spm-cache/packages/proxy && swift package resolve && grep -A3 '"firebase-ios-sdk"' ../../StressMonitor.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved | grep -c "fdc352fabaf5916e7faa1f96ad02b1957e93e5a5" && grep -c "GoogleSignIn_proxied" ../../StressMonitor.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>
  - swift package resolve exits 0 inside the proxy package (no product-name collision diagnostics).
  - StressMonitor/StressMonitor.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved contains firebase-ios-sdk with revision fdc352fabaf5916e7faa1f96ad02b1957e93e5a5 and version 11.15.0, and still pins googlesignin-ios at revision 08d8dcecafb575f98879ffdbb8302c1b9ad65d19.
  - grep -c "GoogleSignIn_proxied" project.pbxproj returns exactly 1 (the renamed productName + comment label line).
  - The pbxproj still contains exactly one XCLocalSwiftPackageReference with relativePath spm-cache/packages/proxy and zero XCRemoteSwiftPackageReference entries (no revert).
  - .proxies/Firebase_proxy/ contains Package.swift plus both shim source files, each carrying the auto-generated shim header comment.
  - git diff on the pbxproj shows changes limited to the productName/comment rename — no build-settings churn.
  </acceptance_criteria>
  <done>The proxy package resolves with Firebase products present and a collision-free GoogleSignIn product name; the project lockfile regains the firebase-ios-sdk 11.15.0 pin at the HEAD-resolved revision.</done>
</task>

<task type="auto">
  <name>Task 3: Archive-from-tree gate — unsigned Release archive from the working tree passes verify-archive.sh</name>
  <files>StressMonitor/build/Phase1-Verify.xcarchive (local verification artifact, gitignored output)</files>
  <read_first>
  - scripts/verify-archive.sh (the gate built in Task 1)
  - .planning/phases/01-binary-manifest-truth/01-RESEARCH.md (§14 SC-6 command form; "Don't Hand-Roll" merged-plist row)
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj (current state after Task 2's rename)
  </read_first>
  <action>
  Prove ENV-04's core assertion — a Release archive is producible from the unmodified working tree (not only from HEAD). From repo root, first a CI-parity smoke build to catch resolution failures fast: xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'generic/platform=iOS Simulator' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO. Then produce the archive: xcodebuild archive -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -configuration Release -destination 'generic/platform=iOS Simulator' -archivePath StressMonitor/build/Phase1-Verify.xcarchive CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO. Run scripts/verify-archive.sh StressMonitor/build/Phase1-Verify.xcarchive --skip-entitlements (unsigned archive; the entitlements chain is proven at source level in plan 04 plus on the signed golden archive — the skip is honest, not a dodge). Confirm specifically that the SDK-manifest check passes: the proxy migration must keep Firebase_*.bundle and GoogleSignIn_GoogleSignIn.bundle resource bundles (with their PrivacyInfo.xcprivacy files) flowing into StressMonitor.app — this is BUILD-01's no-regression half for the dependency set. If the archive step itself fails on package resolution, do NOT revert the migration (prohibition) — diagnose the SwiftPM error; if it is a product-name complaint despite the rename, apply the same rename recipe to the Firebase shim product names per the flagged assumption in frontmatter.
  </action>
  <verify>
  <automated>xcodebuild archive -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -configuration Release -destination 'generic/platform=iOS Simulator' -archivePath StressMonitor/build/Phase1-Verify.xcarchive CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3; bash scripts/verify-archive.sh StressMonitor/build/Phase1-Verify.xcarchive --skip-entitlements; echo "verify_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
  - xcodebuild archive exits 0 producing StressMonitor/build/Phase1-Verify.xcarchive from the working tree (ARCHIVE SUCCEEDED in output).
  - bash scripts/verify-archive.sh StressMonitor/build/Phase1-Verify.xcarchive --skip-entitlements exits 0 (merged plists + credential scan + resource grep + SDK manifests all PASS).
  - find StressMonitor/build/Phase1-Verify.xcarchive -name "PrivacyInfo.xcprivacy" -path "*Firebase_FirebaseCore.bundle*" and the GoogleSignIn_GoogleSignIn.bundle equivalent both return a path.
  - .asc/artifacts/ (build-13 golden) untouched by this task.
  </acceptance_criteria>
  <done>A Release archive is producible from the working tree and passes the artifact gate — ENV-04's blocker is closed and every later archive-producing verification in this phase has a proven pipeline to run against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → SPM network resolution | swift package resolve fetches firebase-ios-sdk from github.com (not in local clone cache) |
| pbxproj → Xcode dependency graph | the 3-place rename either resolves atomically or breaks all builds |
| local binary → extraction (future attacker with IPA) | the credential-scan check models an attacker running strings over the shipped artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering (supply chain) | SPM network fetch of firebase-ios-sdk | high | mitigate | Revision-exact pin fdc352fabaf5916e7faa1f96ad02b1957e93e5a5 in both shim and umbrella packages; Package.resolved must regain 11.15.0; no floating requirements (prohibition) |
| T-01-02 | Tampering | Proxy product rename (3-place atomic edit) | medium | mitigate | Rename lands in shim products + proxy root .product(name:) + pbxproj productName in one change; swift package resolve + xcodebuild resolve check immediately after |
| T-01-03 | Information Disclosure | verify-archive.sh output echoing secret material | low | accept | Script prints pattern names and file paths, never raw matched content; operator is the trusted local user re-running strings for triage |
| T-01-04 | DoS | Migration left half-done → no archive producible | medium | mitigate | Task 3's archive gate is the definition of done; failure path prescribes diagnosis within the locked direction, never a revert |
</threat_model>

<verification>
- Golden-baseline gate: scripts/verify-archive.sh exits 0 on .asc/artifacts/StressMonitor.xcarchive (Task 1).
- Planted-secret red check proves the scan detects what it claims (Task 1 behavior Test 2).
- Proxy resolution: swift package resolve exit 0; Package.resolved regains firebase-ios-sdk fdc352faba…/11.15.0 (Task 2).
- Archive producibility: Release archive from working tree + verify-archive.sh --skip-entitlements exit 0 (Task 3).
</verification>

<success_criteria>
- ENV-04 satisfied at the archive level: the working tree (post-migration, pre-revert-of-nothing) archives successfully.
- The phase's reusable inspection gate exists, is proven bidirectionally (green/red), and is committed.
- SDK privacy-manifest bundles survive the proxy migration (BUILD-01 dependency-set half not regressed).
</success_criteria>

## Artifacts this phase/plan produces

| Artifact | Kind | Notes |
|----------|------|-------|
| scripts/verify-archive.sh | verification script | 5 checks: entitlements ×3, merged plists, credential scan w/ allowlist, AIza resource grep, SDK manifest presence; --skip-entitlements flag |
| .proxies/Firebase_proxy/ (Package.swift + Firebase_FirebaseAuth_shim + Firebase_FirebaseCore_shim) | SPM shim package | products FirebaseAuth + FirebaseCore; pinned revision fdc352fabaf5916e7faa1f96ad02b1957e93e5a5 |
| GoogleSignIn proxy product rename → GoogleSignIn_proxied / GoogleSignInSwift_proxied | package products | 3-place atomic rename incl. pbxproj productName |
| Umbrella package firebase pin | cache-warmer pin | same revision as the shim |
| Package.resolved (project) | lockfile (generated) | regains firebase-ios-sdk 11.15.0 |
| StressMonitor/build/Phase1-Verify.xcarchive | local verification artifact | unsigned Release archive from the working tree |

<output>
Create `.planning/phases/01-binary-manifest-truth/01-01-SUMMARY.md` when done
</output>
