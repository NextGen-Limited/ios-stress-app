---
context: default
phase: none (release session — TestFlight distribution, app 6778478266)
task: 0
total_tasks: 0
status: paused
last_updated: 2026-09-03T03:28:18.551Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: unsigned-archive-loses-entitlements — archiving with `CODE_SIGNING_ALLOWED=NO` + re-signing via `xcodebuild -exportArchive` produces an IPA with NO entitlements blob on app, watch, and widget (strict codesign verify still passes, upload still processes). Build 12 shipped broken this way. Mitigation: always run `codesign -d --entitlements - --xml` on every bundle (app / PlugIns/*.appex / Watch/*.app) and grep for `application-groups`/`icloud-container-identifiers`/`healthkit` BEFORE `asc publish`.

- [ ] CONSTRAINT: spm-proxy-migration-unbuildable — the uncommitted working-tree pbxproj points GoogleSignIn/FirebaseAuth/FirebaseCore at the local `spm-cache` proxy, which declares no Firebase products at all and declares a `GoogleSignIn` product that collides with upstream GoogleSignIn-iOS in Xcode's PIF graph (`GUID already registered`, survives SWBBuildService restarts). No archive is possible from this tree until the migration adds Firebase proxies with non-colliding product names. Mitigation: build releases from HEAD; user's migration snapshot lives at `.asc/backup/spm-migration/`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting strict codesign verify as IPA health check | Build 12 passed `codesign --verify --strict` + upload + ASC processing while missing all entitlements | blocking | Mandatory entitlements dump per bundle before every publish |
| Global CODE_SIGN_IDENTITY pinning via xcargs | Hash-pin hits SPM package targets (force-auto-signed) → "conflicting provisioning settings" errors | advisory | Dual-cert profiles (now in place) make identity pinning unnecessary |
| `asc profiles create --bundle <identifier>` | Fails — needs the opaque App ID resource ID (app=JJ5WBTY73C, watch=JL35Z238SZ, widget=S82PJVC9HS) | advisory | Resolve via `asc bundle-ids list` first |
| Host shell `grep` with parens/brackets in patterns | Mis-parses as regex groups, returns false negatives | advisory | Use the built-in grep tool |

## <current_state>
Release session complete — the default-context handoff was a SUBSTANTIVE call, not a detection gap: phase 03 dirs use prefixed plans (`03-01-PLAN.md` … `03-05-PLAN.md`) so the `phases/*/PLAN.md` glob misses them, but all five plans have matching SUMMARYs — v1.1 is closed and no phase plan work is in flight. Resume should NOT hunt the phase dir for interrupted plan work.

Shipped: app `stress.ai.com` version **1.0.0 build 13** (buildId `7e58956b-0f13-4dcb-beaf-f00dec3ce512`) — processingState VALID, external `BETA_APPROVED`, attached to groups **Qa** (3b8cc178-f2f0-4f27-b594-bc5f35ba411c, internal all-builds) and **Release-1.0.0** (c9dfa833-bcde-42b9-81ef-c9ed71de1ac8, the user-requested external group). What-to-Test notes attached (en-US). Built from HEAD `fed4b6b`. This build is the artifact the pending Phase 03 post-merge drift re-test (5 UAT scenarios in `03-UAT.md`) should now run against.

Build 12 was shipped first, discovered entitlement-less via post-export verification, removed from Release-1.0.0, and expired (was: Qa-only attachment).
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Ship TestFlight build 1.0.0 (13) — Done (VALID, BETA_APPROVED)
- Task 2: Distribute to external group Release-1.0.0 — Done
- Task 3: What-to-Test notes — Done
- Task 4: Beta app review submission — Done (approved)
- Task 5: Expire broken build 12 — Done
- Task 6: Recreate 3 match AppStore profiles dual-cert (WTV47CUC2N,XPT2DHR688) — Done (portal: 9F694L8A4A / HP3SH32B48 / MJ9DJJT69T; local UUIDs 48ca5457 / 3def31a2 / 18db81c0)
- Task 7: Preserve + restore user's uncommitted SPM-proxy migration — Done (tree byte-identical to session start; snapshot in `.asc/backup/spm-migration/`)
</completed_work>

<remaining_work>

- Validate CI `fastlane match` readonly against the dual-cert profiles (next CI run). If rejected: `bundle exec fastlane setup_match` once (force-regenerates single-cert profiles; then local signed archives need the dual-cert swap again — recipe in agent memory).
- Finish SPM-cache proxy migration (user's uncommitted work): add Firebase proxy packages/products (none exist in `.proxies/`), give the GoogleSignIn shim a product name that doesn't collide with upstream. Only then can `asc xcode archive` work from the working tree.
- Pre-existing: Phase 03 post-merge drift re-test — 5 UAT scenarios in `.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-UAT.md`, then `/gsd-new-milestone` (v1.2 submission readiness recommended). NOW actionable: build 13 on TestFlight is the artifact those scenarios test.
- Placeholder flagged (not touched): `.planning/phases/03-*/03-VERIFICATION.md` contains TBD/placeholder markers.
- Optional cleanup: nested stray `StressMonitor/StressMonitor.xcodeproj/StressMonitor.xcodeproj/` (July 12 junk, untracked/ignored).
</remaining_work>

<decisions_made>

- Built from HEAD, not the working tree — the uncommitted SPM-proxy migration cannot archive (structural, not cache); all committed work incl. the StoreKit group-ID fix is in the shipped build.
- Switched from unsigned-archive + manual export to dual-cert portal profiles after the entitlements gap was found — a signed archive compiles entitlements and needs no build-setting overrides.
- Pulled build 12 (removed from external group + expired) — verified entitlement-less; strict codesign and ASC processing both pass without entitlements, so verification had to be the entitlements dump.
- Restored the user's tree byte-for-byte after every HEAD-based build cycle — their uncommitted migration was never lost (backup + restore, verified via diff vs backup).
</decisions_made>

<blockers>
- Working tree cannot archive until the SPM-proxy migration is completed (missing Firebase products + product-name collision): release builds must come from HEAD until then. Snapshot preserved.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state, decisions, build IDs
1. `.asc/backup/spm-migration/` — the user's uncommitted migration snapshot (pbxproj + Package.resolved as found)
1. Agent long-term memory entries (2026-09-03 ×2) — full signing recipe, traps, CI caveat

## Critical Anti-Patterns (do NOT repeat these)
- UNSIGNED-ARCHIVE-SILENT-ENTITLEMENT-LOSS: `CODE_SIGNING_ALLOWED=NO` archive + exportArchive re-sign yields no entitlements, strict-verify still green → verify entitlements per bundle before every publish.
- TRUSTING-TOOL-SUMMARY-OVER-RECEIPT: "added to group" from publish output is not membership proof — confirm via `asc builds list --include betaGroups`.

## Infrastructure State
- App Store Connect: app 6778478266, asc CLI authenticated (keychain `next-labs`)
- Build numbers: next remote-safe = **14** (asc resolves it; don't hand-pick)
- Portal profiles: dual-cert ACTIVE (9F694L8A4A app / HP3SH32B48 watch / MJ9DJJT69T widget); local installs at `~/Library/Developer/Xcode/UserData/Provisioning Profiles` UUIDs 48ca5457 / 3def31a2 / 18db81c0 (9 stale copies deleted 2026-09-03)
- MATCH_GIT_URL / MATCH_PASSWORD exist only as GitHub Actions + Xcode Cloud secrets — fastlane match cannot run locally
- Local artifacts: `.asc/artifacts/` (build-13 IPA + archive), `.asc/profiles/`, `.asc/ExportOptions.appstore.plist`, `.asc/backup/spm-migration/`
- Nested stray project `StressMonitor/StressMonitor.xcodeproj/StressMonitor.xcodeproj/` — old junk, untouched
- No background processes started; nothing running on my behalf

<context>
Release session executed outside any GSD phase: archive → export → entitlements-verify → upload → group attach → test notes → beta review, with two mid-course corrections (signed-archive strategy swap, build 12 pull). Signing state changed materially: profiles are now dual-cert, which is what future local archives rely on. The user's in-flight SPM-proxy migration was never modified — built around it via backup/restore cycles. Everything is committed-or-preserved; nothing dangling except CI validation of the new profiles and the Phase 03 UAT re-test, which build 13 unblocks.
</context>

<next_action>
Start with: `/gsd-resume-work`. If resuming the release lane, nothing is pending — build 13 is live and approved. If resuming code work, complete the SPM-proxy migration (Firebase proxies + non-colliding product names), then smoke-test `asc xcode archive` from the working tree before any release. Phase 03 UAT re-test against build 13 is the next GSD-gated step.
</next_action>
