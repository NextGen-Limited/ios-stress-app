---
phase: 01-binary-manifest-truth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - StressMonitor/StressMonitorWatch Watch App/PrivacyInfo.xcprivacy
  - CLAUDE.md
  - docs-site/legal/privacy.md
  - docs-site/vi/legal/privacy.md
autonomous: true
requirements:
  - BUILD-01
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The watch PrivacyInfo.xcprivacy UserDefaults reasons array reads exactly CA92.1 then 1C8F.1; plutil -lint exits 0 on all three target manifests"
    - "The declared reason set matches a fresh usage scan: every file in the watch target that touches UserDefaults.standard is covered by CA92.1, suite-based access by 1C8F.1, and no undeclared required-reason category (SystemBootTime, DiskSpace, ActiveKeyboard) is used by any target"
    - "Root CLAUDE.md names the real backend contract — StressAPIClient posting to path chat on https://stress-api.dropitx.site with Firebase Auth (anonymous sign-in or GoogleSignIn) — and contains zero references to the Supabase-era LLM service type or a Supabase edge function for chat"
    - "docs-site/legal/privacy.md and docs-site/vi/legal/privacy.md carry the same auth correction in lockstep (EN/VI paragraph-for-paragraph mirrors) and contain zero Supabase-era auth claims"
    - "Zero payload/code churn for D3: the doc task's commits touch no .swift file — StressContextPayload.swift and its tests are byte-identical (docs move toward code, never the reverse)"
    - "BUILD-01/concurrency edge: manifest and doc edits are declarative whole-file states — idempotent, order-free, and re-validation of the same content yields the same verdict"
  artifacts:
    - StressMonitor/StressMonitorWatch Watch App/PrivacyInfo.xcprivacy (CA92.1 added)
    - Corrected CLAUDE.md privacy/architecture prose
    - Corrected docs-site/legal/privacy.md and docs-site/vi/legal/privacy.md (.md sources only)
  key_links:
    - "Watch manifest CA92.1 ↔ the four UserDefaults.standard call sites in the watch target (ViewModels/WatchMoodViewModel.swift, ViewModels/WatchHabitViewModel.swift, Models/TierNamePreferences.swift, Services/CloudKit/WatchCloudKitManager.swift) — the reason code must be backed by real usage, not boilerplate"
    - "CLAUDE.md wording ↔ app PrivacyInfo.xcprivacy declarations (NSPrivacyTracking=false with DeviceID/ProductInteraction entries existing because of the Google/Firebase auth SDKs — 'no third-party analytics or tracking' stays aligned with the manifest)"
  prohibitions:
    - statement: "Docs move toward code, never the reverse — StressContextPayload.swift, its tests, and any payload code must NOT be edited to match stale docs (D3: code is the contract)"
      status: flagged-unverified
      verification: "git diff for the doc task's commits shows no .swift path"
    - statement: "The CA92.1 gap must NOT be closed by removing UserDefaults.standard usage from watch sources instead of declaring it — zero code churn is the locked shape"
      status: flagged-unverified
      verification: "watch Swift sources unchanged in this plan's commits"
    - statement: "Do not edit generated docs-site/.vitepress/dist/* output — only the .md sources (dist regenerates)"
      status: flagged-unverified
      verification: "git status shows no .vitepress/dist paths"
    - statement: "Do not touch the app or widget manifests' collected-data entries — only the watch reasons array changes; the app manifest's HealthAndFitness/PhotoVideo/ProductInteraction/DeviceID/OtherUserContent entries stay as declared"
      status: flagged-unverified
      verification: "git diff limited to the reasons array in the watch manifest; app/widget manifests unmodified"
---

<objective>
Make the privacy declarations true and the docs honest: close the one concrete BUILD-01 manifest gap (watch target missing the CA92.1 UserDefaults reason code) and correct the D3 doc overshoots (root CLAUDE.md and the EN/VI privacy policies describing a Supabase-era architecture the app no longer ships).

Purpose: D3 (CONTEXT decision) resolved that code is the contract — StressContextPayload's derived-score-only behavior is the normative privacy statement, and the documents move to match it with zero payload/code churn. The watch manifest under-declaration is an ITMS-91053-class ASC rejection risk (under-declaration is rejected; over-declaration is tolerated).

Output: watch PrivacyInfo.xcprivacy with CA92.1 + 1C8F.1 reasons, corrected CLAUDE.md prose (real endpoint + Firebase auth, no Supabase-era service references), corrected EN/VI privacy policies.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-binary-manifest-truth/01-CONTEXT.md
@.planning/phases/01-binary-manifest-truth/01-RESEARCH.md
@.planning/phases/01-binary-manifest-truth/01-PATTERNS.md

@StressMonitor/StressMonitor/PrivacyInfo.xcprivacy
@StressMonitor/StressMonitor/Services/LLM/StressContextPayload.swift
@StressMonitor/StressMonitor/Services/API/StressAPIConfig.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CA92.1 to the watch privacy manifest, backed by a fresh usage scan</name>
  <files>StressMonitor/StressMonitorWatch Watch App/PrivacyInfo.xcprivacy</files>
  <read_first>
  - StressMonitor/StressMonitorWatch Watch App/PrivacyInfo.xcprivacy (the edit target — currently declares only 1C8F.1 for UserDefaults; 23 lines total, tab-indented)
  - StressMonitor/StressMonitor/PrivacyInfo.xcprivacy (the analog — its UserDefaults reasons array CA92.1 + 1C8F.1 at lines ~72-91 is the exact shape to reproduce)
  - .planning/phases/01-binary-manifest-truth/01-RESEARCH.md (§5.3 reason-code semantics table and the scan-vs-declaration gap table)
  </read_first>
  <action>
  First re-run the usage scan the research performed, so the declaration is generated from current reality (CONTEXT decision: required-reason declarations are generated from a scan of APIs actually used): grep the watch target directory — which is "StressMonitor/StressMonitorWatch Watch App" and contains a space — for UserDefaults.standard and UserDefaults(suiteName:) call sites, and all three real targets for the other four required-reason categories (SystemBootTime, DiskSpace, ActiveKeyboard, FileTimestamp beyond the already-declared app usage). Expected per research: 4 watch files use UserDefaults.standard (ViewModels/WatchMoodViewModel.swift, ViewModels/WatchHabitViewModel.swift, Models/TierNamePreferences.swift, Services/CloudKit/WatchCloudKitManager.swift); if the scan finds more/fewer files or a new category, adjust the declaration accordingly and record the delta in the SUMMARY.

  Then edit the watch manifest: inside the NSPrivacyAccessedAPICategoryUserDefaults dict, insert a new line with the string CA92.1 immediately before the existing 1C8F.1 string in the NSPrivacyAccessedAPITypeReasons array — matching the app target's ordering (CA92.1 first). Preserve tab indentation, the full plist wrapper, and the empty NSPrivacyCollectedDataTypes array (the watch collects nothing — that stays empty). Make no other change to this file and no change to the app/widget manifests.
  </action>
  <verify>
  <automated>plutil -lint "StressMonitor/StressMonitorWatch Watch App/PrivacyInfo.xcprivacy" && plutil -lint StressMonitor/StressMonitor/PrivacyInfo.xcprivacy && plutil -lint StressMonitor/StressMonitorWidget/PrivacyInfo.xcprivacy && plutil -extract NSPrivacyAccessedAPITypes.0.NSPrivacyAccessedAPITypeReasons raw -o - "StressMonitor/StressMonitorWatch Watch App/PrivacyInfo.xcprivacy" | grep -c CA92.1</automated>
  </verify>
  <acceptance_criteria>
  - plutil -lint prints OK for all three PrivacyInfo.xcprivacy files.
  - plutil -extract of the watch manifest's UserDefaults reasons contains both CA92.1 and 1C8F.1 (grep count ≥ 1 on the extracted value).
  - The scan output listing UserDefaults.standard call sites in the watch target is recorded in the SUMMARY with file names.
  - grep -c "CA92.1" on the watch manifest file returns ≥ 1 while the app and widget manifests are byte-identical to their pre-task state (git diff shows only the watch manifest among manifest files).
  - No watch .swift file is modified by this task.
  </acceptance_criteria>
  <done>The watch manifest declares both UserDefaults reason codes backed by current usage, all three manifests lint clean, and the app/widget manifests are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: D3 doc corrections — CLAUDE.md and EN/VI privacy policies move to the code's truth</name>
  <files>CLAUDE.md, docs-site/legal/privacy.md, docs-site/vi/legal/privacy.md</files>
  <read_first>
  - StressMonitor/StressMonitor/Services/LLM/StressContextPayload.swift (lines ~126-143 — the normative source; DO NOT EDIT: raw readings pinned to nil, derived stressLevel/stressCategory/confidence/stressTrend/factorBreakdown populated)
  - StressMonitor/StressMonitor/Services/API/StressAPIClient.swift (line ~116 — posts to path chat) and StressMonitor/StressMonitor/Services/API/StressAPIConfig.swift (lines ~8-14 — base URL resolution, fallback https://stress-api.dropitx.site)
  - StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift (imports FirebaseAuth/FirebaseCore; anonymous sign-in path) and StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift (the real LLM service that delegates auth)
  - CLAUDE.md (the Privacy & Security section, ~lines 488-494, plus every occurrence of the Supabase-era service type elsewhere in the file)
  - docs-site/legal/privacy.md and docs-site/vi/legal/privacy.md (the AI Coaching Chat sections, ~line 30 each; EN and VI are paragraph-for-paragraph mirrors)
  - .planning/phases/01-binary-manifest-truth/01-RESEARCH.md (§5.6 — the exact overshoot list)
  </read_first>
  <action>
  Correct every Supabase-era claim in the three documents so they match the code (D3: code is the contract — the payload code is NOT touched):

  1. Root CLAUDE.md: in the Privacy & Security section, the sentence describing the AI Coaching Chat transport currently claims chat goes to a Supabase Edge Function — rewrite it to the real shape: derived stress context (score, category, confidence, trend, per-factor scores — never raw HealthKit readings) is sent to the chat endpoint on https://stress-api.dropitx.site by StressAPIClient under a Bearer-authenticated session. Purge every remaining reference in the file to the Supabase-era LLM service type (the type no longer exists; the service is StressLLMService which delegates auth to StressAPIClient/FirebaseAuthService). Keep the "no third-party analytics or tracking" line but ensure its wording stays consistent with the app manifest (tracking false; DeviceID/ProductInteraction entries exist because of the Google/Firebase auth SDKs).
  2. docs-site/legal/privacy.md (EN), AI Coaching Chat section ~line 30: the parenthetical currently claims the Bearer session is established via Supabase Auth — correct to Firebase Auth (anonymous sign-in via Auth.auth().signInAnonymously(), or signed-in via Google Sign-In).
  3. docs-site/vi/legal/privacy.md (VI), same section: apply the same correction in Vietnamese so the two files remain paragraph-for-paragraph mirrors. The VI sentence to fix is the one naming the same Supabase auth provider; replace with the Firebase Auth equivalent in Vietnamese.
  4. Edit .md sources only — never anything under docs-site/.vitepress/ (generated output; assumption A5).

  After editing, run the zero-code-churn check: this task's diff contains no .swift path. If a discovered doc claim contradicts StressContextPayload in some NEW way not in the §5.6 list, fix the doc to match the code and note it in the SUMMARY — the resolution direction is always docs-to-code.
  </action>
  <verify>
  <automated>grep -c "SupabaseLLMService" CLAUDE.md; grep -in "supabase" docs-site/legal/privacy.md docs-site/vi/legal/privacy.md | wc -l; git diff --name-only HEAD | grep -c "\.swift$" || true</automated>
  </verify>
  <acceptance_criteria>
  - grep -c "SupabaseLLMService" CLAUDE.md returns 0.
  - grep -in for the supabase provider name across docs-site/legal/privacy.md and docs-site/vi/legal/privacy.md returns 0 lines.
  - CLAUDE.md contains the real endpoint (https://stress-api.dropitx.site) and names Firebase Auth for the chat session.
  - EN and VI privacy files remain structural mirrors (both edited in the same section).
  - git diff for this task's changes shows zero .swift files (zero code churn — the D3 invariant).
  - No file under docs-site/.vitepress/ is modified.
  </acceptance_criteria>
  <done>All three documents describe the actual architecture (StressAPIClient → stress-api.dropitx.site, Firebase Auth, derived-scores-only payload) with zero payload-code churn, and the corrections are mirrored across EN/VI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared manifest ↔ Apple's ASC validation | under-declaration of required-reason APIs is an ITMS-91053 rejection at upload; the watch target was under-declared |
| public privacy prose ↔ actual payload behavior | the shipped policies made claims (Supabase Auth, Supabase edge function) the binary contradicts — a repudiation/compliance exposure and an App Review honesty risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Repudiation (compliance) | Watch privacy manifest under-declaration (missing CA92.1) | high | mitigate | Scan-then-declare task: fresh grep of UserDefaults usage regenerates the reason set; plutil lint; over-declaration tolerated by Apple, under-declaration is not |
| T-02-02 | Tampering | Doc edits drifting from code truth (docs written from memory, not sources) | medium | mitigate | read_first pins the normative Swift sources; zero-code-churn check proves direction was docs-to-code; SUMMARY records each overshoot fixed |
| T-02-03 | Information Disclosure (false assurance) | Privacy prose overstating privacy (claiming less collection than actual) | medium | mitigate | Corrections align prose with StressContextPayload's real derived-score payload and the manifest's collected-data entries; no wording weakens what the manifest declares |
</threat_model>

<verification>
- plutil -lint OK on all three manifests; watch reasons array contains CA92.1 + 1C8F.1 (Task 1).
- Zero Supabase-era references remain in CLAUDE.md and both privacy policies; real endpoint + Firebase Auth named (Task 2).
- Zero .swift churn across the plan (D3 invariant).
- Phase-level: the archive from plan 04 re-runs the SDK-manifest checks; ASC-upload acceptance lands in plan 05.
</verification>

<success_criteria>
- BUILD-01's manifest-content half is true: every required-reason API the targets actually use is declared, with the watch gap closed.
- D3's doc corrections are complete: every overshoot in the §5.6 list is fixed, EN/VI in lockstep, code untouched.
</success_criteria>

## Artifacts this phase/plan produces

| Artifact | Kind | Notes |
|----------|------|-------|
| Watch PrivacyInfo.xcprivacy reasons array [CA92.1, 1C8F.1] | privacy manifest edit | one inserted string; scan-backed |
| CLAUDE.md corrected privacy/architecture prose | docs | real endpoint, Firebase Auth, no Supabase-era service type |
| docs-site/legal/privacy.md + vi/legal/privacy.md corrected auth wording | docs (public policy) | EN/VI mirrors; .md sources only |

<output>
Create `.planning/phases/01-binary-manifest-truth/01-02-SUMMARY.md` when done
</output>
