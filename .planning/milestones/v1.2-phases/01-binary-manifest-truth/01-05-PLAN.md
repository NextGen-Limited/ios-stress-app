---
phase: 01-binary-manifest-truth
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - .planning/phases/01-binary-manifest-truth/01-ENV-05-CI-RECORD.md
autonomous: false
requirements:
  - ENV-05
  - BUILD-01
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Branch gsd/v1.2-submission-readiness is pushed to origin carrying all Phase-1 commits (plans 01-04), and a DRAFT pull request to main is open and unmerged — main is untouched"
    - "The CI workflow (ci.yml → _test.yml) runs and completes with conclusion success on the PR from the milestone branch — SwiftLint + iOS/watchOS/widget builds + unit tests green on a clean CI machine, proving the proxy migration resolves without the local clone cache"
    - "A deploy.yml workflow_dispatch run on gsd/v1.2-submission-readiness executes upload_beta, whose match(type: appstore, readonly: true) step installs the three profiles (match AppStore stress.ai.com, .watchkitapp, .widget) WITHOUT regeneration — observed in the run logs"
    - "ENV-05/idempotency edge: match readonly is repeatable with identical effect (read-only install from the match repo); the setup_match fallback is one-shot and user-gated — it is never re-run as a routine fix because it regenerates single-cert profiles and breaks the local dual-cert swap"
    - "ENV-05/concurrency edge: concurrent deploy dispatches on the same ref are cancelled by the workflow's concurrency group (deploy-<ref> with cancel-in-progress: true) — no two match runs race on one branch"
    - statement: "The pilot upload from the dispatched run clears App Store Connect processing with no ITMS-91053 (missing required-reason declaration) and no missing-SDK-manifest error — the BUILD-01 SC-1 upload-validation gate, with build 13 as the standing prior"
      verification: backstop
    - statement: "If match readonly rejects because the recreated dual-cert profiles are not mirrored in the match git repo, the documented fallback (one setup_match regeneration via the match.yml workflow's manual job, then re-swap dual-cert locally per the stored recipe) is executed only with explicit user awareness — the outcome either way is recorded"
      verification: backstop
  artifacts:
    - .planning/phases/01-binary-manifest-truth/01-ENV-05-CI-RECORD.md (run URLs, conclusions, match log excerpt, disposition)
    - The draft PR (branch gsd/v1.2-submission-readiness → main, never merged in this phase)
  key_links:
    - "push → PR → ci.yml/_test.yml (build+test surface) — proves ENV-04's migration on clean CI hardware"
    - "workflow_dispatch deploy.yml on the branch → fastlane upload_beta → match(readonly) → gym signed Release archive → pilot → ASC processing — the ENV-05 assertion and the BUILD-01 ASC-validation gate ride one deliberate run"
    - "match.yml workflow_dispatch regenerate job (setup_match) — the documented fallback surface, user-gated, never routine"
  prohibitions:
    - statement: "No TestFlight build may be created without explicit user approval — the deploy dispatch is gated by a blocking human checkpoint because upload_beta increments the build number and is visible to external beta testers (RESEARCH Open Question 2)"
      status: flagged-unverified
      verification: "Task 2 is a checkpoint:human-verify task; dispatch command runs only after user approval"
    - statement: "setup_match regeneration must not run without user awareness — it regenerates single-cert App Store profiles, which breaks the dual-cert local swap documented in the release recipe"
      status: flagged-unverified
      verification: "fallback branch of Task 2 requires a second explicit approval before dispatching match.yml"
    - statement: "main must stay untouched — the PR remains a draft; no merge, no push to main in this phase (CONTEXT ENV-05 decision: branch push only)"
      status: flagged-unverified
      verification: "git log origin/main unchanged; gh pr view shows isDraft: true at phase end"
    - statement: "No CI workflow file is edited to widen triggers this phase — the existing trigger matrix (ci.yml on PR/dispatch, deploy.yml on workflow_run main/release + dispatch) is used as-is"
      status: flagged-unverified
      verification: "git diff shows zero changes under .github/workflows/"
---

<objective>
Run the two concrete CI surfaces that validate what local runs cannot: (1) a draft PR that fires ci.yml and proves the SPM proxy migration builds/tests on clean CI hardware, and (2) a user-approved workflow_dispatch of deploy.yml on the milestone branch whose upload_beta lane exercises fastlane match readonly against the dual-cert App Store profiles (ENV-05) and whose pilot upload clears ASC privacy-manifest processing (BUILD-01 SC-1).

Purpose: a bare branch push fires NO workflow (ci.yml is PR-only plus dispatch; deploy.yml triggers on workflow_run from main/release plus dispatch) — the ENV-05 check needs an explicit surface. deploy.yml's upload_beta is the only workflow that calls match(readonly: true); using it deliberately also produces the signed archive + ASC upload that validates the phase's manifest work end-to-end. Both surfaces keep main untouched.

Output: pushed branch, draft PR with a green CI run, a user-approved deploy run with match readonly green (or the documented fallback executed with awareness), and 01-ENV-05-CI-RECORD.md recording every verdict.
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-binary-manifest-truth/01-CONTEXT.md
@.planning/phases/01-binary-manifest-truth/01-RESEARCH.md
@.planning/phases/01-binary-manifest-truth/01-04-SUMMARY.md

@.github/workflows/ci.yml
@.github/workflows/deploy.yml
@fastlane/Fastfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push the milestone branch, open the draft PR, and observe the ci.yml run to green</name>
  <files>.planning/phases/01-binary-manifest-truth/01-ENV-05-CI-RECORD.md</files>
  <read_first>
  - .github/workflows/ci.yml (triggers: pull_request to main/develop + workflow_dispatch; calls _test.yml)
  - .github/workflows/_test.yml (the actual jobs: SwiftLint + iOS/watchOS/widget builds + unit tests — confirm it performs no signing and no fastlane)
  - .planning/phases/01-binary-manifest-truth/01-01-SUMMARY.md (the proxy migration that this CI run exercises on clean hardware)
  </read_first>
  <precondition>gh CLI is installed and authenticated with repo + actions read access (if not, an auth-gate checkpoint is created dynamically per GSD auth-gate rules); origin is reachable; all plans 01-04 are complete and their commits are on the local branch.</precondition>
  <action>
  1. Assert readiness: git status shows a committed tree (no unstaged changes to tracked files beyond known untracked junk), current branch is gsd/v1.2-submission-readiness, and git log contains the plan 01-04 commit series.
  2. Push: git push -u origin gsd/v1.2-submission-readiness.
  3. Open the draft PR: gh pr create --draft --base main --head gsd/v1.2-submission-readiness with a title like "v1.2 Phase 1: Binary & Manifest Truth" and a body summarizing the phase (privacy manifest truth, plist consolidation, SPM proxy completion, credential-scan gate, widget evidence). The PR fires ci.yml — this is the concrete trigger the bare push could never provide.
  4. Observe: gh run list --workflow=ci.yml --branch gsd/v1.2-submission-readiness then gh run watch on the latest run — the run pulls SPM dependencies fresh on the CI machine (one github fetch of firebase-ios-sdk expected, since CI's SPM cache keys on Package.resolved which just changed) and must reach success. This run is the CI-side proof of ENV-04: the proxy migration resolves on hardware that has never seen the local clone cache.
  5. Create 01-ENV-05-CI-RECORD.md recording: push SHA, PR number + URL, PR isDraft=true, ci.yml run ID + URL + conclusion. If the CI run fails, diagnose from the logs (a proxy-resolution failure here means the local resolve in plan 01 diverged from clean-machine behavior — fix within the locked direction, never by reverting to remote refs), push a fix commit, and record the iteration.
  </action>
  <verify>
  <automated>gh pr view gsd/v1.2-submission-readiness --json number,isDraft,state,url; gh run list --workflow=CI --branch gsd/v1.2-submission-readiness --limit 1 --json databaseId,conclusion,headSha</automated>
  </verify>
  <acceptance_criteria>
  - gh pr view shows state OPEN and isDraft true for the milestone branch PR (main untouched — no merge).
  - gh run list for the CI workflow on the branch shows the latest run conclusion success at the pushed head SHA.
  - 01-ENV-05-CI-RECORD.md exists with push SHA, PR number/URL, run ID/URL, and conclusion sections.
  - git log origin/main is unchanged by this task (no main push).
  </acceptance_criteria>
  <done>The branch is on origin, the draft PR fires the CI workflow, and the clean-machine run is green — ENV-04 proven on CI hardware with the evidence recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: User-approved deploy.yml dispatch — match readonly verdict (ENV-05) and ASC processing verdict (BUILD-01 SC-1)</name>
  <files>.planning/phases/01-binary-manifest-truth/01-ENV-05-CI-RECORD.md</files>
  <read_first>
  - .github/workflows/deploy.yml (workflow_dispatch trigger; runs bundle exec fastlane upload_beta; concurrency group deploy-<ref> cancel-in-progress; uploads gym logs + fastlane/report.xml as artifacts)
  - fastlane/Fastfile (upload_beta lane: match(type: "appstore", readonly: true, api_key:) then gym then pilot; profile names match AppStore stress.ai.com, .watchkitapp, .widget)
  - .github/workflows/match.yml (the manual regenerate job running setup_match — the documented fallback surface)
  - .planning/phases/01-binary-manifest-truth/01-RESEARCH.md (§9 — dual-cert story, the expected-failure mode, and the fallback recipe; Open Questions 2 and 3)
  </read_first>
  <action>
  BLOCKING CHECKPOINT FIRST — present to the user before touching CI:
  - What will happen: gh workflow run deploy.yml --ref gsd/v1.2-submission-readiness runs upload_beta on the branch — fastlane match readonly, a signed Release gym archive, and a pilot upload that creates a NEW TestFlight build (build number increments; visible to external beta testers per the existing tester groups).
  - Why: it is the only CI surface that exercises match(readonly: true) (ENV-05) and the only one whose ASC upload validates the phase's privacy manifests end-to-end (BUILD-01 SC-1) — build 13 remains the standing prior if the user declines.
  - The choice: approve the dispatch now, or defer ENV-05/BUILD-01-SC-1 to the phase-4 submission flow.
  Await explicit approval. Do not proceed on silence.

  AFTER APPROVAL:
  1. Dispatch: gh workflow run deploy.yml --ref gsd/v1.2-submission-readiness; then gh run watch the resulting Deploy to TestFlight run (60-minute timeout on CI side).
  2. ENV-05 verdict from the logs: the match step must show the three profiles (match AppStore stress.ai.com, .watchkitapp, .widget) installed with readonly semantics and NO profile regeneration. Record the match log excerpt in 01-ENV-05-CI-RECORD.md.
  3. FAILURE BRANCH (expected possibility per §9: the portal profiles were recreated dual-cert and XPT2DHR688 may not be mirrored in the match git repo): if match readonly fails, STOP — do not auto-run setup_match. Present the documented fallback to the user (dispatch match.yml's regenerate job → setup_match regenerates single-cert profiles → then re-swap dual-cert locally per the stored release recipe) and execute only on a second explicit approval. Record whichever path was taken.
  4. BUILD-01 SC-1 verdict: after pilot uploads, confirm the build appears and clears processing — the run logs (pilot output) plus the uploaded build-logs artifact show no ITMS-91053 and no missing-SDK-manifest error. Record the verdict; if any ITMS-91053-class error appears, that is a phase-gap finding to bring back to the orchestrator (the manifest scan in plan 02 missed a usage).
  5. Finalize 01-ENV-05-CI-RECORD.md: dispatch approval note, run URL, match verdict (or fallback disposition), ASC processing verdict, and the PR-still-draft confirmation (main untouched).
  </action>
  <verify>
  <automated>gh run list --workflow="Deploy to TestFlight" --branch gsd/v1.2-submission-readiness --limit 1 --json databaseId,conclusion,status; gh pr view gsd/v1.2-submission-readiness --json isDraft</automated>
  </verify>
  <acceptance_criteria>
  - The record file shows an explicit user-approval note BEFORE the dispatch timestamp.
  - gh run list for the Deploy workflow on the branch shows a completed run; its conclusion and the match-step outcome are recorded in 01-ENV-05-CI-RECORD.md.
  - Match readonly verdict is one of: GREEN (profiles installed, no regeneration — ENV-05 satisfied), or FALLBACK-EXECUTED (setup_match ran only after a second explicit approval, with the re-swap recipe referenced), or DECLINED/DEFERRED (user chose to defer — recorded as an open item for the phase wrap-up, not silently dropped).
  - The ASC processing verdict (no ITMS-91053 / no SDK-manifest error) is recorded, or the error is escalated in the SUMMARY.
  - gh pr view still reports isDraft true; origin/main untouched.
  - No file under .github/workflows/ was modified.
  </acceptance_criteria>
  <done>ENV-05 has a real CI verdict (readonly green, or the documented fallback executed with user awareness, or an explicit deferral) and the phase's privacy-manifest work has its ASC-upload validation — both recorded, main untouched.</done>
  <resume-signal>User replies to the checkpoint question with an explicit decision: approve the deploy dispatch (e.g., "approved — dispatch it"), decline/defer it (e.g., "defer ENV-05 to phase 4"), or — on the failure branch — approve/refuse the setup_match fallback. Silence is not a signal; the task does not advance without one of these.</resume-signal>
  <what-built>Prior to the checkpoint: the milestone branch pushed to origin, the draft PR open with a green ci.yml run, and 01-ENV-05-CI-RECORD.md seeded with that evidence (Task 1 complete). At the checkpoint: the approval question with the exact consequences (upload_beta creates a new TestFlight build visible to external testers). After the signal: either the dispatched deploy run's match/ASC verdicts appended to the record, or the recorded deferral.</what-built>
  <how-to-verify>gh pr view gsd/v1.2-submission-readiness --json isDraft,state shows OPEN + draft; gh run list --workflow=CI --branch gsd/v1.2-submission-readiness shows conclusion success at the head SHA; the user's decision message is present in the session before any gh workflow run command; after execution, 01-ENV-05-CI-RECORD.md contains the approval note, run URL, and match/ASC verdicts (or the deferral entry).</how-to-verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → origin/CI | the push + PR + dispatch hand the branch to CI runners holding ASC + match secrets |
| CI secrets → logs/artifacts | deploy.yml uploads gym logs and fastlane/report.xml as artifacts; secret material must not leak into them |
| match git repo ↔ Apple portal profiles | the dual-cert portal state may not be mirrored in the match repo — the exact condition ENV-05 tests |
| user approval → TestFlight visibility | upload_beta creates an externally visible build — a one-way-ish action gated by the blocking checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | Match repo cert handling — setup_match regeneration silently swapping signing identities | high | mitigate | readonly is the only routine path; setup_match exists solely behind the user-approved fallback branch; both approvals and outcomes recorded in 01-ENV-05-CI-RECORD.md |
| T-05-02 | Information Disclosure | CI secrets leaking into uploaded logs / fastlane/report.xml | medium | mitigate | report.xml spot-check already done in plan 04 task 2; deploy artifacts reviewed for token material before recording the verdict |
| T-05-03 | Spoofing | False ENV-05 pass — a run that never exercised match being recorded as validation | medium | mitigate | Verdict requires the actual run URL + match-step log excerpt in the record file; the bare-push-triggers-nothing trap is documented in the plan itself (Pitfall 6) |
| T-05-04 | Elevation (process) | TestFlight build created without consent | high | mitigate | Blocking checkpoint:human-verify task precedes the dispatch; approval note required in the record before the dispatch timestamp |
</threat_model>

<verification>
- Task 1: PR open + draft; CI run success at head SHA; record file populated.
- Task 2: deploy run observed; match verdict + ASC verdict recorded; main untouched; workflows unmodified.
- Phase-level: this plan's records complete the evidence chain for ENV-05 and BUILD-01's upload-validation half (manifest content was plan 02's; SDK-bundle flow was plan 01/04's).
</verification>

<success_criteria>
- ENV-05 carries a genuine CI verdict with evidence, or an explicit user-made deferral — never an assumed pass.
- The BUILD-01 ASC-upload gate is exercised on the phase-final code (or deliberately deferred with build 13 as prior).
- main remains untouched throughout; the PR stays draft.
</success_criteria>

## Artifacts this phase/plan produces

| Artifact | Kind | Notes |
|----------|------|-------|
| .planning/phases/01-binary-manifest-truth/01-ENV-05-CI-RECORD.md | evidence record | push/PR/CI + dispatch approval + match verdict + ASC verdict |
| Draft PR gsd/v1.2-submission-readiness → main | CI surface | stays draft through the phase; fires ci.yml |
| CI (deploy.yml) run on the branch | pipeline run | match readonly + gym signed archive + pilot upload — produces a TestFlight build only with user approval |
| No repo config changes | — | workflows, Fastfile, Matchfile are observe-only this phase |

<output>
Create `.planning/phases/01-binary-manifest-truth/01-05-SUMMARY.md` when done
</output>
