---
phase: 02-delete-correctness-test-suite-trust
threats_open: 0
threats_closed: 12
threats_total: 13
status: open_threats
asvs_level: 1  # not declared in any plan's <config> block — no <config> block found phase-wide; L1 (grep-level presence) applied as the conservative default
block_on: high  # not declared — default per auditor contract
audited: 2026-09-04
auditor: ExcessivePorpoise (gsd-secure-phase)
---

# Phase 02 — Delete Correctness & Test-Suite Trust — Security Audit

**Verdict: OPEN_THREATS (non-blocking).** 12/13 declared threats verified CLOSED against implemented code, tests, and artifacts (not SUMMARY narrative). One low-severity threat (T-02-12) is only partially mitigated — the evidence note's account-identity field is redacted but its screenshot-capture instructions carry no equivalent guidance, and the note's own internal cross-reference to a "§5 privacy note" points at a section that does not exist (§5 is "Machine-verified checks"). Severity is `low`, below the `block_on: high` threshold, so it does **not** count toward `threats_open` and does not block phase close. All `mitigate`-disposition threats were checked by grep/read against the actual file:line cited in each plan's Mitigation Plan column, not against the plans' or SUMMARYs' claims. No `accept`/`transfer`-disposition threats exist in this register except T-02-04, which is `accept (existing)` and was verified against the cited pre-existing code (`checkVerified` at StoreKitService.swift:476-482).

## Threat Verification

| Threat ID | Category | Severity | Disposition | Mitigation Evidence | Verdict |
|---|---|---|---|---|---|
| T-02-01 | Information Disclosure — data survives a claimed delete | high | mitigate | `StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift` — ungated `@Suite "CloudKit Delete Truthiness"` (5 tests: genuine-failure, lying-canary via query-based `remainingRecords`, honest-drain, boundary, idempotency); zero `.enabled(if:`/`.disabled(` constructs in the file (grep confirmed); registered in `project.pbxproj` (8 string hits = PBXBuildFile+PBXFileReference for both new test files); 02-01-SUMMARY.md's mutation red/green proof (exit 65 → exit 0) is consistent with the suite's assertions actually exercising `DataDeleterService.swift:97-100` | **CLOSED** |
| T-02-02 | Tampering — cross-suite static fakes forging a green | low | mitigate | `SeededCloudKitResetService` in the same file: constructor-injected only (`init(behavior:seededRows:)`), no `static var`/`RequestCaptureURLProtocol` (grep for both patterns in the file = 0 matches) | **CLOSED** |
| T-02-03 | Repudiation/DoS — finishing an unverified consumable destroys retry proof | high | mitigate | `StoreKitService.swift:352-356` `handleUnverifiedTransaction(_:)` — body contains only a `Logger.warning` call, zero `transaction.finish()`; pinned by `CreditPurchaseFlowTests.swift:257-276` (`unverifiedDeliveryNeverFinishesTransaction`, `redeliveredUnverifiedStillFinishedZeroTimes`, both asserting `fake.finishCallCount == 0`) | **CLOSED** |
| T-02-04 | Tampering — forged JWS forcing a grant | high | accept (existing) | `StoreKitService.swift:476-482` `checkVerified<T>` — `.unverified` case `throw StoreKitError.receiptValidationFailed`, unchanged by this phase; T-02-03's fix removes only the evidence-destroying finish, not this check | **CLOSED** |
| T-02-05 | Tampering (process) — DEBUG money path silently mocked | high | mitigate | `StressMonitorApp.swift:270-271` (factory site) and `StoreKitServiceEnvironment.swift:18-19` (environment site) both gate `MockStoreKitService(` construction behind `MockIAPMode.isEnabled(...)`; pinned by `StoreKitServiceWiringTests.swift` (`factoryResolvesRealServiceAbsentOptIn`, `factoryResolvesMockWithOptIn`, `environmentDefaultResolvesRealService`) | **CLOSED** |
| T-02-06 | DoS — environment-default fallback (PaywallView) stays mocked | medium | mitigate | `StoreKitServiceEnvironment.swift:18-19` (site B) flipped in the same commit (`43a3a13`) as site A per 02-03-SUMMARY.md; `environmentDefaultResolvesRealService` test asserts `EnvironmentValues().storeKitService is StoreKitService` | **CLOSED** |
| T-02-07 | Tampering (process) — premature un-gate destabilizes CI | medium | mitigate | Git history confirms ordering: `cd5fbf1` (green proof, both simulator rounds) committed 2026-09-03T23:51:56+07, `1ab4d01` (gate removal, "Checkpoint (Task 2, blocking-human decision): user verdict fix-landed... on Task 1's evidence (cd5fbf1)") committed 2026-09-04T13:08:31+07 — the removal commit itself cites and postdates the green-proof commit; `.enabled(if:` grep on `DataDeletionConsolidationTests.swift` = 0 hits (gates actually gone, not just narrated) | **CLOSED** |
| T-02-08 | Information Disclosure (process) — vague dispositions hide coverage loss | medium | mitigate | `StoreKitServiceTests.swift:6-25` and `EntitlementForegroundCorrectionTests.swift:6-26` file headers each carry: exact failure signature (exit 65, N issues, `productNotFound` at named call sites), ruled-out causes (this-session isolation-matrix + prior bisection list), residual risk statement, and date (2026-09-04) — meets the plan's declared disposition bar on both files | **CLOSED** |
| T-02-09 | Tampering (process) — doc/CI invocation drift | medium | mitigate | `AGENTS.md:28-38` vs `.github/workflows/_test.yml:184-195` — read side-by-side: project/scheme/destination/`-derivedDataPath build`/`-resultBundlePath TestResults.xcresult`/`-skipPackagePluginValidation`/`-parallel-testing-enabled NO`/`-maximum-concurrent-test-simulator-destinations 1`/signing pair are identical, flag-for-flag; `docs/TESTING.md` contains exactly 1 `xcodebuild test` mention (prose pointer) and 5 `AGENTS.md` cross-references (grep confirmed) | **CLOSED** |
| T-02-10 | Information Disclosure (process) — dead INFOPLIST_KEY settings imply phantom plist keys | low | mitigate | `AGENTS.md:51` doc-truth paragraph: "custom `INFOPLIST_KEY_*` build settings do not merge into product plists... confirm the key in a merged/built plist, not just the pbxproj," citing the Phase-1 `UIBackgroundModes` finding | **CLOSED** |
| T-02-11 | Information Disclosure — Habit records survive a claimed all-data delete | **high** | mitigate | `DataDeleterService.swift:432` — `try modelContext.delete(model: Habit.self)` added to `performFactoryReset`'s local sweep, directly alongside the pre-existing `CharacterUnlock` deletion at `:431`; pinned by `FactoryResetSweepCompletenessTests` (`DataDeleterCloudKitTruthinessTests.swift:266-351`) asserting `ctx.fetch(FetchDescriptor<Habit>()).isEmpty` post-reset, with a companion idempotency test | **CLOSED** |
| T-02-12 | Information Disclosure — evidence note leaks account identifiers/PII | low | mitigate | **Partial.** `02-DATA-01-EVIDENCE.md:42` §2 does redact the account-context field ("do not record the account email/username — see §5 privacy note"). But that citation is broken: §5 is titled "Machine-verified checks," not a privacy note — no such section exists in the file (confirmed: `## ` header scan lists §1 Disclosure, §2 Environment, §3 Procedure, §4 Screenshots table, §5 Machine-verified checks, §6 Pending human items, §7 Material discoveries). Neither §3's screenshot procedure (steps 1, 7) nor §4's screenshot table instructs the human executor to avoid capturing account-identifying UI (Apple ID email in Settings, developer-account name in the CloudKit Console header) inside the *images themselves* — only the *written* environment field is redacted. The plan's declared mitigation ("screenshots show record-type queries, not account pages") is not present anywhere in the shipped note. | **OPEN — non-blocking** (severity `low` < `block_on: high`) |
| T-02-13 | Tampering (process) — trust gate passed on counts while a suite silently skips | medium | mitigate | `02-TRUST-GATE-RECORD.md` §2 is a 43-row suite-by-name enumeration (not a count), sourced from `xcresulttool get test-results tests`; §2's per-test skip table cross-checks 11 skips against the exact 2 dispositioned suites (1 + 10 = 11, "no residual"); §3 re-runs the disable/gate grep and maps all 4 constructs 1:1 to 02-04's disposition set, explicitly checking for 0 new | **CLOSED** |

## Open — non-blocking

| Threat ID | Category | Severity | Mitigation Expected (per plan) | Gap Found | Files |
|---|---|---|---|---|---|
| T-02-12 | Information Disclosure | low | "Note redacts account emails/user names; screenshots show record-type queries, not account pages" (02-06-PLAN.md threat_model) | Field-level redaction present (§2 row); no screenshot-content redaction instruction anywhere in §3/§4/§6; the note's own "see §5 privacy note" pointer is dangling (no such section exists) | `.planning/phases/02-delete-correctness-test-suite-trust/02-DATA-01-EVIDENCE.md` |

**Why non-blocking:** T-02-12 is declared `low` severity; `block_on` defaults to `high` (no `<config>` block was found anywhere in this phase's plans or `.planning/` root to override it). A low-severity open item does not gate phase close per the auditor contract, and does not count toward `threats_open`.

**Recommended follow-up (not a phase blocker):** before the human executor runs the DATA-01 live two-surface session, add one line to §3 ("do not include the Apple ID email, developer-account name, or any account-identifying chrome in the screenshot — crop to the record-type query result") and fix the §2 cross-reference to point at that instruction (or remove the dangling "§5 privacy note" reference entirely, since §5 is not a privacy section).

## Unregistered Flags

None found. SUMMARY.md files across all 6 plans (02-01 through 02-06) surface no `## Threat Flags` section reporting new attack surface outside the 13 registered threats; the two disclosed-but-unfixed gaps (`deleteAllMeasurements` scoped-path UI-copy overstatement in 02-DATA-01-EVIDENCE.md §7, and the `-mock-iap`/`-demo-mode` combined launch-arg note in 02-03-SUMMARY.md) are both explicitly out-of-scope disclosures already cross-referenced to existing threats/requirements (DATA-01 disclosure, BUILD-04 doc note respectively), not novel unmapped surface.

**threats_open: 0**
