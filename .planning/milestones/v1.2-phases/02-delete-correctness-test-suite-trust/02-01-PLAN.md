---
phase: 02-delete-correctness-test-suite-trust
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
autonomous: true
requirements: [DATA-04]
gap_closure: false
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A CI-visible regression suite fails when DataDeleterService stops propagating CloudKit reset failures — proven by a mutation run (suite red while the orchestrator swallows the failure), then green after revert"
    - "The suite carries three prongs: genuine-failure (throwing double makes deleteAllMeasurements throw DeletionError.cloudKitError), truthiness canary (a lying double returns success while survivors are observable only by querying remainingRecords — never by trusting the success return), and honest-drain (the double's seeded store empties when the pipeline really drives the reset)"
    - "The new suite carries no GSD_CI gate — it runs in the default CI run by construction"
    - "The suite distinguishes zero survivors from exactly one survivor (remainingRecords of 0 vs 1)"
    - "remainingRecords is an exact integer count of surviving seeded rows — no rounding, sampling, or approximation"
    - "A second deleteAllMeasurements run after a successful first run completes without throwing (empty-store re-run is safe)"
    - "The spy is constructor-injected only (no cross-suite statics — WINDOWS #12) and runs deterministically under @MainActor isolation"
  prohibitions:
    - "no store left populated after a claimed-successful delete without the propagation check observing it (flagged-unverified — canary prong enforces the query-based contract here; DATA-01 evidence backstops the live path at phase end)"
    - "no RequestCaptureURLProtocol statics in the new suite (WINDOWS #12 order-dependent pollution)"
    - "no return-context-only SwiftData fixtures — every fixture returns (ModelContainer, ModelContext) with the container held for the whole test body"
  artifacts:
    - StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift (registered in the StressMonitorTests target via project.pbxproj)
  key_links:
    - "New suite -> DataDeleterService.init(modelContext:cloudKitResetService:repository:serverSessionWiper:logger:) -> CloudKitResetServiceProtocol (the DATA-04 DI seam, already shipped)"
---

<objective>
Build the phase's tracer slice: an automated, CI-visible regression suite that makes "CloudKit batch delete reports success while records survive" (the v1.0 CR-01 shape) detectable, through the existing CloudKitResetServiceProtocol DI seam.

Purpose: ROADMAP SC-2 — a regression test must fail if the CR-01 bug returns, and the seam under CloudKitResetServiceProtocol must make that failure injectable. Today the only failure-propagation test (DataDeletionConsolidationTests.swift:252-278) sits inside a GSD_CI-gated suite, so CI currently proves nothing about CR-01.

Output: One new ungated Swift Testing suite + a deletion-aware lying spy, registered in the test target, with a mutation-proven red/green gate (the tracer's end-to-end proof that the suite catches a lying delete).
</objective>

<execution_context>
@/Users/ddphuong/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-CONTEXT.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-RESEARCH.md
@.planning/phases/02-delete-correctness-test-suite-trust/02-PATTERNS.md
</context>

<feature>
  <name>CloudKit delete-truthiness regression suite (DATA-04, per CONTEXT "Regression Seam & Documented Invocation")</name>
  <files>StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj</files>
  <behavior>
    Lying spy conforms to CloudKitResetServiceProtocol (protocol verbatim at CloudKitResetService.swift:9-15 — deleteRecords(ofType:expectedProgress:), deleteRecords(ofType:in:), deleteRecords(ofType:before:), deleteAllRecords(confirmation:includeBaseline:), performDatabaseReset(confirmation:)); every method returns normally WITHOUT mutating a seeded in-memory store; exposes remainingRecords (exact Int).
    - Prong 1 (genuine failure): throwing double -> try await service.deleteAllMeasurements() throws DeletionError.cloudKitError with the expected underlying error (do/catch + Issue.record fallback style, PATTERNS "do/catch error pinning").
    - Prong 2 (truthiness canary): lying double -> deleteAllMeasurements returns success AND the query-based check observes survivors (remainingRecords > 0) — the suite documents that emptiness is only ever established by querying, never by trusting the success return.
    - Prong 3 (honest drain): a draining double -> after a successful run remainingRecords == 0 (the orchestrator really drove the reset through the seam).
    - Boundary: zero-survivor vs exactly-one-survivor cases are both asserted and distinguishable.
    - Idempotency: a second deleteAllMeasurements after a successful first run does not throw.
  </behavior>
  <implementation>Model the spy on FakeServerSessionWiper.store/.remainingSessions (DataDeleterServerWipeTests.swift:20-65) and extend/mirror FakeCloudKitResetService (DataDeletionConsolidationTests.swift:184-228). Inject via DataDeleterService.init (DataDeleterService.swift:45-61) using the makeService helper shape (DataDeleterServerWipeTests.swift:123-135). Tuple fixture per DataDeleterServerWipeTests.swift:106-121 with the _ = container keep-alive idiom. Production code is NOT modified by this plan (behavior is already correct post-CR-01-fix 1761b70); the red side of the gate is the Task 2 mutation run.</implementation>
</feature>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create the ungated truthiness spy suite and prove it green</name>
  <files>StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj</files>
  <read_first>StressMonitor/StressMonitorTests/DataDeleterServerWipeTests.swift (whole file — tuple fixture :106-121, deletion-aware fake :9-66, makeService :123-135, keep-alive :172), StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift (:184-228 FakeCloudKitResetService behaviors, :236-241 the GSD_CI gate to AVOID copying, :252-278 the CI-invisible failure test to mirror), StressMonitor/StressMonitor/Services/DataManagement/CloudKitResetService.swift (:9-15 protocol, :55-100 deleteAllRecords), StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift (:45-61 DI init, :97-111 deleteAllMeasurements, :117-129 error adaptation), 02-PATTERNS.md "Pattern 1" section</read_first>
  <behavior>
    - Test genuine-failure prong: throwing double -> deleteAllMeasurements throws DeletionError.cloudKitError (underlying error preserved)
    - Test truthiness canary: lying double -> success return + remainingRecords > 0 observed by querying
    - Test honest drain: draining double -> success + remainingRecords == 0
    - Test boundary: one seeded survivor vs zero survivors distinguishable (exact integer count)
    - Test idempotency: second deleteAllMeasurements run succeeds on the already-emptied store
  </behavior>
  <action>Create StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift (name is executor's discretion per CONTEXT "the agent's Discretion"; may instead land as a new ungated sibling suite inside DataDeletionConsolidationTests.swift — if so, drop the pbxproj edit). Suite shape: plain @Suite @MainActor struct with NO .enabled(if:)/.disabled trait — CI must see it (locked CONTEXT decision; the placement rule at 02-PATTERNS.md "CRITICAL placement rule"). The spy: @MainActor final class conforming to CloudKitResetServiceProtocol and @unchecked Sendable, seeded store keyed by CloudKitRecordType, all five protocol methods returning normally without mutating the store, remainingRecords as an exact Int sum. Constructor-injected only. SwiftData fixture: makeContextWithOneMeasurement tuple shape (ModelContainer, ModelContext) with isStoredInMemoryOnly + cloudKitDatabase .none, container held via _ = container until assertions done. Register the new file in the StressMonitorTests target in project.pbxproj (PBXBuildFile + PBXFileReference + group child + Sources phase — mirror the CreditPurchaseFlowTests.swift entries; orphaned dirs never build, AGENTS.md). No force unwrapping (SwiftLint force_unwrapping). Do NOT copy the GSD_CI gate onto the new suite.</action>
  <verify>
    <automated>TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/DataDeleterCloudKitTruthinessTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -5 && swiftlint lint --quiet | grep -v '^$' | head -5; echo LINT_DONE</automated>
  </verify>
  <acceptance_criteria>
    - New suite runs green (all @Test cases pass) under the -only-testing invocation above
    - grep -n "DataDeleterCloudKitTruthinessTests.swift" StressMonitor/StressMonitor.xcodeproj/project.pbxproj returns both a PBXBuildFile and a PBXFileReference entry (file is in the target, not just on disk)
    - grep -c "enabled(if\|\.disabled(" over the new suite file returns 0 (ungated by construction)
    - grep -c "RequestCaptureURLProtocol" over the new suite file returns 0
    - swiftlint reports no new violations in the new file
  </acceptance_criteria>
  <done>The ungated spy suite exists in the test target, is green, and pins the CR-01 truthiness signal via the DI seam. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation red-proof — the suite catches a lying orchestrator (tracer end-to-end gate)</name>
  <files>(no committed changes — temporary mutation in StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift, reverted)</files>
  <read_first>StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift (:97-111 deleteAllMeasurements CloudKit phase — the call site to mutate), 02-RESEARCH.md "The CR-01 Bug and the Seam" + Pattern 1</read_first>
  <action>Prove the suite fails if CR-01 returns. Temporarily (NEVER committed): wrap the CloudKit reset call inside deleteAllMeasurements in a swallowing do/catch (the CR-01 shape — failure caught, success reported). Run only the new suite: the genuine-failure prong MUST fail (expect red output captured in the SUMMARY). Restore with git checkout -- StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift and confirm the working tree is clean for that file. Re-run the suite green. Record the red/green evidence (commands + exit codes + failing test name) in the plan SUMMARY under a "Mutation proof" heading — this is the tracer's end-to-end verification artifact, mirroring the Phase-1 verify-archive.sh red/green pattern.</action>
  <verify>
    <automated>git status --porcelain StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift | wc -l | grep -q '^0$' && TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/DataDeleterCloudKitTruthinessTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains the mutation-run record: the temporary swallow made the genuine-failure prong FAIL (test name + failure line quoted), then green after revert
    - git status --porcelain for DataDeleterService.swift is empty after the task (mutation never committed; verify command asserts 0)
    - Final -only-testing run of the suite is green
  </acceptance_criteria>
  <done>Red/green mutation proof recorded in the SUMMARY; production source untouched in the committed tree; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CloudKit service -> delete orchestrator | success/failure reporting crosses this seam; a lying service must be detectable |
| test host -> CI | suite enable/disable state determines what CI can prove |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | DataDeleterService delete pipeline (privacy: data survives a claimed delete) | high | mitigate | DATA-04 spy suite pins the truthiness signal (query-based emptiness contract); live propagation backstopped by DATA-01 evidence in plan 02-06 |
| T-02-02 | Tampering | Test double state (cross-suite statics could fake a green) | low | mitigate | Constructor-injected fakes only; RequestCaptureURLProtocol statics prohibited (WINDOWS #12) |
</threat_model>

<verification>
- New suite green under -only-testing with TEST_RUNNER_GSD_CI=1 exported (proves CI-visibility of the ungated suite)
- Mutation red-proof recorded in SUMMARY; DataDeleterService.swift byte-identical to HEAD afterward
- File registered in the StressMonitorTests target (pbxproj entries present)
</verification>

<success_criteria>
- DATA-04's regression seam exists and is proven: a lying/throwing CloudKit double is injectable, the suite detects both failure modes, and the mutation run shows the suite goes red when the orchestrator swallows failures
- The suite is CI-visible by construction (no gate trait)
</success_criteria>

## Artifacts this phase produces (this plan)

- New suite: StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift (name executor's discretion) — the DATA-04 regression home, ungated
- New spy type: a CloudKitResetServiceProtocol-conforming lying/deleting double with remainingRecords (deletion-aware-fake family, alongside FakeCloudKitResetService and FakeServerSessionWiper)
- Mutation-proof record in 02-01-SUMMARY.md (the tracer red/green evidence)

<output>
Create `.planning/phases/02-delete-correctness-test-suite-trust/02-01-SUMMARY.md` when done
</output>
