---
phase: 03-accessibility-compliance
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift
  - StressMonitor/StressMonitor/Views/Trends/Components/StressBarChartView.swift
  - StressMonitor/StressMonitor/Views/Trends/Components/HRVTrendChart.swift
  - StressMonitor/StressMonitor/Views/Dashboard/Components/SparklineChart.swift
  - StressMonitor/StressMonitor/Views/Dashboard/Components/MiniLineChartView.swift
  - StressMonitor/StressMonitor/Views/Dashboard/Components/StressRingView.swift
  - StressMonitor/StressMonitorTests/ChartAccessibilityTests.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
autonomous: true
requirements: [A11Y-04]
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Every chart on a manifest surface (StressBarChartView, HRVTrendChart, SparklineChart, MiniLineChartView) renders fixed-size geometry but exposes an accessibility series: a one-line trend summary (the VoiceOver entry) plus per-point '{date}: {value}{unit}' labels in the existing timelinePoint shape (D-09)"
    - "The trend-summary copy follows the UI-SPEC contract exactly: '{Metric} {up|down|steady} {percent}% in the last {period}', with the steady variant omitting the percent token"
    - "A single-point series produces a 'steady' summary with no percent token — pinned by ChartAccessibilityTests (the A11Y-04 empty-edge acceptance criterion)"
    - "The stress-ring hero numeral (gauge class, D-09-exempt from scaling) carries the .accessibilityStressLevel value so VoiceOver reads the score the ring shows"
    - "Any interactive chart-legend control inside the chart-component files carries a >=44pt hit area via the token-backed helper (the chart-file slice of A11Y-01)"
  artifacts:
    - StressMonitor/StressMonitorTests/ChartAccessibilityTests.swift
    - Trend-summary builder (pure static function beside VoiceOverLabels in AccessibilityModifiers.swift)
  key_links:
    - "Chart containers on manifest surfaces <-> accessibility series (summary + per-point labels via the extended .accessibilityChart shape)"
    - "ChartAccessibilityTests <-> the trend-summary builder (copy shapes pinned red-first)"
  prohibitions: []
---

<objective>
Give the exempt-from-scaling visuals their required semantic content: charts keep fixed geometry but speak their data (one-line trend summary + per-point series), and gauge numerals/legends carry accessibility values and targets.

Purpose: D-09 completes A11Y-04 — "fixed-size" is only acceptable when VoiceOver conveys what sight shows. The builder is pure logic with defined I/O, so the copy contract is unit-pinned red-first rather than eyeballed.
Output: extended AccessibilityModifiers (chart series + summary builder), four chart components speaking, StressRingView gauge value, ChartAccessibilityTests green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-accessibility-compliance/03-CONTEXT.md
@.planning/phases/03-accessibility-compliance/03-RESEARCH.md
@.planning/phases/03-accessibility-compliance/03-PATTERNS.md
@StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift
@StressMonitor/StressMonitor/Views/Trends/Components/StressBarChartView.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — ChartAccessibilityTests pins the trend-summary copy contract</name>
  <files>StressMonitor/StressMonitorTests/ChartAccessibilityTests.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj, StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift</files>
  <behavior>
    - Test 1: a single-point series yields a steady summary with no percent token — exact output "{Metric} steady in the last {period}"
    - Test 2: an upward multi-point series yields "{Metric} up {percent}% in the last {period}" with the percent rounded to a whole number
    - Test 3: a downward multi-point series yields the down variant
    - Test 4: an exactly-flat multi-point series yields the steady variant (no percent token)
    - Test 5: a zero-percent change over multiple points yields steady, not "up 0%"
  </behavior>
  <read_first>
  - StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift (suite-structure analog for the new manually-registered Swift Testing suite)
  - StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift (VoiceOverLabels at lines 112-114 — the timelinePoint per-point string shape; the accessibilityChart shape at 137-143)
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj lines 48, 144, 323, 553 (the 4-point registration precedent — just executed for ContrastComplianceTests in plan 03-01)
  - .planning/phases/03-accessibility-compliance/03-UI-SPEC.md Copywriting Contract (the exact trend-summary and chart-point copy strings)
  </read_first>
  <action>
  TDD red-first for the copy contract. Create ChartAccessibilityTests.swift as a named Swift Testing suite (@testable import StressMonitor) with the five behavior tests above against a new pure static trend-summary builder sited beside VoiceOverLabels in AccessibilityModifiers.swift — inputs: metric display name, an ordered series of values, and a period string; output: the exact contract string. Add the minimal stub declaration of the builder (returning an empty string) so the suite compiles and runs RED against the contract. Register the test file in the pbxproj with the same 4-point pattern and two fresh unique IDs used for ContrastComplianceTests. Run and record the RED result (all five cases fail against the stub). Percent computation: whole-number rounding of the relative change between the first and last series values; an exactly-zero or single-point change classifies steady. Do not implement the real builder in this task — the stub stays.
  </action>
  <verify>
    <automated>TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/ChartAccessibilityTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | grep -c "Expectation failed\|failed" ; echo RED_COUNT_DONE</automated>
    <fails_when>the failure-line count is 0 — the RED proof requires at least one failing expectation against the stub (an unregistered file also yields 0 and fails this gate identically)</fails_when>
  </verify>
  <done>Test file created, registered (4 pbxproj points), compiling, and failing against the stub builder — the copy contract is pinned before any implementation exists.</done>
  <acceptance_criteria>
  - StressMonitor/StressMonitorTests/ChartAccessibilityTests.swift exists with five named @Test cases covering steady-single-point, up, down, flat-multi-point, and zero-percent-steady
  - grep -c "ChartAccessibilityTests" project.pbxproj returns >= 4
  - The RED run reports failing expectations in all five cases (output pasted in the SUMMARY)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement the builder + apply the accessibility series to charts and the gauge value</name>
  <files>StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift, StressMonitor/StressMonitor/Views/Trends/Components/StressBarChartView.swift, StressMonitor/StressMonitor/Views/Trends/Components/HRVTrendChart.swift, StressMonitor/StressMonitor/Views/Dashboard/Components/SparklineChart.swift, StressMonitor/StressMonitor/Views/Dashboard/Components/MiniLineChartView.swift, StressMonitor/StressMonitor/Views/Dashboard/Components/StressRingView.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Views/Trends/Components/StressBarChartView.swift (existing container accessibility at 31-32: children-containment + label — the live pattern to extend, not replace)
  - StressMonitor/StressMonitor/Views/Trends/Components/HRVTrendChart.swift (same pattern at 37-38)
  - StressMonitor/StressMonitor/Views/Dashboard/Components/SparklineChart.swift and MiniLineChartView.swift (Swift Charts usages — these get the same series treatment)
  - StressMonitor/StressMonitor/Views/Dashboard/Components/StressRingView.swift (the hero numeral + the existing .accessibilityStressLevel modifier it should call)
  - StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift (accessibilityChart at 137-143 and accessibilityStressLevel — both definition-only, zero adopters)
  </read_first>
  <action>
  GREEN in two moves. (1) Implement the trend-summary builder for real: classification (up/down/steady from relative change, steady on single point or exactly-flat), percent as a whole number, and the exact contract strings from the UI-SPEC — steady omits the percent token. (2) Adopt the series on every chart component named above: keep each chart's existing container accessibility (children-containment plus its descriptive label) and extend it — the container's accessibilityValue becomes the one-line trend summary from the builder (the VoiceOver entry point), and per-point labels follow the timelinePoint string shape ("{date}: {value}{unit}", e.g. "Sep 3: 48 ms") composed from the chart's own data series, delivered through the extended .accessibilityChart shape with the updates-frequently trait where the chart refreshes. Charts render fixed-size — do NOT wrap chart geometry in any Dynamic Type scaling (D-09 geometry exemption). Apply .accessibilityStressLevel to the StressRingView hero numeral so the gauge class conveys its score. Any interactive legend control found inside these component files gets the token-backed minimum touch target with a label (the chart-file slice of A11Y-01 — surface files were plan 03-03's). Note on research assumption A3: do not depend on Swift Charts providing its own aggregate accessibility element — the container-level series carries the requirement regardless.
  </action>
  <verify>
    <automated>TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/ChartAccessibilityTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -5 && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
    <fails_when>the test tail contains "** TEST FAILED **" (any of the five contract cases red), or the build tail contains "** BUILD FAILED **"</fails_when>
  </verify>
  <done>ChartAccessibilityTests 5/5 green, all four chart components speak summary + per-point series, the ring gauge carries its score value, and chart geometry stays fixed-size.</done>
  <acceptance_criteria>
  - ChartAccessibilityTests reports 0 failures (5/5 green)
  - Each of StressBarChartView, HRVTrendChart, SparklineChart, MiniLineChartView carries an accessibilityValue built from the trend-summary builder plus per-point label strings
  - StressRingView's hero numeral carries the stress-level accessibility value
  - No chart-component file applies a Dynamic Type scaling modifier to chart geometry
  - App scheme builds with "** BUILD SUCCEEDED **"
  </acceptance_criteria>
</task>

</tasks>

<flagged_assumptions>
- **Research A3 (carried):** Swift Charts' automatic aggregate accessibility/audio-graph behavior is unverified on this SDK. In-plan mitigation: the series does not depend on it — container-level summary + per-point labels carry the requirement regardless of framework behavior.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Accessibility labels restate values already rendered on screen; no new data leaves the device and no input surface is added |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Information Disclosure | chart accessibility labels | low | accept | Labels verbalize only data the chart already renders to sighted users — no additional exposure |
| T-03-SC | Supply chain | package installs | low | accept | No installs — first-party only |
</threat_model>

<verification>
- ChartAccessibilityTests green (5/5) through the registered target.
- App scheme builds; each chart component and the ring gauge carry their series/values (grep for the builder's use at each adoption site pasted in the SUMMARY).
</verification>

<success_criteria>
- The D-09 exempt class (charts, gauge) is no longer silent: VoiceOver receives the same information sight receives.
- The copy contract is machine-pinned, not convention.
</success_criteria>

<artifacts_this_phase_produces>
New symbols/files created by this plan:
- File: StressMonitor/StressMonitorTests/ChartAccessibilityTests.swift (registered, 4 pbxproj points, two new unique IDs)
- Pure static trend-summary builder in AccessibilityModifiers.swift (metric/values/period -> contract string)
- Adoption: accessibilityValue + per-point series on four chart components; .accessibilityStressLevel on the StressRingView hero numeral
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-accessibility-compliance/03-04-SUMMARY.md` when done
</output>
