---
phase: 03-accessibility-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - StressMonitor/StressMonitorTests/ContrastComplianceTests.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  - StressMonitor/StressMonitor/Theme/Color+Wellness.swift
  - StressMonitor/StressMonitor/Theme/Color+Extensions.swift
  - StressMonitor/StressMonitor/Models/StressCategory.swift
  - StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift
  - StressMonitor/StressMonitorWatch Watch App/Theme/Color+Extensions.swift
  - StressMonitor/StressMonitorWatch Watch App/Models/StressCategory.swift
autonomous: true
requirements: [A11Y-02]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "ContrastComplianceTests computes WCAG ratios from the semantic token definitions via UIColor.resolvedColor(with:) in BOTH appearances and asserts by usage class: >=4.5:1 for text pairs, >=3:1 for large-text/UI pairs (D-06)"
    - "A formula sanity test pins black-on-white at 21.00 (accuracy 0.01) so the luminance math itself is regression-guarded"
    - "adaptiveSecondaryText light renders >=4.5:1 on both canvas and card after the in-place token retune (D-04)"
    - "textTertiary and textDescriptive resolve to the adaptive secondary token (computed alias, bannerYellow precedent), inheriting >=4.9:1 on cream"
    - "White-on-fill ratios are asserted for BOTH appearances of every fill token — the ripple fill passes >=3:1 with white text in light, and the fixed #4FC3F7 value is never used as a white-text fill in either appearance"
    - "Every light stress-indicator variant passes >=3:1 on cream; every dark variant passes on #121212; accessibleStressColor(for:highContrast:true:) moderate passes >=3:1 and its false AAA comment is corrected"
    - "The stress-hue source of truth is StressCategory.color; Color.stressColor(for:) delegates to it and the test pins that path"
    - "Watch-side duplicated stress hexes mirror the retuned app values (no shared framework — mirror convention)"
    - "Widget contrast scope is documented in the test file as platform-bounded per D-07: system .primary/.secondary on materials are Apple-guaranteed; tier accent never carries meaning alone (always adjacent to a text label); wallpaper-dependent pairs out of scope by construction"
    - "StressDualCodingModifier renders its category caption text in adaptivePrimaryText/adaptiveSecondaryText, never in the stress hue"
    - "The suite runs ungated in the default CI suite (registered in the StressMonitorTests target, no environment gating)"
  artifacts:
    - StressMonitor/StressMonitorTests/ContrastComplianceTests.swift
  key_links:
    - "Theme/Color+Wellness.swift + Theme/Color+Extensions.swift token definitions <-> ContrastComplianceTests assertion matrix (the test reads the tokens; values live only in Theme/)"
    - "App stress hexes <-> watch-side duplicated copies (StressMonitorWatch Watch App/Theme/Color+Extensions.swift + Models/StressCategory.swift)"
    - "ContrastComplianceTests.swift <-> StressMonitorTests target (4-point pbxproj registration, A026/B026 precedent)"
  prohibitions:
    - statement: "MUST NOT convey stress category by hue alone — every category indicator pairs the hue with an SF Symbol and a text label (dual coding) wherever a category shows on any surface"
      status: resolved
      verification: judgment
    - statement: "MUST NOT fix contrast with per-site color literals — every AA fix is an in-place token retune in Theme/ (D-04); no new Color(hex:) literals are introduced on manifest surfaces"
      status: resolved
      verification: judgment
---

<objective>
Make WCAG AA contrast machine-checked and true: retune the failing semantic color tokens in place (D-04), stand up the permanent token-pair contrast unit test (D-06), fix the one view-level contrast rule (dual-coding caption text), and mirror the retunes watch-side.

Purpose: A11Y-02 ("Color contrast passes WCAG AA on primary surfaces") becomes a permanent CI gate computed from the single source of truth, not a screenshot opinion. This plan is also the phase tracer: it proves the token -> hosted test -> CI-suite architecture end-to-end on the smallest failing pair before any other plan builds on it.
Output: ContrastComplianceTests.swift (green), retuned Theme/ tokens, watch mirrors, one modifier fix.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-accessibility-compliance/03-CONTEXT.md
@.planning/phases/03-accessibility-compliance/03-RESEARCH.md
@.planning/phases/03-accessibility-compliance/03-PATTERNS.md
@.planning/phases/03-accessibility-compliance/03-UI-SPEC.md
@StressMonitor/StressMonitor/Theme/Color+Wellness.swift
@StressMonitor/StressMonitor/Theme/Color+Extensions.swift
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — contrast suite RED->GREEN on the smallest failing pair (test architecture proven end-to-end)</name>
  <files>StressMonitor/StressMonitorTests/ContrastComplianceTests.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj, StressMonitor/StressMonitor/Theme/Color+Wellness.swift</files>
  <read_first>
  - StressMonitor/StressMonitorTests/DataDeleterCloudKitTruthinessTests.swift (suite-structure analog: named @Suite/@Test titles, #expect, @testable import)
  - StressMonitor/StressMonitor/Theme/Color+Wellness.swift (adaptive token definitions, lines 58-79; accessibleStressColor at 137-157)
  - StressMonitor/StressMonitor/Theme/Color+Extensions.swift (Color(light:dark:) dynamic-provider init, lines 28-32 — what the test resolves through)
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj lines 48, 144, 323, 553 (the A026/B026 4-point registration precedent)
  - .planning/phases/03-accessibility-compliance/03-RESEARCH.md Pattern 1 (WCAG formula, validated black/white = 21.00)
  </read_first>
  <action>
  Implement D-06's first slice and prove the whole gate architecture in one task. Create ContrastComplianceTests.swift as a Swift Testing suite following the DataDeleterCloudKitTruthinessTests structure: imports Foundation, UIKit, Testing, @testable import StressMonitor; a named suite; named per-test titles (these names appear in later trust-gate enumerations). Implement the WCAG relative-luminance helper exactly per W3C G18/G145 — sRGB channel linearization (c <= 0.03928 ? c/12.92 : pow((c+0.055)/1.055, 2.4)), luminance L = 0.2126R + 0.7152G + 0.0722B, ratio = (L1 + 0.05) / (L2 + 0.05) with the lighter first — plus a sanity test asserting black-on-white = 21.00 with 0.01 accuracy. Add a resolution helper that resolves any app Color per appearance through UIKit's resolved-color-with-trait-collection mechanism so the Color(light:dark:) dynamic provider is honored in the hosted test target (TEST_HOST verified in the pbxproj). First assertion slice per D-06: adaptiveSecondaryText against BOTH adaptiveBackground and adaptiveCardBackground, parametrized over light and dark userInterfaceStyle, asserting >= 4.5:1. Register the file in the test target with the 4-point manual pbxproj registration (two fresh unique 24-hex-char IDs; PBXBuildFile entry near line 48, PBXFileReference near line 144, PBXGroup child near line 323, PBXSourcesBuildPhase near line 553) — StressMonitorTests is NOT a synchronized group, so the file is dead to xcodebuild without all four. Run the suite RED: the two light-mode cases fail at 4.24 and 4.32. Then implement the D-04 fix: retune the adaptiveSecondaryText light value in Color+Wellness.swift from hex 777986 to hex 6B6E7B (dark value 9CA3AF untouched) and run GREEN. Finally prove the test has teeth mutation-style (the phase-2 red-gate pattern): temporarily restore the old light hex, rerun, confirm the suite fails, then restore 6B6E7B and rerun green. No screenshot-based contrast checking anywhere (D-06 explicitly rejects it).
  </action>
  <verify>
    <automated>TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/ContrastComplianceTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -5</automated>
    <fails_when>the tail output contains "** TEST FAILED **" — a passing run ends with "** TEST SUCCEEDED **" and the executed-tests line shows 0 failures</fails_when>
  </verify>
  <done>Suite registered (4 pbxproj points), formula sanity 21.00 green, secondary-text pairs green in both appearances at the retuned value, and the mutation run demonstrated the suite goes red when the old hex is reintroduced.</done>
  <acceptance_criteria>
  - StressMonitor/StressMonitorTests/ContrastComplianceTests.swift exists and declares a named Swift Testing suite
  - grep -c "ContrastComplianceTests" StressMonitor/StressMonitor.xcodeproj/project.pbxproj returns >= 4 (BuildFile, FileReference, group child, Sources phase)
  - The suite's executed-tests summary reports 0 failures with the retuned value in place
  - Reverting the adaptiveSecondaryText light hex to 777986 makes at least the two light-mode cases fail (mutation red-proof) before the final revert
  - The sanity test pins the black/white ratio at 21.00 with 0.01 accuracy
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full D-06 matrix by usage class + all remaining token retunes + watch mirrors</name>
  <files>StressMonitor/StressMonitorTests/ContrastComplianceTests.swift, StressMonitor/StressMonitor/Theme/Color+Extensions.swift, StressMonitor/StressMonitor/Theme/Color+Wellness.swift, StressMonitor/StressMonitor/Models/StressCategory.swift, StressMonitor/StressMonitorWatch Watch App/Theme/Color+Extensions.swift, StressMonitor/StressMonitorWatch Watch App/Models/StressCategory.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Theme/Color+Extensions.swift (retune targets at lines 106-122: settingsBackground, settingsRippleBlue, accentTeal, textTertiary, textDescriptive; the bannerYellow computed-alias precedent near line 130; stressRelaxed…stressSevere duplicates at 36-40)
  - StressMonitor/StressMonitor/Models/StressCategory.swift (the stress-hue source of truth, lines 13-26)
  - StressMonitor/StressMonitor/Theme/Color+Wellness.swift (accessibleStressColor highContrast set and its comment at line 139; calmBlue light already 0891B2 at line 11)
  - StressMonitor/StressMonitorWatch Watch App/Theme/Color+Extensions.swift (watch stress hexes at 43-47; watch Color(light:dark:) resolves light-only, lines 32-38)
  - .planning/phases/03-accessibility-compliance/03-RESEARCH.md "Pattern 1" ratio matrix (every current failure and pre-computed passing candidate)
  </read_first>
  <action>
  Expand the suite to the complete D-06 matrix, modeling every pair by USAGE CLASS so the same token is judged differently as text, as UI accent, and as a fill under white text: (a) text pairs >= 4.5 — adaptivePrimaryText on canvas and on card, both appearances; the aliased tertiary/descriptive tokens on cream; (b) UI/large-text pairs >= 3.0 — primaryBlue on canvas both appearances; white text on the primaryBlue fill both appearances; the ripple accent on canvas both appearances; white text on the ripple fill BOTH appearances (this is the pitfall the research surfaced: white on the fixed 4FC3F7 is 2.00 in both modes, so the fill must become adaptive); (c) indicator pairs >= 3.0 — every stress category hue on cream (light set) and on 121212 (dark set), asserted through Color.stressColor(for:) delegating to StressCategory.color so the test pins the surviving single source of truth; (d) the accessibleStressColor highContrast moderate variant on cream. Then land the D-04 retunes in place, values per the pre-computed passing candidates with minimal visual shift: settingsBackground dark 0A0A0F -> 121212 (unify with adaptiveBackground, kills the duplicate-canvas drift); settingsRippleBlue and accentTeal change from the fixed 4FC3F7 to adaptive light 0891B2 / dark 4FC3F7 — with the binding usage rule that 4FC3F7 remains stroke/glyph accent only on dark backgrounds and is never a white-text fill in either appearance; textTertiary and textDescriptive convert from fixed hex constants to computed aliases of the adaptive secondary token (static var, bannerYellow precedent) so both inherit >= 4.9:1 on cream; StressCategory.swift light set retunes — relaxed 34C759 -> 00A000, moderate FFD60A -> 8A5A00 (fallback candidate 9A5B00 if visual shift must be smaller), high FF9500 -> B25400 (fallback CC0000), keeping mild 007AFF and severe FF3B30 which already pass; the duplicate stressRelaxed…stressSevere statics in Color+Extensions stay value-in-sync with the retune (their deletion is an A11Y-05-class cleanup dispositioned in plan 03-06, not here); accessibleStressColor's highContrast moderate FFA500 (1.94:1, fails even 3:1) replaced with a passing value and its false WCAG AAA comment corrected. Mirror every retuned hex into the two watch-side duplicated files so the targets do not diverge — this is the D-02 sweep membership for watch surfaces (they join the contrast half of D-02 via their token mirrors; the Dynamic Type half of D-02 is the plan 03-02 Task 4 font-anchor sweep; no shared framework). Add the D-07 widget scope note to the test file header: per D-02 the widget gallery and lock-screen surfaces join the contrast/Dynamic Type sweep — the Dynamic Type half executing in plan 03-02 Task 4 — and widget foregrounds are system primary/secondary on system materials (Apple-guaranteed pairs, asserted as token pairs only); the tier accent is dual-coded and never meaning-alone; wallpaper-dependent contrast is out of scope by construction. If any matrix row fails at a recommended value, adjust that token minimally until the row passes — the unit test is the binding judge, the recommended hexes are planning inputs.
  </action>
  <verify>
    <automated>TEST_RUNNER_GSD_CI=1 xcodebuild test -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:StressMonitorTests/ContrastComplianceTests CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -5</automated>
    <fails_when>the tail output contains "** TEST FAILED **" (any matrix row below its threshold fails the suite)</fails_when>
    <automated>grep -c "6B6E7B" StressMonitor/StressMonitor/Theme/Color+Wellness.swift && grep -c "0891B2" StressMonitor/StressMonitor/Theme/Color+Extensions.swift && grep -c "00A000" StressMonitor/StressMonitor/Models/StressCategory.swift "StressMonitor/StressMonitorWatch Watch App/Models/StressCategory.swift" && grep -c "121212" StressMonitor/StressMonitor/Theme/Color+Extensions.swift; echo RETUNE_GREP_DONE</automated>
    <fails_when>any grep -c prints 0 (a retuned token missing from its file) or a file path errors before RETUNE_GREP_DONE prints</fails_when>
  </verify>
  <done>The full usage-class matrix is green in both appearances, all retunes landed in the four app-side files, both watch mirrors carry the same values, and the D-07 platform-bounded scope note is in the test file header.</done>
  <acceptance_criteria>
  - ContrastComplianceTests asserts text pairs at 4.5:1 and UI/large-text pairs at 3:1, parametrized over light and dark, including white-on-fill rows for both fill tokens in both appearances
  - StressCategory.swift light set contains the passing relaxed/moderate/high values (00A000 / 8A5A00-or-9A5B00 / B25400-or-CC0000 class) and the suite is green through Color.stressColor(for:)
  - textTertiary and textDescriptive are computed aliases (static var) of the adaptive secondary token — grep for their old fixed hex values 808080 and 848484 in Color+Extensions.swift returns 0 hits
  - Both watch files contain the retuned stress hexes matching the app values exactly
  - The accessibleStressColor highContrast moderate value passes >= 3:1 and its comment no longer claims AAA
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Dual-coding caption text leaves the stress hue</name>
  <files>StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift (StressDualCodingModifier around lines 35-45 — the caption Text binding the category hue as its foreground color)
  - .planning/phases/03-accessibility-compliance/03-UI-SPEC.md "Stress-category palette — indicator class, dual-coded" (the rule being implemented)
  </read_first>
  <action>
  Implement the UI-SPEC contrast rule inside StressDualCodingModifier: the category caption Text (the displayName label) changes its foreground color binding from the category hue to Color.Wellness.adaptiveSecondaryText. The hue remains on the icon/ring/dot elements — that is the dual-coding split: color carries the indicator, text stays on the adaptive text token which the D-06 suite already proves >= 4.5:1 (no saturated stress hue reaches 4.5:1 on cream). Change only the caption's foreground binding; do not restructure the modifier (it is definition-only today with zero call sites and gets adopted across surfaces in plan 03-03 — this task makes it safe to adopt). While in the file, leave the animation modifier alone entirely; its per-body-evaluation animation value bug is folded into the plan 03-05 helper consolidation, not fixed here.
  </action>
  <verify>
    <automated>grep -c "foregroundColor(category.color)" StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift; echo HUE_GREP_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
    <fails_when>the grep count is not 0 (caption still hue-bound) before HUE_GREP_DONE, or the build tail contains "** BUILD FAILED **"</fails_when>
  </verify>
  <done>The dual-coding caption renders in the adaptive secondary token, the app target builds, and no other behavior in the file changed.</done>
  <acceptance_criteria>
  - grep -c "foregroundColor(category.color)" over AccessibilityModifiers.swift returns 0
  - The caption Text binds Color.Wellness.adaptiveSecondaryText
  - App scheme builds with "** BUILD SUCCEEDED **"
  - ContrastComplianceTests remains green (rerun of the Task 1/2 command exits without TEST FAILED)
  </acceptance_criteria>
</task>

</tasks>

<assumption_delta_decision>
Detected (advisory) on roadmap goal prose ("alongside", "another", "optional"). Decision: **no-change** for the identity model — the phase adds no second persisted anything; if anything it removes duplicates. One promote-style pin is recorded: StressCategory.color is promoted to the single stress-hue source of truth, the value-duplicated stressRelaxed…stressSevere statics in Color+Extensions.swift are demoted to deletion candidates (dispositioned by the plan 03-06 orphan audit). No invariant test required — the D-06 suite asserting through Color.stressColor(for:) -> StressCategory.color IS the pin.
</assumption_delta_decision>

<flagged_assumptions>
- **A11Y-02 edge probe (unclassified — flagged, not auto-resolved):** the token-pair matrix plus the per-surface sweep is assumed to cover every AA-relevant pair on primary surfaces. Residual risk: a manifest surface composing two tokens in a pairing the matrix does not model (e.g. text over a retuned accent fill at a non-standard usage). Mitigation in-plan: the matrix models pairs by usage class (text / UI / white-on-fill), which is the pairing grammar; any pair discovered during the 03-03 surface sweep moves into this suite as a new row.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | View-layer token values only; no network, auth, crypto, or persistence surface is touched by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | manual 4-point pbxproj registration | medium | mitigate | Byte-identical A026/B026 precedent shape; xcodebuild fails fast on malformed registration; full-suite gate before merge |
| T-03-02 | Repudiation | Contrast suite silently absent from CI (unregistered file) | high | mitigate | RED->GREEN run executes through the StressMonitorTests target — an unregistered file cannot run; suite joins the default ungated run (no GSD_CI gating, phase-2 precedent) |
| T-03-SC | Supply chain | package installs | low | accept | No package installs this phase — first-party Swift/UIKIt only; the periphery CLI used later in the phase is pre-installed, dev-only, never shipped |
</threat_model>

<verification>
- ContrastComplianceTests green in both appearances via the -only-testing invocation (the permanent CI gate).
- Mutation red-proof recorded in the SUMMARY (phase-2 TDD red-gate pattern adapted: a reintroduced failing hex must redden the suite).
- All retuned hexes grep-verified present in app files and both watch mirrors.
- App target builds after the modifier change.
</verification>

<success_criteria>
- Every D-06 matrix row passes at its usage-class threshold in light and dark.
- The contrast truth lives in exactly one place per token family (Theme/ for canvas/text/accent, StressCategory.color for stress hues) and the test pins those paths.
- No per-site color literals were introduced anywhere (D-04).
</success_criteria>

<artifacts_this_phase_produces>
New symbols/files created by this plan:
- File: StressMonitor/StressMonitorTests/ContrastComplianceTests.swift (named @Suite; WCAG contrastRatio helper, appearance-resolution helper, sanity test, full usage-class matrix)
- Token value changes (existing symbols, new values): Color.Wellness.adaptiveSecondaryText light; Color.settingsBackground dark; Color.settingsRippleBlue and Color.accentTeal (become adaptive); Color.textTertiary and Color.textDescriptive (become computed aliases); StressCategory light hues (relaxed/moderate/high); Color.Wellness accessibleStressColor highContrast moderate
- Behavior change: StressDualCodingModifier caption foreground -> adaptive secondary token
- pbxproj: two new unique 24-hex IDs registering the test file (4 sections)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-accessibility-compliance/03-01-SUMMARY.md` when done
</output>
