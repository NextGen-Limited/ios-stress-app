---
phase: 03-accessibility-compliance
plan: 05
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - StressMonitor/StressMonitor/Utilities/Animation+Wellness.swift
  - StressMonitor/StressMonitor/Utilities/AnimationPresets.swift
  - StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift
  - StressMonitor/StressMonitor/Components/Character/CharacterAnimationModifier.swift
  - StressMonitor/StressMonitor/Views/Breathing/BreathingExerciseView.swift
  - StressMonitor/StressMonitor/Views/Breathing/Components/BreathingCircle.swift
  - StressMonitor/StressMonitor/Views/Breathing/Components/RippleBreathingView.swift
  - StressMonitor/StressMonitor/Views/Breathing/BreathingSessionView.swift
  - StressMonitor/StressMonitor/Views/Breathing/BreathingSummaryView.swift
  - StressMonitor/StressMonitor/Views/Breathing/BreathingViewModel.swift
  - StressMonitor/StressMonitor/Views/Dashboard/Components/SkeletonBlock.swift
  - StressMonitor/StressMonitor/Views/Dashboard/Components/StressRingView.swift
  - StressMonitor/StressMonitor/Views/Dashboard/Components/MetricCardView.swift
  - StressMonitor/StressMonitor/Views/Dashboard/Components/LearningPhaseCard.swift
  - StressMonitor/StressMonitor/Views/MiniWalk/MiniWalkView.swift
  - StressMonitor/StressMonitor/Views/MiniWalk/Components/MiniWalkInstructionCard.swift
autonomous: true
requirements: [A11Y-03]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "One helper (the reworked Animation+Wellness.swift) owns every read of the reduce-motion environment value — zero raw reduceMotion/isReduceMotionEnabled references remain anywhere in the app target outside the helper file and the explicitly-dispositioned D-11 breathing carve-out (D-12/D-13, grep-enumerated by file and construct)"
    - "Under Reduce Motion the app cross-fades transitions (.opacity — the .identity hard-cut fallbacks are gone), stops repeatForever loops (shimmer falls back to the static redacted state), stops press-scale effects, and the character idle animation holds a static pose"
    - "The dashboard skeleton's shimmer stops under Reduce Motion via the helper (static redacted state — consolidating the existing branch)"
    - "Data-export progress keeps conveying state under Reduce Motion — only its decorative loops stop through the helper (progress indication is state, not decoration)"
    - "The breathing session under Reduce Motion defaults to haptic pulses plus the text countdown line ('Inhale — 3' style, BoxBreathingStep vocabulary) with an in-session 'Breathing animation' switch, auto-set OFF under Reduce Motion and user-togglable ON (D-11); breathing views take the D-11 carve-out, not the blanket helper"
    - "The scripted-verification seam is a DEBUG-only launch argument following the MockIAPMode precedent — compiled out of Release builds entirely"
    - "The Animation.wellness* explicit-Bool signatures and the AnimationPresets' unmotioned statics are gone or routed through the helper — no caller reads the environment to pass a Bool into an animation factory anymore"
  artifacts:
    - Reworked StressMonitor/StressMonitor/Utilities/Animation+Wellness.swift (the single D-12 helper)
  key_links:
    - "Every former raw reduce-motion read site (13 enumerated files) <-> the single helper"
    - "The helper's motion-allowed decision <-> the SwiftUI reduce-motion environment value OR'd with the DEBUG launch-arg seam"
  prohibitions:
    - statement: "MUST NOT strip state-conveying feedback when animation stops under Reduce Motion — progress indication persists; only decorative loops, transitions, and parallax effects stop (D-12)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT compile the -a11y-reduce-motion launch-argument override into Release builds — the seam is exclusive to DEBUG builds (repo precedent: DemoMode, MockIAPMode)"
      status: resolved
      verification: judgment
---

<objective>
Consolidate every scattered Reduce Motion check onto one app-wide helper (D-12/D-13), make its fallback behavior match the contract (cross-fades, not hard cuts; loops stop; state feedback persists), and ship the D-11 breathing fallback mode.

Purpose: A11Y-03. The app currently branches on the reduce-motion environment value in 13 files (65-66 refs) with at least one helper family returning hard-cut transitions — the opposite of what a Reduce Motion user needs. After this plan, motion is optional in exactly one place.
Output: reworked Animation+Wellness.swift as the single motion gate, 13-file consolidation, breathing haptic+text fallback, DEBUG verification seam.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-accessibility-compliance/03-CONTEXT.md
@.planning/phases/03-accessibility-compliance/03-RESEARCH.md
@.planning/phases/03-accessibility-compliance/03-PATTERNS.md
@StressMonitor/StressMonitor/Utilities/Animation+Wellness.swift
@StressMonitor/StressMonitor/StressMonitorApp.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: The D-12 helper — rework Animation+Wellness.swift in place (cross-fade fallbacks, DEBUG seam, fold the broken animation modifier)</name>
  <files>StressMonitor/StressMonitor/Utilities/Animation+Wellness.swift, StressMonitor/StressMonitor/Utilities/AnimationPresets.swift, StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Utilities/Animation+Wellness.swift (full file: the explicit-Bool Animation.wellness/breathing/fidget/shake/dizzy factories at 12-37 — the scatter source; ReduceMotionAwareModifier + animateIfMotionAllowed at 43-72 — the correct env-reading shape to keep; the AnyTransition accessible family at 76-102 returning hard-cut identity under Reduce Motion)
  - StressMonitor/StressMonitor/Utilities/AnimationPresets.swift (the unmotioned static presets at 5-29; the shimmerLoading extension at ~80 that keeps ShimmerEffectView live)
  - StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift (AccessibleAnimationModifier at ~62-73 — the per-body-evaluation animation-value bug to fold; the press-effect button style)
  - StressMonitor/StressMonitor/StressMonitorApp.swift (MockIAPMode at 6-27 — the injectable DEBUG launch-arg precedent to copy exactly)
  - .planning/phases/03-accessibility-compliance/03-RESEARCH.md Pattern 4 (framework notes: TransitionProperties.hasMotion for custom transitions; the read-only environment value; the designed seam)
  </read_first>
  <action>
  Rework the helper family in place (OQ-2 resolved: rework Animation+Wellness.swift rather than a new file — it preserves the one call-site shape worth keeping). The helper becomes the single owner of the motion decision: an env-reading modifier (the existing ReduceMotionAwareModifier shape) plus a small pure decision function taking the environment Bool and the DEBUG seam flag, so breathing and any non-view caller can ask the same question without their own environment reads. Behavior per D-12: under Reduce Motion the AnyTransition accessible family returns .opacity (a cross-fade) — the hard-cut identity fallback is deleted; repeatForever loops (shimmer, idle bob, staggered pulses) branch to their static fallbacks; scale/offset/parallax effects are removed. Route the AnimationPresets statics through the helper so preset users inherit motion awareness without reading anything themselves; keep the shimmerLoading extension's public shape (ShimmerEffectView stays live through it). Delete the explicit-Bool Animation factories once their callers are migrated in Task 2 (both land in this plan; the delete can trail the migration inside the same task if the compiler orders it). Fold AccessibleAnimationModifier's per-body-evaluation bug into the helper — its animation value becomes a real Equatable driven by the caller, not a fresh UUID each evaluation; the press-scale button style stops scaling under Reduce Motion. Add the scripted-verification seam: a DEBUG-only launch-arg enum copying MockIAPMode exactly (launch argument "-a11y-reduce-motion", an injectable arguments parameter defaulting to the process arguments, an isEnabled check), OR'd into the helper's motion-allowed decision — never read in Release, never a second environment read anywhere else.
  </action>
  <verify>
    <automated>grep -rn "identity" StressMonitor/StressMonitor/Utilities/Animation+Wellness.swift; echo TRANSITION_GREP_DONE && grep -c "a11y-reduce-motion" StressMonitor/StressMonitor/Utilities/Animation+Wellness.swift; echo SEAM_GREP_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
    <fails_when>the transition grep prints any line returning the identity transition as a Reduce Motion fallback before TRANSITION_GREP_DONE, the seam count is 0 before SEAM_GREP_DONE, or the build tail contains "** BUILD FAILED **"</fails_when>
  </verify>
  <done>The helper owns the motion decision with cross-fade fallbacks, the DEBUG seam exists behind the MockIAPMode-shaped gate, presets route through the helper, and the app target builds.</done>
  <acceptance_criteria>
  - Animation+Wellness.swift's transition family returns the opacity transition under Reduce Motion (no identity fallback remains in the family)
  - The DEBUG seam enum exists with the injectable-arguments shape and is compiled only in debug configurations
  - AnimationPresets statics route through the helper
  - App scheme builds with "** BUILD SUCCEEDED **"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Consolidation sweep — 13 files onto the helper (re-baselined at execution)</name>
  <files>StressMonitor/StressMonitor/Components/Character/CharacterAnimationModifier.swift, StressMonitor/StressMonitor/Views/Breathing/BreathingExerciseView.swift, StressMonitor/StressMonitor/Views/Breathing/Components/BreathingCircle.swift, StressMonitor/StressMonitor/Views/Breathing/Components/RippleBreathingView.swift, StressMonitor/StressMonitor/Views/Dashboard/Components/SkeletonBlock.swift, StressMonitor/StressMonitor/Views/Dashboard/Components/StressRingView.swift, StressMonitor/StressMonitor/Views/Dashboard/Components/MetricCardView.swift, StressMonitor/StressMonitor/Views/Dashboard/Components/LearningPhaseCard.swift, StressMonitor/StressMonitor/Views/MiniWalk/MiniWalkView.swift, StressMonitor/StressMonitor/Views/MiniWalk/Components/MiniWalkInstructionCard.swift, StressMonitor/StressMonitor/Utilities/AnimationPresets.swift, StressMonitor/StressMonitor/Utilities/AccessibilityModifiers.swift, StressMonitor/StressMonitor/Utilities/Animation+Wellness.swift</files>
  <read_first>
  - .planning/phases/03-accessibility-compliance/03-PATTERNS.md "Reduce Motion consolidation — the 13 files" (the enumerated baseline table with per-file ref counts)
  - Each of the files listed above at their reduce-motion branch sites (read the branches, not whole files)
  - StressMonitor/StressMonitor/Views/Dashboard/Components/SkeletonBlock.swift (the existing shimmer-to-redacted branch — the consolidation exemplar)
  </read_first>
  <action>
  First RE-BASELINE: run the raw-ref grep fresh (research measured 66/13, the UI-SPEC recorded 65/13 — one-ref drift already; the gate must reflect execution-time reality, enumerated by file and construct, never by count alone). Then consolidate every site outside the helper file onto the Task-1 helper: each view's own environment read of the reduce-motion value and each UIAccessibility global check is replaced by the helper's modifier or decision function; the Animation factories' former callers take the env-reading variants. Behavior is preserved-or-improved per D-12: character idle holds a static pose (CharacterAnimationModifier), the skeleton shimmer falls back to static redacted (already the branch shape), ring/metric/learning-phase decorative motion stops, MiniWalk instruction transitions cross-fade. BreathingExerciseView keeps exactly its D-11 disposition: its session-animation gate reads the motion decision through the helper's decision function (no independent environment read), and the intro view's decorative box animation routes through the blanket helper. RippleBreathingView and MetricCardView are consolidated even though they are orphan-audit candidates — deletion in plan 03-06 comes after this sweep per the phase sequencing, and the trust gate then re-runs against the post-deletion tree. Comments mentioning the retired symbols get reworded so the gate grep stays clean honestly. Paste the post-sweep gate grep output into the SUMMARY.
  </action>
  <verify>
    <automated>grep -rn "accessibilityReduceMotion\|isReduceMotionEnabled" StressMonitor/StressMonitor --include="*.swift" | grep -v "Utilities/Animation+Wellness.swift"; echo RM_GATE_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
    <fails_when>any result line prints before RM_GATE_DONE (a raw reduce-motion read outside the helper file — breathing views must query the helper's decision function, not the environment value directly), or the build tail contains "** BUILD FAILED **"</fails_when>
  </verify>
  <done>Zero raw reduce-motion references outside the helper file, re-baselined enumeration pasted in the SUMMARY, app target builds.</done>
  <acceptance_criteria>
  - The gate grep prints zero result lines before RM_GATE_DONE
  - The SUMMARY records the re-baselined pre-sweep enumeration (per file and construct) and the post-sweep gate output
  - No behavior regression in normal-motion mode (decorative animations still animate when motion is allowed)
  - App scheme builds with "** BUILD SUCCEEDED **"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: D-11 breathing fallback — haptic pulses + text countdown, switchable in-session</name>
  <files>StressMonitor/StressMonitor/Views/Breathing/BreathingViewModel.swift, StressMonitor/StressMonitor/Views/Breathing/BreathingSessionView.swift, StressMonitor/StressMonitor/Views/Breathing/BreathingSummaryView.swift</files>
  <read_first>
  - StressMonitor/StressMonitor/Views/Breathing/BreathingViewModel.swift (the session state machine — where the fallback mode flag and the phase countdown live)
  - StressMonitor/StressMonitor/Views/Breathing/BreathingSessionView.swift + BreathingSummaryView.swift (the animated-guide UI to gate)
  - StressMonitor/StressMonitor/Views/Breathing/BreathingExerciseView.swift (the RM gate at line 14 and 126 — the pattern the fallback decision follows; BoxBreathingStep vocabulary at 298-307: Inhale/Hold/Exhale shortNames)
  - StressMonitor/StressMonitor/Views/Components/HapticManager.swift (the centralized haptic API — never call a UI feedback generator directly)
  - .planning/phases/03-accessibility-compliance/03-UI-SPEC.md Copywriting Contract (the exact fallback strings)
  </read_first>
  <action>
  Implement D-11 as a mode, not a stub. In BreathingViewModel add the animation-enabled state: it initializes from the helper's motion decision (Reduce Motion on -> animated guide OFF by default), the user may toggle it in-session, and the choice does not outlive the session (no persistence — nothing new stored). In BreathingSessionView: when the animated guide is off, the session presents the fallback — a text countdown status line with the exact contract copy ("Inhale — 3" / "Hold — 2" / "Exhale — 4", reusing the BoxBreathingStep shortNames) plus haptic pulses at each phase transition through HapticManager; the animated circle simply does not run. Add the in-session toggle with the exact label "Breathing animation" (ON = animated circle guide). BreathingSummaryView: replace any celebratory/scale animation with the static presentation under Reduce Motion (static badge + haptic via the helper — same D-12 rule, applied here because summary views are part of the D-11 surface set). Extract the fallback-default decision as a small pure function in the view model (motion-reduced input + prior user choice -> animation-enabled output) so it is unit-testable in principle and scriptable through the DEBUG seam for the phase UAT walkthrough. Breathing views take no blanket-helper adoption — their motion handling IS this fallback per D-11.
  </action>
  <verify>
    <automated>grep -c "Breathing animation" StressMonitor/StressMonitor/Views/Breathing/BreathingSessionView.swift; grep -c "Inhale — \|Hold — \|Exhale — " StressMonitor/StressMonitor/Views/Breathing/BreathingSessionView.swift; grep -c "HapticManager" StressMonitor/StressMonitor/Views/Breathing/BreathingViewModel.swift StressMonitor/StressMonitor/Views/Breathing/BreathingSessionView.swift; echo FALLBACK_GREP_DONE && xcodebuild build -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -3</automated>
    <fails_when>any of the three grep counts is 0 before FALLBACK_GREP_DONE (missing switch label, missing countdown copy, or missing haptic wiring), or the build tail contains "** BUILD FAILED **"</fails_when>
  </verify>
  <done>Under Reduce Motion a breathing session defaults to haptic + text countdown with the switch available; the animated guide is opt-in; nothing persists; app target builds.</done>
  <acceptance_criteria>
  - BreathingSessionView contains the "Breathing animation" toggle and the phase-word countdown strings matching BoxBreathingStep vocabulary exactly
  - Haptic pulses route through HapticManager (no direct feedback-generator calls added)
  - The fallback-default decision is a pure function in BreathingViewModel (inputs motion-reduced + user choice)
  - No new persisted state (no UserDefaults/key writes added to the breathing views)
  - App scheme builds with "** BUILD SUCCEEDED **"
  </acceptance_criteria>
</task>

</tasks>

<flagged_assumptions>
- **A11Y-03 edge probe (unclassified — flagged, not auto-resolved):** the consolidation baseline (66/13 at research time) is assumed to be re-measurable and closable to zero raw refs at gate time. Residual risk: refs drift in during the phase (other plans touch some of the same files). Mitigation in-plan: Task 2 re-baselines at execution and plan 03-06 re-runs the gate against the final post-deletion tree — the gate enumerates by file and construct, never by count.
- **No scripted simulator toggle:** Reduce Motion cannot be set via simctl on Xcode 26.3 (verified). The DEBUG seam is the scripted path; the human Settings toggle covers the rest at UAT. RM behavior walkthrough is a backstop marker (see plan 03-06 must_haves).
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DEBUG launch args -> Release binary | The -a11y-reduce-motion seam must never exist in a shipped build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | DEBUG reduce-motion launch-arg seam leaking into Release | high | mitigate | The seam enum is wrapped in the DEBUG-only conditional compilation block exactly like MockIAPMode (proven repo precedent); the helper's Release path is byte-unchanged; the acceptance grep confirms the symbol exists only inside the debug-conditional region |
| T-03-07 | Denial of Service | consolidation mis-gates essential session feedback | low | mitigate | D-11 fallback delivers haptic + text feedback by design; UAT walkthrough verifies the session remains operable motion-free |
| T-03-SC | Supply chain | package installs | low | accept | No installs — first-party only |
</threat_model>

<verification>
- RM gate grep: zero raw reduce-motion reads outside the helper file (output pasted in SUMMARY; final dated record in 03-06).
- App scheme builds after every task.
- Fallback copy/haptics greps pass; no new persisted state.
</verification>

<success_criteria>
- Motion optionality is owned by exactly one helper; the trust-gate grep is clean by construction, not by exclusion list.
- A Reduce Motion user can complete a breathing session with full feedback and can choose the animated guide explicitly.
- Release builds contain no verification seam.
</success_criteria>

<artifacts_this_phase_produces>
New symbols/files created by this plan:
- The single motion helper API in Animation+Wellness.swift (env-reading modifier retained; pure motion-decision function; DEBUG-only launch-arg seam enum "-a11y-reduce-motion" with injectable arguments)
- Behavior changes: accessible-transition family cross-fades under RM; AnimationPresets routed through the helper; AccessibleAnimationModifier folded (per-body-evaluation bug gone); press-scale stops under RM
- Breathing fallback: animation-enabled state + pure default-decision function in BreathingViewModel; "Breathing animation" toggle; countdown status line copy; HapticManager phase pulses
- Deleted symbols: the explicit-Bool Animation.wellness/breathing/fidget/shake/dizzy factories (after caller migration)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-accessibility-compliance/03-05-SUMMARY.md` when done
</output>
