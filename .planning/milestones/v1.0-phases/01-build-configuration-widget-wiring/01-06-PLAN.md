---
phase: 01-build-configuration-widget-wiring
plan: 06
type: execute
wave: 2
depends_on: ["01-05"]
gap_closure: true
files_modified:
  - scripts/verify-portal-match.sh
  - scripts/verify-asc-archive.sh
  - scripts/verify-widget-real-device.md
autonomous: false
requirements: [BUILD-01, BUILD-02, WIRE-01]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A shell script exists at scripts/verify-portal-match.sh that the developer runs with their Match/ASC credentials to independently confirm the Apple Developer Portal App Groups capability state and Fastlane Match profile regeneration — closing the attestation-only gap flagged in WINDOWS.md item #2."
    - "A shell script exists at scripts/verify-asc-archive.sh that guides the developer through archiving the app in Release configuration and running ASC's pre-upload Privacy Manifest validation — closing the BUILD-01 gap where no local tool wraps ASC's own validator."
    - "A structured manual test procedure exists at scripts/verify-widget-real-device.md covering the full WIRE-01 real-device verification: take a measurement, background the app, confirm the widget refreshes with real data (not placeholder) and does not fatalError — closing the WIRE-01 present-behavior-unverified gap."
    - statement: "The verify-portal-match.sh script passes credentialed validation (Match git repo contains profiles embedding the group.stress.ai.com entitlement) when run by a developer with MATCH_GIT_URL and APP_STORE_CONNECT_API_KEY_ID set."
      verification: backstop
    - statement: "An ASC archive upload (Release configuration) passes pre-upload validation with no Privacy Manifest error — the 3 PrivacyInfo.xcprivacy files are accepted by Apple's own validator."
      verification: backstop
    - statement: "On a real device, the home-screen widget updates to reflect a stress measurement taken seconds earlier, within its next timeline refresh window, without a fatalError crash from WidgetDataProvider.init."
      verification: backstop
  artifacts:
    - path: "scripts/verify-portal-match.sh"
      provides: "Credentialed verification script for Developer Portal App Groups + Fastlane Match profile state"
    - path: "scripts/verify-asc-archive.sh"
      provides: "Archive + ASC pre-upload validation procedure for Privacy Manifest acceptance"
    - path: "scripts/verify-widget-real-device.md"
      provides: "Structured manual test procedure for real-device widget refresh verification"
  key_links:
    - from: "scripts/verify-portal-match.sh"
      to: "fastlane/Matchfile (git_url, app_identifier)"
      via: "fastlane match appstore --readonly invocation with developer credentials"
      pattern: "fastlane match appstore --readonly"
    - from: "scripts/verify-asc-archive.sh"
      to: "StressMonitor scheme (Release configuration archive)"
      via: "xcodebuild archive + xcrun altool --validate-app or Xcode Organizer upload"
      pattern: "xcodebuild archive"
---

<objective>
Close the three verification gaps that are inherently human/external — no agent can close them with
code changes alone:

1. **BUILD-02** (PRESENT_BEHAVIOR_UNVERIFIED): The code-level App Group wiring is verified
   (`group.stress.ai.com` in all 3 targets' entitlements), but Developer Portal capability state and
   Fastlane Match profile regeneration rest on user attestation only (WINDOWS.md item #2, open). This
   plan creates a credentialed verification script the developer runs to independently confirm Portal
   state.

2. **WIRE-01** (PRESENT_BEHAVIOR_UNVERIFIED): The full data flow is confirmed end-to-end in source
   (`StressRepository.save()` → `WidgetPublisher.publish()` → App Group `UserDefaults` →
   `WidgetDataProvider` → `StressWidgetProvider.getTimeline` → view `dataState` branching), but no
   real device is available to an agent. This plan documents a structured manual test procedure.

3. **BUILD-01** (UNCERTAIN/human_needed): All 3 `PrivacyInfo.xcprivacy` files are `plutil`-lint clean
   with correct reason code `1C8F.1`. No local tool can run ASC's own validator. This plan documents
   the archive + ASC pre-upload validation procedure.

Purpose: These three gaps were correctly routed to human verification by all 4 SUMMARY.md files — none
of them were introduced by overclaiming. The gap-closure action is creating structured, repeatable
verification procedures so the developer can close them systematically rather than relying on
attestation alone.

Output: Three verification scripts/procedures. No application code changes. The human-verification
checkpoints are the mechanism that closes these gaps — the scripts are the artifact that makes the
verification repeatable and documented.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-configuration-widget-wiring/01-CONTEXT.md
@.planning/phases/01-build-configuration-widget-wiring/01-VERIFICATION.md
@.planning/WINDOWS.md
@fastlane/Matchfile
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Create credentialed verification scripts for Developer Portal/Match state (BUILD-02) and ASC Privacy Manifest upload (BUILD-01)</name>
  <read_first>
    Read fastlane/Matchfile in full — it reads `git_url(ENV["MATCH_GIT_URL"])` and
    `app_identifier` from the project. Read fastlane/Fastfile to identify the `match` lanes
    available (the `setup_match` lane and any `appstore`/`development` readonly lanes). Read all 3
    PrivacyInfo.xcprivacy files (`StressMonitor/StressMonitor/PrivacyInfo.xcprivacy`,
    `StressMonitor/StressMonitorWidget/PrivacyInfo.xcprivacy`,
    `StressMonitor/StressMonitorWatch Watch App/PrivacyInfo.xcprivacy`) to document the exact reason
    codes the ASC validator will see. Read 01-VERIFICATION.md's human_verification section (lines
    27-36) for the exact Portal-check and ASC-validation procedures to codify.
  </read_first>
  <files>scripts/verify-portal-match.sh, scripts/verify-asc-archive.sh</files>
  <action>
    (1) Create scripts/verify-portal-match.sh — a bash script with `set -euo pipefail` that the
    developer runs locally with their Match/ASC credentials set in the environment
    (MATCH_GIT_URL, APP_STORE_CONNECT_API_KEY_ID, APP_STORE_CONNECT_ISSUER_ID, and the API key path).
    The script must: check that all three required env vars are set (print a clear error listing
    which are missing if not); run `bundle exec fastlane match appstore --readonly` from the repo
    root; on success, print a confirmation message listing the three App IDs
    (`stress.ai.com`, `stress.ai.com.watchkitapp`, `stress.ai.com.widget`) and the App Group
    (`group.stress.ai.com`) that the profiles should embed; on failure, print the Match error output
    and note that the Developer Portal UI is the fallback (list the exact Portal navigation path:
    Certificates, Identifiers & Profiles → Identifiers → select each App ID → App Groups capability).
    Make the script executable (`chmod +x`).

    (2) Create scripts/verify-asc-archive.sh — a bash script with `set -euo pipefail` that guides the
    developer through archiving the app in Release configuration and validating it. The script must:
    run `xcodebuild archive -project StressMonitor/StressMonitor.xcodeproj -scheme StressMonitor
    -configuration Release -archivePath build/StressMonitor.xcarchive -skipPackagePluginValidation`
    (note: this requires real signing, not `CODE_SIGNING_REQUIRED=NO`); on archive success, attempt
    `xcrun altool --validate-app -f build/StressMonitor.xcarchive -t xcarchive -u
    $APP_STORE_CONNECT_USERNAME -p $APP_STORE_CONNECT_PASSWORD` if altool is available, or print
    instructions to use Xcode Organizer → Window → Organizer → Distribute App → App Store Connect
    → Upload (with "Validate" step) as the fallback; document that the specific check is "no Privacy
    Manifest validation error is raised." Make the script executable (`chmod +x`).
  </action>
  <verify>
    <automated>
      test -f scripts/verify-portal-match.sh && test -x scripts/verify-portal-match.sh && echo "portal-match script exists and is executable"
      test -f scripts/verify-asc-archive.sh && test -x scripts/verify-asc-archive.sh && echo "asc-archive script exists and is executable"
      grep -c 'MATCH_GIT_URL' scripts/verify-portal-match.sh
      grep -c 'group.stress.ai.com' scripts/verify-portal-match.sh
      grep -c 'PrivacyInfo' scripts/verify-asc-archive.sh
      bash -n scripts/verify-portal-match.sh && echo "portal-match script syntax OK"
      bash -n scripts/verify-asc-archive.sh && echo "asc-archive script syntax OK"
    </automated>
  </verify>
  <acceptance_criteria>
    Both files exist and are executable (`test -x` returns 0). `grep -c 'MATCH_GIT_URL'` on
    verify-portal-match.sh returns at least 1. `grep -c 'group.stress.ai.com'` on verify-portal-match.sh
    returns at least 1. `grep -c 'PrivacyInfo'` on verify-asc-archive.sh returns at least 1. `bash -n`
    on both scripts exits 0 (valid bash syntax, no runtime execution needed).
  </acceptance_criteria>
  <done>
    Two executable shell scripts exist in scripts/ that the developer can run with their credentials
    to independently verify the Portal/Match state (BUILD-02) and the ASC Privacy Manifest validation
    (BUILD-01), replacing the current attestation-only evidence.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of real-device widget refresh, Portal/Match state, and ASC Privacy Manifest acceptance (BUILD-02, WIRE-01, BUILD-01)</name>
  <read_first>
    Read 01-VERIFICATION.md's behavior_unverified_items section (lines 18-26) and
    human_verification section (lines 27-36) — these define the exact three procedures this
    checkpoint consolidates. Read the scripts created in Task 1 (scripts/verify-portal-match.sh,
    scripts/verify-asc-archive.sh) for the exact commands to run. Read
    StressMonitor/StressMonitorWidget/Providers/StressWidgetProvider.swift to confirm the
    getTimeline implementation the real-device test will exercise.
  </read_first>
  <what-built>
    All code-level work for BUILD-01 (3 valid PrivacyInfo.xcprivacy files), BUILD-02 (App Group
    entitlement wired in all 3 targets), and WIRE-01 (full widget data-flow chain from
    StressRepository.save through WidgetPublisher to StressWidgetProvider.getTimeline with
    fresh/stale/empty state resolution) is complete and build-verified. The remaining gap is that
    these three truths have only been verified in source code, not in their real execution
    environments (physical device, Apple Developer Portal, App Store Connect validator).
  </what-built>
  <how-to-verify>
    Complete all three verification procedures below. Each can be done independently.

    **Procedure A — Portal/Match state (BUILD-02, closes WINDOWS.md item #2):**
    1. In a terminal with your Match/ASC credentials set (MATCH_GIT_URL,
       APP_STORE_CONNECT_API_KEY_ID, APP_STORE_CONNECT_ISSUER_ID, ASC_API_KEY_PATH), run:
       `bash scripts/verify-portal-match.sh`
    2. Expected: `fastlane match appstore --readonly` succeeds, confirming the cached App Store
       profiles embed the `group.stress.ai.com` entitlement for all three App IDs.
    3. Fallback (if Match CLI is unavailable): Open
       https://developer.apple.com/account/resources/identifiers/list → click each of the three App
       IDs (`stress.ai.com`, `stress.ai.com.watchkitapp`, `stress.ai.com.widget`) → confirm "App
       Groups" capability is enabled with `group.stress.ai.com` assigned.

    **Procedure B — ASC Privacy Manifest upload (BUILD-01):**
    1. Run: `bash scripts/verify-asc-archive.sh`
    2. Expected: The Release archive builds and the ASC pre-upload validation step reports no
       Privacy Manifest validation error.
    3. Fallback: Open Xcode → Product → Archive → Distribute App → App Store Connect → Upload. The
       uploader runs ASC's own PrivacyInfo.xcprivacy validator before completing the upload. Confirm
       no "Privacy Manifest" error appears.

    **Procedure C — Real-device widget refresh (WIRE-01):**
    1. Install the app on a physical iPhone (Release or Debug build with the wired entitlements).
    2. Launch the app with `-demo-mode` launch argument (or use real HealthKit data).
    3. Wait for a stress measurement to complete on the Dashboard (the stress score updates).
    4. Add the StressMonitor widget to the home screen (Small, Medium, or Large).
    5. Background the app. Wait up to 15 minutes for the widget's next timeline refresh, or force a
       refresh by long-pressing the widget → Edit Widget (if available).
    6. Expected: The widget shows the real stress level and category from the measurement just taken,
       not a placeholder or stale value. The app does not crash with a `fatalError` from
       `WidgetDataProvider.init` (which would indicate the App Group suite is nil).
    7. Document the result: which widget family was tested, whether the refresh showed real data, and
       whether any crash occurred.
  </how-to-verify>
  <resume-signal>Type "approved" with the results of all three procedures (A, B, C), or describe which procedures passed/failed and what errors were observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer credentials ↔ Apple Developer Portal | The verification scripts access the Portal/ASC via the developer's own credentials; no credentials are stored in the scripts themselves |
| Physical device ↔ App Group container | The real-device widget test exercises the OS-level App Group container access boundary that the widget entitlement enables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06-G1 | Information Disclosure | verify-portal-match.sh credential handling | low | accept | The script reads credentials from environment variables only — no hardcoded secrets. The developer runs it locally in their own terminal. No credentials are logged to disk. |
| T-01-06-G2 | Tampering | verify-asc-archive.sh Release archive | low | accept | The Release archive requires real code signing, which is the developer's existing workflow. The script does not modify signing configuration — it uses the project's existing Match-managed signing setup. |

</threat_model>

<verification>
The scripts created in Task 1 pass `bash -n` syntax validation and contain the required content
patterns. Task 2 is a blocking human-verification checkpoint that the developer must complete with
their credentials and physical device. When all three procedures (A, B, C) pass, the three
verification gaps (BUILD-02, WIRE-01, BUILD-01) are closed and WINDOWS.md items #1 and #2 can be
marked fixed.
</verification>

<success_criteria>
- scripts/verify-portal-match.sh exists, is executable, and passes bash syntax validation.
- scripts/verify-asc-archive.sh exists, is executable, and passes bash syntax validation.
- A structured real-device widget test procedure is documented and checkpointed.
- The human-verification checkpoint is completed by the developer with results for all three
  procedures (A, B, C).
</success_criteria>

## Artifacts This Phase Produces

- `scripts/verify-portal-match.sh` — credentialed verification script for Apple Developer Portal App
  Groups capability + Fastlane Match profile state (closes BUILD-02 attestation-only gap)
- `scripts/verify-asc-archive.sh` — Release archive + ASC pre-upload Privacy Manifest validation
  procedure (closes BUILD-01 no-local-validator gap)
- `scripts/verify-widget-real-device.md` — structured manual test procedure for real-device widget
  refresh verification (closes WIRE-01 no-real-device gap)

<output>
Create `.planning/phases/01-build-configuration-widget-wiring/01-06-SUMMARY.md` when done
</output>
