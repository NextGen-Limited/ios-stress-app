---
phase: 01-build-configuration-widget-wiring
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .github/workflows/_test.yml
  - StressMonitor/StressMonitor.xcodeproj/xcshareddata/xcschemes/StressMonitor.xcscheme
autonomous: true
requirements: [BUILD-04]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The reusable CI workflow .github/workflows/_test.yml contains a test job that runs xcodebuild test against the StressMonitor scheme on a fresh macos-15 runner, providing the clean-CoreSimulator execution path this dev host cannot (per D-04 BUILD-04 gap)."
    - "The StressMonitor.xcscheme TestAction contains exactly one TestableReference (StressMonitorTests); the dangling StressMonitorUITests TestableReference referencing non-existent BlueprintIdentifier F2E2EC012F1CC556000C2B53 is removed so xcodebuild test never attempts to resolve a phantom UI-test target."
    - "The CI test job carries its own DerivedData + SPM cache configuration matching the existing lint-and-build job's caching keys, so the test build does not cold-resolve packages every run."
    - statement: "The test job's -destination flag resolves to a real iOS Simulator on the macos-15 CI runner (CI provides a fresh CoreSimulator every run, unlike this dev host where 4+ consecutive attempts failed at the device-pairing layer)."
      verification: backstop
  artifacts:
    - path: ".github/workflows/_test.yml"
      provides: "test CI job executing xcodebuild test on macos-15 runner"
    - path: "StressMonitor/StressMonitor.xcodeproj/xcshareddata/xcschemes/StressMonitor.xcscheme"
      provides: "TestAction with only the StressMonitorTests TestableReference (UITests phantom reference removed)"
  key_links:
    - from: ".github/workflows/ci.yml (workflow_call to _test.yml)"
      to: ".github/workflows/_test.yml (test job)"
      via: "reusable workflow invocation — all jobs in _test.yml run automatically"
      pattern: "test:"
    - from: "StressMonitor.xcscheme TestAction Testables"
      to: "StressMonitorTests PBXNativeTarget"
      via: "single TestableReference BlueprintIdentifier 0685AF23B64355DB99B05140"
      pattern: "StressMonitorTests.xctest"
---

<objective>
Close the BUILD-04 verification gap: `xcodebuild test` has never completed a run on this dev host
(4+ consecutive failures at the CoreSimulator device-pairing layer across 2 sessions, tracked as
WINDOWS.md item #1). The test target compiles cleanly (`TEST BUILD SUCCEEDED`), 11 test files are
registered — the gap is purely runtime execution, not code or wiring. Adding a `test` job to the CI
workflow provides a fresh macos-15 CoreSimulator every run, which is the recommended remediation per
the VERIFICATION.md gap analysis and WINDOWS.md item #1.

This plan also removes the dangling `StressMonitorUITests` TestableReference from the scheme — it
references BlueprintIdentifier `F2E2EC012F1CC556000C2B53` / BuildableName `StressMonitorUITests.xctest`,
but no matching PBXNativeTarget exists in project.pbxproj (confirmed by 01-01-SUMMARY.md). This phantom
reference can cause `xcodebuild test` to waste time resolving a non-existent target.

Purpose: BUILD-04's literal success criterion is "`xcodebuild test` executes a real unit-test bundle and
reports pass/fail." The code and target wiring are proven correct (TEST BUILD SUCCEEDED); the missing
piece is a reliable execution environment. CI provides that.

Output: A CI test job in `_test.yml` and a cleaned scheme file. No application code changes.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-configuration-widget-wiring/01-CONTEXT.md
@.planning/phases/01-build-configuration-widget-wiring/01-VERIFICATION.md
@.planning/WINDOWS.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Add a test job to the CI workflow that runs xcodebuild test on a fresh macos-15 runner (BUILD-04)</name>
  <read_first>
    Read .github/workflows/_test.yml in full (142 lines) — it has three jobs: `lint-and-build`
    (lines 12-72), `build-watchos` (lines 74-105), `build-widget` (lines 107-142). The `test` job
    must be added as a fourth job after `build-widget`, reusing the same runner setup (macos-15,
    Xcode 26.3 selection, xcpretty install, DerivedData + SPM caching) that `lint-and-build` already
    establishes. Read .github/workflows/ci.yml to confirm it calls `_test.yml` via `uses:
    ./.github/workflows/_test.yml` with no job-level filter — all jobs in `_test.yml` run
    automatically on every PR/dispatch.
  </read_first>
  <files>.github/workflows/_test.yml</files>
  <action>
    Add a new `test` job to .github/workflows/_test.yml after the existing `build-widget` job block.
    The job must: (1) use `runs-on: macos-15` and `timeout-minutes: 30`, matching the existing jobs;
    (2) include the same checkout, Xcode-version-selection (`sudo xcode-select -s
    /Applications/Xcode_${{ env.XCODE_VERSION }}.app/Contents/Developer`), and xcpretty-install steps
    as `lint-and-build`; (3) include DerivedData and SPM cache steps using the identical cache keys
    as `lint-and-build` (`key: derived-${{ runner.os }}-${{ hashFiles('StressMonitor/StressMonitor.xcodeproj/project.pbxproj') }}`
    for DerivedData, `key: spm-${{ runner.os }}-${{ hashFiles('**/Package.resolved') }}` for SPM); (4)
    run `xcodebuild test` with the flags: `-project StressMonitor/StressMonitor.xcodeproj`, `-scheme
    "$SCHEME"`, `-destination 'platform=iOS Simulator,name=iPhone 16,OS=latest'`,
    `-derivedDataPath build`, `-skipPackagePluginValidation`, `CODE_SIGN_IDENTITY=""`,
    `CODE_SIGNING_REQUIRED=NO`, piped through `xcpretty` with `set -o pipefail`. Use the env var
    `$SCHEME` already defined at the workflow level (`SCHEME: "StressMonitor"`). Do NOT add
    `-only-testing:StressMonitorTests` — the full scheme test action should run (the scheme's
    TestAction already scopes to the registered TestableReferences).
  </action>
  <verify>
    <automated>
      python3 -c "import yaml; doc=yaml.safe_load(open('.github/workflows/_test.yml')); assert 'test' in doc['jobs'], 'no test job'; steps=[s for s in doc['jobs']['test']['steps']]; cmds=' '.join(str(s.get('run','')) for s in steps); assert 'xcodebuild test' in cmds, 'no xcodebuild test in test job'; assert 'macos-15' in doc['jobs']['test']['runs-on'], 'wrong runner'; print('test job valid')"
      grep -c 'xcodebuild test' .github/workflows/_test.yml
    </automated>
  </verify>
  <acceptance_criteria>
    python3 YAML parse succeeds and confirms a `test` job exists in the `jobs` dict; the test job's
    steps contain `xcodebuild test` with `-scheme "$SCHEME"` and `-destination 'platform=iOS
    Simulator,name=iPhone 16,OS=latest'`; the test job uses `runs-on: macos-15`; DerivedData and SPM
    cache steps are present with matching keys to `lint-and-build`; `grep -c 'xcodebuild test'
    .github/workflows/_test.yml` returns at least 1.
  </acceptance_criteria>
  <done>
    .github/workflows/_test.yml contains a syntactically valid `test` job that runs `xcodebuild test`
    on a macos-15 runner with proper caching. The next CI run on a PR or manual dispatch will execute
    the StressMonitorTests bundle on a fresh CoreSimulator.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the dangling StressMonitorUITests TestableReference from the scheme</name>
  <read_first>
    Read StressMonitor/StressMonitor.xcodeproj/xcshareddata/xcschemes/StressMonitor.xcscheme in full.
    The TestAction's Testables block contains two TestableReference entries: the first references
    StressMonitorTests (BlueprintIdentifier 0685AF23B64355DB99B05140, BuildableName
    StressMonitorTests.xctest — a real target), the second references StressMonitorUITests
    (BlueprintIdentifier F2E2EC012F1CC556000C2B53, BuildableName StressMonitorUITests.xctest — no
    matching PBXNativeTarget exists in project.pbxproj, confirmed by 01-01-SUMMARY.md's "no
    project.pbxproj native target search shows StressMonitorUITests exists"). Read the full file to
    identify the exact XML block to remove.
  </read_first>
  <files>StressMonitor/StressMonitor.xcodeproj/xcshareddata/xcschemes/StressMonitor.xcscheme</files>
  <action>
    Remove the entire second TestableReference XML element from the TestAction's Testables block in
    StressMonitor.xcscheme — the one whose BuildableReference carries BlueprintName
    "StressMonitorUITests" and BlueprintIdentifier "F2E2EC012F1CC556000C2B53". This means deleting
    from the opening `<TestableReference` tag through its closing `</TestableReference>` tag for that
    specific entry. Leave the first TestableReference (StressMonitorTests, BlueprintIdentifier
    0685AF23B64355DB99B05140) untouched. Do not modify any other section of the scheme (BuildAction,
    LaunchAction, ProfileAction, AnalyzeAction, ArchiveAction). The git status already shows this
    file as modified (M) from prior work — read the current on-disk state before editing.
  </action>
  <verify>
    <automated>
      grep -c 'StressMonitorUITests' StressMonitor/StressMonitor.xcodeproj/xcshareddata/xcschemes/StressMonitor.xcscheme
      grep -c 'StressMonitorTests' StressMonitor/StressMonitor.xcodeproj/xcshareddata/xcschemes/StressMonitor.xcscheme
      xmllint --noout StressMonitor/StressMonitor.xcodeproj/xcshareddata/xcschemes/StressMonitor.xcscheme 2>&1 || echo "xmllint not available, skipping XML validation"
    </automated>
  </verify>
  <acceptance_criteria>
    `grep -c 'StressMonitorUITests'` on the scheme file returns 0; `grep -c 'StressMonitorTests'`
    returns at least 1 (the valid TestableReference remains); the file is still well-formed XML
    (xmllint --noout exits 0 if xmllint is available).
  </acceptance_criteria>
  <done>
    The scheme's TestAction contains exactly one TestableReference (StressMonitorTests). The phantom
    StressMonitorUITests reference no longer causes xcodebuild test to attempt resolving a
    non-existent target.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner ↔ GitHub Actions secrets | The test job runs on a GitHub-hosted macos-15 runner; no project secrets are accessed by the test execution |
| CI runner ↔ SPM package registry | The test job resolves SPM packages from the cached resolved-versions; no new package introductions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05-G1 | Tampering | CI _test.yml test job | low | accept | The test job runs `CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO` matching the existing build jobs — no signing credentials are involved. The job adds no new secret access beyond what lint-and-build already uses. |
| T-01-05-G2 | Denial of Service | CI test job timeout | low | mitigate | timeout-minutes: 30 caps the test job runtime. If a simulator hangs, the job is killed rather than consuming runner minutes indefinitely. Matches the existing lint-and-build job's timeout. |

</threat_model>

<verification>
After both tasks: the CI workflow YAML is parseable and contains a `test` job with `xcodebuild test`;
the scheme file has zero `StressMonitorUITests` references and at least one `StressMonitorTests`
reference; `xmllint --noout` on the scheme succeeds. The actual pass/fail signal from the test bundle
will come from the next CI run on a PR or manual dispatch — this plan delivers the execution path, not
the runtime result (which requires a CI runner this dev host cannot provide).
</verification>

<success_criteria>
- .github/workflows/_test.yml has a `test` job that runs `xcodebuild test` on macos-15.
- StressMonitor.xcscheme has no StressMonitorUITests TestableReference.
- The next CI run provides the BUILD-04 pass/fail signal on a clean CoreSimulator.
</success_criteria>

## Artifacts This Phase Produces

- New CI job: `test` in `.github/workflows/_test.yml` — runs `xcodebuild test` against the
  StressMonitor scheme on a macos-15 runner with DerivedData + SPM caching
- Modified scheme: `StressMonitor.xcscheme` TestAction reduced to one TestableReference
  (StressMonitorTests only; phantom UITests reference removed)

<output>
Create `.planning/phases/01-build-configuration-widget-wiring/01-05-SUMMARY.md` when done
</output>
