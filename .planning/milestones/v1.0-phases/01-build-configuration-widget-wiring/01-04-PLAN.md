---
phase: 01-build-configuration-widget-wiring
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
  - docs/project-overview-pdr.md
  - docs/system-architecture.md
  - docs/system-architecture-platform.md
  - docs/INDEX.md
  - docs-site/legal/privacy.md
  - docs-site/vi/legal/privacy.md
autonomous: true
requirements: [BUILD-01]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CLAUDE.md's Privacy & Security section no longer claims zero external API calls — it discloses that derived stress-context (HRV, resting heart rate, sleep, activity, recovery) is sent to the /chat Supabase Edge Function under an authenticated session."
    - "docs/project-overview-pdr.md, docs/system-architecture.md, docs/system-architecture-platform.md, and docs/INDEX.md no longer describe the /chat payload as anonymized or claim health data never leaves the device — each names the specific fields transmitted and states the request is session-linked (Bearer JWT), not anonymous."
    - "docs-site/legal/privacy.md and its Vietnamese mirror docs-site/vi/legal/privacy.md each carry a new AI Coaching Chat section disclosing the same facts, and no longer claim zero data is ever transmitted to StressMonitor's servers."
    - "The ASC privacy nutrition label itself is explicitly out of this plan's scope — it is Phase 5's SHIP-03, gated on this same D3/D-01 resolution but executed separately in App Store Connect's UI."
  artifacts:
    - path: "docs-site/legal/privacy.md"
      provides: "Corrected, disclosed privacy policy including AI Coaching Chat data flow"
    - path: "docs-site/vi/legal/privacy.md"
      provides: "Vietnamese mirror of the same disclosure"
  key_links: []
  prohibitions: []
---

<objective>
Correct every doc that currently misrepresents the AI coaching chat's data flow as "anonymized" or
"never leaves the device", per D-01's locked decision: the backend contract is authoritative — the
app's own docs and privacy policy must accurately disclose which derived health fields the /chat
request actually transmits, not the other way around.

Purpose: D-01 (CONTEXT.md) states verbatim that "the current 'never sent' claim is false and must
not survive this phase." A grep across the repo this session confirmed the claim is still live in
6 files (CLAUDE.md, docs/project-overview-pdr.md, docs/system-architecture.md,
docs/system-architecture-platform.md, docs/INDEX.md) and the actual privacy policy
(docs-site/legal/privacy.md + its Vietnamese mirror) doesn't mention the AI Chat feature at all.
This is independent, parallel-safe work — no file overlap with 01-01/01-02/01-03.

Output: six architecture/root docs and two privacy-policy files corrected to disclose the real
/chat data flow, consistent with 01-01-PLAN.md's PrivacyInfo.xcprivacy disclosure.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-build-configuration-widget-wiring/01-CONTEXT.md
@.planning/phases/01-build-configuration-widget-wiring/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Correct the false non-transmission claim in root/architecture docs</name>
  <read_first>
    Run `git diff HEAD -- CLAUDE.md README.md docs/project-overview-pdr.md
    docs/system-architecture.md docs/system-architecture-platform.md docs/INDEX.md` to confirm
    none of these six files' privacy claims have already been corrected this session (README.md's
    existing diff only touches dependency-count and date lines, unrelated to this task). Confirmed
    this session via grep: CLAUDE.md line ~494 ("No external API calls or servers"),
    docs/project-overview-pdr.md line ~207, docs/system-architecture.md lines ~341/~344,
    docs/system-architecture-platform.md lines ~259/~261, and docs/INDEX.md line ~191 all currently
    describe the /chat payload as anonymized or claim health data never leaves the device.
  </read_first>
  <files>CLAUDE.md, README.md, docs/project-overview-pdr.md, docs/system-architecture.md, docs/system-architecture-platform.md, docs/INDEX.md</files>
  <reversibility rating="costly">Implements D-01's disclosure correction. Reversing later means walking back an already-published, publicly-readable claim about data transmission, which reads as a regression once users or App Review have seen the corrected version.</reversibility>
  <action>
    In CLAUDE.md's "## Privacy &amp; Security" section (line ~494), correct the line asserting the
    app makes zero external network calls — the AI Coaching Chat feature does call an external
    Supabase Edge Function — to instead disclose that derived stress-context (HRV, resting heart
    rate, sleep, activity, recovery — never raw HealthKit samples) is sent to the /chat backend
    under an authenticated session. Leave the adjacent third-party-analytics/tracking line
    untouched (still accurate, unrelated to chat). In docs/project-overview-pdr.md:207,
    docs/system-architecture.md:341+344, docs/system-architecture-platform.md:259+261, and
    docs/INDEX.md:191, correct the phrasing that currently describes the transmitted payload as
    anonymized and states health data never leaves the device — replace it with wording naming the
    specific fields transmitted and stating the request carries a Bearer-JWT-authenticated session,
    so the payload is linked to the user's identity rather than anonymous. README.md's existing
    "Privacy-First" bullet (about third-party analytics, not chat) does not need to change. The ASC
    privacy nutrition label itself is out of this task's scope — that's Phase 5's SHIP-03, gated on
    this same decision but executed separately in App Store Connect's UI.
  </action>
  <verify>
    <automated>
      grep -v '^#' CLAUDE.md | grep -c 'No external API calls or servers'
      grep -c 'anonymized chat context' docs/project-overview-pdr.md docs/system-architecture.md docs/system-architecture-platform.md docs/INDEX.md | awk -F: '{s+=$2} END{print s}'
      grep -c 'Supabase Edge Function\|stress-context' CLAUDE.md
    </automated>
  </verify>
  <acceptance_criteria>
    First grep count equals 0; the aggregate "anonymized chat context" count across the four docs
    equals 0; CLAUDE.md now positively discloses the Supabase Edge Function call (count >= 1).
  </acceptance_criteria>
  <done>
    None of the 6 files retain an absolute "no data leaves the device"/"anonymized" claim about the
    AI Chat feature; each now names the specific derived fields transmitted and discloses the
    session-linkage.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add an AI Coaching Chat disclosure section to the privacy policy (EN + VI)</name>
  <read_first>
    Read docs-site/legal/privacy.md and docs-site/vi/legal/privacy.md in full (both 45 lines,
    line-for-line structurally identical) — neither file currently mentions the AI Coaching Chat
    feature or the Supabase backend at all. Line 17 (EN) / line 17 (VI) each assert zero data is
    ever transmitted to StressMonitor's servers, which contradicts the /chat feature's actual
    behavior.
  </read_first>
  <files>docs-site/legal/privacy.md, docs-site/vi/legal/privacy.md</files>
  <action>
    In docs-site/legal/privacy.md, add a new "## AI Coaching Chat" section (positioned after
    "## iCloud Sync", before "## HealthKit") disclosing: when the user opens AI Coaching Chat,
    derived values (HRV, resting heart rate, sleep, activity, recovery, and the computed stress
    score — never raw HealthKit samples) are sent to StressMonitor's backend to generate a coaching
    response; this data rides an authenticated session, not an anonymous one; chat messages and
    this derived context are retained per the backend's own chat-history retention, separate from
    the on-device health store. In the "## Data We Do Not Collect" section, correct the bullet
    asserting zero data ever reaches the company's servers so it no longer contradicts the new AI
    Coaching Chat section — qualify it to scope correctly given that feature exists. Update the
    "*Last updated:*" date to the date this edit lands. Apply the identical structural correction
    to docs-site/vi/legal/privacy.md: add a new "## Trò Chuyện Cùng AI" heading in the same relative
    position (after "## Đồng bộ iCloud", before "## HealthKit") disclosing the same facts in
    Vietnamese, and correct the equivalent zero-transmission bullet the same way. Exact Vietnamese
    wording is a translation judgment call; the disclosed facts (which fields, session-linkage)
    must match the EN version.
  </action>
  <verify>
    <automated>
      grep -c '^## AI Coaching Chat' docs-site/legal/privacy.md
      grep -c '^## Trò Chuyện Cùng AI' docs-site/vi/legal/privacy.md
      grep -v '^#' docs-site/legal/privacy.md | grep -c 'No data is transmitted to our servers'
      grep -v '^#' docs-site/vi/legal/privacy.md | grep -c 'Không truyền dữ liệu lên máy chủ của chúng tôi'
    </automated>
  </verify>
  <acceptance_criteria>
    Both new-heading grep counts equal 1; both old-claim grep counts equal 0.
  </acceptance_criteria>
  <done>
    Both privacy-policy files disclose the AI Coaching Chat data flow with the same factual
    content, in the same structural position; the old absolute "nothing is transmitted" claim is
    gone from both.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App's user-facing docs/policy ↔ end user / App Review | Documentation is the trust artifact a user or reviewer reads to understand data handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information Disclosure (to users/reviewers, not attackers) | CLAUDE.md/README.md/docs/*.md + docs-site/legal/privacy.md (EN+VI) | medium | mitigate | Tasks 1-2 correct all 8 files' non-transmission/anonymization claims to disclose the actual /chat payload contents and session-linkage, consistent with 01-01-PLAN.md's PrivacyInfo.xcprivacy correction. |

</threat_model>

<verification>
Full-suite gate: none of the 8 files retain the old absolute non-transmission/anonymization claims;
both privacy-policy files carry a new, factually-consistent AI Coaching Chat disclosure section.
</verification>

<success_criteria>
Every doc D-01 named (CLAUDE.md, README.md, docs/system-architecture*.md, the privacy policy) is
internally consistent with the actual /chat data flow and with 01-01-PLAN.md's manifest correction.
</success_criteria>

## Artifacts This Phase Produces (this plan's share)

No new files — this plan edits 8 existing documentation files in place.

<output>
Create `.planning/phases/01-build-configuration-widget-wiring/01-04-SUMMARY.md` when done
</output>
