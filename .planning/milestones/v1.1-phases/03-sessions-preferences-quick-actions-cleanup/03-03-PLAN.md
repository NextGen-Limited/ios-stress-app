---
phase: 03-sessions-preferences-quick-actions-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - StressMonitor/StressMonitor/Models/ServerQuickAction.swift
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+QuickActions.swift
  - StressMonitor/StressMonitor/Services/LLM/ChatQuickActions.swift
  - StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift
  - StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift
  - StressMonitor/StressMonitor/Views/Chat/QuickActionChipsView.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  - StressMonitor/StressMonitorTests/StressAPIClientQuickActionsTests.swift
  - StressMonitor/StressMonitorTests/ChatHistoryRestoreTests.swift
autonomous: true
requirements: [derived-PREF-02, derived-QA-01, AUTH-03]
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The chat sheet opens instantly with the local fallback chips (from ChatQuickActions, contextualized to stress category); when GET /quick-actions lands — called with the live stress level and the user's language/coaching style — the chips swap to the server-suggested set (no loading state, no empty state)"
    - "Tapping any chip sends its resolved prompt through the existing send() → /chat path (credit-metered, session-persisted, streamed) — server chips resolve their prompt from a local id→prompt map mirroring the backend's own table"
    - "The stress-context payload built at send time carries language/coachingStyle read from PreferencesService (one source of truth; hard-coded defaults remain only as the unset-seam fallback)"
    - "The existing chat lifecycle behavior (send/cancel/partial-preservation/402 → paywall) is unchanged under these edits — ChatLifecycleTests, SSEParserTests, PaywallOutOfCreditsGuardTests all green (AUTH-03)"
  artifacts:
    - "StressMonitor/StressMonitor/Models/ServerQuickAction.swift (new)"
    - "StressMonitor/StressMonitor/Services/API/StressAPIClient+QuickActions.swift (new)"
    - "StressMonitor/StressMonitorTests/StressAPIClientQuickActionsTests.swift (pbxproj-registered, IDs A022/B022)"
    - "ChatQuickActions.prompt(forServerActionId:) — the local mirror of the backend prompt table"
  key_links:
    - "ChatBottomSheetView.onAppear → ChatViewModel.fetchQuickActions() → StressAPIClient.getQuickActions(stressLevel:language:coachingStyle:) → quickReplies swap"
    - "Chip tap → send(prompt) → StressLLMService.send → POST /chat (unchanged Phase-2 path)"
    - "ChatViewModel.streamResponse → StressContextPayload.build(language: preferencesService?.language, coachingStyle: preferencesService?.coachingStyle)"
  prohibitions:
    - "Chip taps must NEVER invoke the backend's quick-actions completion route (unmetered 512-token generation bypassing credit deduction — the only permitted quick-actions request is the GET suggestions fetch)"
    - "The chips fetch/swap must NOT clobber user state (no re-render churn while streaming; swap replaces chip data only, never messages or inputText)"
    - "Dead plumbing must NOT linger after cutover: QuickActionChipsView.swift deleted, ChatViewModel.quickActions accessor and sendQuickAction removed (ChatQuickActions itself is kept — it IS the reused local fallback source)"
---

<objective>
Server-driven quick-action chips on the LIVE chat surface plus the preferences-fed payload call site, and the clean cutover of the dead chips plumbing.

Purpose: implements CONTEXT decision "Quick Actions Source & Behavior" (derived-QA-01) and completes derived-PREF-02 (the ChatViewModel half — the builder's values now come from PreferencesService, one source of truth). Research Q1 is resolved here: the live chips surface is ChatBottomSheetView's quickRepliesSection string block (NOT the orphaned QuickActionChipsView component); the minimal-diff wiring swaps that block's data source, and the superseded dead code is deleted per repo convention while ChatQuickActions is REUSED as the instant local fallback (so it is not dead).
Output: ServerQuickAction DTO + +QuickActions GET extension + backend-mirrored prompt map, ChatViewModel quickReplies state with fallback→server swap and prefs-fed payload build, the sheet wiring, and two deletions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-PATTERNS.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/COVERAGE.md
@StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift
@StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift
@StressMonitor/StressMonitor/Services/LLM/ChatQuickActions.swift
@StressMonitor/StressMonitor/Views/Chat/QuickActionChipsView.swift
@StressMonitor/StressMonitorTests/ChatHistoryRestoreTests.swift
@StressMonitor/StressMonitorTests/ChatLifecycleTests.swift

**Q1 resolution (locked into this plan):** wire the LIVE surface — the `quickRepliesSection` block at ChatBottomSheetView.swift:318-357 whose taps already call `sendMessage()`. The local fallback source is `ChatQuickActions.actions(for:)` (titles + prompts, contextualized) — reused, not dead. `QuickActionChipsView.swift` (zero call sites, verified by grep this session), `ChatViewModel.quickActions` (lines 30-32) and `ChatViewModel.sendQuickAction` (107-109) are dead and deleted. `SSEParser`/`StressLLMService` terminal `quick_actions` metadata plumbing STAYS (live SSE contract, pinned by SSEParserTests; driving chips from it is a deferred idea).
**Server prompt map (backend truth, stress-app-be/src/lib/quick-actions.ts:56-67):** breathing → "Guide me through a box breathing exercise right now."; grounding → "Help me with the 5-4-3-2-1 grounding technique."; sleep_tips → "Give me practical tips to sleep better tonight."; mini_walk → "Suggest a simple 5-minute movement routine I can do right now."; recovery → "What recovery strategies should I focus on given my current state?"; resilience → "How can I build long-term stress resilience?"; talk → "I want to talk more about how I'm feeling right now."
**pbxproj IDs:** A022/B022 for the new suite (A018-A021 taken by 03-01/03-02). Deleting QuickActionChipsView.swift needs NO pbxproj edit (zero references — verified; the StressMonitor folder is a filesystem-synced group).
**Scope discipline:** chips refresh on chat open ONLY (SSE-metadata-driven refresh is deferred). No POST to the backend quick-actions route anywhere (COVERAGE.md row 17).
</context>

<tasks>

<task type="tdd">
  <name>Task 1: ServerQuickAction DTO + GET /quick-actions extension + prompt map (RED → GREEN)</name>
  <read_first>
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift — the 03-01 result: authorizedRequest(url:) + URLComponents query construction (this endpoint carries a three-param query)
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+Credits.swift — error enum + status-switch template
  - StressMonitor/StressMonitor/Services/LLM/ChatQuickActions.swift — the fallback catalog being extended with the server prompt map (full file, 61 lines)
  - StressMonitor/StressMonitorTests/StressAPIClientSessionsTests.swift — 03-01's exact-URL query assertions to copy for the three-param case
  - ../stress-app-be/src/routes/quick-actions.ts — query params (stress_level/language/coaching_style defaults 50/en/supportive), response shape
  - ../stress-app-be/src/lib/quick-actions.ts — suggestion rules + the verbatim prompt table mirrored below
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md — GET /quick-actions contract verbatim, "Critical Wiring Correction"
  </read_first>
  <files>StressMonitor/StressMonitor/Models/ServerQuickAction.swift, StressMonitor/StressMonitor/Services/API/StressAPIClient+QuickActions.swift, StressMonitor/StressMonitor/Services/LLM/ChatQuickActions.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj, StressMonitor/StressMonitorTests/StressAPIClientQuickActionsTests.swift</files>
  <behavior>
    - RED: getQuickActions(stressLevel:language:coachingStyle:) issues GET to the exact absolute URL `https://api.test/quick-actions?stress_level=65&language=vi&coaching_style=direct` (query built via URLComponents — no %3F) with a Bearer header
    - RED: decodes `{quick_actions:[{id:"breathing",title:"Box Breathing",type:"exercise"},...]}` into [ServerQuickAction]; unknown `type` strings decode without throwing (type is a plain String)
    - RED: 401 maps to QuickActionsAPIError.unauthorized
    - RED: ChatQuickActions.prompt(forServerActionId:) returns the exact backend prompt string for each of the 7 known ids (table-driven test over all seven) and nil for an unknown id
  </behavior>
  <action>Write StressAPIClientQuickActionsTests.swift first (pbxproj registration at A022/B022), confirm RED. GREEN:
  (1) Models/ServerQuickAction.swift — `struct ServerQuickAction: Codable, Sendable, Identifiable, Equatable` with `let id: String`, `let title: String`, `let type: String` (doc comment: type is "exercise"|"technique"|"tips"|"conversation" server-side; kept as String because iOS does not branch on it).
  (2) Services/API/StressAPIClient+QuickActions.swift — `enum QuickActionsAPIError: Error, LocalizedError, Equatable, Sendable` (.unauthorized, .invalidResponse, .server(statusCode: Int)) + `func getQuickActions(stressLevel: Int, language: String, coachingStyle: String) async throws -> [ServerQuickAction]` on the Credits template, URL built with URLComponents over `baseURL.appendingPathComponent("quick-actions")` with queryItems stress_level/language/coaching_style; decode a private `QuickActionsResponse { let quickActions: [ServerQuickAction] }` envelope (CodingKey `quick_actions`). This extension contains exactly ONE request method and it is a GET — nothing else may ever live here (revenue-bypass guard).
  (3) ChatQuickActions.swift — add `static func prompt(forServerActionId id: String) -> String?` returning the seven-entry table verbatim from the backend (listed in the plan context above — copy exactly, including punctuation), nil for unknown ids; doc comment stating it mirrors stress-app-be/src/lib/quick-actions.ts:56-67 and must be updated in lockstep.</action>
  <verify>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:StressMonitorTests/StressAPIClientQuickActionsTests -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - StressAPIClientQuickActionsTests exits 0 with ≥ 4 @Test functions incl. the exact three-param URL assertion and a table-driven prompt-map test covering all 7 ids + one unknown
    - `grep -c 'authorizedRequest' StressMonitor/StressMonitor/Services/API/StressAPIClient+QuickActions.swift` returns 1 and that single call site passes method "GET" (no POST anywhere in the file: `grep -c '"POST"' StressMonitor/StressMonitor/Services/API/StressAPIClient+QuickActions.swift` returns 0)
    - No `%3F` appears in any asserted URL; suite registered (no orphan)
  </acceptance_criteria>
  <done>The suggestions GET decodes typed chips with exact-query/Bearer pins, and every server chip id resolves to the backend's own prompt locally.</done>
  <reversibility rating="reversible">Additive types/extension + one static function on an existing enum.</reversibility>
</task>

<task type="tdd">
  <name>Task 2: Chips fallback→server swap on the live surface + prefs-fed payload + dead-code cutover</name>
  <read_first>
  - StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift — current state after 03-01 (apiClient seam, restoreHistory), quickActions/sendQuickAction dead members (30-32, 107-109), streamResponse build call site (124-128)
  - StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift — onAppear seam (45-54, extended by 03-01), quickRepliesSection (318-357), defaultQuickReplies (355-357), sendMessage/inputText
  - StressMonitor/StressMonitor/Views/Chat/QuickActionChipsView.swift — the file being deleted (confirm zero external references first)
  - StressMonitor/StressMonitor/Services/Preferences/PreferencesService.swift — the 03-02 result (language/coachingStyle reads)
  - StressMonitor/StressMonitor/Services/LLM/ChatQuickActions.swift — ChatQuickAction model (title/icon/prompt) + Task 1's prompt map
  - StressMonitor/StressMonitorTests/ChatHistoryRestoreTests.swift — the 03-01 suite being extended (FakeLLMService/waitFor patterns)
  - StressMonitor/StressMonitorTests/ChatLifecycleTests.swift — AUTH-03 regression fence
  </read_first>
  <files>StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift, StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift, StressMonitor/StressMonitor/Views/Chat/QuickActionChipsView.swift, StressMonitor/StressMonitorTests/ChatHistoryRestoreTests.swift</files>
  <behavior>
    - RED: a fresh ChatViewModel's quickReplies equals ChatQuickActions.actions(for: its stress category) — the local fallback renders with zero network (instant chips)
    - RED: fetchQuickActions() with a stubbed GET response replaces quickReplies with server titles whose prompts resolve via the map; an unknown server id row is dropped (never rendered as a dead chip)
    - RED: fetch failure leaves the fallback set untouched (no loading state, no empty state — guard on error and keep local)
    - RED: tapping through the VM — sendQuickReply(prompt-path) routes the resolved prompt into send(); with FakeLLMService the recorded stress context payload's language/coachingStyle equal the injected PreferencesService values ("vi"/"direct"), not the hard-coded defaults; with no PreferencesService injected, payload falls back to "en"/"supportive" (existing tests keep passing)
    - GREEN + regression fence: ChatLifecycleTests, SSEParserTests, PaywallOutOfCreditsGuardTests, ChatHistoryRestoreTests (03-01 cases) all green after the edits
  </behavior>
  <action>Add the RED cases to ChatHistoryRestoreTests (chips belong to the chat-open lifecycle; same suite, no new registration needed). Then GREEN:
  (1) ChatViewModel — add `private(set) var quickReplies: [ChatQuickAction]` initialized in BOTH inits from `ChatQuickActions.actions(for: stressResult?.category)`; add `private var fetchedQuickActions = false` and `var preferencesService: PreferencesService?` (injection seam mirroring apiClient — set by the sheet onAppear, unset in existing tests). Add `func fetchQuickActions() async`: guard !fetchedQuickActions, set it (one fetch per presentation), guard let apiClient; call `apiClient.getQuickActions(stressLevel: stressResult.map { Int($0.level) } ?? 50, language: preferencesService?.language ?? "en", coachingStyle: preferencesService?.coachingStyle ?? "supportive")`; on success map server rows whose prompt resolves (`ChatQuickActions.prompt(forServerActionId:)`) into ChatQuickAction(title: server title, icon: "sparkles", prompt: mapped) and assign quickReplies ONLY if the mapping is non-empty (else keep fallback); on throw return without touching quickReplies.
  (2) ChatViewModel.streamResponse — change the StressContextPayload.build call (lines 124-128) to pass `language: preferencesService?.language ?? "en", coachingStyle: preferencesService?.coachingStyle ?? "supportive"` (defaults preserved for the unset seam — existing tests and previews unchanged).
  (3) Delete the dead plumbing: remove `var quickActions` (30-32) and `func sendQuickAction` (107-109) from ChatViewModel; delete Views/Chat/QuickActionChipsView.swift entirely (verify zero references first: grep QuickActionChipsView across StressMonitor/StressMonitor — expect only the definition; it has no pbxproj entries, verified at plan time). ChatQuickActions/ChatQuickAction STAY (fallback source + chip model).
  (4) ChatBottomSheetView — quickRepliesSection's ForEach reads `viewModel.quickReplies` (id: \.id) and the tap closure calls `sendMessage(prompt)`-equivalent: set `inputText = reply.prompt` is NOT wanted (that would echo the long prompt into the composer) — instead call the VM's send path directly with the prompt while keeping the composer untouched: reuse the same path sendMessage() uses today (viewModel.send(reply.prompt)); keep the capsule label rendering `reply.title` with identical styling (chips UI unchanged per CONTEXT). Remove the now-unused `defaultQuickReplies` computed property. Extend onAppear (after 03-01's additions): `viewModel.preferencesService = preferencesService` (@Environment read, CreditService-style) and `Task { await viewModel.fetchQuickActions() }`.
  REFACTOR: re-run all chat suites; keep the diff on the sheet minimal.</action>
  <verify>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:StressMonitorTests/ChatHistoryRestoreTests -only-testing:StressMonitorTests/ChatLifecycleTests -only-testing:StressMonitorTests/SSEParserTests -only-testing:StressMonitorTests/PaywallOutOfCreditsGuardTests -only-testing:StressMonitorTests/StressAPIClientQuickActionsTests -parallel-testing-enabled NO</automated>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO</automated>
    <automated>grep -rn "QuickActionChipsView\|sendQuickAction" StressMonitor/StressMonitor --include="*.swift" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Targeted suites exit 0; full StressMonitorTests suite exits 0
    - The cutover grep returns 0 (QuickActionChipsView and sendQuickAction gone from app sources; QuickQuickActions/ChatQuickActions remain — verify with `grep -c 'enum ChatQuickActions' StressMonitor/StressMonitor/Services/LLM/ChatQuickActions.swift` == 1)
    - The prefs-fed truth is pinned: a FakeLLMService-recorded payload carries the injected service's values; the unset-seam default case also asserted
    - `grep -c 'defaultQuickReplies' StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift` returns 0
    - StressAPIClient+QuickActions.swift still contains exactly one GET request (Task 1 gate re-checked)
    - REFACTOR pass complete with suites re-run green
  </acceptance_criteria>
  <done>Chips render instantly from the local catalog, swap to live server suggestions at chat open, tap through the credit-metered /chat path, and the payload speaks the user's language/style — with the dead chips plumbing fully cut over and AUTH-03 fences green.</done>
  <precondition>03-01 (apiClient seam, ChatHistoryRestoreTests) and 03-02 (PreferencesService) green.</precondition>
  <reversibility rating="reversible">Feature wiring + dead-code deletion, all git-reversible; no stored-data or contract change.</reversibility>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces

| Symbol / Artifact | Kind | Location |
|---|---|---|
| `ServerQuickAction` (Codable DTO) | model | StressMonitor/StressMonitor/Models/ServerQuickAction.swift |
| `QuickActionsAPIError` | error enum | StressMonitor/StressMonitor/Services/API/StressAPIClient+QuickActions.swift |
| `StressAPIClient.getQuickActions(stressLevel:language:coachingStyle:)` — the ONLY quick-actions request, GET | API method | StressAPIClient+QuickActions.swift |
| `ChatQuickActions.prompt(forServerActionId:)` — backend prompt-table mirror (7 ids) | static map | StressMonitor/StressMonitor/Services/LLM/ChatQuickActions.swift |
| `ChatViewModel.quickReplies` + `fetchQuickActions()` + `fetchedQuickActions` guard + `preferencesService` seam | state/behavior | StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift |
| Prefs-fed `StressContextPayload.build` call site | wiring | StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift |
| quickRepliesSection server-swap render + onAppear prefs/chips wiring | UI | StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift |
| DELETED: `QuickActionChipsView.swift`, `ChatViewModel.quickActions`, `ChatViewModel.sendQuickAction`, sheet `defaultQuickReplies` | dead-code removal | — |
| `StressAPIClientQuickActionsTests` (pbxproj A022/B022) | test suite | StressMonitor/StressMonitorTests/StressAPIClientQuickActionsTests.swift |
| Chips/VM behavior cases (fallback, swap, unknown-id drop, tap-through, prefs-fed payload) | tests | StressMonitor/StressMonitorTests/ChatHistoryRestoreTests.swift (extended) |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → backend GET /quick-actions | Firebase ID token + context query params cross; suggestion titles return |
| server-suggested chip content → chat send | server-controlled strings become prompts sent through /chat (user-visible as chip titles) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-09 | Tampering (revenue) | wiring the backend's unmetered completion route (POST /quick-actions) | high | mitigate | The extension structurally exposes exactly one GET; chip taps route through send() → /chat (credit-metered) — pinned by the one-GET acceptance gate and the COVERAGE row-17 lockout; backend metering issue filed by 03-04 |
| T-3-10 | Spoofing | server-controlled chip titles rendered as text | low | accept | SwiftUI Text renders strings inertly (no markup/HTML interpretation); prompts are fixed local strings keyed by id — server cannot inject arbitrary prompt text, only choose among the 7 known prompts |
| T-3-11 | DoS / UX confusion | chips swap clobbering user state mid-interaction | low | mitigate | Swap replaces chip data only (never messages/inputText); one fetch per presentation (fetchedQuickActions guard); failure keeps fallback (no loading/empty state) |
| T-3-12 | Repudiation | prefs-fed payload silently diverging from displayed Settings values | low | mitigate | Same PreferencesService instance app-scope (environment); payload reads it at send time — pinned by the FakeLLMService-recorded-context test |
</threat_model>

<verification>
- StressAPIClientQuickActionsTests green (exact query URL, Bearer, decode, prompt map)
- ChatHistoryRestoreTests green including the new chips + prefs-fed cases; ChatLifecycleTests/SSEParserTests/PaywallOutOfCreditsGuardTests green (AUTH-03)
- Full suite green with -parallel-testing-enabled NO; cutover grep returns 0
</verification>

<success_criteria>
- All must_haves.truths hold at plan close; derived-QA-01 complete and derived-PREF-02 complete (call-site half)
- The three prohibitions hold (no completion-route call, no state clobber, no lingering dead plumbing)
- Live behavior (chips reacting to stress level via the deployed backend) rides the 03-05 UAT script
</success_criteria>

<output>
Create `.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-03-SUMMARY.md` when done.
Record in the SUMMARY: the Q1 cutover outcome (what was deleted vs reused), confirmation the +QuickActions extension holds exactly one GET, and any ChatLifecycleTests changes (expected: none).
</output>
