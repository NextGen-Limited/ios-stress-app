---
phase: 03-sessions-preferences-quick-actions-cleanup
plan: 05
type: execute
wave: 5
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-UAT.md
autonomous: true
requirements: [derived-CLEAN-01, AUTH-03]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The full StressMonitorTests suite runs green via xcodebuild test with -parallel-testing-enabled NO (baseline at Phase-2 close: 97 tests / 17 suites, grown by Phase 3's six new/extended suites)"
    - "A Release-configuration build exits 0 (Phase-2 BUILD-05 bar held under Phase 3 changes)"
    - "The backend deno suite is green against the restarted local postgres on 127.0.0.1:5433 (02-06 instance, datadir /tmp/stress-pg-0206) — never a remote DB"
    - "03-UAT.md exists and lists executable scenarios covering: history restore across relaunch, preferences round-trip via Settings → backend, chip fetch on chat open, 402 → paywall still presenting, factory reset wiping server sessions (verified via GET /sessions after reset) — live checks riding the deployed backend"
    - "The coverage seal holds: every new-suite file registered in the Sources phase (orphan count 0), the Supabase grep gate returns only the two KEEP sites, and the quick-actions extension contains exactly one GET"
  artifacts:
    - ".planning/phases/03-sessions-preferences-quick-actions-cleanup/03-UAT.md (new)"
  key_links:
    - "postgres restart (5433, /tmp/stress-pg-0206) → DATABASE_URL env → deno test → green"
    - "UAT scenarios → deployed https://stress-api.dropitx.site (GET /health → 200 precheck)"
  prohibitions:
    - "No xcodebuild invocation without -parallel-testing-enabled NO (host-verified parallel-clone failure)"
    - "Backend tests must NOT point at any remote database (local 127.0.0.1:5433 only)"
    - "The gate must NOT mark the phase complete on partial evidence (each truth above needs its own recorded result; a failing leg is a recorded gap, not a silent skip)"
---

<objective>
Milestone v1.1 close-out gate: run the full integration matrix (complete iOS suite, Release build, backend suite with the restarted local postgres, remnant/coverage grep gates) and author the UAT script that exercises the live deployed backend for the human-run scenarios.

Purpose: CONTEXT locks this gate ("Final integration testing gate: full StressMonitorTests suite …, targeted new-feature suites, xcodebuild build -configuration Release, backend suite still green, plus a UAT script covering history restore, prefs round-trip, and chip fetch"). It is also the AUTH-03 regression close (every Phase-2 pin green after Phase 3's chat-path edits) and the COVERAGE.md seal check.
Output: recorded gate results + 03-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/COVERAGE.md
@.planning/phases/02-credits-system-iap-transition/02-VERIFICATION.md
@.planning/phases/02-credits-system-iap-transition/02-06-SUMMARY.md

**Environment facts (verified this session):** local test postgres on 127.0.0.1:5433 is DOWN (`pg_isready -p 5433` no response). Restart procedure per 02-06 deviation notes: the instance lives at datadir /tmp/stress-pg-0206 (8 migrations applied) — restart it (e.g. `pg_ctl -D /tmp/stress-pg-0206 start` or the homebrew postgresql@15 server on port 5433 as 02-06 used) and confirm `pg_isready -h 127.0.0.1 -p 5433` before deno test. Deployed backend is live (`GET https://stress-api.dropitx.site/health` → 200). Deno 2.7.5 at /opt/homebrew/bin/deno.
**Backend suite command:** `cd /Users/ddphuong/Projects/next-labs/stress-ai/stress-app-be && DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/stress_app deno test --allow-env --allow-net --allow-read src/` (baseline 17 tests / 50 steps at Phase-2 close).
**This plan writes no app source.** Its files_modified is the UAT doc only; everything else is command evidence recorded in the SUMMARY. If a gate leg FAILS, record the failure and the candidate-fix pointer (which plan/seam), and surface it to the orchestrator as a gap — do not silently patch code outside a recorded gap-closure decision.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restart local postgres and run the backend suite</name>
  <read_first>
  - .planning/phases/02-credits-system-iap-transition/02-06-SUMMARY.md — the 5433 instance provenance and restart precedent
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md — Environment Availability + Validation Architecture (backend suite row)
  - ../stress-app-be/deno.json — task definitions
  </read_first>
  <files>(no repo files — environment + command evidence recorded in 03-05-SUMMARY.md)</files>
  <action>(1) Precheck: `pg_isready -h 127.0.0.1 -p 5433` — expected "no response" (down at plan time). (2) Restart the existing instance: locate its server binary (homebrew postgresql@15) and start it against datadir /tmp/stress-pg-0206 exactly as 02-06 recorded (check `ls /tmp/stress-pg-0206` first; if the datadir is gone — e.g. host reboot wiped /tmp — re-create per 02-06's deviation notes: initdb + run the 8 migrations from ../stress-app-be/migrations in order, and record the recreation in the SUMMARY). (3) Confirm pg_isready accepts. (4) Run the backend suite with the DATABASE_URL above. (5) Record: test/step counts vs the 17/50 baseline. If migrations were applied by the recreation, verify the chat/user_preferences tables exist before running.</action>
  <verify>
    <automated>pg_isready -h 127.0.0.1 -p 5433 && cd /Users/ddphuong/Projects/next-labs/stress-ai/stress-app-be && DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/stress_app deno test --allow-env --allow-net --allow-read src/</automated>
  </verify>
  <acceptance_criteria>
    - pg_isready exits 0 on 127.0.0.1:5433
    - deno test exits 0 with all suites passing (record exact counts; ≥ 17 tests)
    - No DATABASE_URL other than the local 127.0.0.1:5433 was used (recorded command line)
    - If /tmp/stress-pg-0206 had to be recreated, the recreation steps + migration list are in the SUMMARY
  </acceptance_criteria>
  <done>The backend suite is green against a live local postgres; the environment is reproducible from the SUMMARY.</done>
  <precondition>/tmp writable; homebrew postgresql@15 installed (02-06 precedent).</precondition>
  <reversibility rating="reversible">Environment-only; no repo state changes.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Full iOS suite + Release build + coverage/orphan/remnant gates</name>
  <read_first>
  - .planning/config.json — canonical test_command/build_command (pin these forms)
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md — Validation Architecture (full-suite + Release rows, Pitfalls 9/10)
  - .planning/phases/02-credits-system-iap-transition/02-VERIFICATION.md — Phase-2 close baseline numbers
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj — Sources phase 3828578ADDAD4AC5925394DB for the orphan check
  </read_first>
  <files>(no repo files — command evidence recorded in 03-05-SUMMARY.md)</files>
  <action>(1) Full suite: run the canonical test_command from repo root (carries -parallel-testing-enabled NO). Record test/suite counts vs the Phase-2 close baseline (97/17) plus Phase 3's additions (StressAPIClientSessionsTests, ChatHistoryRestoreTests, StressAPIClientPreferencesTests, PreferencesServiceTests, StressAPIClientQuickActionsTests, DataDeleterServerWipeTests + extended StressContextPayloadTests/DataDeletionConsolidationTests). (2) Release build: run the canonical build_command with `-configuration Release` appended. (3) Orphan sweep: every StressMonitorTests/*.swift appears in Sources phase 3828578ADDAD4AC5925394DB (the 02-01 loop check). (4) Remnant gate: the Task-2-of-03-04 grep set re-run (.gitignore + 25-about.html clean; app-source Supabase count == 2 KEEP sites). (5) Revenue gate: StressAPIClient+QuickActions.swift contains exactly one authorizedRequest and it is a GET; `grep -rn '"quick-actions"' StressMonitor/StressMonitor --include='*.swift'` shows only the GET path construction. (6) Quarantine check: CharacterEntitlementSyncTests remains disabled-with-reason (STATE.md carry-over — still excluded, not silently re-enabled). Record every number in the SUMMARY.</action>
  <verify>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO</automated>
    <automated>xcodebuild build -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -configuration Release -destination 'platform=iOS Simulator,name=iPhone 17'</automated>
    <automated>cd StressMonitor && ls StressMonitorTests/*.swift | while read f; do b=$(basename "$f"); grep -q "/\* $b \*/" <(grep -A300 "3828578ADDAD4AC5925394DB /\* Sources \*/" StressMonitor.xcodeproj/project.pbxproj) || echo "ORPHAN: $b"; done | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Full suite exits 0; counts recorded (expect ≥ 23 suites / ≥ 100 tests — exact numbers recorded, not asserted by the plan)
    - Release build exits 0
    - Orphan count 0
    - Remnant gate: .gitignore + 25-about.html grep count 0; app-source Supabase file-level hits == 2 (KEEP sites only)
    - Revenue gate: +QuickActions.swift single-GET confirmed (grep output recorded)
    - Any failing leg is recorded as a named gap with its owning seam — not retried into silence
  </acceptance_criteria>
  <done>The complete integration matrix is green (or explicitly gap-recorded), with numbers for every leg in the SUMMARY.</done>
  <precondition>Plans 03-01..03-04 complete; iPhone 17 simulator available; Tasks 1's postgres not required for iOS legs.</precondition>
  <reversibility rating="reversible">Read-only gates + recorded evidence.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Author 03-UAT.md — live-backend scenario script</name>
  <read_first>
  - .planning/phases/02-credits-system-iap-transition/02-UAT.md — the established UAT script format (scenarios, steps, expected results, live-smoke precedent)
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-CONTEXT.md — the locked gate scope (UAT covers history restore, prefs round-trip, chip fetch)
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/COVERAGE.md — the integrated surface the scenarios exercise
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md — Environment Availability (deployed backend live), Pitfall warning signs for each scenario
  </read_first>
  <files>.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-UAT.md</files>
  <action>Write 03-UAT.md in the 02-UAT.md format (table of scenarios; each with preconditions, numbered steps on the simulator, expected observable results, and a pass/fail column). Required scenarios, each with a live-backend verification step:
  1. **History restore across relaunch** — send a chat message, force-quit the app, relaunch, reopen the chat sheet: prior conversation renders; a second open does not duplicate messages (Pitfall 2 warning sign). Server-side check: GET /sessions lists exactly ONE session for the account whose title is the truncated first message (Pitfall 5 warning sign: two sessions per first message = fail).
  2. **Preferences round-trip** — Settings → AI Coach: switch language to Tiếng Việt and coaching style to Direct; relaunch: values persist; next chat reply arrives in Vietnamese with direct tone (backend `Respond in vi.` behavior). Server-side check: GET /preferences shows language "vi", coaching_style "direct". Switch back to en/supportive.
  3. **Chip fetch on chat open** — with an elevated stress context (or default), open chat: chips appear instantly (local fallback), then swap to server suggestions within a second or two; tapping a chip streams a credit-metered response (no free completion). Server-side check optional: GET /quick-actions?stress_level=75... returns breathing+grounding.
  4. **402 → paywall regression (AUTH-03)** — with zero credits (or the existing Phase-2 smoke procedure), send a chat: paywall presents with the out-of-credits reason; no crash, no dead-end error.
  5. **Factory reset wipes server history** — confirm chat history exists server-side (GET /sessions non-empty), run Settings → Manage data → factory reset, then after re-sign-in GET /sessions returns an empty list and the chat sheet opens empty (dangling-id tolerance also exercised).
  Include the precheck (GET /health → 200) and a curl/`gh`-free verification appendix describing how a tester checks GET /sessions and GET /preferences for the test account (any HTTP client; the tester is authenticated as the app's anonymous/Google account — document the token-extraction step or use the server-side log). Mark all scenarios pending; this plan does not execute them (human_verify_mode: end-of-phase).</action>
  <verify>
    <automated>grep -c "^### \|^## Scenario\|History restore\|Preferences round-trip\|Chip\|paywall\|Factory reset" .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 03-UAT.md exists with all five scenarios, each with preconditions, steps, expected results, and a server-side verification step
    - The history-restore scenario includes the no-duplication and single-session (titled) assertions
    - The factory-reset scenario verifies server-side emptiness, not just local
    - All scenarios marked pending (none pre-marked passed)
  </acceptance_criteria>
  <done>The UAT script is executable by a human against the deployed backend, covering every CONTEXT-named live check plus the AUTH-03 and DATA-01 regression surfaces.</done>
  <precondition>Deployed backend live (recheck GET /health before handing the script over).</precondition>
  <reversibility rating="reversible">A planning document.</reversibility>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces

| Symbol / Artifact | Kind | Location |
|---|---|---|
| `03-UAT.md` — five-scenario live-backend UAT script (history restore, prefs round-trip, chip fetch, 402 paywall, factory-reset server wipe) | documentation | .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-UAT.md |
| Gate evidence record (full-suite counts, Release exit code, backend counts, orphan/remnant/revenue gate outputs, postgres restart provenance) | documentation | 03-05-SUMMARY.md |
| Restarted local postgres instance (5433, /tmp/stress-pg-0206 or recreated) | environment | host-local (provenance in SUMMARY) |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate commands → local postgres (5433) | test database credentials cross to a loopback-only instance |
| UAT tester → deployed backend | live anonymous/Google identity performs real (metered) chat sends and a real factory reset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-17 | Tampering | gate integrity (flags omitted, remote DB substituted, partial evidence reported as pass) | medium | mitigate | Canonical commands from .planning/config.json pinned in the plan; every leg's raw output recorded in the SUMMARY; failing legs recorded as named gaps — the prohibition makes silent skips impossible |
| T-3-18 | Information Disclosure | backend test DB pointed at a remote/hosted instance | low | mitigate | DATABASE_URL fixed to 127.0.0.1:5433 in the recorded command; acceptance criterion forbids any other target |
| T-3-19 | Repudiation | UAT executed against a stale/dead backend | low | mitigate | GET /health → 200 precheck inside the script and re-checked before handover |
</threat_model>

<verification>
- All Task 1/2 automated commands exit 0 with recorded evidence
- 03-UAT.md passes its scenario-completeness grep
- Gaps (if any) are named with owning seams in the SUMMARY for orchestrator triage
</verification>

<success_criteria>
- All must_haves.truths hold with recorded evidence at plan close (or explicitly recorded gaps)
- AUTH-03 regression close: every Phase-2 pin suite green in the full-suite run
- The phase is ready for /gsd-verify-work and the end-of-phase human verification (03-UAT.md scenarios)
</success_criteria>

<output>
Create `.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-05-SUMMARY.md` when done.
Record in the SUMMARY: every gate leg's exact command + exit code + counts, the postgres restart provenance, and the handover note that 03-UAT.md scenarios are pending human execution (end-of-phase verify).
</output>
