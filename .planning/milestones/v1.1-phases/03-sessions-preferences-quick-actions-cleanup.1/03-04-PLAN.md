---
phase: 03-sessions-preferences-quick-actions-cleanup
plan: 04
type: execute
wave: 4
depends_on: ["03-01"]
files_modified:
  - StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift
  - StressMonitor/StressMonitor/Services/DataManagement/DataDeleter.swift
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift
  - .gitignore
  - design/screens/25-about.html
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  - StressMonitor/StressMonitorTests/DataDeleterServerWipeTests.swift
  - StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift
autonomous: true
requirements: [derived-SES-03, derived-CLEAN-01]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Factory reset deletes ALL of the user's server chat sessions (paginated GET /sessions → DELETE /sessions?id= per row, looped to exhaustion under a safety cap) as a new first phase of performFactoryReset, BEFORE the local wipe, while still authenticated"
    - "stressChatSessionId is cleared unconditionally by factory reset (StressLLMService.clearStoredCredentials finally wired into clearCredentialsAndSharedCaches) — a post-reset user never carries the prior session id"
    - "Auth-unavailable (no Firebase user / 401) skips the server wipe with a log line and the reset proceeds (offline-of-auth reset stays possible); any other wipe error (network/server) fails the whole reset loudly following the CloudKit precedent — never a silent partial success"
    - "deleteAllMeasurements (snapshots-only path) does NOT touch chat sessions — chat history wipes only on factory reset"
    - ".gitignore carries no Supabase entry; design/screens/25-about.html's OSS list reflects the current stack; the POST /quick-actions metering note exists as a GitHub issue on phuongddx/stress-app-be"
  artifacts:
    - "StressMonitor/StressMonitorTests/DataDeleterServerWipeTests.swift (pbxproj-registered, IDs A023/B023)"
    - "`ServerSessionWiping` protocol + StressAPIClient conformance (listSessions/deleteSession already exist from 03-01)"
    - "`DeletionError.serverSessionError(Error)` case"
  key_links:
    - "performFactoryReset Phase 0 (server wipe) → CloudKit reset → local wipe → baseline → clearCredentialsAndSharedCaches (now also clearing stressChatSessionId)"
    - "DataDeleterService init → injected ServerSessionWiping seam → fake in tests / StressAPIClient-backed conformer in production"
    - "gh issue create → phuongddx/stress-app-be → metering note for POST /quick-actions"
  prohibitions:
    - "The wipe must NOT report success while server history survives (skip is allowed ONLY for auth-unavailability and is logged; every other failure surfaces an error instead of continuing to 'Factory reset complete')"
    - "The wipe must NOT run after sign-out (it executes while the Firebase token is still obtainable — before clearCredentialsAndSharedCaches)"
    - "Snapshots-only deletion must NOT delete chat history (least-surprise scope separation)"
---

<objective>
DATA-01 closure for chat: factory reset wipes server-side chat history before the local wipe; the two locked Supabase remnant cleanups; and the backend metering note filed as a GitHub issue.

Purpose: implements CONTEXT decisions "Factory reset / delete-all-data wipes server chat history" (derived-SES-03, via the protocol-faked unit-test seam the decision requires), "Cleanup Scope" (derived-CLEAN-01: .gitignore:164 + 25-about.html:207, KEEP-list untouched), and "Record a note/issue for stress-app-be to meter or gate POST /quick-actions" (research Q3 — gh CLI is present and authenticated as phuongddx, so the issue is filed in-plan, non-interactively).
Output: ServerSessionWiping seam + wipe phase + unconditional session-id clear with tests, two remnant file fixes, and a filed backend issue.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-PATTERNS.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/COVERAGE.md
@StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift
@StressMonitor/StressMonitor/Services/DataManagement/DataDeleter.swift
@StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift
@StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift
@StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift

**depends_on/wave note:** source-wise this plan needs only 03-01 (+Sessions + ChatSession); wave 4 (not 3) because 03-03 also registers a test file in project.pbxproj (same-file conflict rule). IDs for this plan: A023/B023.

**Q2 resolution (locked into this plan):** skip-on-auth-unavailable — `LLMServiceError.unavailable` from `getIDToken()` (signed out; FirebaseAuthService.swift:47-50) and `SessionsAPIError.unauthorized` (401) both mean "no authenticated identity": log via the injected DataManagementLogger and continue the reset. Every other wipe error (URLError offline, 5xx) throws the new `DeletionError.serverSessionError` and fails the reset — exactly how a CloudKitResetError fails it today (DataDeleterService.swift:437-439). Tradeoff accepted and documented: an offline factory reset surfaces an error rather than silently claiming server history was deleted (DATA-01 honesty over airplane-mode convenience). Cite: RESEARCH Pitfall 4 + A5.

**Runtime-state facts (verified this session):** `stressChatSessionId` is never cleared today — `StressLLMService.clearStoredCredentials()` (StressLLMService.swift:35-37) has zero call sites, and `clearCredentialsAndSharedCaches` (DataDeleterService.swift:480-484) calls only `FirebaseAuthService.clearStoredCredentials()`. `.gitignore:164` is exactly `supabase/.temp/`; `design/screens/25-about.html:207` is exactly the "Supabase LLM 2.4" oss-row. KEEP untouched: FirebaseAuthService's Supabase keychain sweep + DataDeletionConsolidationTests (upgrader protection — CONTEXT lock).

**Issue body to file (Q3):** title `Meter or gate POST /quick-actions — unmetered 512-token completion bypasses credit deduction`; body states: quick-actions.ts:44-61 calls chatCompletion(..., 512) with no deductCredit anywhere in the route; any client can mint free completions through it; the iOS app deliberately never calls it (COVERAGE opt-out) and chip taps ride the metered /chat path; request: add deductCredit or restructure the route to return only static prompt text. File with `gh issue create --repo phuongddx/stress-app-be --title "<title>" --body-file <tmpfile>` (gh authenticated as phuongddx — verified). If gh fails at execution time, paste the body verbatim into the plan SUMMARY for manual filing and continue (do not block the plan on it).
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Server-session wipe phase + unconditional session-id clear (RED → GREEN)</name>
  <read_first>
  - StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift — injecting init seam (44-57), performFactoryReset phases + error mapping (388-444), clearCredentialsAndSharedCaches (474-484)
  - StressMonitor/StressMonitor/Services/DataManagement/DataDeleter.swift — DeletionError enum (39-57) gaining the new case
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift — listSessions/deleteSession (03-01 result) that the conformance forwards to
  - StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift — clearStoredCredentials (35-37, currently uncalled) + sessionIdDefaultsKey (31)
  - StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift — getIDToken's signed-out throw (47-50) defining the auth-unavailable error shape
  - StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift — @Suite structure + the KEEP-list expectations that must stay green
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md — Pattern 4 (wipe loop + seam), Pitfalls 4/6, Runtime State Inventory
  </read_first>
  <files>StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift, StressMonitor/StressMonitor/Services/DataManagement/DataDeleter.swift, StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj, StressMonitor/StressMonitorTests/DataDeleterServerWipeTests.swift, StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift</files>
  <behavior>
    - RED: with a fake ServerSessionWiping returning two pages (2 sessions, then 1, then empty), performFactoryReset issues list(0) → delete each → list(next offset) → delete → list → empty, in order, and completes without error (recorded call sequence asserted)
    - RED: a safety cap exists — a fake that always returns the same non-empty page terminates the loop (bounded iterations, e.g. ≤ 50 pages) and throws serverSessionError rather than looping forever
    - RED: a fake throwing LLMServiceError.unavailable (signed-out shape) SKIPS the wipe (no delete calls after the failure), logs, and the reset still completes (local wipe happened — assert via existing post-conditions or the fake CloudKit/repository calls)
    - RED: a fake throwing URLError fails the reset with DeletionError.serverSessionError and local wipe did NOT run (CloudKit fake not yet called / error surfaced)
    - RED: after ANY successful factory reset path, UserDefaults "stressChatSessionId" is absent (seed it before the call; assert removed) — including the auth-skip path
    - Existing DataDeletionConsolidationTests cases (Supabase keychain sweep pins) stay green untouched
  </behavior>
  <action>Write DataDeleterServerWipeTests.swift first (pbxproj 4-line registration at A023/B023; fake CloudKitResetService/repository doubles copied from DataDeletionConsolidationTests' approach), confirm RED. GREEN:
  (1) ServerSessionWiping protocol — declare in DataDeleterService.swift near the injection seam (or DataDeleter.swift beside the error taxonomy — executor picks the cohesive home, both are DataManagement files): `protocol ServerSessionWiping: Sendable { func listSessions(limit: Int, offset: Int) async throws -> [ChatSession]; func deleteSession(id: UUID) async throws }`. Conform StressAPIClient in +Sessions.swift (`extension StressAPIClient: ServerSessionWiping {}` — the two methods already exist with matching signatures).
  (2) DataDeleter.swift — add `case serverSessionError(Error)` to DeletionError with errorDescription forwarding localizedDescription (mirroring cloudKitError).
  (3) DataDeleterService — gain `private let serverSessionWiper: ServerSessionWiping?` via a new injecting-init parameter `serverSessionWiper: ServerSessionWiping? = nil` (default nil keeps the existing convenience init callers compiling; the designated init and the convenience init construct the production conformer when nil — `StressAPIClient()` is @MainActor-constructible here since DataDeleterService is @MainActor). Add `private func wipeServerSessions() async throws` implementing the pagination loop: offset starts 0; per iteration `let page = try await serverSessionWiper.listSessions(limit: 20, offset: offset)`; empty page → return; delete each row's id; offset += page.count; hard cap of 50 pages → throw serverSessionError(URLError(.badServerResponse))-style sentinel or a dedicated message. Auth-skip classification INSIDE performFactoryReset's do/catch around the wipe call: catch `SessionsAPIError.unauthorized` and `LLMServiceError` → `logger.log("Server session wipe skipped (signed out): ...")` and continue; all other errors propagate as `DeletionError.serverSessionError`. When serverSessionWiper is nil (not injected), skip the server wipe entirely with a log (test/preview construction without a client).
  (4) performFactoryReset — insert Phase 0 before the CloudKit reset (before line 408): `currentOperation = "Deleting server chat history"; deleteProgress = 0.02; try await wipeServerSessionsOrSkip()` (the skip classification above; it must run while authentication is still live — clearCredentialsAndSharedCaches stays the LAST step at line 428). Renumber the phase comments only if trivial; progress values may shift slightly but stay monotonic 0→1.
  (5) clearCredentialsAndSharedCaches (480-484) — add `StressLLMService.clearStoredCredentials()` beside the FirebaseAuthService call, making the stored chat session id clear unconditional on BOTH delete-all and factory-reset paths (the method is shared by both — that is correct: delete-all also signs out and must not leave a dangling id).
  (6) Extend DataDeletionConsolidationTests with one case pinning that clearCredentialsAndSharedCaches removes "stressChatSessionId" from UserDefaults (seed → call → assert absent).
  REFACTOR: extract the auth-skip catch into a small helper if it reads better; re-run all deletion suites.</action>
  <verify>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:StressMonitorTests/DataDeleterServerWipeTests -only-testing:StressMonitorTests/DataDeletionConsolidationTests -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - DataDeleterServerWipeTests exits 0 with the six RED behaviors as cases incl. the ordered list→delete→list sequence and the safety-cap termination
    - DataDeletionConsolidationTests exits 0 with the new stressChatSessionId-clear case and all pre-existing KEEP cases unchanged
    - `grep -c 'serverSessionError' StressMonitor/StressMonitor/Services/DataManagement/DataDeleter.swift` returns ≥ 2 (case + errorDescription)
    - `grep -c 'clearStoredCredentials' StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift` returns ≥ 1 (the wired call)
    - deleteAllMeasurements body diff is empty (snapshots path untouched — verify via git diff scope)
    - New suite registered (no orphan)
  </acceptance_criteria>
  <done>Factory reset wipes every server chat session before local deletion with protocol-faked orchestration tests, classifies auth-unavailable as skip-with-log and everything else as fail-loudly, and never leaves a dangling session id.</done>
  <precondition>03-01 green (listSessions/deleteSession/ChatSession exist).</precondition>
  <reversibility rating="reversible">Additive phase + seam behind a defaulted init parameter; the added clearStoredCredentials call is the fix itself. Data deletion is destructive by design but only via the pre-existing user-confirmed factory-reset flow.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Supabase remnant cleanup (gitignore + about screen)</name>
  <read_first>
  - .gitignore — line 164 (`supabase/.temp/`) and its surrounding block
  - design/screens/25-about.html — OSS list around line 207
  - StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift — lines 116-125, the KEEP keychain sweep
  - StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift — the KEEP test file
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md — Runtime State Inventory (app-source sweep: exactly two KEEP hits)
  </read_first>
  <files>.gitignore, design/screens/25-about.html</files>
  <action>(1) .gitignore — delete the `supabase/.temp/` line (164) and the now-orphaned blank line above it if that leaves a double blank (keep the file tidy; nothing else changes).
  (2) design/screens/25-about.html line 207 — replace the stale OSS row `<div class="oss-row"><span class="name">Supabase LLM</span><span class="ver">2.4</span></div>` with the current-stack row `<div class="oss-row"><span class="name">Firebase Auth</span><span class="ver">11</span></div>` (matches the Firebase 11.x SDK actually linked — research A6 resolution: replace with a current-stack entry rather than deleting the row slot).
  (3) Explicitly KEEP (verify untouched via git diff): FirebaseAuthService.clearStoredCredentials' Supabase keychain sweep and DataDeletionConsolidationTests — upgrader-protection code per CONTEXT lock, NOT remnants.
  Do not regenerate or edit the generated planning docs (.claude/CLAUDE.md GSD sections, .planning/codebase/*) — out of locked scope, noted for a future docs regen.</action>
  <verify>
    <automated>grep -c -i supabase .gitignore design/screens/25-about.html; echo "---keep-sites---"; grep -rn -i supabase StressMonitor/StressMonitor StressMonitor/StressMonitorTests --include="*.swift" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - First grep prints 0 for both files (no Supabase mention in .gitignore or 25-about.html)
    - The keep-sites count is exactly 2 file-level hits: FirebaseAuthService.swift (keychain sweep strings) and DataDeletionConsolidationTests.swift — both content-identical to before (git diff shows no changes inside them)
    - The new about row renders "Firebase Auth / 11" (grep the file for `Firebase Auth`)
  </acceptance_criteria>
  <done>The two locked remnant sites are cleaned; the app source carries Supabase strings only in the two KEEP sites.</done>
  <reversibility rating="reversible">Two one-line file edits.</reversibility>
</task>

<task type="auto">
  <name>Task 3: File the POST /quick-actions metering issue on stress-app-be</name>
  <read_first>
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-CONTEXT.md — "Record a note/issue for stress-app-be" decision
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/COVERAGE.md — row 17 (the recorded opt-out this issue backs)
  - ../stress-app-be/src/routes/quick-actions.ts — lines 44-61, the route the issue cites
  </read_first>
  <files>(no repo files — external GitHub issue; record the URL in 03-04-SUMMARY.md)</files>
  <action>Write the issue body to a temp file (mktemp) exactly as specified in the plan context: cite quick-actions.ts:44-61, chatCompletion(..., 512) with no deductCredit in the route, the free-completion bypass consequence, the iOS-side lockout (never called; chip taps ride metered /chat), and the requested fix (add deductCredit or make the route return only static prompt text). File it non-interactively: `gh issue create --repo phuongddx/stress-app-be --title "Meter or gate POST /quick-actions — unmetered 512-token completion bypasses credit deduction" --body-file <tmpfile>`. Capture the returned issue URL into 03-04-SUMMARY.md and append the row-17 opt-out reason in COVERAGE.md with the issue URL (edit the row's reason cell to append "— tracked as phuongddx/stress-app-be#<n>"). If gh fails (auth/network), paste the full body verbatim into 03-04-SUMMARY.md under a "MANUAL FILING NEEDED" heading and continue — the plan must not block on an external write. NO backend code changes (locked).</action>
  <verify>
    <automated>gh issue list --repo phuongddx/stress-app-be --search "quick-actions" --state all --json number,title,url --limit 5</automated>
  </verify>
  <acceptance_criteria>
    - The gh query returns an issue whose title contains "quick-actions" with state open (or the SUMMARY carries the full MANUAL FILING NEEDED body if gh was unavailable — exactly one of the two outcomes, recorded)
    - COVERAGE.md row 17 references the issue URL (or the manual-filing note)
    - Zero changes under ../stress-app-be (git -C ../stress-app-be status --porcelain is empty of new modifications from this task)
  </acceptance_criteria>
  <done>The metering gap is formally recorded cross-repo with the iOS opt-out reason cross-linked; no backend code touched.</done>
  <precondition>gh CLI authenticated (verified as phuongddx at plan time); network reachable to github.com.</precondition>
  <reversibility rating="reversible">An external issue can be closed/edited; nothing in either repo changes.</reversibility>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces

| Symbol / Artifact | Kind | Location |
|---|---|---|
| `ServerSessionWiping` protocol (listSessions/deleteSession seam) | protocol | StressMonitor/StressMonitor/Services/DataManagement/ (DataDeleterService.swift or DataDeleter.swift) |
| `StressAPIClient: ServerSessionWiping` conformance | conformance | StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift |
| `DeletionError.serverSessionError(Error)` | enum case | StressMonitor/StressMonitor/Services/DataManagement/DataDeleter.swift |
| `DataDeleterService.wipeServerSessions()` + Phase 0 in performFactoryReset + serverSessionWiper injection | behavior/seam | StressMonitor/StressMonitor/Services/DataManagement/DataDeleterService.swift |
| `clearCredentialsAndSharedCaches` → `StressLLMService.clearStoredCredentials()` wiring (unconditional id clear) | wiring | DataDeleterService.swift |
| `.gitignore` Supabase entry removal | cleanup | .gitignore |
| 25-about.html OSS row swap (Supabase LLM 2.4 → Firebase Auth 11) | cleanup | design/screens/25-about.html |
| `DataDeleterServerWipeTests` (pbxproj A023/B023) | test suite | StressMonitor/StressMonitorTests/DataDeleterServerWipeTests.swift |
| stressChatSessionId-clear pin | test case | StressMonitor/StressMonitorTests/DataDeletionConsolidationTests.swift (extended) |
| GitHub issue on phuongddx/stress-app-be (metering note) | external record | issue URL in 03-04-SUMMARY.md + COVERAGE.md row 17 |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → backend GET/DELETE /sessions (wipe loop) | Firebase ID token crosses; destructive per-id deletes issued |
| factory-reset flow → server availability | local deletion now couples to a server phase (scoped by error classification) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-13 | Tampering / Repudiation | wipe silently no-ops on partial failure (server history survives a "successful" reset) | high | mitigate | Failure classification is explicit: skip ONLY for auth-unavailability (logged); every other error throws serverSessionError and aborts before "Factory reset complete" — pinned by the URLError-fails and skip-is-logged tests; DATA-01 honesty preserved |
| T-3-14 | Elevation of Privilege | wiping another user's sessions via crafted ids | low | accept | Server scopes DELETE by token-derived uid (returns success even for 0 rows — Pitfall 6); iOS iterates only ids from its own authenticated list; no user input reaches the loop |
| T-3-15 | Information Disclosure | prior-identity session id surviving factory reset into a fresh install | medium | mitigate | Unconditional stressChatSessionId clear in clearCredentialsAndSharedCaches (both delete paths) + pinned by the ConsolidationTests case; restore remains 404-tolerant as defense-in-depth (03-01) |
| T-3-16 | DoS | wipe loop never terminating against a misbehaving server | low | mitigate | 50-page safety cap throws instead of looping (pinned by the cap test) |
</threat_model>

<verification>
- DataDeleterServerWipeTests + DataDeletionConsolidationTests green (sequence, cap, auth-skip, fail-loudly, id-clear)
- Remnant grep gates: .gitignore and 25-about.html clean; app source Supabase hits == the two KEEP sites
- Backend issue exists (or full body recorded for manual filing); backend repo untouched
- Full-suite impact check rides 03-05's gate
</verification>

<success_criteria>
- All must_haves.truths hold at plan close
- derived-SES-03 and derived-CLEAN-01 fully implemented per CONTEXT (protocol-faked tests, KEEP-list honored)
- The three prohibitions hold (no silent partial success, wipe precedes sign-out, snapshots path untouched)
</success_criteria>

<output>
Create `.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-04-SUMMARY.md` when done.
Record in the SUMMARY: the issue URL (or the MANUAL FILING NEEDED body), the Q2 skip-vs-fail classification as implemented, and confirmation deleteAllMeasurements was untouched.
</output>
