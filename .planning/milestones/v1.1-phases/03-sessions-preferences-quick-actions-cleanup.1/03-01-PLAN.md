---
phase: 03-sessions-preferences-quick-actions-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - StressMonitor/StressMonitor/Models/ChatSession.swift
  - StressMonitor/StressMonitor/Models/ChatSessionMessage.swift
  - StressMonitor/StressMonitor/Services/API/StressAPIClient.swift
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift
  - StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift
  - StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift
  - StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  - StressMonitor/StressMonitorTests/StressAPIClientTests.swift
  - StressMonitor/StressMonitorTests/StressAPIClientSessionsTests.swift
  - StressMonitor/StressMonitorTests/ChatHistoryRestoreTests.swift
autonomous: true
requirements: [derived-SES-01, derived-SES-02]
estimate:
  tokens: 19000
  raw_tokens: 19000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A user who chats, force-quits, and reopens the chat sheet sees the prior conversation restored from GET /sessions/{id}/messages for the persisted rolling session (server-authoritative; no local SwiftData cache of chat messages)"
    - "The first message of a new conversation creates a titled session server-side (POST /sessions, title = first user message truncated to ~50 chars) BEFORE /chat carries session_id, so no untitled twin session is auto-created by the backend"
    - "A dangling stored session id (server returns 404 on restore) yields an empty chat with the stored id cleared — never a visible error and never a bricked chat open"
    - "A restore that lands after the user already sent a message does not clobber the live conversation (applies only when messages is empty at apply time)"
  artifacts:
    - "StressMonitor/StressMonitor/Models/ChatSession.swift (new)"
    - "StressMonitor/StressMonitor/Models/ChatSessionMessage.swift (new)"
    - "StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift (new)"
    - "StressMonitor/StressMonitorTests/StressAPIClientSessionsTests.swift (pbxproj-registered, IDs A018/B018)"
    - "StressMonitor/StressMonitorTests/ChatHistoryRestoreTests.swift (pbxproj-registered, IDs A019/B019)"
  key_links:
    - "ChatBottomSheetView.onAppear → ChatViewModel.restoreHistory() → StressLLMService.currentSessionId → StressAPIClient.fetchMessages(sessionId:) → ChatMessage[] rendered"
    - "StressLLMService.send Task (nil session) → StressAPIClient.createSession(title:stressContext:) → persisted stressChatSessionId → sendChat(sessionId:)"
    - "SessionsAPIError.notFound → StressLLMService.resetSession() (dangling-id recovery)"
  prohibitions:
    - "History restore must NOT duplicate messages on repeated sheet opens (apply only when messages is empty; one fetch per presentation)"
    - "Session creation must NOT race the first /chat send (creation strictly precedes sendChat inside the same Task; fail-soft to nil id, never block the chat)"
    - "Chat messages must NOT be cached in SwiftData (server-authoritative store; in-memory display only)"
---

<objective>
Tracer slice for Phase 3: one end-to-end server-session path through every layer this phase touches — DTO → API extension → LLM service → view model → chat sheet. The path: a new conversation creates a titled session (POST /sessions) whose id rides the first /chat; relaunching and reopening the chat restores the conversation from GET /sessions/{id}/messages; a server-deleted (404) session degrades to a fresh empty chat.

Purpose: proves the session-continuity architecture on the agent's best early-context tokens before the expansion plans (preferences 03-02, chips 03-03, wipe 03-04) build on the same seams. Implements CONTEXT decisions "Continuous history restore", "Server-authoritative message store", and "Client-side session titles" (derived-SES-01, derived-SES-02).
Output: Two new DTOs, the +Sessions API extension, titled-session creation inside StressLLMService.send, restoreHistory() in ChatViewModel wired at ChatBottomSheetView.onAppear, and two new URLProtocol-pinned test suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-PATTERNS.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/COVERAGE.md
@StressMonitor/StressMonitor/Services/API/StressAPIClient.swift
@StressMonitor/StressMonitor/Services/API/StressAPIClient+Credits.swift
@StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift
@StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift
@StressMonitor/StressMonitorTests/StressAPIClientCreditsTests.swift
@StressMonitor/StressMonitorTests/StressAPIClientTests.swift

**Load-bearing pitfall discovered at plan time (overrides a PATTERNS.md claim):** `baseURL.appendingPathComponent("sessions?limit=20")` produces `https://api.test/sessions%3Flimit=20` — Foundation percent-encodes `?` (verified: `swift -e` this session). Query strings must NOT ride the existing `authorizedRequest(path:)` verbatim. Task 1 adds an `authorizedRequest(url:)` overload and builds query URLs with `URLComponents`.

**Scope discipline:** this plan implements sessions ONLY. Chips, preferences, and the factory-reset wipe are 03-02/03-03/03-04. `listSessions`/`deleteSession` are implemented here (API half) because they belong to the +Sessions extension; their orchestration (wipe loop) is 03-04.
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Sessions DTOs + StressAPIClient+Sessions extension (RED → GREEN)</name>
  <read_first>
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+Credits.swift — the verbatim template (error enum + status-switch extension)
  - StressMonitor/StressMonitor/Services/API/StressAPIClient.swift — authorizedRequest (lines 31-51), getHealth (56), sendChat body-encoding approach (70-105)
  - StressMonitor/StressMonitor/Models/CreditBalance.swift — DTO pattern incl. `freeResetAt: String?` date precedent
  - StressMonitor/StressMonitor/Models/ChatMessage.swift — ChatRole enum (lines 6-10), metadata fields (23-26)
  - StressMonitor/StressMonitorTests/StressAPIClientCreditsTests.swift — makeClient + RequestCaptureURLProtocol assertion pattern
  - StressMonitor/StressMonitorTests/StressAPIClientTests.swift — RequestCaptureURLProtocol definition + MockAuthService
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md — "Backend contracts" (sessions.ts verbatim), Pitfalls 1/6, Pattern 1
  - ../stress-app-be/src/routes/sessions.ts — route truth (limit/offset defaults, 404 shape, DELETE query param)
  </read_first>
  <files>StressMonitor/StressMonitor/Models/ChatSession.swift, StressMonitor/StressMonitor/Models/ChatSessionMessage.swift, StressMonitor/StressMonitor/Services/API/StressAPIClient.swift, StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj, StressMonitor/StressMonitorTests/StressAPIClientTests.swift, StressMonitor/StressMonitorTests/StressAPIClientSessionsTests.swift</files>
  <behavior>
    - RED (StressAPIClientSessionsTests, fixtures WITH fractional-second timestamps like "2026-08-23T07:39:53.953Z" per Pitfall 1): fetchMessages(sessionId:) issues GET to the exact absolute URL `https://api.test/sessions/<uuid>/messages` with a Bearer header and decodes `{messages:[{id,session_id,role,content,tokens_used,created_at}]}` into [ChatSessionMessage] (extra JSON keys ignored; role maps to ChatRole)
    - RED: fetchMessages maps HTTP 404 to SessionsAPIError.notFound (typed case), 401 to .unauthorized
    - RED: createSession(title:stressContext:) issues POST to `https://api.test/sessions` with JSON body containing the title string and a `stress_context` object, decodes the 201 row into ChatSession (id, title, created_at as String)
    - RED: listSessions(limit:offset:) issues GET to the exact URL `https://api.test/sessions?limit=20&offset=0` (query NOT percent-encoded) and decodes the `{sessions:[...]}` envelope
    - RED: deleteSession(id:) issues DELETE to `https://api.test/sessions?id=<uuid>` and treats 200 `{success:true}` as success with no follow-up verification request (Pitfall 6)
  </behavior>
  <action>Write StressAPIClientSessionsTests.swift first (suite + makeClient copying StressAPIClientCreditsTests; register the file in pbxproj with the 4-line pattern at the next-free IDs A018/B018 — the F1A1B2C3D4E500000000A0xx/B0xx scheme, A001-A017 in use), run it, confirm failure. Then implement GREEN:
  (1) Models/ChatSession.swift — `struct ChatSession: Codable, Sendable, Equatable` with `let id: UUID`, `let title: String`, `let createdAt: String?`, `let updatedAt: String?` and CodingKeys `created_at`/`updated_at`; doc comment on dates: ISO-8601 string as delivered (fractional seconds; never decode as Date — Pitfall 1). Codable ignores stress_context/model_used/is_archived.
  (2) Models/ChatSessionMessage.swift — `struct ChatSessionMessage: Codable, Sendable, Equatable` with `let id: UUID`, `let sessionId: UUID` (CodingKey `session_id`), `let role: ChatRole`, `let content: String`, `let tokensUsed: Int?` (`tokens_used`), `let createdAt: String?` (`created_at`).
  (3) StressAPIClient.swift — refactor `authorizedRequest(path:method:body:accept:)` to delegate to a new overload `authorizedRequest(url:method:body:accept:)` containing the existing token/header/timeout logic (path variant = `authorizedRequest(url: baseURL.appendingPathComponent(path), ...)`); no behavior change to existing callers. This exists because `appendingPathComponent` percent-encodes `?` (verified above), so query-carrying endpoints must build URLs via URLComponents.
  (4) Services/API/StressAPIClient+Sessions.swift — `enum SessionsAPIError: Error, LocalizedError, Equatable, Sendable` with cases `unauthorized`, `invalidResponse`, `notFound`, `server(statusCode: Int)` (errorDescription switch in Credits style) + `extension StressAPIClient` with four methods, each using the authorizedRequest(url:) overload and the Credits status-switch (200...299 decode / 401 unauthorized / 404 notFound for fetchMessages / default server):
    - `func listSessions(limit: Int = 20, offset: Int = 0) async throws -> [ChatSession]` — URLComponents on `baseURL.appendingPathComponent("sessions")` with queryItems limit/offset; decode a private `SessionsListResponse { let sessions: [ChatSession] }` envelope struct in this file.
    - `func createSession(title: String, stressContext: StressContextPayload?) async throws -> ChatSession` — POST, body built exactly like sendChat does (JSONEncoder on the payload → JSONSerialization object; `{"title": title, "stress_context": {...}}`); decode the 201 row.
    - `func deleteSession(id: UUID) async throws` — DELETE via URLComponents query `id`; 200...299 → return (no response-body verification).
    - `func fetchMessages(sessionId: UUID) async throws -> [ChatSessionMessage]` — GET `sessions/<uuid>/messages` (path has no query; plain appendingPathComponent is safe); decode a private `MessagesListResponse { let messages: [ChatSessionMessage] }` envelope; 404 → .notFound.
  (5) Extend RequestCaptureURLProtocol in StressAPIClientTests.swift additively: an optional `static var responseByPath: [String: (statusCode: Int, body: Data?)]?` (keyed by URL path, nil = existing single-response behavior) and an ordered `static var capturedRequests: [URLRequest]` accumulator, reset alongside the existing statics — ordered multi-request flows (Task 2) need it; existing suites must stay green.
  App source files need NO pbxproj registration (the StressMonitor folder is a PBXFileSystemSynchronizedRootGroup); only the test file does.</action>
  <verify>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:StressMonitorTests/StressAPIClientSessionsTests -only-testing:StressMonitorTests/StressAPIClientTests -only-testing:StressMonitorTests/StressAPIClientCreditsTests -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - StressAPIClientSessionsTests exits 0 with at least 6 @Test functions covering: fetchMessages decode+Bearer, fetchMessages 404→.notFound, fetchMessages 401→.unauthorized, createSession body+201 decode, listSessions exact query URL, deleteSession exact query URL
    - Fixtures contain fractional-second timestamps (grep the test file for `953Z` or equivalent `.dddZ` — count ≥ 2)
    - The captured absolute URLs in listSessions/deleteSession tests assert equality with expected `...?limit=20&offset=0` / `...?id=<uuid>` strings (no `%3F` in any asserted URL)
    - StressAPIClientTests and StressAPIClientCreditsTests still exit 0 (double extension is additive)
    - `grep -c 'func authorizedRequest' StressMonitor/StressMonitor/Services/API/StressAPIClient.swift` returns 2 (path variant + url overload)
    - Suite appears in xcodebuild output (registered; orphan check `ls StressMonitor/StressMonitorTests/*.swift` all present in Sources phase 3828578ADDAD4AC5925394DB)
  </acceptance_criteria>
  <done>The +Sessions extension decodes every sessions contract with exact-URL and Bearer assertions pinned by a registered green suite; query strings flow through URLComponents-built URLs.</done>
  <precondition>iPhone 17 simulator runtime present (host-verified at Phase 2 close); no backend needed (URLProtocol stubs).</precondition>
  <reversibility rating="reversible">Purely additive models/extension/test infra; the authorizedRequest refactor is behavior-preserving and git-reversible.</reversibility>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Tracer — titled session → chat → relaunch → restored history, one path end-to-end</name>
  <read_first>
  - StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift — send Task capture list (line 72), apply(metadata:) persistence (114-125), resetSession (52-58), sessionIdDefaultsKey (31)
  - StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift — send (89-104), streamResponse context build (113-135), clearConversation (219-224), test-injectable init (73-84)
  - StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift — onAppear service-wiring seam (45-54), per-presentation fresh VM (20-30), onDisappear cancel (57)
  - StressMonitor/StressMonitorTests/ChatLifecycleTests.swift — FakeLLMService double + waitFor helper
  - StressMonitor/StressMonitorTests/SSEParserTests.swift — SSE fixture format for the /chat stub body
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md — Pattern 3 (chat-open lifecycle guards), Pattern 5 (titled-session creation + ordering), Pitfalls 2/3/5, /chat session semantics
  </read_first>
  <files>StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift, StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift, StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj, StressMonitor/StressMonitorTests/ChatHistoryRestoreTests.swift</files>
  <behavior>
    - RED (StressLLMService level, real service + URLProtocol path-keyed stubs, registered suite ChatHistoryRestoreTests): when no session is stored, send() fires POST /sessions FIRST (capturedRequests[0] path == "sessions", body contains the truncated first-user-message title) and the subsequent POST /chat body contains `session_id` equal to the created session's id — ordering pinned, no untitled twin possible
    - RED: when POST /sessions fails (stub 500), send() still completes a /chat send with no session_id in the body (fail-soft per Pattern 5; title lost, chat not blocked)
    - RED: after a completed send, the created id is persisted — a fresh StressLLMService constructed afterwards (same UserDefaults) reads currentSessionId == created id
    - RED (restore): with stressChatSessionId seeded in UserDefaults and a GET messages stub, a ChatViewModel.restoreHistory() populates messages in server order with isSynced == true and system-role rows filtered out
    - RED: 404 from the messages stub clears the stored id (UserDefaults key absent afterwards) and leaves messages empty
    - RED: restore does not clobber — when a message is appended before the stubbed fetch lands, messages keeps the user's live content (isEmpty guard at apply time, re-checked after the async gap)
  </behavior>
  <action>Write the failing ChatHistoryRestoreTests suite first (register at IDs A019/B019). Test construction pattern: seed `UserDefaults.standard.set(uuidString, forKey: "stressChatSessionId")` before constructing the real StressLLMService (its init restores the id), inject it into ChatViewModel via the existing test-injectable init, inject a URLProtocol-stubbed StressAPIClient into the VM (new optional property, below), and clean the Defaults key in a defer/teardown so suites stay isolated. /chat stub bodies reuse the SSEParserTests fixture shape (content token event + terminal metadata event carrying session_id + `[DONE]`).
  Then GREEN:
  (1) StressLLMService.send — inside the existing Task closure, before sendChat: hoist `var sessionId = currentSessionId`; when nil, build the title from `messages.last(where: { $0.role == .user })?.content` — collapse whitespace/newlines to single spaces, prefix(50), append "…" only when truncated (empty → "New Conversation"); call `stressAPIClient.createSession(title:stressContext:)` with `try?` (fail-soft); on success set `sessionId` and persist through a new private `adopt(sessionId:)` helper that does exactly what apply(metadata:) does (set currentSessionId + write UserDefaults under Self.sessionIdDefaultsKey) — refactor apply(metadata:) to call it too, zero duplication. Pass the local `sessionId` to sendChat instead of the captured currentSessionId. Ordering is load-bearing (Pitfall 5): creation must complete before sendChat is invoked within the same Task.
  (2) ChatViewModel — add `var apiClient: StressAPIClient?` (doc comment: set by the owning view on appear, unset in unit tests unless injected; mirrors the presentPaywall injection seam) and `private var restoredHistory = false`; add `func restoreHistory() async`: guard `!restoredHistory && messages.isEmpty`, set restoredHistory = true (one fetch per presentation — Pitfall 2), resolve the session id via `(llmService as? StressLLMService)?.currentSessionId` (nil → return, empty chat), fetch via `apiClient?.fetchMessages(sessionId:)` (apiClient nil → return), map DTOs to ChatMessage with `remoteId: dto.id, sessionId: dto.sessionId, isSynced: true, tokensUsed: dto.tokensUsed, timestamp: Date()` (cosmetic; server order is created_at asc), filter out `.system` rows, and apply only if `messages.isEmpty` still holds after the await (re-check — race guard); on `SessionsAPIError.notFound` call `(llmService as? StressLLMService)?.resetSession()` and leave the chat empty (Pitfall 3); on any other error return silently (auth-unavailable/network — empty chat, no error banner; history is best-effort).
  (3) ChatBottomSheetView — extend the existing onAppear block (lines 49-54): construct nothing new; call `viewModel.apiClient = StressAPIClient()` then `Task { await viewModel.restoreHistory() }`. Keep the existing paywall/convergence wiring untouched. Per-representation VM freshness (State(initialValue:) at line 25) makes fetch-on-open naturally idempotent; restoredHistory guards re-appear within one presentation.
  REFACTOR pass after GREEN: dedupe the ChatMessage mapping, name the guards, re-run the suite.</action>
  <verify>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:StressMonitorTests/ChatHistoryRestoreTests -only-testing:StressMonitorTests/ChatLifecycleTests -only-testing:StressMonitorTests/SSEParserTests -only-testing:StressMonitorTests/StressAPIClientSessionsTests -parallel-testing-enabled NO</automated>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - ChatHistoryRestoreTests exits 0 with the six RED behaviors above as @Test functions; ordering test asserts capturedRequests[0] targets /sessions and capturedRequests[1] targets /chat with the created session_id in its body
    - ChatLifecycleTests + SSEParserTests exit 0 unchanged (AUTH-03 regression fence: send/cancel/partial-preservation/402-paywall behavior untouched)
    - Full StressMonitorTests suite exits 0 (baseline 97 tests / 17 suites at Phase-2 close + new suites)
    - `grep -c 'func restoreHistory' StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift` returns 1; `grep -c 'adopt(sessionId' StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift` returns ≥ 2 (definition + apply(metadata:) call site)
    - ChatViewModel still compiles with no init signature change (existing call sites untouched)
    - REFACTOR pass complete: cleanup pass ran after GREEN with the suite re-run green (RED→GREEN→REFACTOR closed out)
  </acceptance_criteria>
  <done>The tracer path works end-to-end at unit level: first message creates a titled session whose id rides /chat (order-pinned), the id survives service reconstruction, reopening restores the conversation from the server, 404 degrades to a fresh empty chat, and live messages are never clobbered — all pinned by a registered green suite with the Phase-2 chat suites still green.</done>
  <precondition>Task 1 green (DTOs + extension + path-keyed URLProtocol double exist).</precondition>
  <reversibility rating="reversible">Additive service/VM behavior behind optional injection seams; the send-path insertion is fail-soft and git-reversible. No stored-data migration involved.</reversibility>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces

| Symbol / Artifact | Kind | Location |
|---|---|---|
| `ChatSession` (Codable DTO, String dates) | model | StressMonitor/StressMonitor/Models/ChatSession.swift |
| `ChatSessionMessage` (Codable DTO, ChatRole reuse) | model | StressMonitor/StressMonitor/Models/ChatSessionMessage.swift |
| `SessionsAPIError` (.unauthorized/.invalidResponse/.notFound/.server) | error enum | StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift |
| `StressAPIClient.listSessions(limit:offset:)` | API method | StressAPIClient+Sessions.swift |
| `StressAPIClient.createSession(title:stressContext:)` | API method | StressAPIClient+Sessions.swift |
| `StressAPIClient.deleteSession(id:)` | API method | StressAPIClient+Sessions.swift |
| `StressAPIClient.fetchMessages(sessionId:)` | API method | StressAPIClient+Sessions.swift |
| `StressAPIClient.authorizedRequest(url:method:body:accept:)` | overload (query-safe request builder) | StressMonitor/StressMonitor/Services/API/StressAPIClient.swift |
| `StressLLMService` titled-session creation in send + `adopt(sessionId:)` persistence helper | behavior | StressMonitor/StressMonitor/Services/LLM/StressLLMService.swift |
| `ChatViewModel.apiClient` injection seam + `restoreHistory()` + `restoredHistory` guard | behavior/state | StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift |
| ChatBottomSheetView onAppear restore wiring | wiring | StressMonitor/StressMonitor/Views/Chat/ChatBottomSheetView.swift |
| `RequestCaptureURLProtocol.responseByPath` + `capturedRequests` (ordered multi-request stubbing) | test double extension | StressMonitor/StressMonitorTests/StressAPIClientTests.swift |
| `StressAPIClientSessionsTests` (pbxproj A018/B018) | test suite | StressMonitor/StressMonitorTests/StressAPIClientSessionsTests.swift |
| `ChatHistoryRestoreTests` (pbxproj A019/B019) | test suite | StressMonitor/StressMonitorTests/ChatHistoryRestoreTests.swift |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → backend /sessions* | Firebase ID token crosses; session/message JSON returns |
| UserDefaults → restore path | locally persisted session id (non-secret) drives a server fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Elevation of Privilege | fetchMessages / DELETE with another user's session id | low | accept | Server scopes every query by token-derived uid (messages route 404s non-owned, verified in sessions.ts); iOS only ever sends its own persisted id or a server-issued id — no user input reaches these paths |
| T-3-02 | Tampering (stored) | session title from user text via POST /sessions | low | mitigate | Title is JSON-encoded (no SQL/injection surface; parameterized insert); truncated + whitespace-collapsed client-side; stored TEXT rendered only in a future history list which must treat it as untrusted at render time (noted in deferred multi-session UX) |
| T-3-03 | Information Disclosure | dangling stressChatSessionId leaking prior identity into a fresh install | medium | mitigate | Restore is 404-tolerant and clears the stored id on 404 (Pitfall 3); the factory-reset unconditional clear lands in 03-04 with its own test |
| T-3-04 | Tampering / DoS | late restore overwriting a live conversation | low | mitigate | messages.isEmpty guard applied before AND after the await; restoredHistory one-shot flag (pinned by the no-clobber test) |
</threat_model>

<verification>
- StressAPIClientSessionsTests + ChatHistoryRestoreTests green with exact-URL, Bearer, ordering, and 404 assertions (fixtures include fractional-second timestamps)
- Phase-2 regression fence green: ChatLifecycleTests, SSEParserTests, PaywallOutOfCreditsGuardTests, StressAPIClientTests, StressAPIClientCreditsTests
- Full StressMonitorTests suite green with -parallel-testing-enabled NO
- No new test orphans (all new files registered in Sources phase)
</verification>

<success_criteria>
- All must_haves.truths hold on the simulator at plan close
- derived-SES-01 and derived-SES-02 fully implemented per CONTEXT (continuous restore, server-authoritative store, titled creation) with no scope creep into chips/preferences/wipe
- The three must_haves.prohibitions hold (no duplication on reopen, no orphan twin session, no SwiftData chat cache)
</success_criteria>

<output>
Create `.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-01-SUMMARY.md` when done.
Record in the SUMMARY: the appendingPathComponent `?`-encoding discovery and how the URL overload resolved it, any fixture formats reused, and confirmation that /chat's 402 paywall path was not touched.
</output>
