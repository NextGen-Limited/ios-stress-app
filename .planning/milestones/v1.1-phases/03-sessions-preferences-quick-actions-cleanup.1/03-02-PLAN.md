---
phase: 03-sessions-preferences-quick-actions-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - StressMonitor/StressMonitor/Models/UserPreferences.swift
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+Preferences.swift
  - StressMonitor/StressMonitor/Services/Preferences/PreferencesService.swift
  - StressMonitor/StressMonitor/Services/LLM/StressContextPayload.swift
  - StressMonitor/StressMonitor/StressMonitorApp.swift
  - StressMonitor/StressMonitor/Views/Settings/SettingsView.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  - StressMonitor/StressMonitorTests/StressContextPayloadTests.swift
  - StressMonitor/StressMonitorTests/StressAPIClientPreferencesTests.swift
  - StressMonitor/StressMonitorTests/PreferencesServiceTests.swift
autonomous: true
requirements: [derived-PREF-01, derived-PREF-02, derived-CR02]
estimate:
  tokens: 21000
  raw_tokens: 21000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Changing Language or Coaching Style in Settings → AI Coach persists to the server via a single-field PUT /preferences and survives app relaunch (seeded back by GET /preferences at first surface)"
    - "A PUT failure reverts the optimistic local value and surfaces an error message in Settings (local state never diverges silently from the server)"
    - "The stress-context payload's language/coachingStyle parameters are sourced from PreferencesService state once wired at the call site (03-03) — this plan provides the single source of truth"
    - "A newest-first recentHistory with rising stress now computes stressTrend == \"increasing\" (CR-02 fixed); falling → \"decreasing\"; fluctuation within ±5 → \"stable\"; single measurement → nil"
  artifacts:
    - "StressMonitor/StressMonitor/Models/UserPreferences.swift (new)"
    - "StressMonitor/StressMonitor/Services/API/StressAPIClient+Preferences.swift (new)"
    - "StressMonitor/StressMonitor/Services/Preferences/PreferencesService.swift (new)"
    - "StressMonitor/StressMonitorTests/StressAPIClientPreferencesTests.swift (pbxproj-registered, IDs A020/B020)"
    - "StressMonitor/StressMonitorTests/PreferencesServiceTests.swift (pbxproj-registered, IDs A021/B021)"
  key_links:
    - "StressMonitorApp init → app-scope PreferencesService @State → .environment(preferencesService) → SettingsView AI Coach section readers"
    - "SettingsView row tap → PreferencesService.update(language:|coachingStyle:) → StressAPIClient.updatePreferences(single field) → server row"
    - "ChatViewModel/SettingsView onAppear → PreferencesService.seedIfNeeded() → GET /preferences → local state (guarded by hasSeeded)"
  prohibitions:
    - "Preferences sync must NOT overwrite server values with defaults on fresh install (GET seeds; PUT fires only on explicit user change)"
    - "iOS must NOT send the full preferences row back (exactly one allowlisted field per PUT — never a save-all method)"
    - "The coaching-style picker must NOT offer values outside the backend's closed set (supportive/direct/educational); the language picker offers only en/vi but displays a current value outside that set rather than hiding it"
---

<objective>
Preferences sync pair (language + coaching_style) end-to-end on the service side, the deferred 01-REVIEW CR-02 trend-direction fix in the same builder this phase touches, and the Settings "AI Coach" surface: UserPreferences DTO + StressAPIClient+Preferences + PreferencesService (seed-once, optimistic update, revert-on-failure) + app-root environment wiring + the AI Coach section in SettingsView.

Purpose: implements CONTEXT decisions "Preferences Sync" (derived-PREF-01, the service/Settings half of derived-PREF-02) and folds in CR-02 (explicitly deferred to Phase 3 by 02-01's deferral note — same builder, one edit window). The ChatViewModel call-site wiring that consumes PreferencesService at send time lands in 03-03 (ChatViewModel is owned by 03-01's wave).
Output: one new service + one API extension + one DTO, the CR-02 fix with regression coverage, and the Settings UI section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-PATTERNS.md
@.planning/phases/03-sessions-preferences-quick-actions-cleanup/COVERAGE.md
@StressMonitor/StressMonitor/Services/API/StressAPIClient+Credits.swift
@StressMonitor/StressMonitor/Services/Credits/CreditService.swift
@StressMonitor/StressMonitor/Services/LLM/StressContextPayload.swift
@StressMonitor/StressMonitor/StressMonitorApp.swift
@StressMonitor/StressMonitor/Views/Settings/SettingsView.swift
@StressMonitor/StressMonitorTests/StressAPIClientCreditsTests.swift
@StressMonitor/StressMonitorTests/StressContextPayloadTests.swift

**depends_on note:** 03-02 has no source dependency on 03-01; the wave-2 assignment exists solely because both plans register new test files in project.pbxproj (same-file conflict rule). Next-free pbxproj IDs for this plan: A020/B020 and A021/B021 (A018/A019 taken by 03-01).

**Research open questions resolved at plan time:**
- Q4 (language picker contents): backend passes any non-"en" language straight into the system prompt (`Respond in ${language}.` — openrouter.ts:65) and its own test suite pins `language: "vi"` (openrouter.test.ts:111-118). Picker ships exactly two options: English ("en") and Tiếng Việt ("vi"); a current value outside the set still displays (Pitfall 8). No checkpoint needed.
- Coaching style is a closed set server-side (types.ts union + styleInstructions Record: supportive/direct/educational; unknown values silently fall back to supportive — hence the closed picker).

**Scope discipline:** only language + coaching_style. The other five ALLOWED_FIELDS (display_name, theme, notification_enabled, stress_alert_threshold, custom_settings) are never read into state nor sent (CONTEXT lock; COVERAGE.md field-level note). ChatViewModel/ChatBottomSheetView are NOT touched here (03-03).
</context>

<tasks>

<task type="tdd">
  <name>Task 1: CR-02 — fix the inverted trend direction in StressContextPayload.build</name>
  <read_first>
  - StressMonitor/StressMonitor/Services/LLM/StressContextPayload.swift — build() trend block (lines 84-101); language/coachingStyle params (81-82) stay untouched this task
  - StressMonitor/StressMonitor/Services/Repository/StressRepository.swift — fetchRecent SortDescriptor(.reverse) caller (lines 85-96) proving input arrives newest-first
  - StressMonitor/StressMonitorTests/StressContextPayloadTests.swift — existing XCTestCase shape and StressResult/PersonalBaseline constructors (lines 11-13)
  - .planning/phases/01-firebase-auth-api-client-chat-migration/01-REVIEW.md — CR-02 verbatim analysis
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md — "The CR-02 bug and fix site" code excerpt
  </read_first>
  <files>StressMonitor/StressMonitor/Services/LLM/StressContextPayload.swift, StressMonitor/StressMonitorTests/StressContextPayloadTests.swift</files>
  <behavior>
    - RED: newest-first recentHistory where stress ROSE over time (e.g. levels [70, 40] newest-first → chronologically 40 then 70) yields payload.stressTrend == "increasing" — currently returns "decreasing" (the bug)
    - RED: newest-first input where stress FELL yields "decreasing"; mirror of the above
    - RED: fluctuation within ±5 (e.g. [52, 50] either order) yields "stable"; single-measurement history yields stressTrend == nil
    - Existing privacy tests (raw readings excluded, derived score included, wire-format key omission) stay green untouched
  </behavior>
  <action>Add the four regression cases to StressContextPayloadTests first (XCTest style matching the file; construct StressMeasurement histories with explicit timestamps so ordering is unambiguous — newest-first to mirror the repository caller), run, confirm the rising/falling cases fail. Then fix build(): inside the existing `if recent.count >= 2` block, reverse to chronological before the delta — `let chronological = Array(recent.reversed())`, compute levels from chronological so `diff = last - first` becomes newest − oldest; keep the ±5 stable threshold and the trendDelta format identical; keep the force-unwraps under the count guard exactly as now (they are safe only under it). No signature, CodingKeys, or encoding changes. REFACTOR: none expected beyond the two-line reversal — keep the diff surgical.</action>
  <verify>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:StressMonitorTests/StressContextPayloadTests -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - StressContextPayloadTests exits 0 with ≥ 4 new trend cases (increasing/decreasing/stable/nil)
    - `grep -c 'recent.reversed()' StressMonitor/StressMonitor/Services/LLM/StressContextPayload.swift` returns 1
    - The three existing privacy tests still pass (file diff touches only the trend block + new test funcs)
  </acceptance_criteria>
  <done>CR-02 closed: trend direction computed from chronological order regardless of newest-first input, pinned by regression tests in the existing suite.</done>
  <reversibility rating="reversible">Pure computation fix in one function, git-reversible, no storage or contract change (the wire keys are unchanged — only the corrected label value flows).</reversibility>
</task>

<task type="tdd">
  <name>Task 2: UserPreferences DTO + StressAPIClient+Preferences + PreferencesService (RED → GREEN)</name>
  <read_first>
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+Credits.swift — error enum + status-switch template
  - StressMonitor/StressMonitor/Services/API/StressAPIClient+Sessions.swift — the 03-01 result, incl. the authorizedRequest(url:) overload for any query-carrying call (preferences uses plain paths, but stay consistent)
  - StressMonitor/StressMonitor/Services/Credits/CreditService.swift — @MainActor @Observable service shape with injected apiClient
  - StressMonitor/StressMonitorTests/StressAPIClientCreditsTests.swift — makeClient/RequestCaptureURLProtocol pattern
  - StressMonitor/StressMonitorTests/ChatLifecycleTests.swift — @MainActor suite + waitFor structure for the service tests
  - ../stress-app-be/src/routes/preferences.ts — ALLOWED_FIELDS, 400 "No valid fields to update", full-row GET
  - ../stress-app-be/src/routes/preferences.test.ts — the user_id-dropped proof and defaults
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-RESEARCH.md — Pattern 2 (PreferencesService shape), Pitfalls 1/7/8, preferences contract verbatim
  </read_first>
  <files>StressMonitor/StressMonitor/Models/UserPreferences.swift, StressMonitor/StressMonitor/Services/API/StressAPIClient+Preferences.swift, StressMonitor/StressMonitor/Services/Preferences/PreferencesService.swift, StressMonitor/StressMonitor.xcodeproj/project.pbxproj, StressMonitor/StressMonitorTests/StressAPIClientPreferencesTests.swift, StressMonitor/StressMonitorTests/PreferencesServiceTests.swift</files>
  <behavior>
    - RED (StressAPIClientPreferencesTests): getPreferences() GETs `https://api.test/preferences` with Bearer and decodes a full backend row (fixture includes display_name, theme, notification_enabled, stress_alert_threshold, custom_settings as extra keys) into UserPreferences(language, coachingStyle) — extras silently ignored
    - RED: updatePreferences(field:) PUTs with exactly one JSON key (language OR coaching_style) in the body; HTTP 400 maps to PreferencesAPIError.noValidFields; 401 → .unauthorized
    - RED (PreferencesServiceTests, URLProtocol-stubbed StressAPIClient): seedIfNeeded() issues exactly one GET even when called twice (hasSeeded guard) and maps language/coaching_style into state
    - RED: update(language:) optimistically sets state, PUTs `{"language": ...}`, and on a stubbed 500 reverts state to the prior value and exposes the failure (thrown or surfaced error state — assert the revert)
    - RED: defaults before any seed are "en"/"supportive" (backend migration defaults)
  </behavior>
  <action>Write both suites first (pbxproj 4-line registrations at A020/B020 and A021/B021), run, confirm failure. Then GREEN:
  (1) Models/UserPreferences.swift — `struct UserPreferences: Codable, Sendable, Equatable` with `let language: String`, `let coachingStyle: String`, CodingKeys `coaching_style` (language identity); doc comment noting the backend row carries five more allowlisted fields this app deliberately ignores.
  (2) Services/API/StressAPIClient+Preferences.swift — `enum PreferencesAPIError: Error, LocalizedError, Equatable, Sendable` (.unauthorized, .invalidResponse, .noValidFields, .server(statusCode: Int)) + extension with two methods on the Credits template using authorizedRequest(path:): `func getPreferences() async throws -> UserPreferences` (GET "preferences") and `func updatePreferences(fields: [String: String]) async throws -> UserPreferences` (PUT "preferences", body = JSONSerialization of the dictionary; 400 → .noValidFields; 200...299 decode the returned full row). The service layer is responsible for passing exactly one known key per call (Pitfall 7 — never a save-all).
  (3) Services/Preferences/PreferencesService.swift — `@MainActor @Observable final class PreferencesService` (CreditService analog): `private(set) var language: String = "en"`, `private(set) var coachingStyle: String = "supportive"`, `private(set) var hasSeeded = false`, `private(set) var errorMessage: String?`, `private let apiClient: StressAPIClient`, `init(apiClient: StressAPIClient? = nil)`. Methods: `func seedIfNeeded() async` (guard !hasSeeded; GET; map both fields; on success hasSeeded = true; on throw leave defaults and clear hasSeeded so a later surface can retry — log via errorMessage only if repeatedly failing? NO: silent — seeding is best-effort background hydration, do not surface chat-blocking errors); `func update(language: String) async` and `func update(coachingStyle: String) async` — optimistic set + single-field PUT; on throw revert to the captured prior value and set errorMessage (surfaced by SettingsView in Task 3); on success clear errorMessage. No protocol needed this phase (same-module consumers; CreditService's protocol exists for MainTabView injection we don't have — if Task 3's SettingsView consumption demands it, add PreferencesServiceProtocol beside the implementation like LLMServiceProtocol, but do not pre-build it).
  REFACTOR: unify the two update methods' shared optimistic/revert skeleton if it emerges cleanly; re-run suites.</action>
  <verify>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:StressMonitorTests/StressAPIClientPreferencesTests -only-testing:StressMonitorTests/PreferencesServiceTests -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - Both suites exit 0; StressAPIClientPreferencesTests asserts the PUT body contains exactly one key (decode the captured httpBody and check count == 1)
    - PreferencesServiceTests pins: seed-once (GET request count == 1 across two seedIfNeeded calls), optimistic-set-then-revert on failure, defaults en/supportive
    - `grep -c 'func updatePreferences' StressMonitor/StressMonitor/Services/API/StressAPIClient+Preferences.swift` returns 1; no method sends more than one field
    - Both new suites appear in the Sources phase (no orphans)
  </acceptance_criteria>
  <done>The preferences pair round-trips through typed DTOs with single-field PUTs, seed-once GET, and revert-on-failure semantics — all unit-pinned without a live backend.</done>
  <precondition>Task 1 green (same builder file edited — keep commits separate).</precondition>
  <reversibility rating="reversible">All-new types + one extension; no existing behavior modified.</reversibility>
</task>

<task type="auto">
  <name>Task 3: App-root wiring + Settings "AI Coach" section</name>
  <read_first>
  - StressMonitor/StressMonitor/StressMonitorApp.swift — creditService ownership pattern (lines 26-28, 179-182) and .environment injection (line 200)
  - StressMonitor/StressMonitor/Views/Settings/SettingsView.swift — body section list (lines 31-47), sectionLabel (91-99), preferencesSection SettingsCard pattern (265-304), hairlineDivider (460-463), existing @Environment service reads and onAppear Task blocks (56-67)
  - StressMonitor/StressMonitor/Views/Settings/Components/ — existing row components used by the section pattern
  - StressMonitor/StressMonitor/Services/Preferences/PreferencesService.swift — the Task 2 result
  - .planning/phases/03-sessions-preferences-quick-actions-cleanup/03-PATTERNS.md — SettingsView section excerpt with insertion point
  </read_first>
  <files>StressMonitor/StressMonitor/StressMonitorApp.swift, StressMonitor/StressMonitor/Views/Settings/SettingsView.swift</files>
  <action>(1) StressMonitorApp — add `@State private var preferencesService = PreferencesService()` (or the init()-assigned State(initialValue:) form matching how creditService is built at lines 180-182 — follow whichever the file's current style uses) and inject `.environment(preferencesService)` on OnboardingContainerView beside `.environment(creditService)` (line 200). One instance, app scope.
  (2) SettingsView — insert between the Preferences section and the "Data & support" label (body lines 42-45): `sectionLabel("AI Coach")` + a new `aiCoachSection` computed property built as `SettingsCard { VStack(spacing: 0) { ... rows ... hairlineDivider ... } }` following preferencesSection exactly. Two rows:
  - Language row: a Picker with exactly two options — English ("en") and Tiếng Việt ("vi") — bound to the current value; the row displays preferencesService.language's current value even when it is outside the two-option set (label the value text from state, not from the option list — Pitfall 8). On change call `Task { await preferencesService.update(language: newValue) }`.
  - Coaching Style row: a Picker with exactly three options mirroring the backend union — Supportive ("supportive"), Direct ("direct"), Educational ("educational") — same update flow via update(coachingStyle:).
  Use the existing row visuals (navRow-style layout or an inline Picker row consistent with other SettingsCard rows), DesignTokens/Color.Wellness styling, and an accessibilityLabel on each picker. Add `@Environment(PreferencesService.self)` (or the project's established environment-read form used for creditService) and `.onAppear`-adjacent seeding: call `Task { await preferencesService.seedIfNeeded() }` in the existing onAppear block (line 56-67) — the hasSeeded guard makes repeated appears free.
  (3) Surface preferencesService.errorMessage as a small footnote text under the card when non-nil (revert already happened in the service; this is the user-facing signal). Clear-on-success is owned by the service.
  Add a #Preview for any new view component if one is extracted (keep it inline in SettingsView if trivial). Do not touch onboarding (CONTEXT: no onboarding change).</action>
  <verify>
    <automated>xcodebuild build -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO</automated>
    <automated>xcodebuild test -scheme StressMonitor -project StressMonitor/StressMonitor.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:StressMonitorTests/PreferencesServiceTests -only-testing:StressMonitorTests/StressAPIClientPreferencesTests -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - Build exits 0; both Task 2 suites still green
    - `grep -c 'AI Coach' StressMonitor/StressMonitor/Views/Settings/SettingsView.swift` returns ≥ 2 (section label + component name/preview)
    - `grep -c 'environment(preferencesService)' StressMonitor/StressMonitor/StressMonitorApp.swift` returns 1
    - The language picker offers exactly en/vi and the style picker exactly supportive/direct/educational (grep the option strings; no free-text entry — no TextField in the section)
    - seedIfNeeded is invoked from SettingsView onAppear (grep)
  </acceptance_criteria>
  <done>The AI Coach section edits the preferences pair through PreferencesService with visible error surfacing, wired app-scope like CreditService; the service half of derived-PREF-01/02 is complete and user-reachable.</done>
  <precondition>Task 2 green (service exists).</precondition>
  <reversibility rating="reversible">Additive UI section + one environment injection; trivially revertible.</reversibility>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces

| Symbol / Artifact | Kind | Location |
|---|---|---|
| `UserPreferences` (Codable DTO, two-field decode of the backend row) | model | StressMonitor/StressMonitor/Models/UserPreferences.swift |
| `PreferencesAPIError` (.unauthorized/.invalidResponse/.noValidFields/.server) | error enum | StressMonitor/StressMonitor/Services/API/StressAPIClient+Preferences.swift |
| `StressAPIClient.getPreferences()` / `updatePreferences(fields:)` | API methods | StressAPIClient+Preferences.swift |
| `PreferencesService` (@MainActor @Observable: language, coachingStyle, hasSeeded, errorMessage, seedIfNeeded(), update(language:), update(coachingStyle:)) | service | StressMonitor/StressMonitor/Services/Preferences/PreferencesService.swift |
| preferencesService app-scope ownership + .environment injection | wiring | StressMonitor/StressMonitor/StressMonitorApp.swift |
| SettingsView `aiCoachSection` (language picker en/vi + coaching-style picker) + seeding onAppear + error footnote | UI | StressMonitor/StressMonitor/Views/Settings/SettingsView.swift |
| CR-02 fix (chronological reversal in build()) | behavior fix | StressMonitor/StressMonitor/Services/LLM/StressContextPayload.swift |
| Trend regression cases (increasing/decreasing/stable/nil) | tests | StressMonitor/StressMonitorTests/StressContextPayloadTests.swift (existing suite extended) |
| `StressAPIClientPreferencesTests` (pbxproj A020/B020) | test suite | StressMonitor/StressMonitorTests/StressAPIClientPreferencesTests.swift |
| `PreferencesServiceTests` (pbxproj A021/B021) | test suite | StressMonitor/StressMonitorTests/PreferencesServiceTests.swift |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → backend /preferences | Firebase ID token crosses; preferences row JSON returns |
| Settings UI → PreferencesService | user intent crosses into optimistic state + server write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-05 | Tampering | preference poisoning via extra PUT fields | medium | mitigate | Server ALLOWED_FIELDS filter is authoritative (user_id dropped, pinned by backend route test); iOS structurally sends exactly one known key per PUT (asserted by body-count == 1 test) — never the full row, never unknown keys |
| T-3-06 | Tampering | fresh install overwriting server prefs with device defaults | medium | mitigate | GET-before-PUT ordering: seedIfNeeded hydrates before any update can fire (PUT only from explicit picker change; no write path exists from seeding) |
| T-3-07 | Spoofing/Tampering | picker values outside the backend's accepted vocabulary | low | mitigate | Closed coaching-style set (3 cases); language limited to en/vi with out-of-set current values still displayed; unknown server values degrade gracefully (backend falls back to supportive / passes language through) |
| T-3-08 | Repudiation | silent divergence of local vs server preference state | low | mitigate | Revert-on-PUT-failure + surfaced errorMessage (pinned by PreferencesServiceTests); no cached-write path that skips the server |
</threat_model>

<verification>
- StressContextPayloadTests green with the four new trend cases (CR-02)
- StressAPIClientPreferencesTests + PreferencesServiceTests green (exact-URL, Bearer, single-field body, seed-once, revert)
- App build exits 0; SettingsView renders the AI Coach section (visual confirmation rides the phase UAT script in 03-05)
- No new test orphans; existing suites untouched and green
</verification>

<success_criteria>
- All must_haves.truths hold at plan close (the call-site truth completes in 03-03)
- derived-PREF-01 fully implemented; derived-PREF-02 service+UI half implemented; derived-CR02 closed with regression coverage
- The three prohibitions hold (no defaults overwrite, no multi-field PUT, closed pickers)
</success_criteria>

<output>
Create `.planning/phases/03-sessions-preferences-quick-actions-cleanup/03-02-SUMMARY.md` when done.
Record in the SUMMARY: CR-02 closure confirmation (test names), the language-picker resolution (en/vi grounded in openrouter.ts:65 + its vi test), and whether PreferencesServiceProtocol was needed.
</output>
