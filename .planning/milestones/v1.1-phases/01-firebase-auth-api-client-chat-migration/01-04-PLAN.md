---
phase: 01-firebase-auth-api-client-chat-migration
plan: 04
type: execute
gap_closure: true
gap_ids: [G-01-2]
wave: 4
depends_on:
  - 01-02
files_modified:
  - StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift
  - StressMonitor/StressMonitor/ViewModels/AccountViewModel.swift
  - StressMonitor/StressMonitor/Views/Settings/SettingsView.swift
  - StressMonitor/StressMonitorTests/AccountViewModelTests.swift
  - StressMonitor/StressMonitorTests/StressAPIClientTests.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
autonomous: false
requirements: [D-02]
estimate:
  tokens: 42000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Settings → Sync & devices shows a 'Sign in with Google' row; tapping it launches the Google OAuth flow (closes G-01-2 — signInWithGoogle becomes reachable from the app UI)"
    - "Completing the flow links the anonymous account (currentUser.uid unchanged) and the linked email appears in the MeHeroCard and the row's value text"
    - "A failed or cancelled flow surfaces an error alert; the row returns to its pre-tap state"
    - "AccountViewModel.signInWithGoogle is pinned by a unit test using MockAuthService — no live Firebase session required"
    - "iOS Simulator build + full test suite pass"
  artifacts:
    - "StressMonitor/StressMonitor/ViewModels/AccountViewModel.swift (@Observable, @MainActor, injected AuthServiceProtocol)"
    - "'Sign in with Google' navRow in SettingsView.syncDevicesSection"
    - "StressMonitorTests/AccountViewModelTests.swift (MockAuthService call-through tests)"
  key_links:
    - "SettingsView navRow tap → AccountViewModel.signInWithGoogle(presenting:) → FirebaseAuthService.signInWithGoogle(presenting:) → GIDSignIn → link(with: credential)"
    - "AccountViewModel.linkedEmail ← AuthServiceProtocol.currentAccountEmail ← Auth.auth().currentUser?.email"
    - "SettingsView meHeroSection MeHeroCard(email:) ← accountViewModel.linkedEmail (post-link refresh)"
---

<objective>
Close gap G-01-2: make the Google Sign-In flow (already fully implemented in FirebaseAuthService since Plan 01-02) triggerable from the app UI, with post-link state refresh showing the linked email.

Purpose: UAT Test 2 failed because Plan 01-02 shipped signInWithGoogle(presenting:) as a service-only deliverable and deferred the UI to a phase that never existed. The service method is implemented and unit-test-pinned but unreachable — dead code from the user's perspective. This plan owns the deferral: a small AccountViewModel, a Settings row, the UIViewController bridge GIDSignIn needs, and post-link email display.

Output: AccountViewModel.swift, a 'Sign in with Google' row in SettingsView.syncDevicesSection, a linked-email display path into MeHeroCard, an AuthServiceProtocol.email accessor, and AccountViewModelTests pinning the call-through with MockAuthService.
</objective>

<execution_context>
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.claude/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-firebase-auth-api-client-chat-migration/01-UAT.md
@.planning/phases/01-firebase-auth-api-client-chat-migration/01-02-SUMMARY.md

# Source files
@StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift
@StressMonitor/StressMonitor/Views/Settings/SettingsView.swift
@StressMonitor/StressMonitorTests/StressAPIClientTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AccountViewModel with injected AuthServiceProtocol + email accessor on the protocol</name>
  <files>StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift, StressMonitor/StressMonitor/ViewModels/AccountViewModel.swift, StressMonitor/StressMonitorTests/AccountViewModelTests.swift</files>
  <precondition>
    Plan 01-02 completed: FirebaseAuthService.signInWithGoogle(presenting:) is implemented and the GoogleSignIn SPM product is linked. Google Sign-In is enabled in the Firebase Console and the REVERSED_CLIENT_ID URL scheme is registered (both done for 01-02).
  </precondition>
  <read_first>
    - StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift (AuthServiceProtocol at line 8 — extend it; FirebaseAuthService is @MainActor)
    - StressMonitor/StressMonitor/ViewModels/ChatViewModel.swift (existing ViewModel conventions: @Observable, @MainActor, constructor injection, default concrete service)
    - StressMonitor/StressMonitorTests/StressAPIClientTests.swift lines 1-45 (MockAuthService — the pinned test double; its doc comment says "Reused by FirebaseAuthServiceTests — do not duplicate")
    - StressMonitor/StressMonitorTests/FirebaseAuthServiceTests.swift (existing protocol-contract test — check whether it has its own AuthServiceProtocol conformer that the new protocol member breaks)
  </read_first>
  <behavior>
    - Test 1: calling accountViewModel.signInWithGoogle(presenting:) with a MockAuthService configured for success sets isSigningIn true during the call and false after, and invokes the mock's signInWithGoogle(presenting:) exactly once with the same UIViewController
    - Test 2: on success, linkedEmail equals the mock's configured email and errorMessage is nil
    - Test 3: when the mock throws, errorMessage is non-nil, linkedEmail is nil, and isSigningIn returns to false
    - Test 4: refreshAccountState() sets linkedEmail from the mock's currentAccountEmail (nil when the mock reports no linked account)
  </behavior>
  <action>
    Add `var currentAccountEmail: String? { get }` to AuthServiceProtocol. Implement it on FirebaseAuthService as `Auth.auth().currentUser?.email` (nil for anonymous users — Firebase sets it only after a Google link/sign-in). Update MockAuthService in StressAPIClientTests.swift to conform: add a settable `email: String?` property (default nil), record `googleSignInCallCount` and the last `presentingViewController` it received, and change `signInWithGoogle(presenting:)` to succeed (no throw) when `googleSignInError` is nil — its current default throws, so keep that default but let tests construct `MockAuthService(googleSignInError: nil, email: ...)`. Grep for all AuthServiceProtocol conformers before editing the protocol and fix any the compiler flags (expected: only FirebaseAuthService and MockAuthService; FirebaseAuthServiceTests may have its own — read it first).

    Create StressMonitor/StressMonitor/ViewModels/AccountViewModel.swift following existing ViewModel conventions: @Observable, @MainActor, final class, constructor injection with a default concrete service — init(authService: AuthServiceProtocol = FirebaseAuthService()). State: linkedEmail: String?, isSigningIn: Bool = false, errorMessage: String?. Methods: refreshAccountState() sets linkedEmail = authService.currentAccountEmail; signInWithGoogle(presenting viewController: UIViewController) async — guard !isSigningIn, set isSigningIn true with defer-reset, call try await authService.signInWithGoogle(presenting: viewController), on success call refreshAccountState(), on catch map the error to a short user-facing errorMessage string (use the error's localizedDescription; cancel-style errors from GIDSignIn set errorMessage nil so a cancelled OAuth sheet is silent — match how the app treats user-initiated dismissal as non-error). No comments per CLAUDE.md style except non-obvious business rules.

    Create StressMonitorTests/AccountViewModelTests.swift using Swift Testing (import Testing, @Test, #expect — mirror StressAPIClientTests.swift style, @MainActor struct). Write the four behavior tests first, confirm they fail to compile (RED), then implement (GREEN). Test target files are individually referenced in project.pbxproj (unlike the app target's filesystem-synchronized group) — register AccountViewModelTests.swift in all four pbxproj locations (PBXBuildFile, PBXFileReference, group, Sources phase), mirroring how existing test files are registered.
  </action>
  <verify>
    <automated>cd StressMonitor && xcodebuild build -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
    <automated>cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:StressMonitorTests/AccountViewModelTests 2>&1 | tail -25</automated>
  </verify>
  <done>
    AccountViewModel compiles, holds an injected AuthServiceProtocol, and AccountViewModelTests passes: call-through pinned (mock invoked once with the passed UIViewController), success populates linkedEmail, failure populates errorMessage, isSigningIn always resets. MockAuthService gained the email/call-recording seam without breaking its existing consumers (StressAPIClientTests + FirebaseAuthServiceTests still pass).
  </done>
  <reversibility rating="reversible">New ViewModel + one protocol member are additive; removing the row and the accessor reverts cleanly.</reversibility>
</task>

<task type="auto">
  <name>Task 2: 'Sign in with Google' row in SettingsView + UIViewController bridge + post-link email display</name>
  <files>StressMonitor/StressMonitor/Views/Settings/SettingsView.swift</files>
  <precondition>
    Task 1 merged: AccountViewModel and AuthServiceProtocol.currentAccountEmail exist.
  </precondition>
  <read_first>
    - StressMonitor/StressMonitor/Views/Settings/SettingsView.swift lines 143-180 (syncDevicesSection — the row goes here, after the Apple Watch row or at the section top; navRow signature at line 328 — it accepts a free-form icon string, precedent: "rectangle.3.group.fill" at line 276)
    - StressMonitor/StressMonitor/Views/Settings/SettingsView.swift lines 88-101 (meHeroSection — MeHeroCard(email:) currently reads viewModel.displayEmail)
    - StressMonitor/StressMonitor/Views/Settings/SettingsView.swift lines 66-79 (existing .alert pattern for surfacing errors — the sign-in error alert follows this shape)
  </read_first>
  <action>
    Add `@State private var accountViewModel = AccountViewModel()` to SettingsView and call `accountViewModel.refreshAccountState()` in the existing onAppear block. Add an `@State private var showSignInErrorAlert = false` and surface accountViewModel.errorMessage via a `.alert("Sign-In Failed", isPresented:)` attached to the ScrollView, matching the existing Health Access alert pattern.

    In syncDevicesSection, add a new navRow after the Apple Health row (with hairlineDivider separation): icon "g.circle" (free-form SF Symbol string — no AppIconSystem.Setting enum member needed), tint .settingsRippleBlue, title "Sign in with Google", value = accountViewModel.linkedEmail ?? "Link account", action: a signInWithGoogleTapped() method. That method obtains the presenting view controller from UIApplication.shared.connectedScenes → compactMap to UIWindowScene → first where activationState == .foregroundActive → keyWindow → rootViewController; guard it exists (else set an error), then runs `Task { await accountViewModel.signInWithGoogle(presenting: vc); if accountViewModel.errorMessage != nil { showSignInErrorAlert = true } }`. This bridge is the one non-obvious bit (GIDSignIn needs a UIKit presenter that pure SwiftUI does not hand you) — a single brief WHY comment is permitted there per CLAUDE.md's business-rule exception.

    Post-link refresh: in meHeroSection, change the MeHeroCard email argument to `viewModel.displayEmail ?? accountViewModel.linkedEmail` — because accountViewModel is @Observable, the card re-renders automatically when linkedEmail populates after a successful link. Hide or disable the row's action once linked (show the email as the value and skip re-presenting OAuth when accountViewModel.linkedEmail != nil) so a linked account doesn't accidentally start a second flow.
  </action>
  <verify>
    <automated>cd StressMonitor && xcodebuild build -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
    <automated>grep -c 'signInWithGoogle\|AccountViewModel\|connectedScenes' StressMonitor/StressMonitor/Views/Settings/SettingsView.swift</automated>
  </verify>
  <done>
    Build exits 0. SettingsView contains a 'Sign in with Google' row in syncDevicesSection wired through AccountViewModel, a connectedScenes→keyWindow→rootViewController bridge for the presenting parameter, an error alert, and the MeHeroCard email falls back to the linked Google email after linking. Full test suite still passes (no SettingsView regression).
  </done>
  <reversibility rating="reversible">View-layer wiring only; removing the row and state restores the prior SettingsView.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Google Sign-In links the anonymous account end-to-end</name>
  <what-built>
    A 'Sign in with Google' row in Settings → Sync & devices, wired through the new AccountViewModel to FirebaseAuthService.signInWithGoogle(presenting:), with the linked email displayed on the MeHeroCard and row value after success, and an error alert on failure.
  </what-built>
  <how-to-verify>
    1. Build and run on a booted iOS Simulator (`cd StressMonitor && xcodebuild build -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16'`, then launch).
    2. Open Settings → Sync & devices → confirm the 'Sign in with Google' row shows "Link account".
    3. Tap it → confirm the Google OAuth sheet presents (this is the G-01-2 fix — the flow is now reachable from the UI).
    4. Complete the flow with a Google account → confirm: (a) the row value and the MeHeroCard now show the linked email, (b) chat history and credits are preserved, (c) relaunching shows the linked account persists (uid unchanged — no new anonymous user).
    5. On a second run (already linked), confirm tapping the row does not re-present OAuth.
    6. If convenient, cancel the OAuth sheet once on a fresh install → confirm no error alert appears for the user-cancelled case.
  </how-to-verify>
  <resume-signal>Type "approved" if the OAuth sheet presents, the account links with the email displayed, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftUI → UIKit → Google OAuth | The row triggers GIDSignIn via a rootViewController obtained from connectedScenes; the OAuth exchange crosses to Google |
| Anonymous → Google account link | link(with:) preserves the anonymous uid, credits, and chat history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | UIViewController bridge (connectedScenes) | low | mitigate | Use only foregroundActive UIWindowScene's keyWindow rootViewController; guard nil and surface the error alert instead of force-unwrapping. |
| T-04-02 | Spoofing | OAuth presentation | low | mitigate | GIDSignIn runs Google's official flow (system browser/PKCE); credentials verified server-side by Auth.auth() — unchanged from T-02-01. |
| T-04-03 | Repudiation | Double-link (row tapped when already linked) | low | mitigate | Row action is skipped when linkedEmail != nil, preventing a second OAuth presentation over an existing link. |
</threat_model>

<verification>
Plan 01-04 is complete when ALL of the following hold:
1. `cd StressMonitor && xcodebuild build -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0
2. `cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:StressMonitorTests/AccountViewModelTests` passes
3. `grep -c 'signInWithGoogle' StressMonitor/StressMonitor/Views/Settings/SettingsView.swift` returns at least 1 (UI call site exists — the G-01-2 absence)
4. Full test suite passes (no MockAuthService/FirebaseAuthServiceTests regression from the protocol change)
5. Human-verify checkpoint: OAuth flow presents from the Settings row, links the anonymous account (uid unchanged), and the linked email renders on MeHeroCard + row value
</verification>

<success_criteria>
Gap G-01-2 closed: the Google Sign-In flow is triggerable from the app UI (Settings → Sync & devices), links the anonymous account without data loss, and surfaces post-link state (email) and errors. The previously-dead FirebaseAuthService.signInWithGoogle code path is user-reachable, pinned by MockAuthService unit tests, and verified end-to-end on simulator.
</success_criteria>

<output>
Create `.planning/phases/01-firebase-auth-api-client-chat-migration/01-04-SUMMARY.md` when done
</output>
