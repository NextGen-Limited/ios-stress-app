---
phase: 01-firebase-auth-api-client-chat-migration
plan: 03
type: tdd
wave: 3
depends_on:
  - 01-02
files_modified:
  - StressMonitor/StressMonitor/Services/API/StressAPIClient.swift
  - StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift
  - StressMonitor/StressMonitor/Services/API/StressAPIConfig.swift
  - StressMonitor/StressMonitor.xcodeproj/project.pbxproj
autonomous: true
requirements: [D-03, D-07]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "StressAPIConfig resolves STRESS_API_BASE_URL via the 3-tier chain (Info.plist → env → fallback) with documented precedence"
    - "StressAPIClient.authorizedRequest injects the Bearer token returned by its injected AuthServiceProtocol"
    - "StressAPIClient maps HTTP 402 to LLMServiceError.insufficientCredits (not .unavailable)"
    - "StressAPIClient.getHealth() sends NO Authorization header to /health"
    - "FirebaseAuthService exposes a test-injectable AuthServiceProtocol seam so token retrieval can be faked"
    - "All new tests compile and run under the StressMonitorTests target"
  artifacts:
    - "StressMonitorTests/StressAPIConfigTests.swift"
    - "StressMonitorTests/StressAPIClientTests.swift"
    - "StressMonitorTests/FirebaseAuthServiceTests.swift"
  key_links:
    - "StressAPIClientTests → MockAuthService (fake AuthServiceProtocol returning a fixed token) → StressAPIClient.authorizedRequest → assert Bearer header"
    - "StressAPIConfigTests → set ProcessInfo env / UserDefaults / Info.plist value → assert resolution precedence"
---

<objective>
Harden the tracer's service layer with dedicated TDD coverage. Plan 01 shipped the services as a vertical slice without dedicated tests (the tracer's verify was the end-to-end chat round-trip). This plan backfills the unit tests that RESEARCH.md section 6 calls out: StressAPIConfig 3-tier resolution, StressAPIClient request construction + error mapping, and FirebaseAuthService token retrieval.

Purpose: TDD mode is enabled. The tracer correctly prioritized proving the architecture over writing tests first — but the services now need test seams so a regression in token injection, 402 mapping, or URL resolution is caught by an automated test, not by a human tapping chat on a simulator. This plan writes those tests, making minimal production-code changes only where the tests reveal the service isn't injectable.

Output: Three new test files, minor production-code edits to expose test seams (protocol already exists for FirebaseAuthService; StressAPIClient may need a constructor-injectable session or baseURL override if not already present from Plan 01).
</objective>

<execution_context>
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.claude/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-firebase-auth-api-client-chat-migration/01-CONTEXT.md
@.planning/phases/01-firebase-auth-api-client-chat-migration/01-RESEARCH.md
@.planning/phases/01-firebase-auth-api-client-chat-migration/01-01-SUMMARY.md
@.planning/phases/01-firebase-auth-api-client-chat-migration/01-02-SUMMARY.md

# Services under test
@StressMonitor/StressMonitor/Services/API/StressAPIConfig.swift
@StressMonitor/StressMonitor/Services/API/StressAPIClient.swift
@StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift
@StressMonitor/StressMonitor/Services/LLM/LLMServiceProtocol.swift
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD StressAPIConfig + StressAPIClient (config resolution + request construction + 402 mapping)</name>
  <files>
    StressMonitorTests/StressAPIConfigTests.swift,
    StressMonitorTests/StressAPIClientTests.swift,
    StressMonitor/StressMonitor/Services/API/StressAPIClient.swift,
    StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  </files>
  <precondition>
    Plan 01-01 and 01-02 completed: StressAPIConfig.swift and StressAPIClient.swift exist and compile. The StressMonitorTests target exists with individual file references (PBXBuildFile pattern at project.pbxproj lines 19/96/253/473 — new test files must be added the same way).
  </precondition>
  <read_first>
    - StressMonitor/StressMonitor/Services/API/StressAPIConfig.swift (the enum Plan 01 created — verify the 3-tier resolution is testable; if the static `let baseURL` is initialized at type-load time from ProcessInfo, the env-var case may not be unit-testable without process launch — plan for that)
    - StressMonitor/StressMonitor/Services/API/StressAPIClient.swift (the class Plan 01 created — verify it constructor-injects AuthServiceProtocol; if not, this task adds the seam)
    - StressMonitor/StressMonitor/Services/LLM/SupabaseConfig.swift (wait — this was deleted in Plan 02; instead read the resolved config pattern from StressAPIConfig itself and from StoreKitProductCatalog.swift which uses the same ConfigResolver pattern)
    - StressMonitor/StressMonitor/Services/LLM/LLMServiceProtocol.swift (the LLMServiceError.insufficientCredits case Plan 01 added — the 402 mapping test asserts this)
    - StressMonitor/StressMonitor.xcodeproj/project.pbxproj lines 19, 96, 253, 473 (the SupabaseAuthServiceTests.swift pattern was the template — new test files follow the same PBXBuildFile + PBXFileReference + group membership + Sources phase four-line pattern; note the test target does NOT use PBXFileSystemSynchronizedRootGroup)
    - StressMonitorTests/PremiumViewModelTests.swift (an existing test file — match its import style, @testable import StressMonitor, Swift Testing or XCTest setup)
  </read_first>
  <behavior>
    StressAPIConfigTests (per D-03 — API base URL 3-tier resolution):
    - Test: When Info.plist provides STRESS_API_BASE_URL, StressAPIConfig resolves to that value (read via Bundle.main.object(forInfoDictionaryKey:) — if the test host's Info.plist has no key, this test sets the value via UserDefaults stressAPIBaseURL and asserts that path resolves instead)
    - Test: StressAPIConfig.baseURL absoluteString ends with "/health" removed — i.e. the base URL has no trailing path component (healthURL and chatURL are appended separately)
    - Test: StressAPIConfig.healthURL.absoluteString == StressAPIConfig.baseURL.absoluteString + "/health"
    - Test: StressAPIConfig.chatURL.absoluteString == StressAPIConfig.baseURL.absoluteString + "/chat"
    - Test: StressAPIConfig.isConfigured returns true (the fallback URL is always valid)

    StressAPIClientTests:
    - Test: StressAPIClient(authorizedRequest:) returns a URLRequest whose "Authorization" header equals "Bearer <fake-token>" when constructed with a MockAuthService returning "fake-token" — proves token injection
    - Test: authorizedRequest sets "Content-Type" to "application/json"
    - Test: authorizedRequest(path:"chat", method:"POST") has httpMethod == "POST"
    - Test: authorizedRequest attaches the body Data to httpBody
    - Test: getHealth() builds a request with NO "Authorization" header (the /health endpoint is public)
    - Test: When the HTTPURLResponse statusCode is 402, StressAPIClient's error mapper returns LLMServiceError.insufficientCredits (not .unavailable) — per D-07 (HTTP 402 handling); this may require exposing the mapper or testing via a stubbed URLSession; if the mapper is private, test it indirectly through a sendChat() call against a mock URLProtocol
    - Test: When statusCode is 401, the mapper returns .unavailable(reason:)
    - Test: When statusCode is 429, the mapper returns .rateLimited
  </behavior>
  <action>
    RED: Create StressMonitorTests/StressAPIConfigTests.swift and StressMonitorTests/StressAPIClientTests.swift with the behavior tests above. Use Swift Testing (import Testing, @Test, #expect) — the project uses both XCTest and Swift Testing; prefer Swift Testing for new files per the phase conventions. Add a `MockAuthService: AuthServiceProtocol` private struct INSIDE StressMonitorTests/StressAPIClientTests.swift (the test file) — it MUST live under StressMonitorTests/ so test-target membership excludes it from the Release app build. Do NOT add it to StressMonitor/StressMonitor/Services/MockServices.swift or any other main-target file: that path ships in the Release binary and would leak a fake auth service into production (threat T-03-01). The mock returns a configurable fixed token and records sign-out calls. Add both test files to the StressMonitorTests target in project.pbxproj following the four-line PBXBuildFile + PBXFileReference + group + Sources pattern. Run the tests — they MUST fail (RED) because either the test seam doesn't exist or the mapper isn't exposed.

    GREEN: Make minimal production-code changes so the tests pass. Likely changes: (a) if StressAPIClient's error mapper is private static, either expose it as `static func mapHTTPError(_ statusCode: Int) -> LLMServiceError?` (matching SupabaseLLMService's pattern) or test via URLProtocol stub; (b) if StressAPIClient doesn't already constructor-inject AuthServiceProtocol from Plan 01, add `init(authService: AuthServiceProtocol = FirebaseAuthService(), baseURL: URL = StressAPIConfig.baseURL)`; (c) if StressAPIConfig's static let initialization prevents env-var testing, add a `static func resolveBaseURL(infoPlistValue: String?, environmentValue: String?, userDefaultsValue: String?) -> URL` testable helper that the static let delegates to. Do NOT change the production behavior — only extract testable seams.

    Run tests again — they MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:StressMonitorTests/StressAPIConfigTests -only-testing:StressMonitorTests/StressAPIClientTests 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:StressMonitorTests/StressAPIConfigTests` exits 0 — this is the hard gate. If the run fails due to the STATE.md CoreSimulator host blocker, resolve the simulator host (boot a booted destination via `xcrun simctl boot` / pick a different `-destination`), then re-run. Do NOT substitute swiftc -typecheck: `@testable import StressMonitor` requires the built module product and a bare typecheck cannot resolve it, so a typecheck "pass" gives no real signal.
    - `cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:StressMonitorTests/StressAPIClientTests` exits 0 (same resolution path if the host blocks)
    - `grep -c 'insufficientCredits' StressMonitorTests/StressAPIClientTests.swift` returns at least 1 (the 402 mapping test exists)
    - `grep -c 'Bearer' StressMonitorTests/StressAPIClientTests.swift` returns at least 1 (the token-injection test exists)
    - `grep -c 'MockAuthService' StressMonitorTests/StressAPIClientTests.swift` returns at least 1 (the test double exists)
    - The two new test files appear in project.pbxproj (grep -c 'StressAPIConfigTests\|StressAPIClientTests' returns ≥4 — 2 refs each)
  </acceptance_criteria>
  <done>
    StressAPIConfig 3-tier resolution and StressAPIClient request construction + 402→insufficientCredits mapping are covered by automated tests that pass under `xcodebuild test`. If the STATE.md CoreSimulator host blocker reproduces, the executor resolves the simulator host and re-runs rather than substituting a typecheck — the tests must actually execute to count.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TDD FirebaseAuthService token retrieval + sign-out seam</name>
  <files>
    StressMonitorTests/FirebaseAuthServiceTests.swift,
    StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift,
    StressMonitor/StressMonitor.xcodeproj/project.pbxproj
  </files>
  <precondition>
    Plan 01-02 completed: FirebaseAuthService exists with Anonymous sign-in, getIDToken, clearStoredCredentials, and signInWithGoogle. AuthServiceProtocol is the test seam.
  </precondition>
  <read_first>
    - StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift (the full implementation — verify getIDToken and clearStoredCredentials are testable without a live Firebase project; Auth.auth() is a singleton so true unit-testing requires either a protocol wrapper around Auth.auth() or testing at the AuthServiceProtocol boundary with a mock)
    - StressMonitorTests/StressAPIClientTests.swift (Task 1 of this plan places MockAuthService here — reuse the same mock rather than re-declaring it; if a mock auth service already exists in another StressMonitorTests/ file, extend it there. Do NOT place MockAuthService in StressMonitor/StressMonitor/Services/MockServices.swift — that file is in the main app target and ships in Release)
    - StressMonitorTests/CharacterCollectionViewModelTests.swift or StressMonitorTests/PremiumViewModelTests.swift (existing test patterns — match their @testable import, Swift Testing vs XCTest setup, and mock-service injection style)
  </read_first>
  <behavior>
    - Test: FirebaseAuthService conforms to AuthServiceProtocol (compile-time assertion via `let service: AuthServiceProtocol = FirebaseAuthService()` — if this requires a constructor arg, assert the constructor-injected form)
    - Test: A MockAuthService conforming to AuthServiceProtocol can substitute for FirebaseAuthService in any consumer (StressAPIClient) — this is the injectability proof, tested by constructing StressAPIClient(authService: MockAuthService(...)) and verifying no Firebase SDK call is triggered at construction time
    - Test: FirebaseAuthService.clearStoredCredentials() is callable as a static method and does not crash when Auth.auth().currentUser is nil (no user signed in — sign-out on nil currentUser is a no-op via try?)
    - Test: FirebaseAuthService.signInWithGoogle() throws a descriptive error when called without a presenting UIViewController (or when Google Sign-In is not configured) — the stub-failure path is observable
    - Test: AuthServiceProtocol declares signInAnonymously, getIDToken, signInWithGoogle, signOut — the protocol contract is stable (assert via a conforming mock)
  </behavior>
  <action>
    RED: Create StressMonitorTests/FirebaseAuthServiceTests.swift with the behavior tests above. Because Auth.auth() is a Firebase singleton that cannot be mocked without a Firebase SDK test utility, these tests assert the injectability seam (MockAuthService substituting for FirebaseAuthService in StressAPIClient) rather than the live Firebase behavior. Add the file to the StressMonitorTests target in project.pbxproj. Run — they MUST fail if AuthServiceProtocol doesn't have the right shape or if FirebaseAuthService doesn't cleanly accept constructor injection.

    GREEN: If the tests reveal FirebaseAuthService can't be constructed without triggering a Firebase SDK call (e.g. it calls Auth.auth() in init), refactor init to be lazy — store dependencies, call Auth.auth() only inside signInAnonymously/getIDToken. This makes StressAPIClient(authService:) safe to construct in tests with a MockAuthService. Do NOT add a mock for Auth.auth() itself — that's a Firebase SDK testing concern beyond this phase.

    Run tests — they MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:StressMonitorTests/FirebaseAuthServiceTests 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `cd StressMonitor && xcodebuild test -scheme StressMonitor -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:StressMonitorTests/FirebaseAuthServiceTests` exits 0 — hard gate, same resolution path as Task 1 if the CoreSimulator host blocks. No swiftc -typecheck substitute.
    - `grep -c 'MockAuthService' StressMonitorTests/FirebaseAuthServiceTests.swift` returns at least 1
    - `grep -c 'AuthServiceProtocol' StressMonitorTests/FirebaseAuthServiceTests.swift` returns at least 2 (the conformance + the injectability proof)
    - `grep -c 'FirebaseAuthServiceTests' StressMonitor/StressMonitor.xcodeproj/project.pbxproj` returns at least 2 (file ref + build file)
    - FirebaseAuthService.init does not call Auth.auth() (grep the constructor body — if it does, the lazy-refactor landed)
  </acceptance_criteria>
  <done>
    FirebaseAuthService is unit-testable via the AuthServiceProtocol seam (MockAuthService substitutes cleanly). The protocol contract (signInAnonymously, getIDToken, signInWithGoogle, signOut) is asserted by a passing test suite. clearStoredCredentials is verified not to crash on nil current user.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New test files:**
- `StressMonitorTests/StressAPIConfigTests.swift` — Swift Testing suite covering 3-tier URL resolution + endpoint URL construction
- `StressMonitorTests/StressAPIClientTests.swift` — Swift Testing suite covering Bearer injection, Content-Type, body attachment, 402→insufficientCredits mapping, 401→unavailable, 429→rateLimited, getHealth no-auth
- `StressMonitorTests/FirebaseAuthServiceTests.swift` — Swift Testing suite covering AuthServiceProtocol contract + MockAuthService injectability + clearStoredCredentials nil-safety

**Modified production files (test-seam extraction only — no behavior change):**
- `StressMonitor/StressMonitor/Services/API/StressAPIClient.swift` — possibly: static mapHTTPError exposed, constructor injection formalized
- `StressMonitor/StressMonitor/Services/API/StressAPIConfig.swift` — possibly: resolveBaseURL helper extracted for testability
- `StressMonitor/StressMonitor/Services/Auth/FirebaseAuthService.swift` — possibly: lazy Auth.auth() (moved out of init)

**Symbols:** StressAPIConfigTests, StressAPIClientTests, FirebaseAuthServiceTests, MockAuthService
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan adds test coverage to services whose boundaries were threat-modeled in Plans 01 and 02.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | Test-only mock auth service accidentally shipped in Release | medium | mitigate | MockAuthService is pinned to StressMonitorTests/ (test-target membership excludes it from the Release app build). It MUST NOT be added to StressMonitor/StressMonitor/Services/MockServices.swift or any other main-target file — MockServices.swift is compiled into the app target and would ship the fake auth service in production regardless of DEBUG gating. |
</threat_model>

<verification>
Plan 03 is complete when ALL of the following hold:
1. The three new test files exist and are registered in the StressMonitorTests target
2. `xcodebuild test -only-testing:StressMonitorTests/StressAPIConfigTests -only-testing:StressMonitorTests/StressAPIClientTests -only-testing:StressMonitorTests/FirebaseAuthServiceTests` passes (if the STATE.md CoreSimulator host blocker reproduces, the executor boots/chooses a working simulator destination and re-runs — no swiftc -typecheck substitute, since `@testable import StressMonitor` requires the built module)
3. StressAPIClient's 402→insufficientCredits mapping is covered by an automated assertion
4. MockAuthService substitutes for FirebaseAuthService in StressAPIClient construction without triggering a Firebase SDK call
</verification>

<success_criteria>
The service layer shipped in Plans 01-02 is hardened by automated tests. A regression in Bearer token injection, 402 error mapping, URL resolution precedence, or auth-service injectability is caught by `xcodebuild test`, not by a human chat round-trip. The phase's must_haves.truths are now backed by executable assertions.
</success_criteria>

<output>
Create `.planning/phases/01-firebase-auth-api-client-chat-migration/01-03-SUMMARY.md` when done
</output>
